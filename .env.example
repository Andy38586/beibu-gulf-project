# 仓库根环境变量示例（复制为 .env 后按需填写；真实密钥不入库，见 backend/.env.example 与 CI Secrets）
# 前端（frontend/.env.local 或本文件——Vite 以 cwd=frontend 运行，读取 frontend/.env*）

# ⚠️ VITE_DATA_SOURCE 已废弃（z156，2026-09-12 清理）：数据源切换代码已随
# algorithm-service 退役删除，配置此变量无效——勿再引入（防僵尸 env 漂移）。

# API 基址（默认 /api 由 dev proxy 转发）
# VITE_API_BASE=/api
# 天地图 key（申请：https://console.tianditu.gov.cn/）
# VITE_TIANDITU_KEY=

# 后端
# PORT=3000
# LOG_DIR=./logs
# NODE_ENV=development

# P0（EP-09 / EH-07）：生产必须注入数据库口令（docker-compose.yml 用 ${POSTGRES_PASSWORD:?} 强制校验，
# 未注入则 compose 直接中断；禁止沿用开发默认口令 postgres）。示例（勿提交真实值）：
# POSTGRES_PASSWORD=<强口令>

# FastAPI 洪涝演算服务
# FLOOD_SERVICE_PORT=8000
