# 本机工具目录（不入 git），避免 prettier --check . 扫描第三方 bundle 噪音
.workbuddy/
.reasonix/
# 本机专属区（agent 文档/临时文件，不入库），不做格式门禁
.local/
# 构建产物
**/dist*/
**/coverage/
**/node_modules/
**/.venv/
# 问题台账文档：表格采用紧凑列宽（无对齐填充），不随 prettier 宽列对齐
docs/待解决问题.md
docs/已解决问题.md
# 施工手册（活文档/实施日志）：宽表格与紧排段落保持原样，不随 prettier 重排（2026-09-04 加入）
docs/v3-施工手册.md
# 服务回写的数据产物（forecast 模型重算等不保证行尾换行），不受人写格式门禁约束
backend/data/
# React 原型暂存目录（非项目源码，用途待用户裁决；2026-09-01 出现导致 format:check 全仓红）
.staging-react/
# pdf_build 教材/浏览器扩展产物（pdf_build/ep_guide 为扩展浏览器临时数据，非项目源码）
pdf_build/
# 性能测评报告产物（2026-09-10 会话生成、未跟踪的本地报告：单文件 HTML，
# 需保留生成时样式，不随 prettier 重排；未忽略会导致本机 format:check 全仓红）
性能测评-20260910/
# ZCode 会话计划产物（agent 运行时生成，非项目源码；2026-09-11 加入，
# 未忽略会导致本机 format:check 全仓红——先例同性能测评报告产物）
.zcode/
# 本机个人研究笔记（2026-09-16 加入：未跟踪的 LaTeX 长文，非项目文档，
# 不随 prettier 重排；未忽略会导致 pre-push format:check 全仓红——先例同上）
8472915306144-规律研究-*.md
# 本机个人面试材料（2026-09-16 加入：未跟踪、非项目文档；同上）
/面试*.md
# 本机临时工作目录（2026-09-17 加入：tmp-pinglu/ 建模中间件、tmp-resume/ 简历产物等
# 未跟踪的会话产物；其中 tmp-pinglu/*.json 实为 OSM 拉取失败的 406 错误页伪装成 JSON，
# prettier 解析即报错；未忽略会导致 pre-push format:check 全仓红——先例同上）
tmp-*/
# 平陆运河已入库的瓦片与影像产物（2026-09-22 加入：`backend/static/pinglu/` 由建模脚本
# 生成、随仓库分发（cab20aad 31 glb + 31 b3dm + tileset 清单），非手写代码。
# prettier 会重排 tileset.json 的键序与缩进（tileset.json diff 1594 行、b3dm 套 1620 行），
# 而 tools/v3-guard/tiles3d-check.mjs 钉的正是这份产物的字节面 ⇒ 重排等于改资产。
# 未忽略会导致 format:check 全仓红——先例同上）
backend/static/pinglu/**
# 平陆运河 3D Tiles 产出目录（2026-09-17 加入：本机会话产物、未跟踪；内含 Cesium 发行包
# 161 个 js/html/json——第三方代码不随本项目格式门禁重排。未忽略则 163 文件全仓红——先例同上）
pinglu-canal-3dtiles/
# 根目录会话文档（未跟踪、非项目文档；同 8472915306144-规律研究 先例）
/平陆运河3DTiles-*.md
/融安实习-*.md
# 根基文档·规则与清单（2026-09-22 加入：三份均为规则/判据类文档，表格采用紧凑列宽
# （无对齐填充），不随 prettier 宽列对齐；04-防复发清单另有 49 处段落空行会被 prettier 插入。
# 未忽略会导致 pre-push format:check 全仓红——先例同 docs/待解决问题.md）
docs/根基文档/04-防复发清单.md
docs/根基文档/审查体系专项/审查体系约定.md
docs/根基文档/审查体系专项/专项2-生命周期复查清单.md
