# AGENTS.md — 开发流程（强制协议）

> 给 AI agent 的作业协议。开工前读，交付前查。**违反任一条 = 不合格交付，重做。**
> 配套：`docs/根基文档/项目全景.md`（架构与红线）｜`docs/根基文档/04-防复发清单.md`（技术规则）｜`docs/待解决问题.md`（台账）

---

## 一、三条铁律

1. **证据先行** — 结论挂 `file:line` 或实测输出。无法确认就写「无法确认，需读 X」。
2. **不猜、不补、不编** — 先 grep 确认机制**存在且被调用**。禁止把注释当实现。
3. **改完必验证** — 不跑验证等于没完成。「应该没问题」不是验证。

---

## 二、开工前必读（顺序固定，不许跳）

1. `docs/根基文档/项目全景.md` §三–§六（分层/职责/原则/**红线**）
2. `docs/根基文档/04-防复发清单.md` **对应节**（渲染 A｜数据 B｜状态 C｜错误 D｜架构 E｜门禁 F｜安全 G｜文档 H｜清理 I）
3. 涉及模块的 `README.md` + `docs/根基文档/核心流程与数据流.md` 对应章节
4. `docs/待解决问题.md` — 已立案的按**原编号**接手，不新开号

---

## 三、流程（按任务选一条，照做）

**A 修 bug**
定位 → 找**根因**（自检同族能否一次修完）→ 查 04 对应节 → 最小改动 → 验证 → 提交

**B 新增功能/模块**
读 01 §四 + 02 §六 → **列出本次要同步改的全部位置**（清单化，禁止靠记忆）→ 先写两侧类型/契约 → 实现 → 补测试（含阳性对照）→ 查守护规则是否随之扩张 → 验证 → 提交

**C 审查/审计**
全文读不抽样 → 每条含 `file:line` + 证据 + 触发条件 + 等级 → 与前轮副本去重（禁止复读）→ 按「审查体系约定 §4.1」归并后才立编号 → 4 列产出

**D 部署/运维**
先确认对镜像/env/端口/探针/日志的影响 → 按 01 §七 与部署历程记录执行 → 变更前后都有可验证状态 → 不动生产数据

**E 文档/台账**
读 `docs/README.md` 定位置 → 规则进文档、状态进台账 → 台账 4 列、编号不复用 → 归并/重排必出「旧编号 → 新编号」映射

---

## 四、必须停下来问用户（8 条，禁止自行决定）

新增依赖 ｜ 改 DB schema 或删数据 ｜ 重写 git 历史 ｜ 涉及凭据密钥 ｜ 跨 3 个以上模块 ｜ **发现文档与实现矛盾（不自行选边）** ｜ 顺手修无关代码 ｜ 验证无法完成

---

## 五、验证协议

| 改动类型           | 必跑                                                       |
| ------------------ | ---------------------------------------------------------- |
| 任何 TS            | `npm run typecheck` + `npm run typecheck --prefix backend` |
| 任何代码           | `npm test`（前端）/ `npm --prefix backend test`            |
| 依赖方向/分层      | `npm run cruise`                                           |
| 跨端字段/枚举/契约 | `npm run types:check` + 人工核对两侧文件                   |
| 样式/token         | `npm run stylelint`                                        |
| 守卫/门禁/v3-guard | `npm run guard:v3` + 附能证明它变红的样本                  |
| 准备合并           | `npm run ci:local`                                         |

- 命令与结果**必须贴出**，不许"按逻辑应当通过"。
- 新增门禁/守卫**必须附阳性对照**，否则视为不存在。
- 验证失败如实报告，并标明归属：本次引入 / 既有 flake（已知：`backend/test/task.e2e-spec.ts` 墙钟计时断言）。

---

## 六、记录义务

- commit 格式 **Conventional Commits**（由 `.husky/commit-msg` 的 commitlint 强制，配置 `@commitlint/config-conventional`）
  - 形式 `type(scope): 中文说明`；type 取 `feat｜fix｜docs｜chore｜refactor｜perf｜test｜ci｜build｜style｜revert`；scope 可选且小写（如 `task`/`auth`/`ci`）
  - 例：`fix(task): 穷尽派生 TASK_DOMAINS，修复 forecast-map 域恒 400`
  - 标题 ≤100 字符、不以句号结尾
- **每修一项即提交**；禁 squash；禁 `--no-verify` / `--no-gpg-sign`
- 修复完：从 `待解决问题.md` 移入 `已解决问题.md`（按原编号）
- 新发现：先归并筛选，再分配编号。禁止给症状级问题编号
- 注释只写「为什么」；不写行号、清单、"已 X"

---

## 七、六类禁忌（违反 = 不合格）

| #   | 禁忌       | 要求                                                                            |
| --- | ---------- | ------------------------------------------------------------------------------- |
| 1   | 契约单向   | 改跨端字段/枚举 → 两侧同改 + 补跨边界断言。禁止只改一侧或 `as` 缝合             |
| 2   | 写了就不管 | 新增守卫/清理函数/schema/能力检查 → 同一 commit 内给出**调用点或测试**          |
| 3   | 无据的宣称 | 注释里每个"已 X"都要有断言能验证；**引用的路径必须存在**                        |
| 4   | 错级释放   | 新增监听/定时器/rAF/请求 → 同 commit 内写注销/abort，且与注册同作用域           |
| 5   | 无对照门禁 | 新增/改门禁 → 附一个能让它**变红**的样本。不许存在从未红过的门禁                |
| 6   | 扩展不继承 | 新增模块/通道/字段 → 同步检查守护规则、契约校验、生命周期清理、文档清单是否扩张 |

---

## 八、交付前自查（逐条打勾，未过不许提交）

- [ ] 读了 01 与 04 对应节
- [ ] 只做了任务要求的事，没有顺手重构
- [ ] 跨端/跨层改动两侧都改，且有断言
- [ ] 新增的守卫/清理/schema 有调用点或测试
- [ ] 新增的监听/定时器/请求有配对注销
- [ ] 注释的"已 X"有断言，引用的路径存在
- [ ] 验证跑了，命令 + 结果已贴
- [ ] 失败项归属已说明（本次引入 / 既有 flake）
- [ ] commit message 写清根因
- [ ] 台账已更新，编号未复用

---

## 九、输出风格

直接给结论，不寒暄不铺垫｜表格 + `file:line` + 定量数据优先｜按 ROI 排序｜无法确认处明确标注｜反对过度工程（不新增依赖、不为假想的未来做设计）｜破坏性操作先警告 + 列清单 + 等确认

---

## 十、维护

本文件管**流程**，`04-防复发清单.md` 管**规则**。规则进这里、状态进台账，不写清单/数字/行号。某类问题**第 2 次**出现时，在 §七 追加禁忌（首次只修，不立规）。
