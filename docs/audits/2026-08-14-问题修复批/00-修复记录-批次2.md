# 00-修复记录 · 批次2（2026-08-14）

> 批次1 见同目录 `00-修复记录-批次1.md`。裁决基准同批次1（01/02/03 + API契约文档 + v3 路径）。

## 一、已修复（15 项）

| # | 原编号 | 问题 | 改动 | 验证 | 回滚手段 |
| --- | --- | --- | --- | --- | --- |
| 1 | 副-18 | 日志保留 14 天偏短 | `backend/utils/logger.js` MAX_FILES 14→30（含头注释） | 代码审查 | revert 或改回 14 |
| 2 | 副-28 | 日志 PII 打码缺手机/邮箱 | `backend/middleware/logSanitizer.js` SENSITIVE_KEYS 增 phone/email/idcard/id_card/mobile | 代码审查 | 还原键列表 |
| 3 | 副-31 | OLRenderer:529 TODO 无上下文 | 补充"为什么现在不做"（v3 新选址再实现），避免为假想需求预留 API | 代码审查 | revert |
| 4 | 副-15 | backend 测试 5s 超时 flake | `backend/vitest.config.js` testTimeout/hookTimeout 20000 | 配置审查 | revert |
| 5 | 待裁决-1 | PointFeature 注释"回退 (0,0)"过期 | `frontend/src/types/renderer.ts:20` 注释改为"跳过该要素（不落 0,0 哨兵，a059）"；配套 heatmap.test.ts 用例标题同步 | 渲染器代码确认（OL/Cesium 均为 return null/跳过） | revert |
| 6 | S7-25 | ci.yml 注释用专项编号 7-02 | 改台账编号 z101（01 §7.2 编号铁律） | 代码审查 | revert |
| 7 | 副-09 | Python 依赖无锁 | 新增 `backend/flood-service/requirements.lock.txt`（venv pip freeze 固定 26 包），头注释说明生成方式 | freeze 来源真实 | 删除文件 |
| 8 | 副-07 | flood-service CORS 通配 | `main.py` allow_methods→GET/POST、allow_headers→Content-Type/Authorization（生产 nginx 同源反代无需 CORS） | 代码审查 | revert |
| 9 | 副-27 | fix 提交无测试约定 | 03 §1.6 补"fix 必须附测试/回归说明"约定 | 文档 | revert |
| 10 | C-4/6 | mapDataService 无 zod 校验 | `types/schemas.ts` 新增 portSchema/portsArraySchema；`mapDataService.getPorts` safeParse 替代 Array.isArray，错误分支同步 | 代码审查 | revert 两文件 |

> **C-4/6 回归教训（2026-08-14 补记）**：portSchema 新增必填 `address` 曾致 `mapDataService.test.ts` 4 个 mock 用例失败（全量测试 4 failed）——已由测试 mock 补 address 字段修复（5/5 通过，见后遗症副本"新雷"条）。教训：加 schema 必填字段须同步 grep 全部 mock 用例（入档质量门禁第 1 条实例）。
| 11 | C-10 | 测试注释称渲染器带 @ts-nocheck（z065 已移除） | 4 处测试注释修正为"运行时成员经断言暴露" | grep 确认渲染器本体无 @ts-nocheck | revert |
| 12 | P1-9 | floodStatistics riskLevelCode 前端类型缺 | `frontend/src/types/business/base.ts` FloodStatistics 补 `riskLevelCode?: number`（schema 已有该字段） | 字段对照 | revert |
| 13 | 8-2 | 演算无回归测试（全 mock） | 新增 `backend/flood-service/tests/test_real_engine.py`（真 DEM 演算：0 水位无淹没/面积单调/GeoJSON 合法/性能哨兵；CI 无 DEM 自动 skip） | **pytest 4/4 通过（10.6s）** | 删除文件 |
| 14 | 8-3 | createReadCache 零单测 | 新增 `backend/utils/__tests__/createReadCache.test.js`（命中/TTL/FIFO 上限/has/clear） | 代码审查（vitest 待 CI） | 删除文件 |
| 15 | 副-11 | dist 体积阈值 / emptyOutDir | **裁决不修**：chunkSizeWarningLimit:1000 有 c011 已知权衡注释；emptyOutDir:false 有 WorkBuddy 本地拦截注释（CI 前序清理）——均属已知权衡非残留 | — | — |

## 二、已裁决不修（批次2 新增）

| 原编号 | 裁决 | 依据 |
| --- | --- | --- |
| 8-5 container 叙事冲突 | **已解决，不修** | README:12 已是诚实表述（"cargo/container 真数据，berth/traffic 诚实标注合成示意"），b055 已修 cargo 残留 |
| 副-11 | 已知权衡（见上表 #15） | 两处均有设计注释 |

## 三、批次信息

- 提交：`fix: 审查问题批次2（测试补强/日志与CORS收窄/注释漂移/文档约定）`
- 涉及：backend logger/logSanitizer/vitest/ci.yml/flood-service(main.py+requirements.lock+测试)/schemas/mapDataService/base.ts/渲染器测试注释/03 文档
- 遗留：批次3（端点对账 P1-2、可观测性 P1-10/11/12、S7 文档批、nginx 限流/CSP、plansController 测试、UI 类副-03/04）继续推进。
