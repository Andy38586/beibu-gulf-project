# 00-修复记录 · 批次4（2026-08-14）

> 批次1-3 见同目录。本批：专项8 算法剩余 + 专项3 类型债。

## 一、专项8（5 修 + 1 裁决）

| 原编号 | 问题 | 改动 | 验证 | 回滚 |
| --- | --- | --- | --- | --- |
| 8-4 | 设施点坐标零防御（{} 点致整类 0 分） | `scoringService.js` `buildFacilityIndex` 过滤无效坐标（lng/lat 非有限值跳过+告警）；`scoreXiaoqu` 小区坐标无效按 0 分计+告警 | 代码审查 | revert |
| 8-9 | linearDecay NaN（无效坐标穿透到除法） | `linearDecay` 加 `!Number.isFinite(distance)` 守卫（NaN 按 0 分，02 §5.6 不变量 5）——实测 NaN 源是 turf.distance 对无效坐标返回 NaN，非除零（0>=0 已覆盖） | 代码审查 | revert |
| 8-8 | 北部湾 bbox 两处不一致（前端 112/23.5 vs 后端 115/25） | `crs.ts` BEIBU_GULF_BBOX 统一到后端权威 115/25 + 同源注释 | 与 siteAnalysisService.js 对照 | revert |
| 8-12 | 历史数据不足错误塞成功信封 | `forecastService.js` 两处 `engineResult.metadata?.error` 抛 `BusinessError(ANALYSIS_FAILED)`（R7 错误通道） | 代码审查 | revert |
| 8-14 | cargo 缓存键冗余（scenarioLevel 恒 1.0 致同结果多键） | `getCacheKey` 对 MODEL_INDICATORS 忽略 scenarioLevel | 代码审查 | revert |
| 8-10 | api/online 口径不一致 | **裁决已解决**：controller 已传 floodZone（查表→回退 6 档→空间筛选同源），floodService 对空 floodZone 返回空——elevation 兜底仅剩 0 档边界且已由 B-9 脏数据防御覆盖 | 代码取证 | — |

## 二、专项3 类型债（3 项，typecheck 全绿验证）

| 原编号 | 问题 | 改动 | 验证 | 回滚 |
| --- | --- | --- | --- | --- |
| C-1/3/7 | CesiumRenderer 25 处 renderer:any 类型债 | ① 15 处导出函数参数 `renderer: any` → `CesiumRenderer`（同文件类类型）；② 42 处 `renderer.viewer` 非空断言（挂载契约，调用方保证）；③ **类型化暴露并修复 4 个真实类型 bug**：`_geoJsonTokens` 声明 `Map<symbol,symbol>`→`Map<string,symbol>`（实际用字符串 id）；`LayerState` 扩展 allFeatures/cameraListener/_spatialIndex/_viewportRafId（Cesium 子类字段）；`WaterSurfaceEntry.options` `Record<string,unknown>`→`WaterSurfaceOptions`；`_screenSpaceEventHandler` 伪结构→`ScreenSpaceEventHandler`（含 `computeViewRectangle(scene.globe)`→`globe.ellipsoid` 参数修正）；④ instance unknown 窄化（GeoJsonDataSource typeof 守卫/Entity 数组断言/ImageryLayer-DataSource 容器尝试） | **vue-tsc exit=0** | revert（改动集中在 CesiumRenderer/MapRenderer 两文件） |
| C-2 | OLRenderer un 注销 4 处 as any | listener 声明 `(evt:unknown)=>void` → `EventsKey['listener']`（OL 宽签名），4 处 `as any` 移除 | **vue-tsc exit=0** | revert |
| C-9 | restoreSettings `Record<string,any>` | `Record<string,unknown>` + 字段级守卫（selected/importance 类型窄化） | **vue-tsc exit=0** | revert |

**类型债收口结果**：全前端（排除测试）`: any` 从 30+ 处降至 **1 处**——`applyGeoJsonDataSourceStyle` 的 entity: any，有明确注释（Cesium 运行期 Property 鸭子访问，属 2.6 合理使用场景）。

## 三、批次信息

- 提交：`fix: 审查问题批次4（选址坐标防御/bbox统一/错误信封/缓存键/渲染器类型债收口）`
- 涉及：scoringService.js / forecastService.js / crs.ts / CesiumRenderer.ts / MapRenderer.ts / OLRenderer.ts / SiteAnalysisControlPanel.vue / tools（fix-renderer-any、fix-ol-un-any）
- 遗留（批次5+）：专项2 F-5~F-8、专项5 副-03/04/14/20-21/25、专项6 副-02/03/04、专项1 P2-1/P2-2/P2-4/P3-1、S7 样式批、模拟 9 条 P3、台账可修项。
