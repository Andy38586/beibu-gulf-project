# 00-修复记录 · 批次5（2026-08-14）

> 批次1-4 见同目录。

## 一、已修复（11 项）

| # | 原编号 | 问题 | 改动 | 验证 | 回滚 |
| --- | --- | --- | --- | --- | --- |
| 1 | F-5 | CesiumRenderer errorEvent 匿名监听无 remover | 两处回调提为具名函数（imageryErrorHandler / hillshadeErrorHandler），注释说明 provider 生命周期与图层绑定（无独立泄漏路径） | typecheck ✓ | revert |
| 2 | F-6 | perfReporter entries 无限增长 | dev 明细 entries 环形上限 MAX_DEV_ENTRIES=1000（两处 push 后 shift） | 代码审查 | revert |
| 3 | F-7 | forecastStore.dataCache 死状态（无写路径，currentData 恒 null） | **移除** dataCache/currentData（类型/state/restoreState/reset/导出），ForecastPage 快照序列化同步，测试断言改 requestCache.size；import 清理 | **typecheck ✓** | revert（涉及 store/页面/测试三文件） |
| 4 | F-8 | ADR8 备注过期 | 03 ADR8 更新：useLatestRequest 收敛表述 + useApiRequest 基础层分工 + z071 路由级取消未做 | 与实现对照（useLatestRequest 4 处实例化 ✓） | revert |
| 5 | 副-02 | shared 深路径导入 17+ 文件 | shared/index.ts 补 `export * from './utils/perfReporter'`；8 个 shared 外部文件 9 处 `@/shared/utils/*` → `@/shared`（01 §五.10 桶入口） | **typecheck ✓** | revert |
| 6 | 副-04(专项6) | 根缺 .env.example | 新增 `.env.example`（VITE_DATA_SOURCE/API_BASE/TIANDITU_KEY 占位 + 后端变量注释） | 文件 | 删除 |
| 7 | 副-14 | plansController 0% 覆盖 | 新增 `plansController.test.js` 17 用例（CRUD/鉴权 403/404/400/409/204） | **vitest 17/17 通过** | 删除文件 |
| 8 | 副-25 | business 三模块无 README | 新增 site-selection / forecast / flood-analysis 三个 README（职责/入口/数据流/禁止/已知） | 文件 | 删除 |
| 9 | P2-1 | API契约文档 §5 缺端点 | 补 `GET /flood-online/api/flood/impact`（在线设施影响评估）；核对其余 20 端点已齐全 | 与 routes 对账（check-endpoints.mjs） | revert |
| 10 | 副-04(专项5) | 三态覆盖不全 | **裁决部分已解决**：FloodAnalysisPage 现 L85-86/124/144 均有 showWarning + logger.warn（c036 错误呈现已落地）；errorHandler 重试按钮仍缺 → 演示场景 toast 足够，记录可接受 | 代码取证 | — |
| 11 | 副-03(专项6) | core 业务耦合（LayerControlPanel 标签图标映射） | **裁决 v3 推迟**：图标映射数据驱动需 manifest/图层元数据扩展，与 a066 命名空间化同批（v3 阶段2 热插拔） | — | — |

## 二、已裁决（专项1 剩余）

| 原编号 | 裁决 | 依据 |
| --- | --- | --- |
| P2-2 | **不修** | `buildCoverageGeoJson` 命名符合 03 §1.2（动词+对象+细节）；落位 composable 内属模块内部实现，无跨层违规 |
| P2-4 | **已注明，不修** | base.ts affectedFacilities 计数/数组同名不同义已有语义注释（L51）；ports.json id（000001）vs berth.json portId（qinzhou）是不同键体系（港口目录 vs 预测数据键），统一收益低风险高 |
| P3-1 | **已解决** | .env.local 当前为真实配置（VITE_DATA_SOURCE=online，无 mock 残留）；loadStatic 注释正常 |

## 三、批次信息

- 提交：`fix: 审查问题批次5（死状态移除/监听具名化/深路径收口/env示例/plans测试/模块README/契约补录）`
- 涉及：CesiumRenderer / perfReporter / forecastStore+ForecastPage+测试 / 03 文档 / shared/index + 8 文件 / .env.example / plansController.test / business README×3 / API契约文档
- **验证**：backend 全量 **207/207 通过**；typecheck ✓。
- 遗留（批次6）：S7 样式批可安全项（S7-10 SOP、S7-26 统计脚本、S7-01/03/11 绑定注释、S7-16/18 token 化）、模拟 9 条 P3 裁决、台账可修项（a064/b027/b065/c037/c038/d051/d054/d080/d081/d082/z038/z076/z077/z103/z105/a065/a066）、副-20/21 ts-prune（需 npm i）。
