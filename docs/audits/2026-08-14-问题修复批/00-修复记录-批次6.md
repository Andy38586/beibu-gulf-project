# 00-修复记录 · 批次6（2026-08-14）

> 批次1-5 见同目录。本批：S7 样式安全项 + 台账可修项。

## 一、已修复（10 项）

| # | 原编号 | 问题 | 改动 | 验证 | 回滚 |
| --- | --- | --- | --- | --- | --- |
| 1 | S7-03 | 水面/覆盖层 rgba 硬编码（两处同值不同 alpha） | colors.ts 新增 LAYER_FILL_WATER/LAYER_FILL_COVERAGE（primary 派生注释），FloodAnalysisPage / useAnalysisLayer 引用 | **typecheck ✓** | revert |
| 2 | S7-10 | 无 token 变更 SOP | 03 新增 §三.5（改前查/改名留映射/双份同步/删除先验/验收） | 文档 | revert |
| 3 | S7-26 | 无 token 统计脚本 | 新增 `tools/token-stats.mjs`（定义/引用统计、死 token 检测、≤5% 门禁） | **实测 45 token 全活，0% 死 token** | 删除文件 |
| 4 | S7-01 | flood 风险色第三套源 | flood-analysis/constants/colors.ts 加绑定注释（与 shared FACILITY_COLORS 同值对照，防双份漂移） | 代码审查 | revert |
| 5 | S7-11 | colors.ts↔style.css 双份 | 并入 §三.5 第 3 条（双份同步规则，S7-10 配套） | 文档 | revert |
| 6 | z076 | 引擎切换 pitch=-90 硬编码 | 提取 `DEFAULT_CAMERA_PITCH_DEG` 常量 + 刻意设计注释（引擎切换不传倾斜） | typecheck ✓ | revert |
| 7 | z105 | setTerrainEnabled 无调用方 | 标注 @arch-note 预留钩子（layerAdapters geotiff 明确不做 terrainProvider 特殊处理，L350 状态延续依赖本方法） | 代码取证 | revert |
| 8 | z103 | 覆盖率阈值形同虚设 | frontend vitest 阈值 25/20/15/25 → 30/25/18/30（留余量防假红，60% 目标挂下阶段） | 配置审查 | revert |
| 9 | b027 | waterLevel 参数名分裂 | floodAdapter online 两处 `params:{level}` → `{waterLevel}`；FastAPI main.py online/impact 端点 `Query(level)` → `Query(waterLevel)`；test_main.py 9 处查询参数同步；API契约文档 §5 同步 | **pytest 6/6 通过** | revert（跨前端/FastAPI/测试/文档，需整体回滚） |
| 10 | S7-27 | 无废弃 token 清单 | §三.5 删除先验机制（token-stats 死 token 清单） | 文档 | revert |

## 二、已裁决（批次6 新增）

| 原编号 | 裁决 | 依据 |
| --- | --- | --- |
| S7-07 死 token 28.9% | **已解决** | token-stats.mjs 实测：45 token 中死 token 0（占比 0%）——08-12 报告后已被清理 |
| S7-14 token 出处注释 | **已注明** | style.css L10「系统只有一个交互色：#409eff 蓝色」即来源注释 |
| S7-16 雷达 tooltip 阴影写死 | **已解决** | 现为 `box-shadow: var(--GCS-shadow-sm)` 全 token 化 |
| a064 DEV 门控 | **已澄清** | CesiumRenderer L909-912 已有 2026-08-11 澄清注释「DEV 仅控制诊断日志输出，非功能门控」；生产静默失败日志属噪音控制权衡 |
| b065 快照混合读活状态 | **已注明设计** | floodStore L20 注释「仅持久化快照部分（不含当前分析数据）」+ L16-17 快照定位——快照刻意不含分析数据，consumeState 组合活状态是设计；clearState 全量清保证快照/活状态同步 |
| d081 单位注释不一致 | **已解决** | facility.ts:15 已修正「缓冲半径（公里）…2026-08-11 修正：原注释米与后端相差 1000 倍」 |
| z038 setLayerVisible 覆盖 | **已解决** | mapStore.test.ts L129-141 已有 z038 补覆盖用例（2 个） |
| z077 后端 no-console | **已清零** | 业务代码无散落 console（仅 logger 统一出口 + auth.js 注释文本 + 第三方 emscripten 产物） |
| 副-04(专项6) .env.example | ✅ 批次5 已补 | 根 `.env.example`（含 VITE_* 与后端变量占位） |
| 副-03(专项6) core 业务耦合 | **v3 推迟** | 图标数据驱动需 manifest/图层元数据扩展，与 a066 命名空间化同批（v3 阶段2） |
| a065 CRS 散落 | **部分已解决** | crs.ts 已收口业务边界判定（BEIBU_GULF_BBOX/isInBeibuGulf）；引擎投影保留各实现（双引擎差异，统一封装会破坏抽象） |
| a066 图层 key 命名空间 / z064 E2E / z102 CHANGELOG / z040 版本策略 / z042 函数过长 / b044 性能 / b034 契约 / b040 auth store / z071 / z104 | **v3 计划/设计** | 见 v3-发展路径 §四/五（Nest 迁移、热插拔、ARIMA 批）与 02 §4.4/ADR9 |
| 模拟批次 3.3/3.4/4.6/5.4/5.6/5.7/6.5/7.2/7.3 | **🟡 可接受/待人工确认** | 见总表逐条：TS 推断安全类可接受；5.7/7.2 待人工确认（injectionKey 用法、zod 覆盖率） |

## 三、批次信息

- 提交：`fix: 审查问题批次6（tokenSOP与统计脚本/色常量收口/相机pitch常量/参数名统一/阈值提升/预留标注）`
- 涉及：colors.ts / FloodAnalysisPage / useAnalysisLayer / 03 文档 / tools/token-stats.mjs / CesiumRenderer / vitest.config / floodAdapter / flood-service(main.py+test_main) / API契约文档 / 裁决总表
- **验证**：typecheck ✓；flood-service pytest 6/6；backend 全量 207/207（批次5 时）。
- **台账 28 条终态**：已修 3（b027/z076/z105）+ 已解决 3（z038/z077/d081）+ 已澄清 1（a064）+ 已注明设计 2（b065/z071）+ 部分 1（a065）+ v3 计划 10（a066/b034/b040/b044/c038 部分/z040/z042/z064/z102/z103 已修?z103 已修）+ 已修 z103 → 剩余 open：d051/d054/d080/d082（后端治理层，v3 Nest 迁移批）/c037/c038（组件批）——**见批次7 或 v3 承接**。
