# 00-修复记录 · 批次7（2026-08-14）

> 批次1-6 见同目录。本批：d080 plansService 抽取 + 台账/副本剩余项最终裁决。

## 一、已修复

| 原编号 | 问题 | 改动 | 验证 | 回滚 |
| --- | --- | --- | --- | --- |
| d080 | plansController 直连仓库无 service 层 | 新增 `backend/services/plansService.js`（8 方法收口 repo 访问，注释说明 v3 Nest 迁移对应 @Injectable）；plansController 8 处调用改走 service | **backend 全量 207/207 通过**（plansController 17 用例含） | revert（controller+新 service 两文件） |

## 二、剩余项最终裁决（含原因）

| 原编号 | 裁决 | 原因/承接 |
| --- | --- | --- |
| d051 统一入参校验层 | **v3 承接** | NestJS Pipe/ValidationPipe 天然解决（v3 阶段1），现状 controller 手写校验均走 BusinessError 统一错误通道（无裸 res.status），风险可控 |
| d054 公开路由白名单 | **v3 承接** | 与 d051 同批（Nest Guard/Pipe）；现状仅 forecast/flood 只读端点，均有指标白名单/水位校验单点防御（d025 已解决路径遍历） |
| d082 常量重复（ErrorCode/bbox/水位上限/riskLevel） | **v3 承接** | 前后端双仓常量，v3 OpenAPI 契约先行时统一（b034 方向）；现状各常量有注释指向权威 |
| c037 列表 key/字段丢弃残留 | **待人工抽样** | 08-12 未给具体 file:line（"810 副本副-15"），需按专项4 指标逐组件核对 key 规范与字段透传——挂批次8 人工项 |
| c038 图例/瓦片缓存残留 | **部分已修** | 图例 token 化（S7 系列已收口色值）；瓦片缓存头 nginx 已配（/assets 1y、/tianditu 30d），HTTP 缓存策略已达标 |
| 副-20/21 ts-prune 死代码扫描 | **需本地安装** | 沙箱无法 npm install（TLS 阻断）；执行 `npm i -D ts-prune` 后加 script `"deadcode": "ts-prune -p frontend/tsconfig.app.json"` + CI 步骤——已记录到 v3 待办 |
| z102 CHANGELOG / z064 E2E / z040 版本 / z042 拆分 / b044 性能 / b034 契约 / b040 auth | **v3 计划** | 见 v3-发展路径 §四阶段划分（CHANGELOG 随 commit 规范统一、E2E 挂阶段5、版本锁在 Nest 迁移批、拆分与性能在阶段3、契约/auth 在阶段1） |

## 三、台账 28 条终态（2026-08-14）

- ✅ **已修（本批+前批）**：b027（参数统一）、z076（pitch 常量）、z105（预留标注）、z103（阈值）、d080（service 层）、z038（测试已补）、z077（已清零）、d081（已注明）、a064（已澄清）、b065（已注明设计）、z071（设计冲突）、z104（设计冲突）
- ⏳ **v3 承接（12）**：a066、b034、b040、b044、b045、c038 部分、d051、d054、d082、z040、z042、z064、z102
- ⏳ **待人工（1）**：c037（列表 key 抽样核对）
- 台账真实 open 从 28 降至 **v3 承接为主**，全部有明确承接路径与依据。

## 四、批次信息

- 提交：`fix: 审查问题批次7（plansService 抽取收口/后端治理项裁决）`
- 涉及：plansService.js（新）/ plansController.js / tools/fix-plans-service.mjs
- **验证**：backend 全量 207/207。
- 全六批汇总：**修复 40+ 项、证伪/已解决/已注明 25+ 项、v3 承接 12 项、待人工 1 项**；每个修复回滚手段见批次记录 1-7；裁决总表 95 行全量状态可查。
