# 00-修复记录 · 批次8（2026-08-14）

> 批次1-7 见同目录。本批：S7 样式批剩余 + c037 精确核对 + ts-prune 环境裁决。

## 一、已修复（7 项）

| # | 原编号 | 问题 | 改动 | 验证 | 回滚 |
| --- | --- | --- | --- | --- | --- |
| 1 | S7-08/23 | GCSModal/GCSToast 引用未定义 --GCS-cell / --GCS-font-size-body（静默 fallback） | style.css :root 补正式定义（--GCS-cell: 80px 与 layout CELL_PIXEL 同源注释；--GCS-font-size-body: 14px），组件 fallback 变正式 token | 代码审查 | revert |
| 2 | S7-04 | UnifiedMap z-index 槽位硬编码（1/100/90） | 4 处换 var(--GCS-z-map / -map-status / -map-overlay) | 与 token 清单对照 | revert |
| 3 | S7-12 | 剖面面积渐变 rgba 硬编码 | colors.ts 新增 PROFILE_AREA_STOP_STRONG/WEAK（success 绿 #67C23A 派生注释），WaterLevelProfilePanel 引用 | **typecheck ✓** | revert |
| 4 | S7-06 | 自绘按钮无键盘 focus 环 | style.css 全局 `:focus-visible { outline: 2px solid var(--GCS-border-focus); outline-offset: 2px }` | 样式审查 | revert |
| 5 | S7-21 | placeholder 对比度不足（亮 1.75:1 / 暗 2.98:1） | 两主题 --GCS-text-placeholder 提至与 text-muted 同档（亮 #909399 ~3.2:1 / 暗 #7a8694），注释留痕 | 对比度计算 | revert |
| 6 | S7-19 | 滑块 thumb 尺寸不一致（14px vs 18px） | ForecastControlPanel conf-slider 统一 16px + primary 边框/阴影（与 t-slider 视觉对齐） | 样式审查 | revert |
| 7 | S7-18 | 占位框边框色写死 rgb(255 255 255 / 20%) | FloodAnalysisPage 占位框改 `1px dashed var(--GCS-border-default)` | 代码审查 | revert |

## 二、已裁决（批次8 新增）

| 原编号 | 裁决 | 依据 |
| --- | --- | --- |
| S7-17 散落字号 18/15px | **可接受** | 唯一命中为 GCS-drawer 关闭按钮图标字号（18px），非文本档位散落；无 font 档位 token 体系外的违规 |
| S7-20 dark 块 token 不对称 | **已解决** | dark 块（L82-92）已有 primary/error/success/warning/danger 全定义，与亮色对称（08-12 报告后已补齐） |
| S7-13 半透明叠层 | **部分已收口** | --GCS-bg-overlay / --GCS-bg-panel-translucent 已定义并广泛使用；残余为加载遮罩/错误条特殊语义（rgb 85-90%），可接受 |
| S7-22 底图暗色变体 | **记录不修** | 功能性增强（brightness 滤镜）非缺陷，跨渲染器改动收益低，v3 地图配置重构时评估 |
| c037 列表 key/字段丢弃残留 | **已解决** | 精确检测（v-for 后 4 行内 :key）：全前端 19 处 v-for **缺 key 0**；早前 grep 单行匹配误报 |
| 副-20/21 ts-prune | **环境受限** | `npm i -D ts-prune` 失败：依赖 npm-normalize-package-bin@6 engines 要求 node ^22.22.2/^24.15/>=26，实际 node v25.5.0 不在范围（0.10.3 旧版同因）——非沙箱问题，npm 生态对 node 25 兼容缺口；记录 v3 待办（等生态适配或锁 node 版本后接入） |

## 三、批次信息

- 提交：`fix: 审查问题批次8（token补定义/z-index槽位/渐变与边框token化/focus环/placeholder对比度/thumb统一）`
- 涉及：style.css / UnifiedMap.vue / colors.ts / WaterLevelProfilePanel.vue / ForecastControlPanel.vue / FloodAnalysisPage.vue
- **验证**：typecheck ✓。
- 全八批终态：**修复 47 项 / 证伪·已解决·已注明 30 项 / v3 承接 13 项 / 环境受限 1 项（ts-prune）/ 待人工 0 项**——副本 95 行 + 台账 28 条全部有终态裁决。
