# T0.2 分支门禁基线复验 实施日志

- 执行者：GLM-5.3-Flash
- 日期：2026-09-01
- 任务卡版本：docs/v3-施工手册.md 初版

## 做了什么

- 确认无 data 提交风险后，按 CI 翻车复盘 §三 口径清空 NODE_OPTIONS（规避 safe-delete 守卫假红），执行 `npm run ci:local`。
- 全链 exit 0：format:check / lint / stylelint / cruise / typecheck / 前端 vitest --coverage / backend vitest --coverage / build（rolldown ✓ built in 6.78s）。
- 确认 cruise 配置扫描范围 `includeOnly: ^(frontend/src|backend)`——`backend/nest` 建成后自动落入扫描，T1.1 需追加 Nest 专属规则（DB 访问收口），见 T7.1。

## 偏离与理由

- 无偏离。

## 问题与测试固化

- 无（纯验证任务）。

## 验收结果

- `NODE_OPTIONS= npm run ci:local` → exit 0，全绿（ci.yml 同集）。
- 环境备注：本机 3000（Express dev）/ 5173（Vite dev）有用户自启服务在跑；本任务与其共存无冲突，后续提交前按三连败规则 1 用 `git status` 复核 `backend/data`。

## 遗留

- `npm audit` 本地 404（npmmirror 环境差异），该项由 CI audit job 兜底，与既有口径一致。
- Docker Desktop 冷启动缓慢，T0.1 的 PostGIS 复活仍在等待 daemon 就绪（不影响本任务结论）。
