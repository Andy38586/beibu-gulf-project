# 已解决问题

> **定位**: 所有已修复的问题归档。只增不改。面试时可展示迭代能力。
> **最后更新**: 2026-07-27

---

## 一、架构修复

| # | 日期 | 问题 | 修复 |
|---|------|------|------|
| R01 | 2026-07 | core→business 反向依赖（UnifiedMap 曾 import useAnalysisLayer） | 业务层通过 mapStore.registerAnalysisHandler 注入 |
| R02 | 2026-07 | MapRenderer 接口不完整 | 补全 renderer.ts 接口定义 + renderers.d.ts 类型声明 |

## 二、工程规范建设（2026-07-27）

| # | 问题 | 修复 |
|---|------|------|
| R03 | CSS 变量体系缺失，`#409eff` 散落 19 个文件，`--gcs-cell-48px: 0.6cell` 无效 CSS | 建立 `--gcs-*` 24 个全局 Token，全部核心/共享组件改用 `var()` 引用 |
| R04 | TypeScript 迁移进度 ~22%（仅 types/ 和 composables） | 9 个 Store + 4 个 Service 全部迁移 .ts，30 个 Vue 组件添加 `lang="ts"`，新增 4 个类型文件 + renderers.d.ts |
| R05 | 三套 lint 工具并存（ESLint + oxlint + Prettier），职责不清 | 卸载 oxlint，`eslint-config-prettier` 分流格式→Prettier、质量→ESLint |
| R06 | 无 `.prettierrc`，Prettier 用默认配置 | 创建 `.prettierrc`（semi:false, singleQuote:true, printWidth:100） |
| R07 | types/plan.ts 中 3 处 `any` | 替换为 `Record<string, unknown>` |
| R08 | 缺 API 契约文档 | 编写 31 端点完整契约（`docs/API契约文档.md`） |
| R09 | 缺项目编码规范 | 编写 `CODING_STYLE.md`（10 节，地理术语优先） |
| R10 | 缺技术债务清单 | 编写 P0/P1/P2 三级技术债务清单 |

## 三、项目配置修复

| # | 日期 | 问题 | 修复 |
|---|------|------|------|
| R11 | 2026-07 | console 调试输出未清理 | 接入统一 logger（`shared/utils/logger.js`），生产环境自动静默 |
| R12 | 2026-07 | 天地图 KEY 硬编码 | 改为从环境变量读取，缺失时显式报错 |

## 四、构建优化

| # | 日期 | 问题 | 修复 |
|---|------|------|------|
| R13 | 2026-07-27 | Element Plus 未独立分包 | 新增 `ui-vendor` 分包，减少主 bundle |
| R14 | 2026-07-27 | 无打包分析工具 | 引入 `rollup-plugin-visualizer`，新增 `build:analyze` 脚本 |

---

> **关联**: 待修复的问题见 `待解决问题.md`
