# 已解决问题（代码验证版 · 2026-07-29 复核）

> **定位**：所有经**真实代码取证**确认已修复的问题归档。只增不改（除非重新裁定）。面试时可展示迭代与自查能力。
> **方法**：每条附 `file:line` 证据；本次对旧报告中标"已修复"的项做了独立复验，绝大多数成立，少数（见 `待解决问题.md` 第六节）被发现不实/过时并已退回待解决或订正。
> **关联**：未修复问题见 `待解决问题.md`；面试审计见 `面试官视角.md`；设计约束见 `开工前必读/`。

---

## 一、架构与接口修复（代码验证）

| # | 原编号 | 问题 | 修复证据（2026-07-29 实跑） |
|---|--------|------|------------------------------|
| R01 | — | core→business 反向依赖（UnifiedMap 曾 import useAnalysisLayer） | 业务层通过 `mapStore.registerAnalysisHandler` 注入；dependency-cruiser 无 `visualization→business` 违规。 |
| R15 | D02 | 零 CI | `.github/workflows/ci.yml` 存在（1650B），steps 为 `format:check → lint → typecheck → test → build`。✅ 本次 `ls` 确认文件存在。 |
| R16 | D04 | 双认证矛盾（同时发 Bearer + Cookie） | `frontend/src/shared/composables/useApiRequest.ts:56` 注释"认证统一通过 HttpOnly Cookie（credentials:'include'），不发 Bearer header"；全文件 grep `Authorization|Bearer|setRequestHeader` **无匹配**。✅ 单通道 Cookie-only。 |
| R17 | D05 | 坐标字段命名不统一 | `frontend/src/types/crs.ts` `normalizePoint` 统一 `lng/lat` + 兼容 `lon/longitude`；渲染器 `?? item.lon ??` 仅为兼容。 |
| R18 | D10 | 无空间索引（rbush） | `frontend/src/shared/utils/spatialIndex.ts` 已实现 rbush 视口裁剪（阈值 1000）+ 单测。 |
| R19 | B03 | 选址分析用户输入错误返 500（应 400） | `backend/controllers/siteAnalysisController.js:10/19/32/43/56` 均 `res.status(400)`。✅ 本次读取确认。 |
| R20 | B04 | 业务错误以 HTTP 200 返回 `{error}` | `siteAnalysisController.js:72` 返 422、`:78` 返 `error.status`、`:81` 返 500，不再以 200 包错误。✅ |
| R21 | B06 | 评分权重未校验（可负数/字符串/NaN） | `siteAnalysisController.js:55-56` 校验"权重应为 0-10 之间的数字"，非法返 400。✅ |
| R27 | A05 | visualization 反向依赖 business（useRadarChart→facilityConfig） | `frontend/src/shared/constants/colors.ts` 新增 `FACILITY_COLORS_MAP` 单一事实源；`useRadarChart.js:19`/`RadarScoreTooltip.vue:13` 改从 shared 取色。 |
| R28 | D03/E07 | floodState 未用持久化工厂 | `frontend/src/stores/floodState.ts` 全量 `createPersistedState({...})` 封装；`floodPolygons` 改 `shallowRef([])`。 |
| R29 | E36 | forecastState 的 `unknown` 槽 | `cacheData(time,data)` 入参收窄为 `ForecastSeries`；`dataCache` 改 `shallowRef(new Map())`。 |
| R30 | D09 | `layerCatalog` 用 `ref` 存数组（应 `shallowRef`） | `frontend/src/stores/mapStore.ts:85` `const layerCatalog: ShallowRef<LayerEntry[]> = shallowRef([])`；所有写操作不可变更新（`:154/:170/:183/:259/:271/:298/:308/:318/:344/:371`）。✅ 本次 grep 确认。 |
| R31 | E02 | mapStore 双图层注册机制 `entry.show!` 崩溃 | `mapStore.ts` 去除 `entry.show!`/`entry.hide!` 非空断言（`grep 'entry.show!'\|'entry.hide!'` **无匹配**）。✅ |
| R32 | E20 | BusinessLayerManager.setVisible 绕过 Pinia action 直改 `catalogEntry.visible` | `mapStore.ts:363` 新增 `setLayerVisible(key,visible)` action；`BusinessLayerManager.js:172` `this._mapStore.setLayerVisible(key,visible)`。✅ 本次 grep 确认。 |
| R33 | B02 | 注册 TOCTOU 竞态 → 重复用户名 | `backend/services/userService.js:18` `return sequential(async () => {...})` 互斥锁串行化 + `:21-23` 锁内二次查重（注释"锁内查重，消除 TOCTOU 竞态"）；`authController.js:53-55` catch `BusinessError` 返 409。✅ 应用层串行化已消解竞态（原待解决问题.md 标 P1 OPEN 已过时，2026-07-29 复核迁入）。 |
| B01 | B01 | markers 水平 IDOR（GET /api/markers/:id 任意登录用户读他人标注） | `backend/controllers/markersController.js:23-24` `if (marker.userId !== req.user.id) throw new BusinessError(ErrorCode.FORBIDDEN, '无权查看他人标注')`（404 先于 403，防存在性探测）；`__tests__/markersController.test.js` 新增本人/他人/不存在/getAll/createOne 5 组回归（全绿）。✅ 2026-07-29 阶段一落地，原 `待解决问题.md` P0 唯一致命漏洞已闭环。 |
| R34 | 6.5 | 令牌无吊销（logout/改密后旧 token 仍有效 7d） | `backend/data/users.json:7` 加 `tokenVersion:0`；`backend/middleware/auth.js:53-70` 校验 `user.tokenVersion !== decoded.tokenVersion` → 401；`authController.js:105-106` logout 自增版本；`middleware/__tests__/auth.test.js` 增补令牌失效用例（全绿）。✅ 2026-07-29 阶段六 6.5 决策 A 落地。 |

---

## 二、前端模块修复（代码验证）

| # | 原编号 | 问题 | 修复证据（2026-07-29 实跑） |
|---|--------|------|------------------------------|
| B09 | B09 | ProfilePage `loadPlans()` 双触发（watch+onMounted） | `frontend/src/views/ProfilePage.vue` grep `onMounted` **无匹配**，仅 `watch(user, immediate)` 触发 loadPlans。✅ |
| B12 | B12 | scoring `Math.min` POI 量大时栈溢出 | `backend/services/scoringService.js` grep `Math.min` **无匹配**，原路径已重构。✅ |
| C02 | C02 | 数据流断裂：forecastState `dataCache` 无写入 | `frontend/src/stores/forecastState.ts:30` `dataCache: ShallowRef<Map<string,ForecastSeries>>`；`:32-34` computed 读取；`:52-57` `cacheData` 正确 get/set。✅ 本次读取确认。 |
| D06 | D06 | 业务图层数据生命周期缺陷（引擎切换丢图层 + data 不持久化） | `BusinessLayerManager.js:115` 持久化 data；`:142` `reapplyAll(renderer)`；`App.vue:103` 监听 currentRenderer 调 `businessLayerManager.reapplyAll(renderer)`。✅ 本次 grep 确认 reapplyAll 存在且被调用。 |
| E12 | E12 | ErrorBoundary 仅包 RouterView，地图崩溃可能白屏 | `App.vue:117-119` `<ErrorBoundary>` 包 `<UnifiedMap>`；`:121-125` 包 `<RouterView>`。✅ 本次读取确认。 |
| E14 | E14 | 路由守卫鉴权依赖内存 token，与 HttpOnly Cookie 主通道脱节（功能级阻断） | `frontend/src/router` grep `beforeEach|requiresAuth` **无匹配**，路由全公开；原"内存 token 弹回登录"前提已不存在（设计变更）。⚠️ 注意：引入"前端无守卫"低危接受项，见 `待解决问题.md` E14 残留 / 安全报告维03。 |
| E15 | E15 | useAuth 在组合式函数内无条件挂生命周期钩子（与 E14 同源） | 无路由守卫调用 `useAuth`；`useAuth.ts:197/205` `onMounted/onUnmounted` 现仅在 App.vue 组件上下文执行。✅ |
| E17 | E17 | Cesium 点图层 entity id 重复 `-undefined` 崩溃 | `CesiumRenderer.js:442` `` `${id}-${item.id \|\| item.name \|\| index}` `` 有 `|| index` 兜底，避免 `-undefined` 碰撞。✅ |
| E19 | E19 | 加载失败静默吞掉（仅超时提示） | `UnifiedMap.vue:120-124` 新增 `else` 分支：非超时失败显示"地图数据加载失败，请刷新重试"。✅ |
| E22 | E22 | `vite.config.js` `build.target:'es2015'` 与 browserslist 矛盾 | `frontend/vite.config.js:68` `target: 'es2020'`。✅ |
| E25 | E25 | `UnifiedMap.setupLayers` 直接读 `renderer._layers` 私有字段 | `UnifiedMap.vue:195-201` `hasLayer(renderer,id)` 优先 `renderer.hasLayer(id)` 公开 API，仅降级读 `_layers`（私有访问已封装）。✅ |
| E32/F01 | E32/F01 | 硬编码 hex 残留 / WaterLevelProfilePanel 颜色常量带引号失效 | `WaterLevelProfilePanel.vue:238/242/256/261` 用 `PROFILE_COLORS.safe/.water`（无引号）；全部 hex 收敛至 `shared/constants/colors.ts`。✅ 本次读取确认。 |
| E33 | E33 | 浸没分析 `AffectedFacility` 字段名未统一 `lng/lat` | `floodAdapter.ts:164-165` `lng: raw.lng ?? raw.longitude ?? 0`、`lat: raw.lat ?? raw.latitude ?? 0` 归一。✅ |
| E34 | E34 | `forecastAdapter.ts` 死代码（无运行时代码引用） | `forecastAdapter.ts` 被 `ForecastPage.vue:31/72/138`、`useForecastLayer.js:12/115`、`useForecastRequest.ts:6/11` 引用，且自有单测。✅ 本次 grep 确认。 |
| E35 | E35 | `CesiumRenderer` 直写外部单例私有 `_baseLayers` | 全仓 grep `_baseLayers` 仅 `CesiumRenderer.js` 内部使用 + `renderers.d.ts:103` 类型声明；外部无直写，已有公开 `setBaseLayers/getBaseLayers`。✅ |
| C03 | C03 | registerLayer 重注册回调累加（潜在） | `frontend/src/stores/mapStore.ts:158` `if (show) next.show = show`（改为替换语义，防重复执行）；首次注册 else 分支不变。✅ 2026-07-29 阶段五落地。 |
| E06 | E06 | CRS 边界守卫零调用（潜在） | `frontend/src/services/mapDataService.ts:5` 导入、`getPorts():63` 调 `isInBeibuGulf` 过滤越界港口（DEV `:65-66` warn）；assertCRS 仍仅定义未接（冗余非缺陷）。✅ 2026-07-29 阶段六 6.4 决策 A 落地。 |
| E08 | E08 | 魔法数字散落（裸 `10000` 超时） | 全部抽常量：`mapDataService.ts:9 FETCH_TIMEOUT_MS`、`floodAdapter.ts:30 FETCH_TIMEOUT_MS`、`UnifiedMap.vue:58 LOAD_TIMEOUT_MS`；此前 `useApiRequest.ts:26 API_TIMEOUT_MS`、`useBoundaryLayer.js:21 CACHE_EXPIRY` 已抽。✅ 2026-07-29 阶段二(E08-a)+阶段四(E08-b)落地，前端无裸 `10000` 超时。 |
| E18 | E18 | Cesium addGeoJsonLayer 给 entity.properties.featureType 赋值未判空 | `frontend/src/core/map/renderers/CesiumRenderer.js:642-643` `if (!entity.properties) entity.properties = {}` 后赋值。✅ 2026-07-29 阶段三落地。 |
| E21 | E21 | AbortError 误判为超时 | `frontend/src/shared/composables/useApiRequest.ts:123` 区分 `controller.signal.aborted`（内部超时→TIMEOUT）vs 外部 signal 取消（→REQUEST_FAILED，`:10` 已定义）。✅ 2026-07-29 阶段四落地。 |
| E23 | E23 | Cesium creditContainer 隐藏版权（可能违反 attribution 许可） | `frontend/src/core/map/renderers/CesiumRenderer.js` 已移除 `creditContainer: document.createElement('div')`，恢复默认 credit 容器。✅ 2026-07-29 阶段六 6.2 决策 A 落地。 |
| E24 | E24 | OLRenderer 热力图未用 normalizePoint | `frontend/src/core/map/renderers/OLRenderer.js:17` 导入、`addHeatmapLayer:348`/`updateHeatmapLayer:395` 改用 `normalizePoint(f.geometry.coordinates)`，与 addPointLayer 对齐。✅ 2026-07-29 阶段三落地。 |
| E29 | E29 | 生产路径 console.error 未移除（复核重开） | `frontend/src/services/mapDataService.ts` 4 处 console.error（现 `:72/:77/:115/:120`）均加 `import.meta.env.DEV` 守卫；仅 `:81` console.warn 原有守卫。✅ 2026-07-29 阶段二落地，E29 重新闭环。 |
| E31 | E31 | 后端反向依赖前端静态目录 | `backend/controllers/forecastController.js:12`（注释）、`backend/services/forecastService.js:7` `DATA_DIR = join(__dirname, '../data/forecast')`；7 个预测 JSON 迁至 `backend/data/forecast/`，前端 mock 夹具独立保留。✅ 2026-07-29 阶段六 6.3 决策 A 落地。 |

---

## 三、工程规范 / 构建优化（历史修复，状态稳定）

| # | 问题 | 修复 |
|---|------|------|
| R03 | CSS 变量体系缺失，`#409eff` 散落 19 文件 | 建立 `--gcs-*` 全局 Token，核心/共享组件改用 `var()` |
| R04 | TS 迁移 ~22% | 9 Store + 4 Service 迁移 .ts，30 Vue 组件 `lang="ts"` |
| R05 | 三套 lint 并存 | 卸载 oxlint，eslint-config-prettier 分流 |
| R06 | 无 `.prettierrc` | 创建（semi:false, singleQuote:true, printWidth:100） |
| R07 | types/plan.ts 3 处 `any` | 改 `Record<string, unknown>` |
| R08 | 缺 API 契约文档 | 编写 31 端点契约 |
| R09 | 缺编码规范 | 编写 `CODING_STYLE.md` |
| R10 | 缺技术债务清单 | 编写 P0/P1/P2 清单 |
| R11 | console 调试输出未清理 | 接入统一 logger（`shared/utils/logger.js`），生产静默 |
| R12 | 天地图 KEY 硬编码 | 改环境变量读取，缺失显式报错 |
| R13 | Element Plus 未独立分包 | 新增 `ui-vendor` 分包 + 按需引入；`ui-vendor` 116KB gzip、首屏总 payload 720→480KB gzip |
| R14 | 无打包分析 | 引入 `rollup-plugin-visualizer` + `build:analyze` |
| R23 | 预测请求无取消机制 | `useForecastRequest.js` 用 `AbortController` + `startTransaction()` + `cancelAll()` |
| R24 | 路由切换返回后可视化面板/图层空白 | `FloodAnalysisPage.vue:191/194` 调 `renderFloodAreas/renderAffectedFacilities` |
| R25 | 坐标偏差 | 诊断确认 `floodArea`/`facilityPoints` 坐标格式一致（WGS84 `[lng,lat]`），无需修改 |
| R26 | 硬编码 hex ~71 行 | 收敛至 `shared/constants/colors.ts`（RISK/FACILITY/CHART/LAYER_DEFAULTS/PROFILE/INSPECTION） |

---

## 四、历史移除项补录（旧待解决误留，已统一迁入）

| 原编号 | 问题 | 标记 |
|--------|------|------|
| B05 | 标记字段名不一致（note/description） | 已统一 `note` |
| B07 | plans/markers ID 用 `Date.now()` → 并发碰撞 | 改 `crypto.randomUUID()` |
| B08 | plans 先改内存后写文件，写失败缓存脏 | 构造新数组不原地修改 |
| B11 | 认证策略不一致（`/api/gcs/*` 全裸） | 已加 `router.use(authenticate)` |
| B13 | facilities 缓存永不过期 | 加 TTL 5 分钟 |
| — | `useFloodStore`/`@/stores/floodStore` 残留引用 | 改 `useFloodState`/`@/stores/floodState` |
| D07 | `/api/forecast/*` 无认证 | 前端 `meta.requiresAuth` 不要求认证，与后端对齐（预测数据视为公开为稳定决策）。✅ **2026-07-29 阶段六 6.1 决策 B 已闭环**：移除 `backend/routes/forecast.js` 的 `router.use(authenticate)` 及 `import { authenticate }`，后端恢复公开访问，与设计文档 4 处声明一致；前端 router meta 无需改。详见 `待解决问题实施计划/修复阶段6-用户决策项.md` 6.1。 |
| E28 | `floodAdapter` 模块顶层 `useApiRequest` 反模式 | 移除顶层调用，反模式消除 |
| F01 | WaterLevelProfilePanel 颜色常量带引号回归 | 重建 hex 收敛时修正引号（见 E32/F01） |
| — | 6.7 | `tools/throughput_model.cjs` 用途待确认（墓碑报告列"一次性脚本嫌疑"） | 2026-07-29 决策 A（保留 + 挂 npm script）：`package.json:21` 早已存在 `forecast:model`（实测跑通 exit 0，三港回测 MAPE 1.43%/1.84%/2.30%）；路径已修指 `backend/data/forecast/`；新增 `tools/README.md` 说明管道。✅ |

---

## 五、复核说明

- 本次独立复验**确认成立**的"已修复"项：R15、R16、R19、R20、R21、R27–R33、B09、B12、C02、D06、E12、E14、E15、E17、E19、E22、E25、E32/F01、E33、E34、E35（均有 `file:line` 或 grep 证据，见上表）。
- **复核中发现不实/过时、已退回或订正**（详见 `待解决问题.md` 第六节）：E29（称已修复，实则未修，重开）、E05（路径写错）、E18（行号过时）、墓碑报告 3 处 P1（users.json/gitignore 已修复；forecast 鉴权代码已加但与设计文档冲突，定性需修正，为未闭环设计冲突）、B02（原标 OPEN 已过时，userService 已用 sequential 锁消解，迁入本文件 R33）。
- **B01（markers 水平越权）已于 2026-07-29 阶段一修复**，证据见本文件 B01 行（getOne 归属校验 + 5 例回归测试）。本文件不再将其列为待解决。

> **2026-07-29 实施计划落地复核（代码验证）**：`docs/待解决问题实施计划/待解决问题修复-2026-07-29.md` 阶段一~五 + 阶段六 6.1–6.7 已全部落地，经真实代码复验确认新增已修复项：B01、C03、E06、E08、E18、E21、E23、E24、E29、E31、R34（令牌吊销）、6.7（throughput_model）。上述项已从 `待解决问题.md` 迁入本文件。
