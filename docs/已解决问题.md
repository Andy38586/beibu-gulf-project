# 已解决问题（重组版 · 2026-07-30 · 类型分节 + 保留原编号）

> **定位**：所有经真实代码取证确认**已闭环**的问题归档。只增不改（除非重新裁定）。面试时可展示迭代与自查能力。
>
> **编号规则**：`a`=地图层、`b`=业务层、`c`=组件层、`d`=后端层、`z`=暂未归类。**编号由 `待解决问题.md` 首次分配，移入本文件时保留原编号、不重排**——已解决问题不要求编号连续（问题不可能按编号顺序解决），但全局唯一（本文件与 `待解决问题.md` 之间无重复编号）。状态由文件名决定：本文件 = 已解决。
>
> **入档质量门禁（AI 自查，证据留消息框不入文档）**：
> 1. 解决方案是否引入新漏洞（"解决 A 埋 N 个雷"）？
> 2. 修复是否低风险、高效率（最小作用域、不顺手重构、不违反架构硬约束）？
> 3. 修复是否经代码/测试/typecheck 验证？
> 以上三条全部通过 → 准予入档。证据在对话消息框出示，**不写入本文档**。
>
> **时间戳规则**：每个条目 `问题编号` 下方必须标注（格式以 `discover:20260730` / `solve:20260730` 为准）：
> - `discover:YYYYMMDD` — 保留首次写入 `待解决问题.md` 的时间
> - `solve:YYYYMMDD` — 新增移入本文件的时间（旧条目按 git log/文档版本推算，标注 `~` 前缀）
>
> **问题等级**：`p0` 致命｜`p1` 高｜`p2` 中｜`p3` 低（详见 `待解决问题.md` 图例）。
> **来源整合**：整合原《已解决问题.md》六节全部已闭环项，并承接《待解决问题.md》经 2026-07-30 基准复核迁出的设计意图类（A01–A04、E01、E16、D08、E14残留、S01–S06）。

---

## 地图层（图层生命周期 / 地图渲染 / 地图引擎集成）

> 以下条目均已逐条标注 `discover:20260730` / `solve:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p2 | a001-rbush空间索引<br>`discover:20260730`<br>`solve:20260730` | 无空间索引（rbush）→ 视口裁剪缺失。 | `shared/utils/spatialIndex.ts` 已实现 rbush 视口裁剪（阈值 1000）+ 单测。 |
| p2 | a002-registerLayer替换语义<br>`discover:20260730`<br>`solve:20260730` | `registerLayer` 重注册回调累加（潜在）。 | `mapStore.ts:158` 改为替换语义（`if (show) next.show = show`），防重复执行。 |
| p2 | a003-业务图层生命周期<br>`discover:20260730`<br>`solve:20260730` | 业务图层数据生命周期缺陷（引擎切换丢图层 + data 不持久化）。 | `BusinessLayerManager.js:115` 持久化 data、`:142` `reapplyAll`；`App.vue:103` 监听 currentRenderer 调 `reapplyAll`。**⚠️ 间接后果**：reapplyAll 机制正确，但 SiteSelectionPage 的 `analysis-coverage`/`analysis-matched` 卸载不清理 → 0626-07-30 作为 a013 修复（`onUnmounted` 改调 `clearAnalysisLayers()`），现已在 `已解决问题.md` a013 归档。 |
| p2 | a004-CRS边界守卫<br>`discover:20260730`<br>`solve:20260730` | CRS 边界守卫零调用（潜在）。 | `mapDataService.ts:5` 导入、`getPorts():63` 调 `isInBeibuGulf` 过滤越界港口。 |
| p2 | a005-Cesium属性判空<br>`discover:20260730`<br>`solve:20260730` | Cesium `addGeoJsonLayer` 给 `entity.properties.featureType` 赋值未判空。 | `CesiumRenderer.js:642-643` `if (!entity.properties) entity.properties = {}` 后赋值。 |
| p2 | a006-OL热力图normalizePoint<br>`discover:20260730`<br>`solve:20260730` | OLRenderer 热力图未用 `normalizePoint`。 | `OLRenderer.js:17` 导入、`addHeatmapLayer:348`/`updateHeatmapLayer:395` 改用。 |
| p2 | a007-UnifiedMap私有字段<br>`discover:20260730`<br>`solve:20260730` | `UnifiedMap.setupLayers` 直接读 `renderer._layers` 私有字段。 | `UnifiedMap.vue:195-201` `hasLayer` 优先公开 `renderer.hasLayer(id)`，私有访问已封装。 |
| p2 | a008-路由切换图层空白<br>`discover:20260730`<br>`solve:20260730` | 路由切换返回后可视化面板/图层空白。 | `FloodAnalysisPage.vue:191/194` 调 `renderFloodAreas`/`renderAffectedFacilities`。 |
| p2 | a009-坐标偏差<br>`discover:20260730`<br>`solve:20260730` | 坐标偏差嫌疑。 | 诊断确认 `floodArea`/`facilityPoints` 坐标格式一致（WGS84 `[lng,lat]`），无需修改。 |
| p3 | a010-Cesium id重复<br>`discover:20260730`<br>`solve:20260730` | Cesium 点图层 entity id 重复 `-undefined` 崩溃。 | `CesiumRenderer.js:442` `` `${id}-${item.id ?? item.name ?? index}` `` 兜底。 |
| p3 | a011-Cesium版权<br>`discover:20260730`<br>`solve:20260730` | Cesium `creditContainer` 隐藏版权（可能违反 attribution 许可）。 | `CesiumRenderer.js` 已移除 `creditContainer`，恢复默认 credit 容器。 |
| p3 | a012-Cesium私有_baseLayers<br>`discover:20260730`<br>`solve:20260730` | `CesiumRenderer` 直写外部单例私有 `_baseLayers`。 | 全仓 grep `_baseLayers` 仅内部使用 + `renderers.d.ts:103` 类型声明，已有公开 `setBaseLayers`/`getBaseLayers`。 |
| p1 | a013-浸没图层跨路由泄漏<br>`discover:20260730`<br>`solve:20260730` | SiteSelectionPage 卸载不清理 `analysis-coverage`/`analysis-matched` 图层，跨路由残留。 | 2026-07-30 修复：`SiteSelectionPage.vue:onUnmounted` 中 `handleHideFacilityLayer()` 替换为 `clearAnalysisLayers()`（已含 analysis-coverage/matched 移除 + 设施 POI），零副作用。 |
| p1 | a018-Cesium idle-destroy推翻§4.2<br>`discover:20260730`<br>`solve:20260730` | 代码落地 idle-destroy 但项目根基 §4.2 仍写"单例不销毁"，文档与代码对撞。 | 2026-07-30 修复：更新 `项目根基.md §4.2` 为"Cesium Viewer 生命周期规则"，写明 30s idle-destroy 机制 + trade-off；同步更新 §4.4 引擎切换链路。代码行为未改——idle-destroy 本身为治内存泄漏的合理设计。 |

---

## 业务层（业务逻辑 / Adapter / Store / 业务页特性）

> 以下条目均已逐条标注 `discover:20260730` / `solve:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | b001-职责集中设计取舍<br>`discover:20260730`<br>`solve:20260730` | 核心组件职责集中（mapStore 累积 analysisHandler 职责）原被质疑。 | `项目根基.md §6.2` 明确为"已知设计取舍"，非缺陷 → 闭环。 |
| p1 | b002-双引擎路由级<br>`discover:20260730`<br>`solve:20260730` | 双引擎被误认为同页共存。 | `项目根基.md §1/§4.4` 明确为"路由级切换"，架构不变规则 → 闭环。 |
| p1 | b003-三套响应格式<br>`discover:20260730`<br>`solve:20260730` | API 多方式并存（三套响应格式）被误判不一致。 | `项目根基.md §8.3` + `API契约文档.md §1.3` 明确为"各子系统有意约定" → 闭环。 |
| p1 | b004-注册TOCTOU<br>`discover:20260730`<br>`solve:20260730` | 注册 TOCTOU 竞态 → 重复用户名。 | `userService.js:18` `sequential` 互斥锁串行化 + `:21-23` 锁内二次查重；`authController.js:53-55` catch 返 409。 |
| p2 | b005-forecastState缓存<br>`discover:20260730`<br>`solve:20260730` | 数据流断裂：`forecastState.dataCache` 无写入。 | `forecastState.ts:30` `dataCache: ShallowRef<Map>`、`:52-57` `cacheData` 正确 get/set。 |
| p2 | b006-三套响应格式约定<br>`discover:20260730`<br>`solve:20260730` | 三套响应格式并存缺共享响应类型。 | `项目根基.md §8.3` 明确为有意约定；新增接口统一 RESTful → 闭环（未来改进不阻塞）。 |
| p2 | b007-前端无守卫设计<br>`discover:20260730`<br>`solve:20260730` | 前端无路由守卫（路由全公开）。 | `项目根基.md §8.4/§5.1` 明确"后端 `authenticate` 为真网关"，前端无守卫为低危接受项 → 闭环。 |
| p2 | b008-floodAdapter反模式<br>`discover:20260730`<br>`solve:20260730` | `floodAdapter` 模块顶层 `useApiRequest` 反模式。 | 移除顶层调用，反模式消除。 |
| p2 | b009-浸没字段lng/lat<br>`discover:20260730`<br>`solve:20260730` | 浸没分析 `AffectedFacility` 字段名未统一 `lng/lat`。 | `floodAdapter.ts:164-165` 归一 `lng/lat`（兼容 `longitude`/`latitude`）。 |
| p2 | b010-forecastAdapter死代码<br>`discover:20260730`<br>`solve:20260730` | `forecastAdapter.ts` 死代码嫌疑。 | grep 确认被 `ForecastPage.vue`/`useForecastLayer.js`/`useForecastRequest.ts` 引用且有单测。 |
| p2 | b011-floodState工厂<br>`discover:20260730`<br>`solve:20260730` | `floodState` 未用持久化工厂。 | `floodState.ts` 全量 `createPersistedState` 封装；`floodPolygons` 改 `shallowRef([])`。 |
| p2 | b012-forecastState unknown槽<br>`discover:20260730`<br>`solve:20260730` | `forecastState` 的 `unknown` 槽。 | `cacheData(time,data)` 入参收窄为 `ForecastSeries`；`dataCache` 改 `shallowRef(new Map())`。 |
| p3 | b013-JSON文件存储<br>`discover:20260730`<br>`solve:20260730` | JSON 文件存储（无数据库）。 | `项目根基.md §12` 明确为"当前阶段刻意简化"，未来接 PostgreSQL+PostGIS → 闭环。 |
| p3 | b014-AppLayout硬编码<br>`discover:20260730`<br>`solve:20260730` | `AppLayout` 硬编码默认图表数据。 | `项目根基.md §10 Step 5` 规划项（接库后改动态）→ 闭环（未来改进）。 |
| p3 | b015-水面坐标硬编码<br>`discover:20260730`<br>`solve:20260730` | 浸没分析水面坐标硬编码兜底。 | `项目根基.md` 刻意简化清单（未来替换真实 DEM）→ 闭环。 |
| p3 | b016-剖面面板占位<br>`discover:20260730`<br>`solve:20260730` | 剖面分析面板为占位。 | `项目根基.md` 刻意简化清单（功能开发规划）→ 闭环。 |
| p3 | b017-部分mock数据<br>`discover:20260730`<br>`solve:20260730` | 部分 mock 数据。 | `项目根基.md` 反模式 6 刻意简化；且 E31 已修复后端反向依赖 → 闭环。 |
| p3 | b018-throughput_model用途<br>`discover:20260730`<br>`solve:20260730` | `tools/throughput_model.cjs` 用途待确认（"一次性脚本嫌疑"）。 | 决策 A：保留 + 挂 npm script（`forecast:model` 实测 MAPE 1.43%/1.84%/2.30%）+ `tools/README.md`。 |
| p2 | b020-向上取档UI未明示<br>`discover:20260730`<br>`solve:20260731` | 浸没范围“向上取档”语义未在前端 UI 明示（请求 2.5m 实际返回 3.0m），仅 logger.info 提示，用户无感知。 | FloodAnalysisPage.vue:246-248 showWarning('当前水位 Xm…已使用 Ym 档位') 可见提示，档位语义对用户透明。 |
| p2 | b021-flood api未闭环<br>`discover:20260730`<br>`solve:20260731` | flood mock 改读 /data/*.json，但 api 分支仍 throw('真实 API 尚未接入')，设计预留未闭环。 | floodAdapter.ts:224-275 api 分支已实装 apiRequest('/flood/flood-areas?waterLevel=…') + 影响评估 POST，不再是 throw；api 模式可闭环。 |
| p2 | b022-forecast adapter默认api<br>`discover:20260730`<br>`solve:20260731` | forecast adapter 默认 'api'（forecastAdapter.ts:123），flood 默认 'mock'，三模块数据源策略三套，dev 无后端时 Forecast 跑不通。 | main.ts 读 VITE_DATA_SOURCE（默认 mock）+ 两 adapter setDataSource；bootstrap 后 env 驱动切换，默认统一 mock。 |
| p2 | b024-无env数据源开关<br>`discover:20260730`<br>`solve:20260731` | forecastAdapter/floodAdapter 无 import.meta.env 读取，setDataSource 全靠手动调用，无按环境自动切换。 | 新增 VITE_DATA_SOURCE（默认 mock），main.ts bootstrap 读 env 调 setDataSource；与 b022 同源，env 驱动自动切。 |
---

## 组件层（UI 组件 / CSS / 面板 / 视图组件 / 前端打包）

> 以下条目均已逐条标注 `discover:20260730` / `solve:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | c001-ElementPlus分包<br>`discover:20260730`<br>`solve:20260730` | Element Plus 未独立分包（首屏臃肿）。 | 新增 `ui-vendor` 分包 + 按需引入；首屏 payload 720→480KB gzip。 |
| p2 | c002-ProfilePage双触发<br>`discover:20260730`<br>`solve:20260730` | `ProfilePage.loadPlans()` 双触发（watch + onMounted）。 | grep `onMounted` 无匹配，仅 `watch(user, immediate)` 触发。 |
| p2 | c003-ForecastControlPanel定时器<br>`discover:20260730`<br>`solve:20260730` | `ForecastControlPanel` 播放用 `setInterval` 可能定时器泄漏。 | 实 grep 无 `setInterval`/`stopPlayback`，已重构为 `currentTime` watch 同步驱动。 |
| p2 | c004-ErrorBoundary包地图<br>`discover:20260730`<br>`solve:20260730` | `ErrorBoundary` 仅包 `RouterView`，地图崩溃可能白屏。 | `App.vue:117-119` 包 `<UnifiedMap>`、`:121-125` 包 `<RouterView>`。 |
| p2 | c005-useAuth生命周期<br>`discover:20260730`<br>`solve:20260730` | `useAuth` 在组合式函数内无条件挂生命周期钩子。 | 无路由守卫调用 `useAuth`；`useAuth.ts:197/205` 现仅在 App.vue 上下文执行。 |
| p2 | c006-加载失败提示<br>`discover:20260730`<br>`solve:20260730` | 加载失败静默吞掉（仅超时提示）。 | `UnifiedMap.vue:120-124` 新增 `else` 分支显示"地图数据加载失败，请刷新重试"。 |
| p2 | c007-vite target<br>`discover:20260730`<br>`solve:20260730` | `vite.config.js build.target:'es2015'` 与 browserslist 矛盾。 | `frontend/vite.config.js:68` `target:'es2020'`。 |
| p2 | c008-呼吸动画RAF<br>`discover:20260730`<br>`solve:20260730` | 呼吸动画未停（RAF 泄漏）。 | `App.vue:60` 路由 watcher 每次切换调 `stopBreathing`；卸载路径亦清理。 |
| p2 | c009-硬编码hex<br>`discover:20260730`<br>`solve:20260730` | 硬编码 hex 残留 / `WaterLevelProfilePanel` 颜色常量带引号失效。 | `WaterLevelProfilePanel.vue:238/242/256/261` 用 `PROFILE_COLORS.safe/.water`；全部 hex 收敛 `colors.ts`。 |
| p2 | c010-天地图KEY<br>`discover:20260730`<br>`solve:20260730` | 天地图 KEY 硬编码。 | 改环境变量读取，缺失显式报错。 |
| p3 | c012-ProfilePage CSS选择器错<br>`discover:20260730`<br>`solve:20260731` | ProfilePage.vue:586 CSS 选择器 .fav-section :deep(.favorite-list-panel) 类名错误，PaginatedListPanel 根 class 实为 .paginated-list-panel，:deep 永不命中，收藏区背景覆盖失效（视觉）。 | ProfilePage.vue:586 改为 .paginated-list-panel；证据 PaginatedListPanel.vue:309，收藏区背景覆盖生效。 |
| p3 | c014-LoginPanel归属shared<br>`discover:20260730`<br>`solve:20260731` | LoginPanel.vue 位于 shared/components/ 但全仓仅 ProfilePage 引用，与“独属于个人中心”语义不符。 | git mv frontend/src/shared/components/LoginPanel.vue → frontend/src/views/LoginPanel.vue（D 状态）；仅 ProfilePage 引用，归属修正为视图组件。 |
| p3 | c015-flood配色散落<br>`discover:20260730`<br>`solve:20260731` | flood 风险配色散落组件（FloodAnalysisPage.vue:371-399），未抽 colors.ts。 | colors.ts:71 FLOOD_RISK_COLORS 单一事实源，FloodAnalysisPage.vue:380/384 引用；配色集中。 |
---

## 后端层（后端代码 / API / 认证 / 服务 / 工程架构）

> 以下条目均已逐条标注 `discover:20260730` / `solve:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p0 | d001-城市按钮固定结构<br>`discover:20260730`<br>`solve:20260730` | AppLayout 顶部城市按钮固定结构被误判为 bug。 | `项目根基.md §10/§11.1` 明确"固定结构，不在 slot 覆盖范围"，文档化已知限制 → 闭环。 |
| p1 | d002-零CI<br>`discover:20260730`<br>`solve:20260730` | 零 CI。 | `.github/workflows/ci.yml` 存在，steps = `format:check → lint → typecheck → test → build`。 |
| p1 | d003-双认证矛盾<br>`discover:20260730`<br>`solve:20260730` | 双认证矛盾（同时发 Bearer + Cookie）。 | `useApiRequest.ts:56` 注释"认证统一 HttpOnly Cookie（credentials:'include'），不发 Bearer"；grep 无匹配。 |
| p1 | d004-选址输入错误码<br>`discover:20260730`<br>`solve:20260730` | 选址分析用户输入错误返 500（应 400）。 | `siteAnalysisController.js:10/19/32/43/56` 均 `res.status(400)`。 |
| p1 | d005-业务错误状态码<br>`discover:20260730`<br>`solve:20260730` | 业务错误以 HTTP 200 返回 `{error}`。 | `siteAnalysisController.js:72` 返 422、`:78` 返 `error.status`、`:81` 返 500。 |
| p1 | d006-评分权重校验<br>`discover:20260730`<br>`solve:20260730` | 评分权重未校验（可负数/字符串/NaN）。 | `siteAnalysisController.js:55-56` 校验"权重应为 0-10 之间的数字"，非法返 400。 |
| p1 | d007-注册TOCTOU锁<br>`discover:20260730`<br>`solve:20260730` | 注册 TOCTOU 竞态（应用层消解，详见 b004）。 | `userService.js:18` `sequential` 锁 + 锁内查重；`authController.js:53-55` 返 409。 |
| p2 | d008-forecast无认证<br>`discover:20260730`<br>`solve:20260730` | `/api/forecast/*` 无认证被误判冲突。 | 阶段六 6.1 决策 B：移除 `forecast.js` 的 `authenticate`，后端恢复公开，与设计文档 4 处声明一致。**⚠️ 间接后果**：公开路由 + 无输入白名单 + 路径拼接 → 暴露路径遍历漏洞（见 `待解决问题.md` d032），需独立修复（加指标白名单 + 拒绝 `..`）。 |
| p2 | d009-魔法数字常量<br>`discover:20260730`<br>`solve:20260730` | 魔法数字散落（裸 `10000` 超时）。 | 抽 `FETCH_TIMEOUT_MS`/`LOAD_TIMEOUT_MS`/`API_TIMEOUT_MS`/`CACHE_EXPIRY` 常量。 |
| p2 | d010-路由守卫设计变更<br>`discover:20260730`<br>`solve:20260730` | 路由守卫鉴权依赖内存 token（功能级阻断）。 | `router` grep `beforeEach`/`requiresAuth` 无匹配，路由全公开；原"内存 token 弹回登录"前提已不存在（设计变更）。 |
| p2 | d011-AbortError区分<br>`discover:20260730`<br>`solve:20260730` | `AbortError` 误判为超时。 | `useApiRequest.ts:123` 区分 `signal.aborted`（内部超时）vs 外部取消。 |
| p2 | d012-console.error守卫<br>`discover:20260730`<br>`solve:20260730` | 生产路径 `console.error` 未移除（复核重开后再闭环）。 | `mapDataService.ts` 4 处 console.error 均加 `import.meta.env.DEV` 守卫。 |
| p2 | d013-后端反向依赖<br>`discover:20260730`<br>`solve:20260730` | 后端反向依赖前端静态目录。 | 7 个预测 JSON 迁至 `backend/data/forecast/`；前端 mock 夹具独立保留。 |
| p2 | d014-core反向依赖<br>`discover:20260730`<br>`solve:20260730` | core→business 反向依赖（`UnifiedMap` 曾 import `useAnalysisLayer`）。 | 业务层通过 `mapStore.registerAnalysisHandler` 注入；dependency-cruiser 无违规。 |
| p2 | d015-坐标字段统一<br>`discover:20260730`<br>`solve:20260730` | 坐标字段命名不统一。 | `types/crs.ts` `normalizePoint` 统一 `lng/lat` + 兼容 `lon/longitude`。 |
| p2 | d016-visualization反向依赖<br>`discover:20260730`<br>`solve:20260730` | visualization 反向依赖 business（`useRadarChart→facilityConfig`）。 | `colors.ts` 新增 `FACILITY_COLORS_MAP` 单一事实源；`useRadarChart.js:19` 改从 shared 取色。 |
| p2 | d017-layerCatalog shallowRef<br>`discover:20260730`<br>`solve:20260730` | `layerCatalog` 用 `ref` 存数组（应 `shallowRef`）。 | `mapStore.ts:85` `shallowRef([])`；所有写操作不可变更新。 |
| p2 | d018-mapStore非空断言<br>`discover:20260730`<br>`solve:20260730` | `mapStore` 双图层注册机制 `entry.show!` 崩溃。 | 去除 `entry.show!`/`entry.hide!` 非空断言。 |
| p2 | d019-setLayerVisible<br>`discover:20260730`<br>`solve:20260730` | `BusinessLayerManager.setVisible` 绕过 Pinia action 直改 `catalogEntry.visible`。 | `mapStore.ts:363` 新增 `setLayerVisible` action；`BusinessLayerManager.js:172` 调用。 |
| p2 | d020-令牌吊销<br>`discover:20260730`<br>`solve:20260730` | 令牌无吊销（logout/改密后旧 token 仍有效 7d）。 | `users.json:7` 加 `tokenVersion`；`middleware/auth.js:53-70` 校验版本；`authController.js:105-106` logout 自增。 |
| p2 | d021-scoring栈溢出<br>`discover:20260730`<br>`solve:20260730` | `scoring Math.min` POI 量大时栈溢出。 | grep `Math.min` 无匹配，原路径已重构。 |
| p2 | d022-gcs路由鉴权<br>`discover:20260730`<br>`solve:20260730` | `/api/gcs/*` 全裸（已改名 `/api/flood`）。 | 已加 `router.use(authenticate)`。 |
| p2 | d023-标记字段统一<br>`discover:20260730`<br>`solve:20260730` | 标记字段名不一致（note/description）。 | 已统一 `note`。 |
| p2 | d024-ID随机UUID<br>`discover:20260730`<br>`solve:20260730` | plans/markers ID 用 `Date.now()` → 并发碰撞。 | 改 `crypto.randomUUID()`。 |
| p2 | d025-plans写文件<br>`discover:20260730`<br>`solve:20260730` | plans 先改内存后写文件，写失败缓存脏。 | 构造新数组不原地修改。 |
| p2 | d026-facilities缓存<br>`discover:20260730`<br>`solve:20260730` | facilities 缓存永不过期。 | 加 TTL 5 分钟。 |
| p3 | d027-颜色引号回归<br>`discover:20260730`<br>`solve:20260730` | `WaterLevelProfilePanel` 颜色常量带引号回归。 | 重建 hex 收敛时修正引号（见 c009）。 |
| p0 | d032-forecast路径遍历<br>`discover:20260730`<br>`solve:20260730` | 后端 forecast 无鉴权 + 无白名单 → 路径遍历；`engineCache` 无上限；`ENOENT` 假成功。 | 2026-07-30 修复：`forecastService.js` 加 `ALLOWED_INDICATORS` 白名单（throughput/berth/traffic/pressure/development）+ `..` 拒绝 + `validateIndicator()` + `MAX_CACHE_SIZE=100` + ENOENT→`BusinessError(NOT_FOUND)`；3 测试更新；全量 89 测试通过。 |
| p3 | d028-floodStore引用<br>`discover:20260730`<br>`solve:20260730` | `useFloodStore`/`@/stores/floodStore` 残留引用。 | 改 `useFloodState`/`@/stores/floodState`。 |
| p3 | d029-无CI已补<br>`discover:20260730`<br>`solve:20260730` | 没有 CI/CD（被误列未解决）。 | 已加 GitHub Actions（见 d002）→ 闭环。 |
| p0 | d030-nginx无静态缓存<br>`discover:20260730`<br>`solve:20260731` | nginx 无静态资源缓存（expires/Cache-Control 全无），每次回访重下 265KB，Lighthouse 缓存项崩，CDN 失效全量回源。 | nginx.conf:24-25 expires 1y + Cache-Control "public, immutable"；静态资源可长缓存。 |
| p0 | d031-无preconnect天地图<br>`discover:20260730`<br>`solve:20260731` | 无 <link rel=preconnect> 到天地图，首张瓦片现场 DNS+TLS+RTT。 | index.html:8-10 preconnect t0/t1/t2.tianditu.gov.cn；首屏瓦片提前建链。 |
| p1 | d033-认证恢复依赖localStorage<br>`discover:20260730`<br>`solve:20260731` | 认证恢复依赖 localStorage，与 HttpOnly Cookie 主通道不一致：restoreAuth 仅当 localStorage 恢复 user 非空才调 /auth/me，否则 return null。 | useAuth.ts restoreAuth 始终调 /auth/me（Cookie 为权威），不再以 localStorage 前置；证据 useAuth.ts:79-106。 |
| p1 | d034-flood水位校验缺失<br>`discover:20260730`<br>`solve:20260731` | flood 水位仅 isNaN 校验，缺 isFinite/下限/上限，'Infinity' 可绕过 isNaN。 | floodAnalysisController.js Number.isFinite(level) && level>=0 && level<=MAX_WATER_LEVEL 校验；输入加固。 |
| p2 | d036-剖面面板鉴权不一致<br>`discover:20260730`<br>`solve:20260731` | 剖面面板直连 flood API（/flood/terrain-profiles），与免登录 mock 主页不一致，剖面必 401，产品语义分裂。 | routes/floodAnalysis.js:45 /terrain-profiles 注册在 :54 router.use(authenticate) 之前→公开，与 mock/d008 一致。按权限设计原则（2026-07-31）：只读数据默认开放，仅收藏写链路需登录，开放即符合设计，非鉴权漏洞。 |
| p2 | d037-注册无限流<br>`discover:20260730`<br>`solve:20260731` | 注册接口无限流，仅吃全局 100/15min，/register 无专属 limiter（/api/auth/login 有 max5）。 | app.js:37-42 registerLimiter 挂 /api/auth/register；注册限流。 |
| p2 | d038-响应体回传JWT<br>`discover:20260730`<br>`solve:20260731` | 登录/注册响应体回传 JWT（{token,user}），削弱 HttpOnly 收益，XSS 可读响应/内存 token 绕过 Cookie。 | authController.js 响应仅 {user}，token 仅 Set-Cookie（HttpOnly）；响应体不再回传 JWT。 |
| p3 | d046-engineCache无上限<br>`discover:20260730`<br>`solve:20260731` | forecastService.js:28 engineCache 无上限，匿名攻击者可枚举 confidence 制造内存放大（与 d032 组合）。 | forecastService.js MAX_CACHE_SIZE=100 + FIFO 淘汰；缓存有上限。 |
| p1 | d048-天地图密钥入库<br>`discover:20260730`<br>`solve:20260731` | frontend/.env:2 曾提交真实 VITE_TIANDITU_KEY（天地图密钥）；若 .env 已进版本库，密钥进 git 历史即泄露。 | frontend/.env:3 仅占位 your_tianditu_key_here；.env 已 gitignored + 未跟踪，无真 key 入版本库；密钥未泄露。 |
---

## 暂未归类（文档 / 工程规范 / 构建 / 设计意图）

> 以下条目均已逐条标注 `discover:20260730` / `solve:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p2 | z001-CSS变量体系<br>`discover:20260730`<br>`solve:20260730` | CSS 变量体系缺失，`#409eff` 散落 19 文件。 | 建立 `--gcs-*` 全局 Token，核心/共享组件改用 `var()`。 |
| p2 | z002-TS迁移<br>`discover:20260730`<br>`solve:20260730` | TS 迁移 ~22%。 | 9 Store + 4 Service 迁移 .ts，30 Vue 组件 `lang="ts"`。 |
| p2 | z003-console logger<br>`discover:20260730`<br>`solve:20260730` | console 调试输出未清理。 | 接入统一 logger（`shared/utils/logger.js`）。**⚠️ 仅部分实现**：`mapDataService.ts` 6 处 `console.*` 已加 DEV 守卫，但全站仍有 28 处 `console.*` 散落（业务组件 debug 遗留），"生产静默"不实——剩余残留见 `待解决问题.md` z028。 |
| p2 | z004-预测请求取消<br>`discover:20260730`<br>`solve:20260730` | 预测请求无取消机制。 | `useForecastRequest.js` 用 `AbortController` + `startTransaction()` + `cancelAll()`。 |
| p2 | z005-硬编码hex收敛<br>`discover:20260730`<br>`solve:20260730` | 硬编码 hex ~71 行。 | 收敛至 `shared/constants/colors.ts`（RISK/FACILITY/CHART/LAYER_DEFAULTS/PROFILE/INSPECTION）。 |
| p2 | z006-MapRenderer 3D-only<br>`discover:20260730`<br>`solve:20260730` | `MapRenderer` 基类声明 3D-only 方法，OLRenderer 不实现。 | `项目根基.md §4.2` 明确"调用时返回 false + DEV warn"为文档化行为 → 闭环。 |
| p2 | z007-Cesium Viewer单例<br>`discover:20260730`<br>`solve:20260730` | Cesium `Viewer` 单例、离开 3D 仅 `v-show` 隐藏不销毁。 | `项目根基.md §4.2/§4.4` 明确为"性能取舍" → 闭环。**⚠️ 文档滞后**：代码已落地 idle-destroy（`CesiumRenderer.js:33-34` `IDLE_DESTROY_DELAY=30000`，30s 空闲后 `viewer.destroy()`），与 §4.2"不销毁"声明矛盾——§4.2 需更新，见 `待解决问题.md` a018。 |
| p3 | z008-三套lint<br>`discover:20260730`<br>`solve:20260730` | 三套 lint 并存。 | 卸载 oxlint，eslint-config-prettier 分流。 |
| p3 | z009-.prettierrc<br>`discover:20260730`<br>`solve:20260730` | 无 `.prettierrc`。 | 创建（semi:false, singleQuote:true, printWidth:100）。 |
| p3 | z010-types any<br>`discover:20260730`<br>`solve:20260730` | `types/plan.ts` 3 处 `any`。 | 改 `Record<string, unknown>`。 |
| p3 | z011-API契约文档<br>`discover:20260730`<br>`solve:20260730` | 缺 API 契约文档。 | 编写 31 端点契约。 |
| p3 | z012-编码规范<br>`discover:20260730`<br>`solve:20260730` | 缺编码规范。 | 编写 `CODING_STYLE.md`。 |
| p3 | z013-技术债务清单<br>`discover:20260730`<br>`solve:20260730` | 缺技术债务清单。 | 编写 P0/P1/P2 清单。 |
| p3 | z014-打包分析<br>`discover:20260730`<br>`solve:20260730` | 无打包分析。 | 引入 `rollup-plugin-visualizer` + `build:analyze`。 |
| p3 | z015-双引擎设计<br>`discover:20260730`<br>`solve:20260730` | （同 b002）双引擎路由级切换。 | `项目根基.md` 已声明 → 闭环。 |
| p3 | z016-三套响应格式设计<br>`discover:20260730`<br>`solve:20260730` | （同 b006）三套响应格式有意约定。 | `项目根基.md §8.3` 已声明 → 闭环。 |
| p3 | z017-前端无守卫设计<br>`discover:20260730`<br>`solve:20260730` | （同 b007）前端无守卫 = 后端为真网关。 | `项目根基.md §8.4/§5.1` 已声明 → 闭环。 |
| p3 | z018-CGCS2000豁免条款<br>`discover:20260730`<br>`solve:20260730` | 规范未写入"CGCS2000 等坐标系术语豁免 GCS 全大写约束"条款。 | 已补 `工程规范与性能参考.md §3.6` + `项目根基.md §7.8` 豁免条款（GCS 全大写约束 + CGCS2000 坐标系豁免）。 |
| p3 | z019-GCS残留小写归一化<br>`discover:20260730`<br>`solve:20260730` | 残留小写 `gcs` 标识符（`GcsFeature`/`GcsBottomNavBar`/`.gcs-panel` 等）。 | 2026-07-30 全量归一化为 `GCS`（代码 9 文件 28 处），`typecheck` 0 错误、`build` 通过；代码 `(?i)gcs` 残留 0。 |
| p3 | z028-全站console残留<br>`discover:20260730`<br>`solve:20260730` | z003 声称"生产静默"过度（全站 28 处残留）。 | 2026-07-30 交叉验证核实：全站 `console.*` 已全量加 `import.meta.env.DEV` 守卫（`BusinessLayerManager.ts` 3 处本次补加），实际已是生产静默；z003 已修正描述。 |
| p2 | z021-dist重复chunk<br>`discover:20260730`<br>`solve:20260731` | dist 重复 chunk 堆积（vite.config.js:72 emptyOutDir:false 致 17 份 index/AppLayout 等，dist 23MB），浪费磁盘/CI/部署包。 | vite.config.js:72 emptyOutDir:true（生产构建触发清空），无孤儿 hash 堆积。 |
| p3 | z024-withTimeout不abort<br>`discover:20260730`<br>`solve:20260731` | withTimeout 超时后不 abort 底层 promise，后台继续写已卸载组件 ref。 | UnifiedMap.vue withTimeout 用 AbortController 超时 abort()；超时即取消底层请求。 |
| p3 | z025-重复provide mapStore<br>`discover:20260730`<br>`solve:20260731` | App.vue 与 UnifiedMap.vue 重复 provide('mapStore',…)。 | 全仓仅 App.vue 一处 provide('mapStore')（UnifiedMap.vue:49 的 provide 已删）；单一事实源。 |
| p2 | z029-无运行时错误捕获<br>`discover:20260730`<br>`solve:20260731` | 全仓无 app.config.errorHandler / window.onerror / onunhandledrejection；未捕获错误白屏无痕。 | main.ts 注册 app.config.errorHandler→errorHandler.showError+logger.error，挂 window.onerror/onunhandledrejection 兜底；运行时错误可捕获。 |
| p2 | z031-errorHandler未接请求边界<br>`discover:20260730`<br>`solve:20260731` | useApiRequest 抛 ApiError，但页面各自 catch；useSiteAnalysisApi.ts:43-68 手写 ApiError.code→文案 映射，与 errorHandler 重复且口径不一。 | useSiteAnalysisApi.ts:45 统一走 errorHandler，手写 switch 已删；请求错误口径统一。 |
| p3 | z032-logger非结构化<br>`discover:20260730`<br>`solve:20260731` | shared/utils/logger.ts:7-8 warn/error 生产环境仍直接 console.*，且输出非结构化（无 level/module/timestamp）。 | logger.ts 结构化 {level,module,action,message,timestamp,data?}；warn/error 生产保留脱敏，debug/info 仍 DEV 门控；预留 transport 钩子。 |