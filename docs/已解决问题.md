# 已解决问题

> **定位**：所有已修复的问题归档。只增不改（除非重新裁定）。面试时可展示迭代能力。
> **最后更新**：2026-07-28（整合三份源文档 + 既有双轨内容，做分类复核与错配修正；**本轮再整合 `docs/reports/`（TS迁移进度、中高级问题收口）两报告：E32 确认已修复迁入 R26**；**本轮再整合 `docs/分步审查/`（7 份 v2 深度审查）：其已修复结论（如 R26 的 hex 收敛、D06 架构评审称已修复）已与主文件对齐，新发现均落在 `待解决问题.md`**）
> **说明**：本文件覆盖各源文档的全部"已修复"问题项；按你指示执行 diff 收口：已覆盖源文档（`审核报告-文档出入与重构进步`、`问题清单复核总览`、`docs/reports/*`）已删除；未跟踪的新审查（`docs/分步审查/*`、`首屏分包分析.md`）已隔离至 `docs/_trash-2026-07-28/` 待你最终确认后清除。
> **关联**：未修复的问题见 `待解决问题.md`；面试备战见 `面试备战-三向整合版.md`；设计约束见 `开工前必读/`。

---

## 一、架构修复

| # | 日期 | 问题 | 修复 |
|---|------|------|------|
| R01 | 2026-07 | core→business 反向依赖（UnifiedMap 曾 import useAnalysisLayer） | 业务层通过 mapStore.registerAnalysisHandler 注入 |
| R02 | 2026-07 | MapRenderer 接口不完整 | 补全 renderer.ts 接口定义 + renderers.d.ts 类型声明 |

## 二、工程规范建设（2026-07-27）

| # | 问题 | 修复 |
|---|------|------|
| R03 | CSS 变量体系缺失，`#409eff` 散落 19 个文件，`--gcs-cell-48px: 0.6cell` 无效 CSS | 建立 `--gcs-*` 24 个全局 Token，全部核心/共享组件改用 `var()` 引用 |
| R04 | TypeScript 迁移进度 ~22%（仅 types/ 和 composables） | 9 个 Store + 4 个 Service 全部迁移 .ts，30 个 Vue 组件添加 `lang="ts"`，新增 4 个类型文件 + renderers.d.ts |
| R05 | 三套 lint 工具并存（ESLint + oxlint + Prettier），职责不清 | 卸载 oxlint，`eslint-config-prettier` 分流格式→Prettier、质量→ESLint |
| R06 | 无 `.prettierrc`，Prettier 用默认配置 | 创建 `.prettierrc`（semi:false, singleQuote:true, printWidth:100） |
| R07 | types/plan.ts 中 3 处 `any` | 替换为 `Record<string, unknown>` |
| R08 | 缺 API 契约文档 | 编写 31 端点完整契约（`docs/开工前必读/API契约文档.md`） |
| R09 | 缺项目编码规范 | 编写 `CODING_STYLE.md`（10 节，地理术语优先） |
| R10 | 缺技术债务清单 | 编写 P0/P1/P2 三级技术债务清单 |

## 三、项目配置修复

| # | 日期 | 问题 | 修复 |
|---|------|------|------|
| R11 | 2026-07 | console 调试输出未清理 | 接入统一 logger（`shared/utils/logger.js`），生产环境自动静默 |
| R12 | 2026-07 | 天地图 KEY 硬编码 | 改为从环境变量读取，缺失时显式报错 |

## 四、构建优化

| # | 日期 | 问题 | 修复 |
|---|------|------|------|
| R13 | 2026-07-27 | Element Plus 未独立分包 | 新增 `ui-vendor` 分包 + 改为按需引入（`unplugin-vue-components` + `unplugin-auto-import`，移除全量 `app.use(ElementPlus)` 与 4 处显式 `import ... from 'element-plus'`）；实测 `ui-vendor` JS 320→116KB gzip、CSS 48→11.85KB gzip，首屏 element-plus 占用 368→128KB gzip；首屏总 payload 720→480KB gzip（4G 下 ~1.5–2.5s）；build/44 test/lint 全过，显式补回 `@types/geojson` 修复 TS2307 | ✅ 已修复 |
| R14 | 2026-07-27 | 无打包分析工具 | 引入 `rollup-plugin-visualizer`，新增 `build:analyze` 脚本 |

## 五、本轮代码验证确认已修复（2026-07-28，去重后从待解决问题迁入）

| # | 原编号 | 问题 | 代码验证证据 | 裁定 |
|---|--------|------|--------------|------|
| R15 | D02 | 零 CI | `.github/workflows/ci.yml` 存在，steps 为 `format:check → lint → typecheck → test → build` 全阻断，**无 `continue-on-error`** | ✅ 已修复 |
| R16 | D04 | 双认证机制矛盾（同时发 Bearer + Cookie） | `useApiRequest.ts:103` 注释明确"仅 HttpOnly Cookie，不发 Bearer"；全文件无 `Authorization`/`Bearer` 发送；`token` 仅前端 `isAuthenticated` 判断 | ✅ 已修复（审核报告 #2 假文档已更正为 Cookie-only 单通道） |
| R17 | D05 | 坐标字段命名不统一 | `frontend/src/types/crs.ts` `normalizePoint` 统一 `lng/lat` + 防御性兼容 `lon/longitude`；业务层无字段乱用；渲染器 `?? item.lon ??` 仅为兼容（架构评审误判已修正） | ✅ 已实质解决 |
| R18 | D10 | 无空间索引（rbush） | `frontend/src/shared/utils/spatialIndex.ts` 已实现 rbush 视口裁剪（阈值 1000）+ `__tests__/spatialIndex.test.ts` 单测 | ✅ 已修复（注：当前数据量数百要素不触发，属"预留扩展"） |
| R19 | B03 | 选址分析用户输入错误返回 500（应返 400） | `backend/controllers/siteAnalysisController.js:10,19,32,43,51,56` 均 `res.status(400)`；非法权重返 400 | ✅ 已修复（深度复核已对 `backend/` 路径复核确认） |
| R20 | B04 | 选址分析业务错误以 HTTP 200 返回 `{error}` | `siteAnalysisController.js:72` 返 422、`:78` 返 `error.status`、`:81` 返 500，不再以 200 包错误 | ✅ 已修复 |
| R21 | B06 | 选址评分权重未校验（可传负数/字符串/NaN） | `siteAnalysisController.js:56` 校验"权重应为 0-10 之间的数字"，非法返 400 | ✅ 已修复（后端 controller 层拦截；service 层仍用 `?? 1` 兜底） |
| R22 | 预测 #1a/#1b | 热力图/点图层注册无 `nextTick` 延迟、无 `manager.has` 存在性检查 | `useForecastLayer.js:48,99` 已加 `await nextTick()`；`:53,67,70,98,100,142` 均有 `manager.has(key)` 检查 | ✅ 已修复 |
| R23 | 预测 #4 | 预测请求无取消机制 | `useForecastRequest.js` 已用 `AbortController` + `startTransaction()` + `cancelAll()`（诊断报告本身确认 ✅） | ✅ 已实现 |
| R24 | 浸没 #6 | 路由切换返回后可视化面板/图层空白（onMounted 恢复分支跳过渲染） | `FloodAnalysisPage.vue:191` 调 `renderFloodAreas(savedState.floodFeatures)`、`:194` 调 `renderAffectedFacilities(...)` | ✅ 已修复 |
| R25 | 浸没 #8 | 坐标偏差 | 诊断报告确认 `floodArea`/`facilityPoints` 坐标格式一致（WGS84 `[lng,lat]`），数据正确，无需修改 | ✅ 无需修改 |
| R26 | E32 | 硬编码 hex 残留（~71 行） | `reports/TS迁移进度-2026-07-28.md` 代码验证：硬编码 hex 全部收敛至 `src/shared/constants/colors.ts`（`RISK_COLORS`/`FACILITY_COLORS`/`CHART_COLORS`/`LAYER_DEFAULTS`/`PROFILE_COLORS`/`INSPECTION_COLORS`），`OLRenderer.js`/`CesiumRenderer.js` 引擎回退色、`WaterLevelProfilePanel.vue`/`GcsInspectionOverlay.vue` 等改用集中常量；结果：所有 `.vue`/`.js` 业务/渲染/图层代码已零 `#hex`，仅 `style.css`(CSS token) 与 `colors.ts`(渲染层集中色，canvas/WebGL/SVG 无法解析 CSS 变量必须具体色值) 作为单一事实源保留；`vue-tsc --noEmit` EXIT 0 | ✅ 已修复（原待解决「待确认」冲突项，本轮据报告确认闭环，从 bug 表移除迁入本文件） |

## 六、历史移除项补录（原待解决问题"已从清单移除"迁移）

> 以下项在旧 `待解决问题.md` 中标注"已修复/已统一/已从清单移除"，但误留于待解决文件。本轮统一迁入本文件。

| # | 原问题 | 修复标记 | 移除日期 |
|---|--------|---------|---------|
| B05 | 标记字段名不一致：创建存 `note`，更新白名单用 `description` | 已统一为 `note` | 2026-07-27 |
| B07 | plans/markers ID 用 `Date.now()` → 并发碰撞 | 改用 `crypto.randomUUID()` | 2026-07-27 |
| B08 | plans 先改内存缓存后写文件，写失败时缓存已脏 | 构造新数组，不原地修改 | 2026-07-27 |
| B11 | 认证策略不一致（`/api/gcs/*` 全裸） | 已加 `router.use(authenticate)` | 2026-07-27 |
| B13 | facilities 缓存永不过期 | 加 TTL 5 分钟 | 2026-07-27 |
| — | `useFloodStore` / `@/stores/floodStore` 残留引用（3 处） | 已改为 `useFloodState` / `@/stores/floodState` | 2026-07-27 |
| D07 | `/api/forecast/*` 无认证保护与其他模块不一致 | 前端路由 `meta.requiresAuth` 改为不要求认证，与后端 forecast 路由无 authenticate 对齐；预测数据视为公开数据为稳定设计决策 | 2026-07-28 |

## 七、审计后新回归已修复（迁入）

| # | 原编号 | 问题 | 修复 | 来源 |
|---|--------|------|------|------|
| E28 | E28 | `floodAdapter` 模块顶层 `const { apiRequest } = useApiRequest()`：组合式函数在 setup 外调用（反模式，一旦 `useApiRequest` 加生命周期钩子即崩） | 2026-07-28 重建：floodAdapter 移除顶层 `useApiRequest`，反模式消除（原 `floodAdapter.ts:34`） | 待解决问题（原第六节）/深度复核第七轮登记 |
| F01 | F01 | `WaterLevelProfilePanel.vue:238,242,256,261` 颜色常量被写成字符串 `'PROFILE_COLORS.safe'/'PROFILE_COLORS.water'`，ECharts 颜色失效（hex 清理引入的新回归） | 2026-07-28 重建 hex 收敛时修正引号，现为 `PROFILE_COLORS.safe/water` 无引号（对比 `colors.ts:31-34` 定义；同类 `useAnalysisLayer.js:67`/`useBoundaryLayer.js:103`/`usePortLayer.js:45` 改对） | 深度复核第七轮登记 / 待解决问题（原第七节） |

## 八、本轮分类复核说明

> 本文件由三份源文档（`审核报告-文档出入与重构进步`、`深度代码复核报告`、`问题清单复核总览`）整合重写，并对照"状态标签 vs 所在桶"做了错配修正。

**1. 从 `待解决问题.md` 迁入本文件（源文档/旧文件标"已修复"却误留待解决）：**
- `E28` floodAdapter 顶层 `useApiRequest` 反模式 → 已修复
- `F01` WaterLevelProfilePanel 颜色常量带引号回归 → 已修复
- `B05` / `B07` / `B08` / `B11` / `B13` / `useFloodStore 残留` / `D07` → 旧待解决"已从清单移除"区，均标已修复/已统一，统一迁入

**2. 保留项：** R01–R25 及一~四节既有分类不变（深度复核抽样确认 R16/R18 属实，R19/R20/R21 已对 `backend/` 路径复核，建议后续仍对后台三项目独立核验以免建立在错误路径上）。

**3. 源文档覆盖确认：** 三份源文档的全部"已修复"问题项均已落入本文件（R15–R25 / 第六节历史移除项 / 第七节新回归修复），无遗漏。

**4. 待你确认项（见 `待解决问题.md` 第八节第 3 点）：** `E12`（ErrorBoundary 包地图）、`D06`（reapplyAll）在源文档标 OPEN，但主理人当日早段自述已完成；若代码核实属实，应从待解决迁入本文件。`E32`（hex 残留）已据 `reports/TS迁移进度-2026-07-28.md` 代码验证确认修复，已迁入 R26。
