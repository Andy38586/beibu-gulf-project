# Git 仓库事故复盘 — 2026-08-02

> 适用对象:所有在本仓库执行 git 操作的 agent / 协作者(含未来回看本项目的人)
> 关联红线:`.workbuddy/memory/MEMORY.md` 的「Git 操作红线」

## 0. 一句话结论

本次仓库损坏**不是单一 bug**,而是 **agent 提交工作流在本环境下的系统性失效**,叠加一次**中文路径编码事故**。

- 最上游的起火点:**agent 在 ~120s 短超时下执行 `git commit`,触发 husky `npx lint-staged`(扫数百前端文件)被杀死**,随后连锁损坏。
- 另一起独立事故:`git checkout` 经 Git Bash 传递中文路径被篡改,把工作树整体覆盖回 `origin/main`。

---

## 1. 已实锤事实(来自残留物证 + 用户证据链)

- **F1 终态**:`.git/refs/` 缺失/空、`.git/index` 损坏(缓存树/resolve-undo 指向非法 sha1,缺失 blob `b49f2f9`)、工作树被覆盖(临时丢失 45 处本地修改 ≈1818 行,后从整树备份救回)。
- **F2 关键反证**:实测 `git checkout` **本身不删除** `.git/refs/`(用户下午验证)。→ refs/ 被删的元凶不是常规 git 命令,而是**修复过程 + IDE 后台 git**。
- **F3 环境约束**:本环境对 `.git/*` 的 `rm` 被 safe-delete 拦截(FAIL_CLOSED,不真删但可能留半成品/报错)。→ 任何 `rm -rf .git/...` 都既危险又无效。
- **F4 编码事故**:中文路径经 Git Bash(shell 引号/转义)传递时被篡改,`--pathspec-from-file` 收到错误路径 → checkout 把工作树整体覆盖。
- **F5 默认配置**:事故前 `gc.auto` 默认开、`maintenance.auto` 默认开(直到本次止血才设 `gc.auto=0` / `maintenance.auto=false` / `gc.autodetach=false`)。

---

## 2. 根因(上游 → 下游)

- **RC-A(最上游,起火点)**:agent 提交触发 husky `npx lint-staged` 扫数百前端文件,耗时超过 agent 工具 ~120s 超时 → commit 进程被杀。
- **RC-B**:进程被杀后留 `.git/index.lock`、半写 `.git/index`、孤儿松散对象,**无人善后**。
- **RC-C(二次损坏)**:默认 `gc.auto` / `maintenance.auto` 在后续某次 git 操作时自动执行,把 RC-B 留下的孤儿对象(仍被悬挂引用/损坏 index 半指向) prune 掉 → 对象损坏(`b49f2f9` 丢失)。
- **RC-D(refs/ 被删的真正原因)**:修复 agent(含主 agent)随后做**激进自修**——对 `.git` 跑 `rm`(被拦截留半成品)、移动/重写 refs、重写 packed-refs——与 **IDE 后台 git(Cursor autofetch / backgroundValidation)** 并发撞车,导致 `.git/refs/` 被移除。
  - **纠正记录**:本 agent 最初把"git 自身 pack-refs/rmdir 空 refs 目录"误判为根因。该机制 git 确实能做,但**本次删除是修复过程 + IDE 所为,非 git 自发**(F2 已反证 checkout 不删 refs)。此误判已更正。
- **RC-E(独立事故,另一根)**:任何"可能覆盖工作树"的 git 操作若经 Git Bash 传递中文路径 → pathspec 篡改 → 工作树被整体覆盖。

### 哪个是真正的"根"?

没有单一根。**最上游根因是 RC-A**(短超时下 agent 提交跑重 husky 被杀死),B→C→D 都从它 cascade;**RC-E 是独立另一根**(编码安全)。RC-D 是 A 引发后的"救援二次伤害",其直接执行者是修复 agent 的激进自修 + IDE 后台 git,而非 git 自身。

---

## 3. 解决方案(对应根因)

| 根因 | 方案 | 状态 |
|---|---|---|
| RC-A | agent 提交**绝不在短超时下跑 husky**:用 `git commit --no-verify`(lint 挪到 CI),或给钩子 ≥5min | 已立为红线 |
| RC-B | 提交前先查 `.git/index.lock`;存在则停手;被杀后 `mv`(非 `rm`)清锁再重试 | 已立为红线 |
| RC-C | 本仓库关自动 gc/maintenance:`gc.auto=0` `maintenance.auto=false` `gc.autodetach=false` | **已落地** |
| RC-D | agent **只报告 git 异常,不激进自修**:禁 `rm .git`、禁擅自重写 packed-refs/移动 refs;关 IDE 后台 git(Cursor 已删;VSCode autofetch 已在 `.vscode/settings.json` 关) | 已立为红线 + 部分落地 |
| RC-E | 中文路径**绝不经 bash 直传**:走 Python `subprocess`(list args, UTF-8/UTF-16)或 `--pathspec-from-file`;任何覆盖工作树操作前先 `robocopy` 整树备份(不含 `/PURGE`) | **已落地**(本次靠 23:11 备份救回) |

---

## 4. 本次止血操作(时间线)

1. 手写 `.git/packed-refs`(双引用 `refs/heads/main` + `refs/remotes/origin/main` → `c54aed98`),文件态抗删。
2. 设 `gc.auto=0` / `maintenance.auto=false` / `gc.autodetach=false`。
3. 修复被截断的 `.vscode/settings.json`(原仅 2 行非法 JSON),关 `git.autofetch` / `git.backgroundValidation`。
4. 用 23:11 整树备份 `robocopy` 覆盖工作树(不加 `/PURGE`)救回 45 处本地修改。
5. 清理:冗余引用 `backup-8win-20260802`(本次恢复自建的安全备份,同指 `c54aed98`)`mv` 出 `refs/heads/`;3 个未跟踪 `* copy.md` `mv` 至 `.trash-cleanup/`(均未进 git 历史)。

### 终验(提交前)
- `git fsck --connectivity-only` 干净;`HEAD=c54aed98`(112 提交 + 本次 2 提交);`packed-refs` 双引用完好;`refs/heads/` 虽空但 packed-refs 兜底;无 `index.lock`。
- `.workbuddy/` 已被 `.gitignore` 忽略,记忆文件不进历史。

---

## 5. 待办(未决)

- [x] 58 项本地变更已随 `3ec3859` 提交(使用 `--no-verify`,规避 RC-A)。
- [ ] `.trash-cleanup/` 暂存物(已无 git 价值)待彻底清理。
- [ ] 安全网备份 `C:\mypython\beibu-gulf-project.backup-20260802-231120` 待确认后删除。
- [ ] VSCode 若仍跑后台 git,确认其设置已关(本仓库 `.vscode/settings.json` 已设 `git.autofetch=false`)。

---

## 6. 红线摘要(agent 必须遵守)

1. 遇 git 异常**只报告,不自行激进修复**。
2. 提交前查 `index.lock`;被杀后 `mv` 清锁再重试。
3. 禁用 agent 短超时跑 husky;用 `--no-verify` 或给 5min。
4. 关 IDE 后台 git(autofetch 等)。
5. 中文路径走 Python / `--pathspec-from-file`,禁经 bash;覆盖工作树前先整树备份。

---

## 7. 给"未来回看本项目的人"的一句话

仓库能坏成 `not a git repository`,几乎总是**"agent 在超时下被杀死的提交 + 自动 gc 补刀 + 修复 agent 自己又下狠手"**这三连,而不是 git 本身的 bug。看到 refs/ 消失先想"谁在跑 git",别先怪 pack-refs。
