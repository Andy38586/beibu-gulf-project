# 当前状态速查

> 生成时间：2026-07-24 | 基于四阶段修复完成后的代码事实

---

## ��、已修复（42 项，2026-07-23 完成）

| 阶段 | 数量 | 高亮 |
|------|:--:|------|
| 阶段一 安全与数据契约 | 9 | P0-02 越权三件套、P1-06 TOCTOU 注册竞态、P1-14 密码转义、P2-09 UUID、P2-10 缓存脏写 |
| 阶段二 前端用户功能 | 11 | P1-01 收藏弹窗、P1-02 防重复提交、P1-04 重命名变量、P1-05 选址状态恢复、P1-13 退出按钮、P2-06 登出清 store |
| 阶段三 地图引擎与浸没 | 10 | P1-10 flyTo 秒、P1-11 点要素样式、P1-12 监听器泄漏、P2-01 CSS 类名、P2-02 水位乱序 |
| 阶段四 技术债务 | 12 | R-01 createFileStore 工厂、P3-05 useGCS 单例、P3-08 spread 栈溢出、P3-07 KEY 外置、P3-10 importance 取整 |

完整 diff 见 git log：`beabf81` → `05eed77` → `7ab3806` → `68845f2`

---

## 二、伴随 bug 修复

| Bug | 现象 | 根因 | 修复 |
|------|------|------|------|
| 引擎切换视角倾斜 | OL→Cesium 切回后视角变斜 | `_setCameraState` 恢复时用了导出的 pitch（用户交互后的倾斜值） | pitch 硬设 -90°，不读导出值 |
| OL 页面 Panel 不渲染 | Cesium 访问后所有 2D 页面 Panel 消失 | Cesium DOM 操作干扰 Vue `<transition mode="out-in">`，新组件挂载被阻塞 | 移除 `mode="out-in"` |

---

## 三、未修复（留待后续）

| 项目 | 原因 |
|------|------|
| 预测分析模块 | 全新功能开发，非 bug 修复 |
| lon→lng 统一 (P3-01) | 数据文件迁移 + 多渲染器改造，风险高 |
| 响应格式统一 (R-02) | flood 6 端点需统一改造 |
| CI/CD 缺失 | 工程建设专项 |
| TypeScript 迁移 | 渐进改造 |
| 裸 console 替换 | logger 已建，替换待批处理 |

---

## 四、不可当 bug 修（项目设计准则 §11）

- JSON 文件存储非数据库 → 刻意简化
- AppLayout 图表硬编码 → 首页占位，非缺陷
- 水面坐标硬编码兜底 → 已改为运行时 fetch + 兜底
- console 调试输出 → logger 已接入
- 天地图 KEY → 已改环境变量
