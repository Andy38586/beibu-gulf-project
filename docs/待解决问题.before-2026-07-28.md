# 待解决问题

> **定位**: 当前所有已知但尚未修复的问题。修一个删一个。
> **最后更新**: 2026-07-27
> **关联**: 已修复的问题见 `已解决问题.md`，可执行的修复方案见 `技术债务清单.md`，API 稳定契约见 `开工前必读/API契约文档.md`

---

## 一、架构问题

| # | 问题 | 影响 |
|---|------|------|
| A01 | AppLayout 直接 import LineChart/BarChart/RadarChart，硬编码城市按钮（钦州/北海/防城港） | 新增业务无法通过 slot 替换顶部按钮组，需改 AppLayout |
| A02 | UnifiedMap 职责过重（550+ 行，9 种职责） | 维护困难，需拆分 |
| A03 | 引擎切换不支持同页面多引擎共存 | 当前业务不需要 |
| A04 | API 调用三种方式并存（裸 fetch / useApiRequest / 自定义） | 维护成本 |
| A05 | visualization 反向依赖 business（useRadarChart → facilityConfig） | 影响组件独立性，违反分层约束 §2.3 Rule 3 |

## 二、后端安全与正确性

| # | 优先级 | 问题 | 文件 |
|---|:-----:|------|------|
| B01 | **P0** | `GET /api/markers/:id` 无归属校验（IDOR：任何已登录用户都能读取他人标注） | `controllers/markersController.js` |
| B02 | **P1** | 注册 TOCTOU 竞态 → 重复用户名 | `controllers/authController.js` |
| B03 | P1 | 选址分析用户输入错误返回 500（应返 400） | `services/siteAnalysisService.js` |
| B04 | P1 | 选址分析业务错误以 HTTP 200 返回 `{error}`（应返 4xx） | `controllers/siteAnalysisController.js` |
| B06 | P2 | 选址评分权重未校验（可传负数/字符串/NaN） | `services/scoringService.js` |
| B09 | P2 | ProfilePage `loadPlans()` 双触发（watch + onMounted 各触发一次） | `views/ProfilePage.vue` |
| B10 | P2 | 浸没范围"向上取档"语义未在前端 UI 明示（请求 2.5m 实际返回 3.0m，非就近匹配） | `controllers/floodAnalysisController.js` |
| B12 | P3 | scoring `Math.min` POI 量大时栈溢出 | `services/scoringService.js` |

## 三、预测分析模块

| # | 问题 | 影响 |
|---|------|------|
| C01 | 热力图：`addGeoJsonLayer` 不支持 `{type:'heatmap'}` 和 style 函数 | 需在 OLRenderer 中新增 |
| C02 | 数据流断裂：`ForecastPanel.currentValue` 读 `dataCache` 但无写入 | 需重设计 |
| C03 | registerToggleable 闭包累积：每次更新注册导致旧回调堆积 | 改为只注册一次或先 remove |
| C04 | 播放竞态：500ms/帧无取消机制，响应乱序 | 参考 `analysisSeq` 模式 |

## 四、工程实践

| # | 优先级 | 问题 |
|---|:-----:|------|
| D01 | **P0** | 全项目零测试（仅 `UnifiedMap.test.js` 骨架测试） |
| D02 | **P0** | 零 CI/CD |
| D03 | P0 | 状态管理重复（`siteSelectionState` 和 `floodState` 结构一致，未泛型化） |
| D04 | P1 | 双认证机制矛盾（同时用 Cookie + Bearer header，token 在模块级变量中无法主动清除） |
| D05 | P1 | 坐标字段命名不统一（`facilityPoints.json` 用 `longitude`/`latitude`，`terrainProfile.json` 用 `lon`，其余用 `lng`/`lat`。前端通过 `crs.ts normalizePoint()` 归一化，但数据源应统一） |
| D05a | P2 | `terrainProfile.json` 仍使用 `lon` 字段（应改为 `lng`，消除前端 fallback 兼容代码） |
| D06 | P1 | 引擎切换图层数据丢失（exportState 只存 visible，不存数据源） |
| D08 | P1 | 三套响应格式并存（RESTful / GCS / 混合），新增接口应统一使用 RESTful |
| D09 | P2 | layerCatalog 用 `ref` 存数组（大量 push 触发超量重渲染，应改 `shallowRef`） |
| D10 | P2 | 无空间索引（rbush），10000+ 设施点时全量渲染 |
| D11 | P2 | API 无版本号前缀 `/api/v1/` |

## 五、刻意简化（不要当 bug 修）

| # | 简化项 | 未来计划 |
|---|--------|---------|
| S01 | JSON 文件存储（无数据库） | 接 PostgreSQL + PostGIS |
| S02 | AppLayout 硬编码默认图表数据 | 接数据库后改为动态数据 |
| S03 | 浸没分析水面坐标硬编码兜底 | 替换为真实 DEM |
| S04 | 剖面分析面板为占位 | 功能开发 |
| S05 | 部分 mock 数据 | 接真实数据源 |
| S06 | 没有 CI/CD | 加 GitHub Actions |

---

## 已从清单移除（代码已修复）

| # | 原问题 | 修复标记 | 移除日期 |
|---|--------|---------|---------|
| B05 | ~~标记字段名不一致：创建存 `note`，更新白名单用 `description`~~ | 已统一为 `note` | 2026-07-27 |
| B07 | ~~plans/markers ID 用 `Date.now()` → 并发碰撞~~ | FIX:P2-09 改用 `crypto.randomUUID()` | 2026-07-27 |
| B08 | ~~plans 先改内存缓存后写文件，写失败时缓存已脏~~ | FIX:P2-10 构造新数组，不原地修改 | 2026-07-27 |
| B11 | ~~认证策略不一致（`/api/gcs/*` 全裸）~~ | FIX:P2-11 已加 `router.use(authenticate)` | 2026-07-27 |
| B13 | ~~facilities 缓存永不过期~~ | FIX:P3-09 加 TTL 5 分钟 | 2026-07-27 |
| — | ~~`useFloodStore` / `@/stores/floodStore` 残留引用（3 处）~~ | 已改为 `useFloodState` / `@/stores/floodState` | 2026-07-27 |
| D07 | ~~`/api/forecast/*` 无认证保护与其他模块不一致~~ | 前端路由 `meta.requiresAuth` 改为不要求认证，与后端 forecast 路由无 authenticate 对齐；预测数据视为公开数据为稳定设计决策 | 2026-07-28 |
