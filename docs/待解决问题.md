# 待解决问题（整合主清单 · 2026-08-01 更新 · 去重自三份审查报告 + 已解决尾巴）

> **定位**：当前所有经真实代码取证确认**仍未闭环**的问题。修一个 → 从本文件删除，按**原编号**移入 `已解决问题.md`（状态由"在哪一文件"决定，解决即移走）。
> **编号规则**：按类型分节，四位数。`a`=地图层、`b`=业务层、`c`=组件层、`d`=后端层、`z`=暂未归类。**本文件内部顺序编号**；移入 `已解决问题.md` 时**保留原编号不重排**——已解决问题不要求编号连续，因为问题不可能按编号顺序解决。
> **时间戳规则**：每个条目 `问题编号` 下方必须标注 `discover:YYYYMMDD`（首次写入本文件的时间，可附 `, HH:MM` 精确时间）。移入 `已解决问题.md` 时在原 `discover` 下补 `solve:YYYYMMDD`。
> **问题等级**：`p0` 致命/高危阻断｜`p1` 高（正确性/稳定性/安全非致命/面试短板）｜`p2` 中（待改进·低危 bug·语义不一致）｜`p3` 低（接受/硬化/文档/视觉 nits）。
> **来源标注**：每条标 `来源:`（审查文档 / 实测 / 已解决尾巴），便于追溯原始审查出处。

---

## 整合说明（2026-08-01）

- 本清单于 2026-08-01 重新整合：合并《工程化全面审查报告-2026-07-31》《技术面试压力测试报告-2026-08-01》《接口收口缺口清单-2026-07-31》三份审查文档的问题 + 《已解决问题.md》中**带尾巴 / 仍泄漏**的项，全部去重、赋予全局唯一编号。
- 三份审查文档已从 `docs/` 移除，其问题已尽收于本清单；原《待解决问题.md》2026-07-30 旧版（行 42-131 残留）与 2026-07-31 有效版大量重复，已清理——旧版条目（a015/a017/b019/c011-c020/d040-d047/z020/z022/z023/z026/z030/z034 等）均已归档于《已解决问题.md》，不再重复列。
- **编号修正**：原 `a014`（site-selection 无专属 adapter）与《已解决问题.md》`a014`（设施 POI 绕过图层管理器，已解决）撞号且异义，重编为 **`a021`**。
- **CI 门禁冲突（已闭环）**：《已解决问题.md》`d002` 称 `.github/workflows/ci.yml` 已存在（format:check→lint→typecheck→test→build），与《工程化报告》"P1 CI 门禁未落地"矛盾；**用户 2026-08-02 确认 CI 已生效（d002 成立）**，本清单不单列该项。
- **已确认闭环、本期不再列入**：响应契约分裂（b003/b006/z031）、数据源切换散落（b022/b024）、flood api 半通（b021 主体）、floodStore/floodState（d028）、reapplyAll 单测（z020 主体）、logger transports（z030/z032）、OLRenderer 3D-only 冗余（z006）、mapStore 职责集中（b001 文档化取舍）、useAuth 顶层钩子（c005）、.env 烘焙（d050 主体）、rbush 索引（a001）、typecheck 全红（现 @ts-nocheck 后 0 错）、GcsPanel 大小写（现 GCSPanel 大写）——上述在《已解决问题.md》有实码证据，已从 open 移除，仅其**尾巴**以新编号回流（见 d053/d054/d055/d056/z037/z038/b029/b030）。

---

## 地图层（图层生命周期 / 地图渲染 / 地图引擎集成）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | a019-Cesium视口裁剪BBox估算不准<br>`discover:20260801` | `CesiumRenderer._getViewportBBox:475-494` 用相机位置为中心的圆形/方形估算，非真实视口四角投影；`pitch≠-90` 倾斜视角下边缘 POI 被错误裁剪。OL 侧 a001（rbush 索引）已解决"有无裁剪"，但 Cesium 几何精度未覆盖。 | 改为视口四角经纬度投影求 bbox。`来源:面试报告 Q13/Q14` |
| p2 | a020-WebGIS性能未实测+缺优化<br>`discover:20260801` | GeoJSON 大小 / Feature 数量 / 数据量均未实测（README:26 自承"数据规模有限"）；缺 WebWorker / 矢量瓦片 / 数据切片三大优化手段，优化无据可依。 | 先实测数据量基线，再按需引入 WebWorker / 矢量瓦片。`来源:面试报告·性能章` |
| p2 | a021-site-selection无专属adapter<br>`discover:20260730` | site-selection 经 `useSiteAnalysisApi.ts:26` 走统一 `apiRequest` 边界（非直连 fetch），但全项目唯一无专属 adapter 抽象层（forecast/flood 有 `forecastAdapter`/`floodAdapter`）。原 a014 撞号重编为 a021。 | 补 `siteAnalysisAdapter` 与业务 adapter 同构，或明确豁免。`来源:待解决有效版` |

---

## 业务层（业务逻辑 / Adapter / Store / 业务页特性）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p0 | b025-预测热力图/算法造假嫌疑<br>`discover:20260801` | `backend/services/forecastEngine.js:101-140` 用 `Math.random()` 在 3 港口周围生成 120 个散射点填热力图；预测算法本质为线性外推+情景系数，非"根据历史数据预测"，与 README 宣称冲突。 | 改核密度 / 真实聚合；算法改"趋势外推法"表述。`来源:面试报告 P0-2/预测章` |
| p2 | b027-waterLevel传参姿势分裂<br>`discover:20260801` | `floodAdapter.ts:198/202` 用 query（`?waterLevel=`）与 `:244` 用 body（`/flood/analysis/disaster` POST），同语义字段两套序列化/解析，前后端易漂移。（行号以实测为准，原 224/269 有误） | 统一为 query 或 body 其一。`来源:接口清单 P1-2` |
| p2 | b029-flood mock不响应水位(尾巴)<br>`discover:20260801` | 《已解决》b019 仅加 `logger.warn` 诚实标注 + 固定返回 2.5m 档，mock 模式下演示滑块仍无效（诚实化≠修复）。 | mock 模式接真实水位函数或 UI 明确禁用滑块。`来源:已解决 b019 尾巴` |
| p2 | b030-getDEM/三维淹没仍mock(尾巴)<br>`discover:20260801` | `floodAdapter.ts:256-259` "DEM 管线待接入"，三维淹没模拟仍依赖 mock DEM；与 README"三维淹没模拟"叙事有差距。 | 接真实 DEM 或改叙事为"预设水位档位 3D 水面可视化"。`来源:已解决 b021 尾巴/面试简历章` |

---

## 组件层（UI 组件 / CSS / 面板 / 视图组件 / 前端打包）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | c021-登录态UI未反映<br>`discover:20260801` | `BottomNavBar.vue:55-62` "个人中心"恒显，未随 `useAuth.user` 变化。⚠️ 2026-08-01 复核：`ProfilePage.vue` 用 `v-if="!user"` 切换 LoginPanel，恒显入口**符合"默认开放、动作层软校验"的鉴权设计取向**，属体验取舍非逻辑缺陷，p1 或偏高。 | 绑定 `user`，未登录隐藏或显"登录"（可选优化）。`来源:工程化报告 P1` |

---

## 后端层（后端代码 / API / 认证 / 服务 / 部署配置）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | d035-无HTTPS/HTTP2<br>`discover:20260730` | nginx `listen 80` 无 443/TLS，auth cookie 明文、安全头缺失；brotli 缺失属 p3 性能合并于此。⚠️ 2026-08-01 复核：`docker-entrypoint.sh:6-56` 已有条件 HTTPS（检测证书生成 https.conf），非"仅预留"；默认无证书仍走 80。用户决策：无证书暂缓。 | 配证书后 entrypoint 自动启用 TLS/HTTP2。`来源:待解决有效版/工程化 P0` |
| p3 | d039-API无版本前缀<br>`discover:20260730` | API 无 `/api/v1/` 版本前缀。用户决策：不加（单版本无演进需求）。 | 接受 / 暂缓。`来源:待解决有效版` |
| p2 | d051-缺统一入参校验层<br>`discover:20260801` | 2026-08-01 复核修正：`siteAnalysisController.js`/`authController.js` **均走 `BusinessError` + 全局错误中间件**（无手写 `res.status(400)`，原表述失实）；剩余成立部分=无统一 `validate` 中间件，参数校验散落各 controller。 | 建统一 `validate` 中间件。`来源:接口清单 P2-2` |
| p3 | d053-.env防护无CI校验(尾巴)<br>`discover:20260801` | 《已解决》d050 三重防护（`.dockerignore`+compose `env_file`+`NODE_ENV=production`）到位，但无 CI 校验，改错即重新泄露。 | CI 加 `.env` 不外泄检查。`来源:已解决 d050 尾巴` |
| p3 | d054-公开路由白名单兜底确认<br>`discover:20260801` | d008/d036 决策"只读开放"，forecast 侧 d032 已加指标白名单；flood 侧 GET 参数遍历风险待统一确认（`validateWaterLevel` 有校验，但其他 GET 遍历需排查）。 | 全公开路由统一输入白名单。`来源:已解决 d008/d036 尾巴` |
| p3 | d055-认证恢复逻辑微妙(尾巴)<br>`discover:20260801` | 《已解决》d033 改 `restoreAuth` 始终调 `/auth/me`；`useAuth.ts:81-104` 用占位 token 启用 `isAuthenticated`、`checkAuth` 在 token 空时短路 `return null`。⚠️ 2026-08-01 复核：d038 注释明确"Cookie 为权威，成功后 setToken"——**是有意设计**，边界为防御性考量非缺陷，p3 恰当。 | 边界用例补充测试（可选）。`来源:已解决 d033 尾巴` |
| p2 | d056-git历史SQLi探针账号(尾巴·泄漏)<br>`discover:20260801` | 《已解决》d044 仅备 `tools/git-clean-history.sh`（filter-repo），"上线前手动执行"未执行。2026-08-01 实测修正：git 历史 `users.json` 密码**均为 bcrypt 哈希（非明文，原"明文"表述失实）**；但 username 字段含 `admin' OR '1'='1'` SQLi 探针账号，泄漏态仍在（HEAD 已无该文件、工作区仅 1 个 webgis）。 | 上线前执行 filter-repo 清历史。`来源:已解决 d044 尾巴` |

---

## 暂未归类（文档 / 工程质量 / 测试覆盖 / 杂项）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p2 | z033-mapDataService直接fetch<br>`discover:20260730` | `mapDataService.ts:28` / `useBoundaryLayer.ts:49` 直接 `fetch` 静态 GeoJSON，绕过 adapter 与统一 `errorHandler`/`logger`，错误无统一处理（与 adapter mock 三套缓存互不认）。 | 抽 `mapDataAdapter` 或错误走 `errorHandler`+`logger`。`来源:待解决有效版/接口清单 P2-3` |
| p2 | z036-stats.html无条件生成 ✅已闭环<br>`discover:20260801`<br>`solve:20260801` | 原称"每次 `vite build` 无条件生成 `dist/stats.html`"。**2026-08-01 实测：`vite.config.js:52-56` 已有 `ANALYZE === 'true'` 守卫，此问题已修复，文档滞后**。 | 已闭环，无需处理。`来源:工程化报告 P2` |
| p2 | z037-console残留数字对不齐 ✅已清点<br>`discover:20260801`<br>`solve:20260801` | 原称"102 处/28 处数字矛盾"。**2026-08-01 实测：真实 `console.*` 残留 ≈ 0**——前端仅 `logger.ts:32` 实现内 1 处；后端仅 `logger.js:17` 实现内 1 处 + `auth.js:12/33` 注释字符串 2 处（非调用）。生产静默已由 logger 实现保证，两数字均失实。 | 已清点闭环。`来源:已解决 z028/z003 尾巴/工程化 z028` |
| p2 | z038-mapStore关键链路单测覆盖待确认(尾巴)<br>`discover:20260801` | 《已解决》z020 称补 `mapStore.test.ts`（9 测试）；2026-08-01 实测：`mapStore.test.ts` 9 个 it **确实未覆盖** `registerAnalysisHandler`/`setLayerVisible`；但 `reapplyAll` 不在 mapStore 而在 `BusinessLayerManager.ts:161` 且 `BusinessLayerManager.test.ts:147` **已有测试**（原表述将 reapplyAll 误归于 mapStore）。 | 补 `mapStore.test.ts` 对 `registerAnalysisHandler`/`setLayerVisible` 的覆盖即可。`来源:已解决 z020 尾巴/面试 P2-2` |
| p3 | z040-领先版本过激<br>`discover:20260801` | Vite8 / TS6 / vue-tsc3 全为大版本，unplugin* / vite-plugin-cesium 磨合风险，@vueuse 已现 `INVALID_ANNOTATION` 警告。 | 谨慎升级，锁版本。`来源:工程化报告·技术栈` |
| p3 | z041-profileStore vs useAuth.user混淆<br>`discover:20260801` | `profileStore`（选址方案选中态）与 `useAuth.user`（登录态）命名相近、职责不同，新人易混。 | 重命名 / 加注释区分。`来源:工程化报告·架构` |
| p3 | z042-函数过长<br>`discover:20260801` | `CesiumRenderer` 多处方法过长（`addPointLayer:394-434` 等）、`floodAdapter.ts` 单文件 264 行，可读差。 | 拆分。`来源:面试报告·代码质量` |
| p3 | z043-文档与代码同步漂移+维护风险<br>`discover:20260801` | `docs/开工前必读/项目根基.md` 1230 行，与代码同步成本极高，半年必漂移；改 Renderer / GCS / DEM 维护风险极高。 | 减负 / 文档拆分。`来源:面试报告·维护风险` |
| p0 | z045-简历/README/代码叙事冲突(致命)<br>`discover:20260801` | README 自述"学习性质个人项目"(:165)+"不追求业务闭环"(:26)却用于面试；宣称"三维淹没模拟"(:13)/"历史数据预测"(:12)但 flood DEM mock、berth/traffic 前端 fixture、forecast 热力图 `Math.random`。面试官可当场拆穿。 | 改叙事：mock/模拟实现如实说明；删"预测/三维淹没"暗示；严禁写 PostGIS / GeoServer / 大规模空间分析。`来源:面试报告 P0-1/简历章` |
| p2 | z046-核心renderer仍@ts-nocheck<br>`discover:20260801` | `CesiumRenderer.ts`/`OLRenderer.ts` 第 1 行 `@ts-nocheck`（D-6 渐进迁移策略），类型安全已延期；`npm run typecheck` 现 0 错但靠排除两文件，非真正类型覆盖。 | 渐进补类型后移除 `@ts-nocheck`。`来源:D-6/面试 P1-1` |

---

## 风险观察（非 bug · 面试 / 架构叙事）

- **Renderer 抽象 / GCS 布局过度设计嫌疑**：`MapRenderer` 277 + `OLRenderer` 607 + `CesiumRenderer` 1064 ≈ 1948 行仅服务于 2D↔3D 切换；GCS `useGCS.ts` 225 行 + 组件仅 4 业务页。面试需准备 ROI 辩护（对应面试报告架构章）。
- **依赖安全审计本机不可执行**：`npm audit` 在 npmmirror 镜像返回 `NOT_IMPLEMENTED`（d042 已记环境限制），上线前须切官方源跑。
- **性能优化无基线**：a020 已列，此处重申——没有数据量基线，任何"优化"都是空话。

---

## 附：整合计数与状态（2026-08-01）

- 本清单整合自三份审查文档（工程化 / 面试 / 接口收口）+ 已解决尾巴，全部去重。
- **2026-08-02 登记并闭环（08-01 发现 · 08-02 修复）**：以下 9 项为 08-01 三 agent 审计 + 实码核验新发现、08-02 已修复的问题。按编号规则先在本文件登记 `discover:20260801`，随即移入 `已解决问题.md`（补 `solve:20260802`），本文件不保留。编号接续两份文档各系列最大编号（a021→**a022**、b030→**b031**、c021→**c022**、d056→**d057-d061**、z046→**z047**）：
> - **a022**（p1）2D↔3D 切换业务图层丢失：`clearLayerCatalog` 清空含业务条目 → `reapplyAll` 读 catalog 找不到 → 跳过。修：BusinessLayerManager 改读 `_registry.visible`。+2 回归测试。
> - **b031**（p2）401 软登录缺口：`usePlans` 全部写操作 + `useSiteAnalysisApi` catch 缺 `isAuthError→handleAuthError`。修：补全链路。
> - **c022**（p3）GCSPanel CSS 变量小写 `--gcs-panel-*` 违规（GCS 铁律全大写）。修：改名 `--GCS-panel-*`。
> - **d057**（p2）userService 缓存原地变异（P2-10 缺失，写盘失败污染 cache）。修：createUser/updateTokenVersion/updatePassword 构造新数组。
> - **d058**（p2）markers 坐标无校验（NaN/字符串/越界入库）。修：createOne/updateOne 数值+范围校验（M-01）。+4 测试。
> - **d059**（p2）`filterFacilitiesInCoverage` 未复用空间索引（O(F·K)）。修：createSpatialIndex+queryByPolygon。+5 测试。
> - **d060**（p2）typeSettings 缺键致 500（`setting.defaultRadius` TypeError）。修：resolveRadiusSettings 补防御（400）。
> - **d061**（p2）trust proxy 未设，nginx 后限流按 127.0.0.1 统一计数（形同虚设）。修：app.js `app.set('trust proxy')`。
> - **z047**（p1）`forecastState.ts:5` store 反向 import business（stores→business 禁止引铁律）。修：DEFAULT_CONFIDENCE 上提 shared/constants/forecast.ts。
> - 另有 **b026**（overview 路由错配，p1）已在 08-01 登记于本文件，08-02 修复后按规则移入已解决（同上补 solve）。
- **2026-08-01 实码复核修订**：b028（GET 模板字符串，apiRequest 已 URLSearchParams）与 z039（路由嵌套混用，实为全 kebab-case）**失实删除**；z036（stats.html 已有 ANALYZE 守卫）与 z037（console 真实残留≈0）**已闭环**；d051/d056 表述修正；c021/d055 注明设计取向。
- 当前 OPEN 项：**地图层 3**（a019/a020/a021）、**业务层 4**（b025/b027/b029/b030）、**组件层 1**（c021）、**后端层 7**（d035/d039/d051/d053/d054/d055/d056）、**暂未归类 9**（z033/z036-z046，z036/z037 闭环仍列备查，z039 删除，z044 并入 z043）→ 合计 **24 项**。
  - 按等级：p0 × 2（b025、z045）｜p1 × 3（a019、c021、d035）｜p2 × 11（a020/a021、b027/b029/b030、d051/d056、z033/z038/z046，z036/z037 已闭环）｜p3 × 8（d039/d053/d054/d055、z040/z041/z042/z043）。
- 清理：移除 2026-07-30 旧版重复行（均已归档于《已解决问题.md》）。
- 编号冲突已解：a014（无专属 adapter）→ a021；a016 双状态已收敛（2026-08-02 更新《已解决》a016 为最新完整版并移除本清单条目，单一状态）。c016（CSS !important 系统性）用户 2026-08-02 确认已闭环（见《已解决》c016，solve 校正为 20260802），非本清单 open 项。
- 三份审查文档已不在 `docs/`，其问题尽收本清单。
