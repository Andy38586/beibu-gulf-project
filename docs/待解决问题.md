# 待解决问题（重组版 · 2026-07-30 · 类型分节 + a/b/c/d/z 编号）

> **定位**：当前所有经真实代码取证确认**仍未闭环**的问题。修一个 → 从本文件删除，按**原编号**移入 `已解决问题.md`（状态由"在哪一文件"决定，解决即移走）。
> **编号规则**：按类型分节，四位数。`a`=地图层、`b`=业务层、`c`=组件层、`d`=后端层、`z`=暂未归类。**本文件内部顺序编号**（a014→a015→...）；移入 `已解决问题.md` 时**保留原编号不重排**——已解决问题不要求编号连续，因为问题不可能按编号顺序解决。
> **时间戳规则**：每个条目编号下方必须标注 `discover: YYYYMMDD, HH:MM`（首次写入本文件的时间）。移入 `已解决问题.md` 时补 `solve: YYYYMMDD, HH:MM`。
> **问题等级**：`p0` 致命/高危阻断｜`p1` 高（正确性/稳定性/安全非致命/面试短板）｜`p2` 中（待改进·低危 bug·语义不一致）｜`p3` 低（接受/硬化/文档/视觉 nits）。
> **来源整合**：整合原《待解决问题.md》（A01–A10 审计）、`排雷清单与待解决问题diff-2026-07-30.md`（B-R1–B-R7 全量覆盖）、`双文档交叉验证报告-2026-07-30.md`（新增 a018 Cesium §4.2 矛盾 / z028 console 残留 / b023 损失金额单位）、`首屏加载性能评估-2026-07-29.md`（PERF 系列）、`个人中心与登录面板-用户意图记录-2026-07-30.md`（PROF 系列）、`GCS命名规范与歧义排查-2026-07-30.md`（CGCS2000/GCS 残留；代码层残留小写类名 + GCSFeature 误称已于 2026-07-30 22:37 全部清零，归入 `已解决问题.md z018/z019`，无新增未闭环项）、`工程化收口方案-2026-07-30.md`（G-1~G-10 工程化缺口 / 密钥泄露，新增 b024 / d048 / z029-z034）。所有松散报告已于 2026-07-30 整合后移出 docs/ 主目录，内容以本文件为准。
> **说明**：设计意图/刻意简化类（原 A01–A04、E01、E16、D08、E14残留、S01–S06）经 2026-07-30 基准复核确认非缺陷，已迁入 `已解决问题.md`，不计入本文件。

---

## 地图层（图层生命周期 / 地图渲染 / 地图引擎集成）

> 以下条目 `discover` 均为 `~20260728`–`~20260730`（基准审计/排雷清单期间首次记录）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p2 | a014-设施POI绕过图层管理器 | 设施 POI 直调 `renderer.removeLayer`/`addPointLayer`/`registerToggleable` 绕过 `BusinessLayerManager`，双轨图层管理，`reapplyAll` 管不到；另 site-selection 全项目唯一无 adapter 直连后端（`useSiteAnalysisApi.ts:24`）。 | 统一经 `businessLayerManager` 注册；证据 `SiteSelectionPage.vue:124/139/146/158`。 |
| p2 | a015-热力图色带未参数化 | `addHeatmapLayer` 默认 `LAYER_DEFAULTS.heatmapGradient`，业务层从未传 `options.gradient`。 | 业务层传入 `gradient`/`colorScale`；证据 `OLRenderer.js:344`。 |
| p2 | a016-裁剪集成无单测 | 裁剪集成路径（`_addCulledPointLayer`/`_refreshCulledLayer`/`moveend` 增量刷新）无单测。 | 补 cull 单元测试。 |
| p3 | a017-z-index无刻度 | `z-index` 无 `--GCS-z-*` 刻度，`UnifiedMap.vue:426` 地图容器 `z-index:1` 把内部 `.map-loading(100)`/`.map-error(100)`/`.boundary-warning(90)` 封顶，地图加载/错误提示可能被面板压住。 | 建立 z-index 刻度令牌；证据 `UnifiedMap.vue:426`。 |

---

## 业务层（业务逻辑 / Adapter / Store / 业务页特性）

> 以下条目 `discover` 均为 `~20260728`–`~20260730`（基准审计/排雷清单/交叉验证期间首次记录）。b023 已单独标注精确时间戳。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | b019-flood mock忽略水位 | flood mock 完全忽略水位参数（`getFloodAnalysis(_waterLevel)` 参数 `_` 前缀显式未用），滑块与分析结果脱节，核心演示链路语义失真；`api` 分支直接 `throw`。 | `floodAdapter` 接入真实水位；证据 `floodAdapter.ts:184-213`/`:217-238`。 |
| p2 | b020-向上取档UI未明示 | 浸没范围"向上取档"语义未在前端 UI 明示（请求 2.5m 实际返回 3.0m），仅 `logger.info` 提示，用户无感知。 | `FloodAnalysisPage` 增加档位提示 UI；证据 `floodAnalysisController.js:62`/`:209`、`FloodAnalysisPage.vue:245-247`。 |
| p2 | b021-flood api未闭环 | flood mock 改读 `/data/*.json`，但 `api` 分支仍 `throw('真实 API 尚未接入')`，设计预留未闭环。 | 接真实 flood API 或显式标注 mock-only；证据 `floodAdapter.ts:181/214/238/245`。 |
| p2 | b022-forecast adapter默认api | forecast adapter 默认 `'api'`（`forecastAdapter.ts:123`），flood 默认 `'mock'`，三模块数据源策略三套，dev 无后端时 Forecast 跑不通。 | 统一默认 `'mock'` 或 bootstrap 显式 `setDataSource`；证据 `floodAdapter.ts:13`。 |
| p1 | b023-损失金额单位虚标<br>`discover: 20260730, 16:06` | `FloodAnalysisReportPanel.vue:45-50` `loss>=10000` 时 `(loss/10000).toFixed(1)+' 亿'`——除 10000 却标"亿"（1 亿=1e8，应为"万"或除 1e8）；同一值在 `AffectedFacilityListPanel.vue:43` 标"万"，两面板数据对不上，面试展示直接打脸。 | 统一单位换算常量（建议 `LOSS_UNIT_DIVISOR`），报告面板改成"万"或除 1e8；证据 `FloodAnalysisReportPanel.vue:45-50`、`AffectedFacilityListPanel.vue:43`。 |
| p2 | b024-无env数据源开关<br>`discover: 20260730, 16:29` | `forecastAdapter`/`floodAdapter` 无 `import.meta.env` 读取，`setDataSource('mock'|'api')` 全靠手动调用，无按环境自动切换；与 b022（默认不一致）相邻，本项缺"env 驱动自动切"。 | 新增 `VITE_DATA_SOURCE`（默认 `mock`），`main.ts` bootstrap 读 env 调 `setDataSource`；证据 `forecastAdapter.ts:123`/`floodAdapter.ts:13`。 |

---

## 组件层（UI 组件 / CSS / 面板 / 视图组件 / 前端打包）

> 以下条目 `discover` 均为 `~20260728`–`~20260730`。c020 已单独标注精确时间戳。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | c011-ElementPlus首屏110KB | Element Plus 110KB gzip 进首屏（占 44%），`ui-vendor` 合并全站 El 组件并在首屏 `modulepreload`，落地页只用到 ElButton 却为全站买单；已核实为按需打包真实代价非 bug。 | 权衡换轻量自研组件或细分 `ui-vendor` 包；证据 `main.js:44-47`、`WaterLevelProfilePanel.vue`。 |
| p3 | c012-ProfilePage CSS选择器错 | `ProfilePage.vue:586` CSS 选择器 `.fav-section :deep(.favorite-list-panel)` 类名错误，`PaginatedListPanel` 根 class 实为 `.paginated-list-panel`，`:deep` 永不命中，收藏区背景覆盖失效（视觉）。 | 改为 `.paginated-list-panel`；证据 `PaginatedListPanel.vue:309`。 |
| p3 | c013-登录与个人中心嵌套 | `ProfilePage` 登录面板与个人中心嵌套（`LoginPanel` 嵌在 `GCSPanel` 顶部 25% + 收藏夹），用户要求改为"两张屏" `v-if` 互换；待拍板。 | 同一路由 `/profile` 内 `v-if` 切换登录面板/个人中心面板；证据 `ProfilePage.vue:265`/`:268`/`:271`。 |
| p3 | c014-LoginPanel归属shared | `LoginPanel.vue` 位于 `shared/components/` 但全仓仅 `ProfilePage` 引用，与"独属于个人中心"语义不符；待拍板是否移出 `shared/`。 | 评估移入 `views/profile/` 或 `business/.../profile/`；证据 `LoginPanel.vue`、`router/index.js:35`。 |
| p3 | c015-flood配色散落 | flood 风险配色散落组件（`FloodAnalysisPage.vue:371-399`），未抽 `colors.ts`。 | 抽 `FLOOD_RISK_COLORS` 入 `shared/constants/colors.ts`。 |
| p3 | c016-CSS!important系统性 | `GCSPanel` 内联写死定位，`style.css:163` 与 `FloodAnalysisPage.vue:505` 只能靠 `!important` 反杀，每加变体多一组。 | 抽定位令牌，消除 `!important`。 |
| p3 | c017-重复样式块 | 小区列表行（`SiteSelectionPage.vue:382` vs `ProfilePage.vue:591`）、2 列按钮网格（3 份）、滑块 thumb（3 份）、`.info-row`（3 份）重复。 | 抽公共样式。 |
| p3 | c018-前端硬编码魔法值 | 港口名硬编码（`ForecastPage.vue:53`）、`BASE_YEAR/END_YEAR`（`ForecastControlPanel.vue:100`）、默认置信度 `0.8` 写死 5 处、水位刻度阈值（`WaterLevelProfilePanel.vue:79`）。 | 抽常量。 |
| p3 | c019-死代码 | `useFacilities.ts` 整文件无引用；`colors.ts` 的 `RISK_COLORS`/`CHART_COLORS` 从未 import；`RadarChart.vue:38` 死 `close` emit；`FloodAnalysisReportPanel.vue:63` `showReport` 恒真死分支；死 CSS 多处。 | 清理。 |
| p3 | c020-GCS Token合规缺口<br>`discover: 20260730, 16:10` | 三项合并：①半透明面板背景 `rgba(255,255,255,0.95)` 4 处绕过 `--GCS-bg-panel-translucent`（PortInfoPanel/FloodAnalysisReportPanel/WaterLevelProfilePanel/FloodAnalysisPage）；②散落字号 `13/15/11/10px` 脱离 GCS 字号令牌；③渲染层风险色散落组件未抽 `colors.ts`（非 flood 域，c015 仅覆盖 flood）。 | 全局收敛至 `--GCS-*` Token + `colors.ts`；证据 `排雷清单` Nits。 |

---

## 后端层（后端代码 / API / 认证 / 服务 / 部署配置）

> 以下条目 `discover` 均为 `~20260728`–`~20260730`。d047 已单独标注精确时间戳。d032 已于 2026-07-30 修复移入 `已解决问题.md`。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p0 | d030-nginx无静态缓存 | nginx 无静态资源缓存（`expires`/`Cache-Control` 全无），每次回访重下 265KB，Lighthouse 缓存项崩，CDN 失效全量回源。 | `location /assets/ { expires 1y; add_header Cache-Control "public, immutable"; }`；证据 `nginx.conf`。 |
| p0 | d031-无preconnect天地图 | 无 `<link rel=preconnect>` 到天地图，首张瓦片现场 DNS+TLS+RTT。 | `index.html` head 加 `preconnect t0.tianditu.gov.cn`；证据 `首屏加载性能评估`。 |
| p1 | d033-认证恢复依赖localStorage | 认证恢复依赖 localStorage，与 HttpOnly Cookie 主通道不一致：`restoreAuth` 仅当 localStorage 恢复 user 非空才调 `/auth/me`，否则 `return null`；Cookie 有效但清了 localStorage 的会话被判未登录。 | 以 Cookie 为权威，`restoreAuth` 始终尝试 `/auth/me`；证据 `useAuth.ts:79-106`、`useApiRequest.ts:38`。 |
| p1 | d034-flood水位校验缺失 | flood 水位仅 `isNaN` 校验，缺 `isFinite`/下限/上限，`'Infinity'` 可绕过 `isNaN`。 | 加 `Number.isFinite(level) && level>=0 && level<=上限`；证据 `floodAnalysisController.js:199-203`。 |
| p1 | d035-无brotli/HTTP2 | 无 brotli/无 HTTP2：nginx 仅 gzip，`listen 80` 无 HTTPS/HTTP2，比 brotli 劣 15–20%。 | 启 `ngx_brotli` + TLS/HTTP2；证据 `nginx.conf`。 |
| p2 | d036-剖面面板鉴权不一致 | 剖面面板直连需鉴权的 flood API（`/flood/terrain-profiles`），与免登录 mock 主页不一致，剖面必 401，产品语义分裂。 | 统一鉴权策略或剖面也走 mock；证据 `WaterLevelProfilePanel.vue:128`、`floodAnalysis.js:3,8`。 |
| p2 | d037-注册无限流 | 注册接口无限流，仅吃全局 100/15min，`/register` 无专属 limiter（`/api/auth/login` 有 max5）。 | `app.js` 给 `/register` 加 `authLimiter`；证据 `app.js:21-34`。 |
| p2 | d038-响应体回传JWT | 登录/注册响应体回传 JWT（`{token,user}`），削弱 HttpOnly 收益，XSS 可读响应/内存 token 绕过 Cookie。 | 移除响应体 token，仅 `Set-Cookie`；证据 `authController.js:52`/`:94`、`useAuth.ts:152`/`:167`。 |
| p2 | d039-API无版本前缀 | API 无版本号前缀 `/api/v1/`。 | 加版本前缀；证据 `routes/*`、`apiRequest`。 |
| p2 | d040-Controller测试不全 | Controller 层测试不全（已补 `forecastController`，其余缺测）。 | 补 controllers 单测。 |
| p3 | d041-flood控制器手算评估 | flood 控制器内手算评估（`facility.value*damageRate`）且无 repository/缓存，与 forecast/site-analysis 分层不一致。 | 抽 `floodService` repository + 缓存。 |
| p3 | d042-依赖安全未审计 | 依赖安全未审计（`npm audit` 本机镜像 `NOT_IMPLEMENTED`）。 | 上线前官方源跑 `npm audit`。 |
| p3 | d043-无操作审计日志 | 成功操作无审计日志。 | 加操作审计日志；证据 `authController.js`。 |
| p3 | d044-users.json git历史 | git 历史 `users.json` 明文测试/探针账号（含 SQLi 探针）未清理；决策 A（`git filter-repo`）已批准，实施推迟。 | mirror 备份后 `git filter-repo` 清理。 |
| p3 | d045-userService无缓存 | `userService` 特意 `useCache:false`（`userService.js:10`），每个认证请求都读盘（auth 层 N+1）。 | 启用用户缓存。 |
| p3 | d046-engineCache无上限 | `forecastService.js:28` `engineCache` 无上限，匿名攻击者可枚举 `confidence` 制造内存放大（与 d032 组合）。 | 加 LRU / 上限。 |
| p3 | d047-scoringService双重循环<br>`discover: 20260730, 16:10` | `scoringService` 距离评分仍线性双重循环（O(n²)），rbush 空间索引已实现但近邻查询未用上。 | 接入 rbush 近邻替代全量遍历；证据 `排雷清单` 🟡 Suggestions。 |
| p1 | d048-天地图密钥入库<br>`discover: 20260730, 16:29` | `frontend/.env:2` 提交真实 `VITE_TIANDITU_KEY`（天地图密钥）；若 `.env` 已进版本库（非 gitignore），密钥进 git 历史即泄露，且 `VITE_` 前缀变量会被打包进客户端 JS。 | 密钥移 `.env.local`（gitignore 优先）+ 到天地图控制台轮换 key；`.env` 仅留占位；证据 `frontend/.env:2`。 |

---

## 暂未归类（文档 / 工程质量 / 测试覆盖 / 杂项）

> 以下条目 `discover` 均为 `~20260728`–`~20260730`。z028 已于 2026-07-30 验证后移入 `已解决问题.md`。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p2 | z020-Store/API测试覆盖 | 测试覆盖：Store/API 核心仍未全覆盖（测试文件由 4 增至 13，但核心 Store/API 仍缺），面试最大短板。 | 补 store/api 单测。 |
| p2 | z021-dist重复chunk | dist 重复 chunk 堆积（`vite.config.js:72 emptyOutDir:false` 致 17 份 `index`/`AppLayout` 等，dist 23MB），浪费磁盘/CI/部署包。 | `emptyOutDir` 按环境（生产 `true`）。 |
| p2 | z022-天地图外部依赖 | 天地图外部依赖不可控，底图延迟由 `tianditu.gov.cn` 决定。 | 后续同源代理/自托管瓦片。 |
| p3 | z023-README脱节 | README 与代码脱节（仍为"港口选址分析平台"+JWT+方案管理，未提 forecast/flood/mock 适配器/GCS）。 | 上服务器前重写 README。 |
| p3 | z024-withTimeout不abort | `withTimeout` 超时后不 abort 底层 promise，后台继续写已卸载组件 ref。 | 加 `AbortController` 传播；证据 `UnifiedMap.vue:60-62`。 |
| p3 | z025-重复provide mapStore | `App.vue` 与 `UnifiedMap.vue` 重复 `provide('mapStore',…)`。 | 仅保留一处 provide；证据 `App.vue:28`、`UnifiedMap.vue:49`。 |
| p3 | z026-mapStore unknown槽 | `mapStore.lastAnalysisResult` 的 `unknown` 槽来自 `sessionStorage` `JSON.parse`，无静态类型。 | 收敛类型；证据 `mapStore.ts`。 |
| p3 | z027-两张屏路由决策 | "两张屏"是同一路由 `/profile` 内 `v-if` 切换，还是拆独立 `/login` 路由——用户未拍板。 | 待用户决策。 |
| p2 | z029-无运行时错误捕获<br>`discover: 20260730, 16:29` | 全仓无 `app.config.errorHandler` / `window.onerror` / `onunhandledrejection`；开发者漏写的 try/catch、async 未 catch、组件渲染错误会白屏无痕。 | `main.ts` 注册 `app.config.errorHandler`→`errorHandler.showError`+`logger.error`，挂 `window.onerror`/`onunhandledrejection` 兜底；证据 全仓 grep 无 `config.errorHandler`。 |
| p3 | z030-无错误上报/监控<br>`discover: 20260730, 16:29` | `errorHandler.ts:4-5` 注释"后续可接入"但无上报/监控后端，生产错误无法聚合分析。 | 预留 `logger.transport`/reporter 钩子，未来接 Sentry 等；与 z029 联动。 |
| p2 | z031-errorHandler未接请求边界<br>`discover: 20260730, 16:29` | `useApiRequest` 抛 `ApiError`，但页面各自 catch；`useSiteAnalysisApi.ts:43-68` 手写 `ApiError.code→文案` 映射，与 `errorHandler` 重复且口径不一（A 页"超时"、B 页"网络异常"）。 | 规则：apiClient 只抛 `ApiError`，统一在调用点/`useAsyncData` `catch→errorHandler.showError`；删 `useSiteAnalysisApi` 手写 switch；证据 `useSiteAnalysisApi.ts:43-68`。 |
| p3 | z032-logger非结构化/生产仍console<br>`discover: 20260730, 16:29` | `shared/utils/logger.ts:7-8` `warn`/`error` 生产环境仍直接 `console.*`，且输出非结构化（无 `level/module/timestamp` 字段）。注：`已解决问题.md z028` 称"全站 console 已 DEV 守卫、生产静默"不实——logger 自身未被守卫。 | 输出结构化 `{level,module,action,message,timestamp,data?}`；`warn/error` 生产保留脱敏，`debug/info` 仍 DEV 门控；预留 transport 钩子；证据 `logger.ts:7-8`。 |
| p2 | z033-mapDataService直接fetch<br>`discover: 20260730, 16:29` | `mapDataService.ts:28` / `useBoundaryLayer.ts:49` 直接 `fetch` 静态 GeoJSON，绕过 adapter 与统一 `errorHandler`/`logger` 边界，错误无统一处理。 | 方案待拍板：① 视为地图 infra 免 adapter，但错误走 errorHandler+logger 结构化；② 抽 `mapDataAdapter` 与业务 adapter 同构（为阶段二 GeoServer 演进铺路）；证据 `mapDataService.ts:28`/`useBoundaryLayer.ts:49`。 |
| p3 | z034-无useAsyncData封装<br>`discover: 20260730, 16:29` | 复杂页（Forecast/Flood/SiteSelection）各自维护 `loading/error/refresh/cancel`，重复造状态、易出竞态/取消 bug。 | 新增 `shared/composables/useAsyncData(fn,{immediate})` 封装 `data/loading/error/refresh/cancel(AbortSignal)`；仅复杂业务用，简单页保持 `onMounted`（D-4 克制）；证据 全仓无此 composable。 |

---

## 附：审计评分与修复优先级（2026-07-30）

- 独立代码审计整体 **69/100**（架构 78 / 代码质量 70 / 性能 72 / 可维护性 74 / 工程规范·安全 62）。扣分主因 d032（安全）、b019/a013/a014/d033 等（正确性/稳定性）；新增 a018（代码/文档对撞）进一步压制架构评分。
- TOP10 修复顺序（收益/成本）：**b019 → d033 → b023 → a014 → d036 → d038 → d037 → c012 → d034 → d035**。
- b023（损失金额单位虚标）已于 2026-07-30 双文档交叉验证中补入独立编号，原 A05 备忘归档。
