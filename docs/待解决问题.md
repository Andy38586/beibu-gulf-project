# 待解决问题（统一 open 项清单 · 2026-08-03 验证版 + 压力面试合并 + 2026-08-10 更新 + 2026-08-12 格式重整）

> **定位**：当前所有经真实代码取证确认**仍未闭环**的问题。修一个 → 从本文件删除，按**原编号**移入 `已解决问题.md`（状态由"在哪一文件"决定，解决即移走）。
> **副本规则（2026-08-10 定）**：本文件可与副本（分支）分离。**任何新发现问题、大审查产物，一律先记入副本**，副本经过用户裁决后：有价值 → 转正进入本文件（分配正式编号）；已解决/证伪 → 留在副本或删除。副本不污染本文件编号系统，保证台账质量与"项目发展记录"意义。新建副本流程：① 阅读本规则；② 新建 `docs/问题副本-<yyyy-mm-dd>-<主题>.md`（**命名必须含当天日期**）；③ 副本内编号随意（如 `副-01`），仅副本内部使用；④ 转正时 `discover` 日期 = 副本标题日期，正式编号按本文件编号序列分配，不追溯副本编号。
> **编号规则**：按类型分节，四位数。`a`=地图层、`b`=业务层、`c`=组件层、`d`=后端层、`z`=暂未归类。**本文件内部顺序编号**；移入 `已解决问题.md` 时**保留原编号不重排**。
> **时间戳规则**：待解决问题每条 `问题编号` 下方仅标注 `discover:YYYYMMDD`（**无 solve 时间戳**，解决后移入 `已解决问题.md` 时补 `solve:YYYYMMDD`）。
> **问题等级**：`p0` 致命/高危阻断｜`p1` 高（正确性/稳定性/安全非致命/面试短板）｜`p2` 中（待改进·低危 bug·语义不一致）｜`p3` 低（接受/硬化/文档/视觉 nits）。
>
> **列格式规则（2026-08-12 定）**——每条 4 列：
> - **列1 问题等级**：p0/p1/p2/p3。
> - **列2 问题编号**：编号 + `<br>discover:YYYYMMDD`（无 solve）。
> - **列3 问题描述及复现方法/对应文件**：问题描述 + **复现方法**（可复现的写复现路径/操作）；无复现方法的写**对应文件**（file:line 锚定项目内位置，便于定位）。**不写"影响哪些文件"**（那是已解决问题的列3 内容）。
> - **列4 解决记录**：三段——**预期解决思路**（去项目里看代码后写的具体改法）/ **实施风险**（改这个会碰的雷、副作用、前置验证）/ **尝试日志**（实际尝试过的精简成几句话：尝试了什么、反响；第一版尚未尝试则如实写"第一版尚未实际尝试"，留待后续推进；可二次解决）。

---

## 2026-08-03 代码二次验证（溯源收口）

对全部候选 open 项逐条核对 `file:line` 与问题真实性，结果：

- **闭环（移出 open）**：`z033`（静态 fetch 已收口 `loadStatic`）、`b030`（真 DEM 流水线已落地 `tools/dem-pipeline`，`getDEM` 降级为无调用方钩子；残留"三维水面非真实高程演算"叙事并入 `z037`）。
- **降级（保留 open）**：`b028` 原 `Math.random()` 造假已不成立——`forecastEngine.js:115` 改 `seededRandom` 确定性种子、`:105` 顶部诚实标注"示意性合成数据"，欺骗性质疑解除，由 p0 降 p1；残留"算法非历史数据驱动"属 `z037` 叙事。
- **其余 open 项**行号/等级已就地更新，问题确认仍成立。

**2026-08-10 全面体检**：闭环 9 项（b028/b029/b038/c021/d039/d055/z041/z062/z037）移入 `已解决问题.md`（补 solve:20260810，z037 为 p0 重取证后闭环）；z076/z077 回副本待真处理；b044/z038/z042/z072/d054 五条证据重写。**真实 open：17 项**。

**2026-08-12 格式重整 + 取证更新**：按新列格式重写全部 open 项；`a043`/`a049` 经代码取证确认已落地解决并已按原编号移入 `已解决问题.md`（solve:20260812）。**重整后真实 open：13 项**。

---

## 地图层（图层生命周期 / 地图渲染 / 地图引擎集成 / 坐标系 / 空间数据性能 / 图层状态 / 地图交互）

| 问题等级 | 问题编号 | 问题描述及复现方法/对应文件 | 解决记录 |
| --- | --- | --- | --- |

---

## 业务层（业务逻辑 / Adapter / Store / 业务页特性）

| 问题等级 | 问题编号 | 问题描述及复现方法/对应文件 | 解决记录 |
| --- | --- | --- | --- |
| p2 | b027-waterLevel传参姿势分裂<br>`discover:20260801` | waterLevel 参数名三套并存，前后端易漂移。**对应文件/复现**：`floodAdapter.ts:144-150` api 模式 GET 用 `params:{waterLevel}`；`:190` api 模式 POST 用 `body:{waterLevel}`；`:176` online 模式 GET 用 `params:{level:waterLevel}`；`_fetchOnlineFlood` 内 online 用 `params:{level}`。grep floodAdapter.ts 可见 waterLevel/level 混用。 | **预期思路**：统一参数名为 `waterLevel`——online 模式 `params:{level}` 改 `params:{waterLevel}`；FastAPI flood-service 端点参数名同步改 `waterLevel`；api GET/POST 已一致保持。<br>**实施风险**：①FastAPI flood-service（Python）端点签名需同步改，否则 online 400；②后端 Express `/flood-online` 反代参数透传需验证；③回归测试需覆盖 api/online 两模式。<br>**尝试日志**：第一版尚未实际尝试（活动范围限文档，未改代码）。 |
| p1 | b034-前后端类型同步/职责边界<br>`discover:20260802` | 无 OpenAPI/契约测试；zod 已落地为前端校验源，但前后端仍非单一事实源；adapter 与 service 职责边界未文档化，业务规则是否前后端重复计算未界定。**对应文件**：`package.json:37` zod；`types/schemas.ts`/`useApiRequest.ts`/`loadStatic.ts` 前端校验源；后端无 OpenAPI 文件。 | **预期思路**：方案A——用 zod schema 作单一事实源，由 schema 生成 OpenAPI（zod-to-openapi）供后端对照；方案B——手写 OpenAPI + 契约测试（openapi-backend）；明确业务规则后端为权威。<br>**实施风险**：①OpenAPI 与 zod 双维护漂移风险（方案A 用生成器规避）；②工作量大（31 端点）；③后端 Express 无 TypeScript，契约对齐靠人工。<br>**尝试日志**：第一版尚未实际尝试。 |
| p1 | b040-useAuth/useApiRequest模块级业务状态<br>`discover:20260802` | 核心业务状态（user/token）为模块顶层单例可变变量，应进 store。**对应文件**：`useAuth.ts:46` `const user: Ref<User\ | null> = ref(readStoredUser())`；`useApiRequest.ts:29` `const token: Ref<string> = ref('')`。 | **预期思路**：创建 `authStore`（Pinia）容纳 user/token/isAuthenticated/setToken/clearToken；useAuth/useApiRequest 模块级仅保留纯函数（apiRequest 等）；所有调用方改读 authStore。<br>**实施风险**：①useAuth 被多处调用（App.vue/各业务页），迁移需全量改读 store；②useApiRequest 与 useAuth 的依赖关系（useAuth 解构 useApiRequest 的 token）需重构；③HMR/测试隔离——模块级单例测试隔离困难正是迁移动机，但 store 需确保 reset 可测；④isAuthenticated 是 computed 透传 token，迁入 store 后响应性需验证。<br>**尝试日志**：第一版尚未实际尝试。 |
| p1 | b044-site-selection算法瓶颈(逐POI buffer+全量union)<br>`discover:20260803` | 每 POI 一 `turf.buffer` + 全量 `turf.union`，O(n²) 级。**复现**：`siteAnalysisService.js:81-83` `validPoints.map(p => turf.buffer(turf.point([p.lng,p.lat]), radiusKm))`；`:98` `turf.union(turf.featureCollection(validBuffers))` 全量合并。372 点场景实测 union 为瓶颈（a035 性能基线：服务端核心 1.55s）。 | **预期思路**：①引入 rbush 空间索引聚类邻近 POI，对每簇做一次 buffer 后再 union（减少 union 输入数）；②或网格化批量 union（按 H3/网格分组，组内 union 后跨组合并）；③`scoringService` 已接入 rbush（d038 已解决），可复用 `shared/utils/spatialIndex.ts`。<br>**实施风险**：①算法等价性——聚类后 union 结果与逐点 union 需几何等价回归测试；②turf.union 对大量多边形本身慢，需压测确认簇粒度；③`logger.debug('建议实现聚类或空间索引')`（:44-47）为 TODO 注释，实际优化需替换。<br>**尝试日志**：第一版尚未实际尝试。 |
| p2 | b045-三业务模块结构不一致<br>`discover:20260803` | flood/forecast/site-selection 三模块目录结构不统一，新增模块无统一模板。**对应文件**：flood 有 `constants/` 目录无 composables；forecast 有 `constants.ts` 有 composables；site-selection 无 constants。 | **预期思路**：抽公共业务模块骨架（`constants/` + `composables/` + `adapter` 同构），新模块按模板；现有三模块对齐补齐缺失层。<br>**实施风险**：①重构面大（三模块目录调整）；②site-selection 已确认设计豁免无 adapter（a021 已解决，数据流契约 R5），强行补 adapter 违背既定决策；③回归风险。<br>**尝试日志**：第一版尚未实际尝试。**注意**：a021 闭环决议为"仅 flood 有 adapter"，本条与 a021 有张力，需重新裁定范围。 |

---

## 组件层（UI 组件 / CSS / 面板 / 视图组件 / 前端打包）

| 问题等级 | 问题编号 | 问题描述及复现方法/对应文件 | 解决记录 |
| --- | --- | --- | --- |

---

## 后端层（后端代码 / API / 认证 / 服务 / 部署配置）

| 问题等级 | 问题编号 | 问题描述及复现方法/对应文件 | 解决记录 |
| --- | --- | --- | --- |
| p2 | d051-缺统一入参校验层<br>`discover:20260801` | 参数校验散落各 controller，无统一中间件。**对应文件**：`backend/middleware/` 仅 `auth.js`/`logSanitizer.js`，无 `validate.js`；`siteAnalysisController.js:35` 手写校验（均走 `BusinessError`+全局中间件，无裸 `res.status(400)`）。 | **预期思路**：新建 `backend/middleware/validate.js`（用 zod 或 ajv 校验 req.body/query/params），挂路由级中间件；现有 controller 手写校验逐步迁移。<br>**实施风险**：①现有 controller 校验语义需逐一核对（避免新中间件与旧手写校验重复/冲突）；②过渡期双轨；③后端无 TypeScript，zod 在 JS 侧需额外配置或用 ajv。<br>**尝试日志**：第一版尚未实际尝试。 |
| p3 | d054-公开路由白名单兜底确认<br>`discover:20260801` | 公开路由无统一输入白名单/validateQuery。**对应文件**：`backend/routes/*.js` 实测无 `sanitize`/`whitelist`/`validateQuery`（仅 `validateWaterLevel` 单点校验 + `logSanitizer` 日志脱敏）。 | **预期思路**：全公开路由（forecast/flood 只读端点）统一输入白名单中间件，拒绝非预期字段。<br>**实施风险**：①白名单遗漏致合法功能失效；②需盘点所有公开路由的合法入参；③与 d025（已解决的 forecast 路径遍历）`ALLOWED_INDICATORS` 白名单同源，可复用扩展。<br>**尝试日志**：第一版尚未实际尝试。 |

---

## 暂未归类（文档 / 工程质量 / 测试覆盖 / 类型安全 / 架构守护 / 杂项）

| 问题等级 | 问题编号 | 问题描述及复现方法/对应文件 | 解决记录 |
| --- | --- | --- | --- |
| p2 | z038-mapStore关键链路单测覆盖待确认(尾巴)<br>`discover:20260801` | setLayerVisible 仍 0 覆盖。**对应文件**：`mapStore.ts:130-140`（2026-08-10 实测）setLayerVisible 方法；`mapStore.test.ts` 现有 setMapType/registerBusinessLayer/setBaseLayer/setCurrentRenderer/resetMapState 五组，无 setLayerVisible 用例。 | **预期思路**：补 `mapStore.test.ts` 对 setLayerVisible 的覆盖（设置可见/取消可见/影响 layerCatalog 条目）。<br>**实施风险**：低，纯测试补充，无代码改动。<br>**尝试日志**：第一版尚未实际尝试。 |
| p3 | z040-领先版本过激<br>`discover:20260801` | 核心依赖大版本，磨合风险。**对应文件**：`package.json:64` `"vite":"^8.0.16"`、`:60` `"typescript":"^6.0.3"`、`:68` `"vue-tsc":"^3.3.8"`；@vueuse 已现 `INVALID_ANNOTATION` 警告。 | **预期思路**：谨慎升级/锁版本，pin 到当前可用的稳定 patch 版本；评估 @vueuse 警告是否需降级。<br>**实施风险**：①降级可能引入其他不兼容；②锁版本后安全补丁需手动跟进；③vite 8 / ts 6 为很新的大版本，生态插件（unplugin/vite-plugin-cesium）磨合风险持续。<br>**尝试日志**：第一版尚未实际尝试。 |
| p3 | z042-函数过长(指标恶化)<br>`discover:20260801` | 可读性最差的两处。**对应文件**：`CesiumRenderer.ts` 1877 行（P8 合并 4 个假拆分文件后翻倍）；`OLRenderer.ts` 775 行；`floodAdapter.ts` 已改善至 222 行。 | **预期思路**：按职责拆分——CesiumRenderer 已拆出 CesiumEvents/CesiumViewportCulling/CesiumLayerRegistrar/CesiumWaterSurface（z053 已解决），但 P8 合并 4 个假拆分文件后反弹至 1877 行，需重新按职责拆（水面/图层/事件/裁剪进一步细分）；OLRenderer 按图层类型拆（点/线/面/热力）。<br>**实施风险**：①拆分破坏类型（z065 已移除 @ts-nocheck，拆分需保持类型完整）；②测试可能因模块路径变更失败；③需渐进拆分避免大爆炸式重构。<br>**尝试日志**：第一版尚未实际尝试。 |
| p1 | z072-错误上报SDK未接入<br>`discover:20260802` | 无错误上报，未捕获错误不可追溯。**对应文件**：`logger.ts` 全文实测已无 transport 钩子（emit 仅输出控制台，z020 曾实现的 `addLogTransport` 已被后续清理删除）；`main.ts:73` 注释"logger 已预留 addLogTransport 钩子，Sentry 就绪后一行接入"——**注释与代码不符**（钩子已不存在）；`main.ts:78-86` window.onerror/onunhandledrejection 仅 logger.error 无上报。 | **预期思路**：方案A——`logger.ts` 重新加 transport 数组 + `addLogTransport(fn)` 钩子，main.ts 接 `@sentry/vue` 后 `addLogTransport(Sentry.captureMessage)`；方案B——直接 main.ts 接 `@sentry/vue`，errorHandler 里 `Sentry.captureException(err)`，不动 logger。<br>**实施风险**：①引入 `@sentry/vue` 新依赖（包体积 +~30KB gzip）；②DSN 需环境变量配置（Sentry DSN/环境/release）；③采样率配置避免噪音；④方案A 的 transport 钩子曾被当死代码删除（z020→清理），重新引入需证明有调用方；⑤main.ts:73 注释需同步修正。<br>**尝试日志**：第一版尚未实际尝试。**重要发现**：main.ts:73 注释引用的 addLogTransport 钩子在 logger.ts 中已不存在，属文档/注释与代码漂移。 |
| p2 | z064-无集成/E2E测试<br>`discover:20260802` | 核心业务流程无跨模块流程测试，回归靠人工。**对应文件**：`package.json:10` `"test":"cd frontend && vitest run"` 仅单元测试；`cypress`/`playwright` 0 命中；`frontend/e2e`/`cypress`/`tests/e2e` 目录均不存在。 | **预期思路**：引入 Playwright 覆盖核心业务流程 E2E（登录→选址→洪涝分析→预测）；或先补集成测试（API 层串联）。<br>**实施风险**：①E2E 维护成本高（UI 变更即失败）；②CI 时间显著增加；③需测试环境（后端+前端+FastAPI flood-service 全启动）；④mock/api/online 三数据源模式 E2E 路径不同。<br>**尝试日志**：第一版尚未实际尝试。 |
| p1 | z071-无路由守卫做store重置<br>`discover:20260802` | 路由切换时无显式 store 重置、无全局请求取消。**对应文件**：`router/index.ts` beforeEach/afterEach/beforeResolve 0 命中（仅 createRouter + routes）；mapStore/forecastState 不在 resetBusinessStores 中；useSiteAnalysisApi 无 AbortController。 | **预期思路**：添加 `router.beforeEach` 守卫，路由切换时重置非持久化业务 store + 取消在途请求（AbortController）。<br>**实施风险**：①重置范围/时机需精确——误清 mapStore 会导致底图/引擎状态丢失，应只重置离开的业务页 store；②resetBusinessStores 现由登出触发（useAuth），路由守卫需复用还是独立需设计；③useSiteAnalysisApi 已有实例级 AbortController（b021 已解决），但路由级全局取消需统一信号；④首页/个人中心等非业务页不应触发业务 store 重置。<br>**尝试日志**：第一版尚未实际尝试。 |

---

## 风险观察（非 bug · 面试 / 架构叙事）

> ⚠️ **历史记录区（2026-08-10 起归档）**：以下观察不再维护更新，内容可能已过时；新风险请经副本流程进入本清单。

- **Renderer 抽象 / GCS 布局过度设计嫌疑**：`MapRenderer` 277 + `OLRenderer` 607 + `CesiumRenderer` 1064 ≈ 1948 行仅服务于 2D↔3D 切换；GCS `useGCS.ts` 225 行 + 组件仅 4 业务页。面试需准备 ROI 辩护（对应面试报告架构章）。
- **依赖安全审计本机不可执行**：`npm audit` 在 npmmirror 镜像返回 `NOT_IMPLEMENTED`（d037 已记环境限制），上线前须切官方源跑。
- **性能优化无基线**：a035 已列，此处重申——没有数据量基线，任何"优化"都是空话。
- **压力面试暴露的根因聚类（2026-08-03）**：①凭据/AI痕迹类（`d047`+`d046`、`z054`）→ 安全与可信度；②伪三维/叙事类（`z037`+`a036`+`a026`）→ 简历诚信；③双渲染器类型与架构类（`z065`+`a025`+`b038`）→ 工程分；④性能资产类（`a024`+`a035`）→ 面试演示稳定性。

---

## 附：open 计数与状态（2026-08-03 验证版 + 压力面试合并 + 2026-08-12 取证更新）

### 总计

- **真实 open：13 项**（2026-08-12 格式重整后：`a043`/`a049` 已移入 `已解决问题.md`（solve:20260812）；真实 open = 13）。
- **按等级**：p0 × 0｜p1 × 5（b034 / b040 / b044 / z071 / z072）｜p2 × 5（b044 / b045 / d051 / z038 / z064）｜p3 × 3（d054 / z040 / z042）。
- **按层级**：地图层 2 待移走（a043/a049）｜业务层 5（b044/b034/b040/b044/b045）｜组件层 0｜后端层 2（d051/d054）｜暂未归类 6（z038/z040/z042/z064/z071/z072）。

### 已闭环（本次代码验证 · 2026-08-03）

> 以下项经代码二次验证确认问题已不再成立，从 open 移除；证据附后。如需归档请按原编号移入 `已解决问题.md`（补 `solve:20260803`）。

| 原编号 | 原等级 | 原 claim | 验证结论（证据） | 处置 |
| --- | --- | --- | --- | --- |
| z033 | p2 | mapDataService.ts:28 / useBoundaryLayer.ts:49 直接 fetch 绕过 adapter | `mapDataService.ts:8,23` 已 `import { loadStatic }` + `loadStatic<unknown>(url)`；`useBoundaryLayer.ts:54` `// z021: 静态资源 fetch 收口 loadStatic`；两文件**无裸 fetch**（z021 已闭环）。DISCONNECTED | 移出 open（与 z021 重复闭环） |
| b030 | p2 | getDEM/三维淹没仍 mock | `tools/dem-pipeline/01~05` 真 DEM 流水线已落地；`floodAdapter.ts:330-336` `getDEM` 降级为"无调用方钩子"；`:333` 自承"三维水面为预设水位档位可视化（非真实高程演算）"→ 属 `z037` 叙事。DISCONNECTED | 移出 open；残留叙事并入 z037 |
| b028 | p0→p1 | forecastEngine.js:101-140 用 Math.random 造假 | `forecastEngine.js:115` `seededRandom` 确定性种子（Math.random 已移除）；`:105` 顶部诚实标注"示意性合成数据"。欺骗性质疑解除，降级 p1；算法非历史数据驱动属 z037 叙事。DISCONNECTED 局部 | 保留 open，降级 p1 + 改写描述 |

### 2026-08-12 取证闭环（待移入已解决）

| 原编号 | 原等级 | 原 claim | 验证结论（证据） | 处置 |
| --- | --- | --- | --- | --- |
| a043 | p2 | 地图无 resize 响应 | `UnifiedMap.vue:417-436` 已挂 ResizeObserver 观察容器调 `currentRenderer.value?.updateSize()`，含环境不支持静默跳过防御。DISCONNECTED | 待移入 `已解决问题.md`（补 solve:20260812）；残留：未核实 onUnmounted disconnect |
| a049 | p2 | MapRendererKey 还活在 provide 侧 | `grep MapRendererKey` 代码零命中（仅文档自引）；`useMapRenderer.ts` 文件已不存在；`UnifiedMap.vue:54` 注释"mapStore 已由 App.vue 统一 provide，此处不重复"。DISCONNECTED | 待移入 `已解决问题.md`（补 solve:20260812） |

### 压力面试合并说明（2026-08-03 下午）

> 两轮压力面试（面试官视角报告 + 用户自审）暴露的问题，按"同根因直接合并"原则并入本清单：

- **合并进已有 open 项（不新增编号）**：`d047`（升 p3→p0，并入天地图密钥历史泄露）、`z037`（并入伪三维自承 `CesiumLayerRegistrar.ts:~276` + 首页假数据 `HomePage.vue:38-56`）、`z065`（并入 313 处类型逃生，升 p3→p2）、`b038`（并入双机制 `mapStore.ts:342-346` + 单槽 `analysisHandler`）。
- **已闭环不再新增**：`getDEM` 死代码（`floodAdapter.ts:330`）属 `b030` 已闭环范围（预留钩子无调用方）；GCS 拆 `core/layout`+`shared/layout` 为 `z044` 重构后既定布局（设计取舍，非缺陷）；`waterLevelStore` 仅 2 字段为合理 Pinia 模式（非缺陷）。
- **新增 11 项**（a024/a025/a026、b044/b045、d048、z054/z055/z056/z076/z077），编号接续全局最大空闲号，未与 `已解决问题.md` 碰撞。

### 编号体系与文档关系

- 本文件为唯一 open 清单，编号体系贯通（接续主清单 + 已解决最大编号）。
- 原有 open 项除 `b030`/`z033` 经本次验证闭环外，其余保留并就地更新行号/等级。
- 已闭环项按原编号移入 `已解决问题.md`（补 `solve:20260803`）。
- 2026-08-12 取证闭环的 `a043`/`a049` 已按原编号移入 `已解决问题.md`（solve:20260812）。

