# 待解决问题（统一 open 项清单 · 2026-08-03 验证版 + 压力面试合并 + 2026-08-10 更新）

> **定位**：当前所有经真实代码取证确认**仍未闭环**的问题。修一个 → 从本文件删除，按**原编号**移入 `已解决问题.md`（状态由"在哪一文件"决定，解决即移走）。
> **副本规则（2026-08-10 定）**：本文件可与副本（分支）分离。**任何新发现问题、大审查产物，一律先记入副本**，副本经过用户裁决后：有价值 → 转正进入本文件（分配正式编号）；已解决/证伪 → 留在副本或删除。副本不污染本文件编号系统，保证台账质量与"项目发展记录"意义。新建副本流程：① 阅读本规则；② 新建 `docs/问题副本-<yyyy-mm-dd>-<主题>.md`（**命名必须含当天日期**）；③ 副本内编号随意（如 `副-01`），仅副本内部使用；④ 转正时 `discover` 日期 = 副本标题日期，正式编号按本文件编号序列分配，不追溯副本编号。
> **编号规则**：按类型分节，四位数。`a`=地图层、`b`=业务层、`c`=组件层、`d`=后端层、`z`=暂未归类。**本文件内部顺序编号**；移入 `已解决问题.md` 时**保留原编号不重排**。
> **时间戳规则**：每条 `问题编号` 下方标注 `discover:YYYYMMDD`；移入 `已解决问题.md` 时补 `solve:YYYYMMDD`。
> **问题等级**：`p0` 致命/高危阻断｜`p1` 高（正确性/稳定性/安全非致命/面试短板）｜`p2` 中（待改进·低危 bug·语义不一致）｜`p3` 低（接受/硬化/文档/视觉 nits）。
> **溯源锚定**：问题源于代码、归属代码；每条以 `file:line` 证据直接锚定项目内位置，便于定位。不再标注问题源自哪份审查/审计文档（旧文档随专项收口会被删除，不可作长期溯源依据）。

---

## 2026-08-03 代码二次验证（溯源收口）

对全部候选 open 项逐条核对 `file:line` 与问题真实性，结果：

- **闭环（移出 open）**：`z033`（静态 fetch 已收口 `loadStatic`）、`b030`（真 DEM 流水线已落地 `tools/dem-pipeline`，`getDEM` 降级为无调用方钩子；残留"三维水面非真实高程演算"叙事并入 `z045`）。
- **降级（保留 open）**：`b025` 原 `Math.random()` 造假已不成立——`forecastEngine.js:115` 改 `seededRandom` 确定性种子、`:105` 顶部诚实标注"示意性合成数据"，欺骗性质疑解除，由 p0 降 p1；残留"算法非历史数据驱动"属 `z045` 叙事。
- **其余 open 项**行号/等级已就地更新（`a019`/`b027`/`b029`/`c021`/`z038`/`z042`/`z046`/`b034`/`b038`/`a027`/`b040`/`z048`/`z064`/`z071`/`z072` 等行号以 2026-08-03 实测为准），问题确认仍成立。

**2026-08-10 全面体检**：闭环 9 项（b025/b029/b038/c021/d039/d055/z041/z043/z045）移入 `已解决问题.md`（补 solve:20260810，z045 为 p0 重取证后闭环）；z076/z077 回副本待真处理；b027/z038/z042/z072/d054 五条证据重写。**真实 open：17 项**。

---

## 地图层（图层生命周期 / 地图渲染 / 地图引擎集成 / 坐标系 / 空间数据性能 / 图层状态 / 地图交互）

| 问题等级 |                                  问题编号                                   | 问题内容以及影响                                                                                                                                                                                                                                                                                                                           | 如何解决                                                                                                                                   |
| :------: | :-------------------------------------------------------------------------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ |
|    p2    | a043-地图无resize响应（窗口缩放后OL/Cesium画布失配）<br>`discover:20260806` | `core/map/` 全目录无 `window.resize`/`ResizeObserver` 监听驱动 `updateSize()`（`CesiumRenderer.ts:852` `viewer.resize()`、`OLRenderer.ts:671` `map.updateSize()` 仅初始化/引擎切换时调用）——窗口缩放/旋转/侧栏折叠后容器尺寸已变而画布不变 → 变形/黑边/留白；`main.ts:51` 引入 ResizeObserver polyfill 但地图层从未使用（仅 ECharts 用）。 | UnifiedMap 对两个容器挂 ResizeObserver（或 window resize 防抖）调 `currentRenderer.updateSize()`；卸载时 disconnect（注册/移除配对契约）。 |
|    p2    |          a049-MapRendererKey还活在provide侧<br>`discover:20260809`          | `useMapRenderer.ts:10` 定义 `MapRendererKey`，`UnifiedMap.vue:55` 仍 `provide(MapRendererKey, currentRenderer)`，但 `inject(MapRendererKey)` 的 grep **零命中**——业务组件已全部走 `mapStore.currentRenderer`/`useMapControls`。删了消费端和工厂，provide 端残留。                                                                          | 删 `MapRendererKey` 定义 + `UnifiedMap.vue` 的 provide 调用（含 provideKeys 导出清理），跑 typecheck + core/map 测试确认。`                |

---

## 业务层（业务逻辑 / Adapter / Store / 业务页特性）

| 问题等级 |                                  问题编号                                  | 问题内容以及影响                                                                                                                                                                                                                                               | 如何解决                                                                      |
| :------: | :------------------------------------------------------------------------: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
|    p2    |             b027-waterLevel传参姿势分裂<br>`discover:20260801`             | 实测分裂仍存在且更甚（2026-08-10 取证）：api 模式 `floodAdapter.ts:158/:163` 用 `params:{waterLevel}`、`:206` 用 `body:{waterLevel}`；online 模式 `:78/:191` 用 `params:{level}`——三套参数名并存，前后端易漂移。                                               | 统一为 query 或 body 其一。`                                                  |
|    p1    |            b034-前后端类型同步/职责边界<br>`discover:20260802`             | 无 OpenAPI/契约测试；zod 已在 `package.json:37` + `types/schemas.ts`/`useApiRequest.ts`/`loadStatic.ts` 落地为**前端**校验源，但**无 OpenAPI 文件、无契约测试**，前后端仍非单一事实源；adapter 与 service 职责边界未文档化，业务规则是否前后端重复计算未界定。 | 中期补 OpenAPI + 契约测试；明确业务规则后端为权威。`                          |
|    p1    |      b040-useAuth/useApiRequest模块级业务状态<br>`discover:20260802`       | `useAuth.ts:52` `user`(ref) 与 `useApiRequest.ts:27` `token`(ref) 均为模块顶层单例可变变量（行号 2026-08-03 实测，原 48-55/24 微漂移）。核心业务状态应进 authStore。                                                                                           | 创建 authStore 容纳 user/token/isAuthenticated；模块级仅保留纯函数。`         |
|    p1    | b044-site-selection算法瓶颈(逐POI buffer+全量union)<br>`discover:20260803` | `siteAnalysisService.js:87-103` 每 POI 一 `turf.buffer` + 全量 `turf.union`（372 点场景）；`:44-47` 仅 `logger.debug('建议实现聚类或空间索引')` 代替实际优化。最大算法瓶颈，O(n²) 级。                                                                         | 引入 rbush 空间索引 / 聚类后 buffer；或网格化批量 union。`                    |
|    p2    |              b045-三业务模块结构不一致<br>`discover:20260803`              | flood=有 `constants/` 目录 + 无 composables；forecast=有 `constants.ts` + 有 composables；site-selection=无 `constants`。同层三模块目录结构不统一，新增模块无统一模板。                                                                                        | 抽公共业务模块骨架（constants + composables + adapter 同构），新模块按模板。` |

---

## 组件层（UI 组件 / CSS / 面板 / 视图组件 / 前端打包）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
| :------: | :------: | ---------------- | -------- |

---

## 后端层（后端代码 / API / 认证 / 服务 / 部署配置）

| 问题等级 |                      问题编号                      | 问题内容以及影响                                                                                                                                                                                            | 如何解决                    |
| :------: | :------------------------------------------------: | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
|    p2    |    d051-缺统一入参校验层<br>`discover:20260801`    | `backend/middleware/` 仅 `auth.js`/`logSanitizer.js`，**无 `validate.js`**；`siteAnalysisController.js:35` 手写校验，参数校验散落各 controller（均走 `BusinessError`+全局中间件，无裸 `res.status(400)`）。 | 建统一 `validate` 中间件。` |
|    p3    | d054-公开路由白名单兜底确认<br>`discover:20260801` | 2026-08-10 确认结论：`backend/routes/*.js` 实测仍无 `sanitize`/`whitelist`/`validateQuery`（仅 `validateWaterLevel` 单点校验 + logSanitizer 日志脱敏），"待统一确认"闭环为：白名单确缺失，保留 p3。         | 全公开路由统一输入白名单。` |

---

## 暂未归类（文档 / 工程质量 / 测试覆盖 / 类型安全 / 架构守护 / 杂项）

| 问题等级 |                             问题编号                             | 问题内容以及影响                                                                                                                                                                                                                                                                                                       | 如何解决                                                                                   |
| :------: | :--------------------------------------------------------------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
|    p2    | z038-mapStore关键链路单测覆盖待确认(尾巴)<br>`discover:20260801` | `setLayerVisible`（`mapStore.ts:226` 实测 2026-08-10）仍 0 覆盖（2026-08-10 P0-3 清理后 setAnalysisResult 用例已删，mapStore.test 现有 setMapType/registerBusinessLayer/setBaseLayer/setCurrentRenderer/resetMapState 五组，仍无 setLayerVisible 用例）。`reapplyAll` 在 `BusinessLayerManager.test.ts:147` 已有测试。 | 补 `mapStore.test.ts` 对 `setLayerVisible` 的覆盖即可。`                                   |
|    p3    |             z040-领先版本过激<br>`discover:20260801`             | `package.json:64` `"vite":"^8.0.16"`、`:60` `"typescript":"^6.0.3"`、`:68` `"vue-tsc":"^3.3.8"` 全为大版本，unplugin\*/vite-plugin-cesium 磨合风险，@vueuse 已现 `INVALID_ANNOTATION` 警告。                                                                                                                           | 谨慎升级，锁版本。`                                                                        |
|    p3    |          z042-函数过长(指标恶化)<br>`discover:20260801`          | 2026-08-10 实测：`CesiumRenderer.ts` **1877 行**（P8 合并 4 个假拆分文件后翻倍）、`OLRenderer.ts` **775 行**——可读性最差的两处；`floodAdapter.ts` 已改善至 **222 行**。                                                                                                                                                | 拆分。`                                                                                    |
|    p1    |          z072-错误上报SDK未接入<br>`discover:20260802`           | `logger.ts:5` 注释"2026-08-09：addLogTransport/transports 零调用方死代码已删（Sentry 接入时再补）"；`main.ts:78-86` 仍为 `// import * as Sentry` 注释（2026-08-10 实测）——无错误上报的残余成立，证据行已更新。                                                                                                         | 引入 Sentry SDK，main.ts 初始化 + errorHandler 调用上报；带 release/version/environment。` |
|    p2    |            z064-无集成/E2E测试<br>`discover:20260802`            | `package.json:10` `"test":"cd frontend && vitest run"`，仅单元测试；`cypress`/`playwright` **0 命中**，`frontend/e2e`/`cypress`/`tests/e2e` 目录均不存在。核心业务流程无跨模块流程测试，回归靠人工。                                                                                                                   | 引入 Playwright 覆盖核心业务流程 E2E；或先补集成测试（API 层串联）。`                      |
|    p1    |        z071-无路由守卫做store重置<br>`discover:20260802`         | `router/index.ts` `beforeEach`/`afterEach`/`beforeResolve` **0 命中**。路由切换时无显式 store 重置（mapStore/forecastState 不在 resetBusinessStores 中）；无全局请求取消（useSiteAnalysisApi 无 AbortController）。                                                                                                    | 添加 `router.beforeEach` 守卫，路由切换时重置非持久化 store、取消在途请求。`               |

---

## 风险观察（非 bug · 面试 / 架构叙事）

> ⚠️ **历史记录区（2026-08-10 起归档）**：以下观察不再维护更新，内容可能已过时；新风险请经副本流程进入本清单。

- **Renderer 抽象 / GCS 布局过度设计嫌疑**：`MapRenderer` 277 + `OLRenderer` 607 + `CesiumRenderer` 1064 ≈ 1948 行仅服务于 2D↔3D 切换；GCS `useGCS.ts` 225 行 + 组件仅 4 业务页。面试需准备 ROI 辩护（对应面试报告架构章）。
- **依赖安全审计本机不可执行**：`npm audit` 在 npmmirror 镜像返回 `NOT_IMPLEMENTED`（d042 已记环境限制），上线前须切官方源跑。
- **性能优化无基线**：a020 已列，此处重申——没有数据量基线，任何"优化"都是空话。
- **压力面试暴露的根因聚类（2026-08-03）**：①凭据/AI痕迹类（`d056`+`d053`、`z073`）→ 安全与可信度；②伪三维/叙事类（`z045`+`a027`+`a037`）→ 简历诚信；③双渲染器类型与架构类（`z046`+`a036`+`b038`）→ 工程分；④性能资产类（`a035`+`a020`）→ 面试演示稳定性。

---

## 附：open 计数与状态（2026-08-03 验证版 + 压力面试合并）

### 总计

- **真实 open：15 项**（2026-08-10 全面体检后：闭环 9 + 回副本 2；随后 P0-3 修复闭环 a048（mapStore 死状态）/b057（选址双通道），open 再减 2）。
- **按等级**：p0 × 0｜p1 × 5（b034 / b040 / b044 / z071 / z072）｜p2 × 7（a043 / a049 / b027 / b045 / d051 / z038 / z064）｜p3 × 3（d054 / z040 / z042）。
- **按层级**：地图层 2（a043/a049）｜业务层 5（b027/b034/b040/b044/b045）｜组件层 0｜后端层 2（d051/d054）｜暂未归类 6（z038/z040/z042/z064/z071/z072）。

### 已闭环（本次代码验证 · 2026-08-03）

> 以下项经代码二次验证确认问题已不再成立，从 open 移除；证据附后。如需归档请按原编号移入 `已解决问题.md`（补 `solve:20260803`）。

| 原编号 | 原等级 | 原 claim                                                              | 验证结论（证据）                                                                                                                                                                                        | 处置                          |
| :----: | :----: | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- |
|  z033  |   p2   | mapDataService.ts:28 / useBoundaryLayer.ts:49 直接 fetch 绕过 adapter | `mapDataService.ts:8,23` 已 `import { loadStatic }` + `loadStatic<unknown>(url)`；`useBoundaryLayer.ts:54` `// z032: 静态资源 fetch 收口 loadStatic`；两文件**无裸 fetch**（z032 已闭环）。DISCONNECTED | 移出 open（与 z032 重复闭环） |
|  b030  |   p2   | getDEM/三维淹没仍 mock                                                | `tools/dem-pipeline/01~05` 真 DEM 流水线已落地；`floodAdapter.ts:330-336` `getDEM` 降级为"无调用方钩子"；`:333` 自承"三维水面为预设水位档位可视化（非真实高程演算）"→ 属 `z045` 叙事。DISCONNECTED      | 移出 open；残留叙事并入 z045  |
|  b025  | p0→p1  | forecastEngine.js:101-140 用 Math.random 造假                         | `forecastEngine.js:115` `seededRandom` 确定性种子（Math.random 已移除）；`:105` 顶部诚实标注"示意性合成数据"。欺骗性质疑解除，降级 p1；算法非历史数据驱动属 z045 叙事。DISCONNECTED 局部                | 保留 open，降级 p1 + 改写描述 |

### 压力面试合并说明（2026-08-03 下午）

> 两轮压力面试（面试官视角报告 + 用户自审）暴露的问题，按"同根因直接合并"原则并入本清单：

- **合并进已有 open 项（不新增编号）**：`d056`（升 p3→p0，并入天地图密钥历史泄露）、`z045`（并入伪三维自承 `CesiumLayerRegistrar.ts:~276` + 首页假数据 `HomePage.vue:38-56`）、`z046`（并入 313 处类型逃生，升 p3→p2）、`b038`（并入双机制 `mapStore.ts:342-346` + 单槽 `analysisHandler`）。
- **已闭环不再新增**：`getDEM` 死代码（`floodAdapter.ts:330`）属 `b030` 已闭环范围（预留钩子无调用方）；GCS 拆 `core/layout`+`shared/layout` 为 `z054` 重构后既定布局（设计取舍，非缺陷）；`waterLevelStore` 仅 2 字段为合理 Pinia 模式（非缺陷）。
- **新增 11 项**（a035/a036/a037、b044/b045、d069、z073/z074/z075/z076/z077），编号接续全局最大空闲号，未与 `已解决问题.md` 碰撞。

### 编号体系与文档关系

- 本文件为唯一 open 清单，编号体系贯通（接续主清单 + 已解决最大编号）。
- 原有 open 项除 `b030`/`z033` 经本次验证闭环外，其余保留并就地更新行号/等级。
- 已闭环项按原编号移入 `已解决问题.md`（补 `solve:20260803`）。
