# 待解决问题（唯一 open 台账 · 2026-08-14 重整）

> **定位**：当前所有经真实代码取证确认**仍未闭环**的问题。修一个 → 从本文件删除，按**原编号**移入 `已解决问题.md`（主本；精选版 `已解决问题-精选版.md` 为其精华聚合副本），编号不重排。
>
> **编号规则**：`a`=地图层、`b`=业务层、`c`=组件层、`d`=后端层、`z`=暂未归类。编号全局唯一（本文件与已解决台账之间无重复）。
>
> **时间戳规则**：每条 `问题编号` 下方仅标注 `discover:YYYYMMDD`（发现日）；`solve:YYYYMMDD` 在移入已解决台账时补。
>
> **问题等级**：`p0` 致命/高危阻断｜`p1` 高（正确性/稳定性/安全非致命/面试短板）｜`p2` 中（待改进·低危 bug·语义不一致）｜`p3` 低（接受/硬化/文档/视觉 nits）。
>
> **列格式规则（4 列，每条必须完整）**：
> - **列1 问题等级**：p0/p1/p2/p3。
> - **列2 问题编号**：编号 + `<br>discover:YYYYMMDD`（无 solve）。
> - **列3 问题描述及复现方法/对应文件**：问题描述 + **复现方法**（可复现的写复现路径/操作）；无复现方法的写**对应文件**（file:line 锚定项目内位置）。不写"影响哪些文件"。
> - **列4 解决记录**：三段——**预期思路**（去项目里看代码后写的具体改法）/ **实施风险**（改这个会碰的雷、副作用、前置验证）/ **尝试日志**（实际尝试过的精简成几句话；第一版尚未实际尝试则如实写"第一版尚未实际尝试"）。
>
> **列宽换行标准（2026-08-14 定）**：单元格内单行显示宽度 **≤ 88 半角单位**（全角按 2 计、半角按 1 计，≈44 个全角字符）。超宽时用 `<br>` 在语义断点处换行——断点优先标点（`；。，、：！？` 空格 `,` `:` `→` `)` `）` `>` `/`），**反引号代码段内不断行**（代码完整性优先，允许超宽），已有 `<br>` 保留。换行只作用于列3/列4，列1/列2 不换行。
>
> **表格对齐标准（2026-08-14 定）**：全文档所有分区表格共用**同一列宽模板**（按各列最大显示宽度），行与行之间用空格补齐，使 `|` 竖线在等宽字体下上下对齐；表头/分隔行/数据行统一按模板生成。新增/修改条目后跑一次对齐脚本保持竖线整齐。
>
> **副本规则（两级，2026-08-14 与 00 §4 对齐）**：① 子 agent 工作副本 = 批次目录内 `docs/audits/<日期>-<批次名>/专项N-*-问题副本.md`（审查产出统一落批次目录，禁止散落 docs 根目录）；② 用户裁决副本 = 主 agent 从批次目录聚合生成 `docs/问题副本-<yyyy-mm-dd>-<主题>.md`（命名含当天日期），两处不重复书写。副本经用户裁决后：有价值 → 转正进入本文件（分配正式编号，`discover` = 批次日期）；已解决/证伪 → 留在副本或删除。副本不污染本文件编号系统。

---

## 地图层（图层生命周期 / 地图渲染 / 地图引擎集成 / 坐标系 / 空间数据性能 / 图层状态 / 地图交互）

| 问题等级 | 问题编号                                            | 问题描述及复现方法/对应文件                                                             | 解决记录                                                                                |
| -------- | --------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| p2       | a064-CesiumRenderer DEV门控仍在<br>`discover:20260810`                     | `CesiumRenderer.ts:906` 等渲染路径 DEV 门控（import.meta.env.DEV）仍在——生产行为与 <br>dev 不一致，观测/告警被门控剥离（810 副本副-03 取证）。   | **预期思路**：与生产可观测性（z095 批次）统一处理：采样日志/告警不依赖 DEV 门控。<br>**实施风险**：生产日志噪音需采样率控制。<br>**尝试日志**：第一版尚未实际尝试。       |
| p2       | a065-CRS散落无统一投影封装<br>`discover:20260810`                          | 坐标投影/CRS 转换散落渲染器与业务各处，无统一封装（810 副本副-20 取证）。               | **预期思路**：抽统一投影工具（fromLonLat/toLonLat/CRS 判定收口一处）。<br>**实施风险**：双引擎行为等价需回归验证。<br>**尝试日志**：第一版尚未实际尝试。                  |
| p2       | a078-双渲染器呼吸动效同构复制<br>`discover:20260903`                        | startBreathing/startFacilityBreathing 在 OLRenderer.ts:928-1030 与 <br>CesiumRenderer.ts:718-760、779-823 算法/常量/注释逐行复制（10±5 脉动、<br>0.5±0.3 透明度、rAF 循环）——改一处需同步 2-3 处；相机换算已有<br>共享先例 zoomToHeight（core/config/map），呼吸却未收敛（2026-09-03 取证）。 | **预期思路**：抽引擎无关的"脉动参数推导/动画帧调度"共享工具，双渲染器<br>各自保留引擎 API 适配（仿 zoomToHeight 先例）。<br>**实施风险**：动效视觉回归需双引擎冒烟。<br>**尝试日志**：第一版尚未实际尝试。                      |
| p2       | a079-渲染器大文件：行数非债，按变更理由组织<br>`discover:20260903`          | CesiumRenderer.ts 1858 行 / OLRenderer.ts 1093 行。经取证：零业务耦合（import <br>仅 core/shared）、方法粒度小、分区注释清晰——**行数本身不是债**（推翻既往<br>"按行数拆分"判断）；真实债为跨引擎复制（见 a078）与多职责单文件的可再组织性。<br>**对应文件**：`core/map/renderers/CesiumRenderer.ts`；`OLRenderer.ts`。        | **预期思路**：仅按"变更理由"组织（如水面/裁剪/事件各自独立文件，仿能力接口<br>先例），禁止以行数为目标拆分（防拆而复并）。<br>**实施风险**：拆分后双引擎回归。<br>**尝试日志**：第一版尚未实际尝试。                  |
| p1       | a091-renderError 匿名监听永不移除<br>`discover:20260912` | **复现**：单例 Viewer 复用下反复挂载/卸载，`scene.renderError` 监听逐次累加（匿名箭头无引用可摘），一次渲染错误弹 N 次提示，闭包永久持有已销毁 renderer——与已修 a085（webglcontextlost）同病根的第三处漏网。**对应文件**：`core/map/renderers/CesiumRenderer.ts:144`（匿名箭头；全仓无对应 remove 调用）。 | **预期思路**：回调具名化实例字段 + register/unregister（幂等：先摘再挂），`destroyEvents` 末尾摘除——完全复刻 a085 已验证修法。<br>**实施风险**：低（照抄既有模式）；需补白盒断言（三轮挂卸后 added=0 / removed=3）。<br>**尝试日志**：第一版尚未实际尝试（专项2 sliceD D-1 已给行号与判据）。 |
| p3       | a092-3D→2D 未取消相机防抖定时器<br>`discover:20260912` | 3D 切 2D 时相机防抖（debounce）定时器未取消，窗口期内后台多触发一次渲染（非功能阻断，属资源浪费）。**对应文件**：`core/map/UnifiedMap.vue:471-478` + `core/map/renderers/CesiumRenderer.ts:1100-1152`。 | **预期思路**：切换路径取消在途 debounce（clearTimeout + 失效标记）。<br>**实施风险**：需确认 debounce 归属（renderer 实例级）避免误伤跨实例调用。<br>**尝试日志**：第一版尚未实际尝试（专项2 sliceB B-1，可选 P3）。 |
| p3       | a093-选址/图层面板三处派生缺陷<br>`discover:20260912` | ①选址页呼吸态读 catalog 镜像而非 registry 权威派生（`SiteSelectionPage.vue:122-124`）；②`LayerControlPanel.vue:48-50` setup 期一次性捕获 `props.layerOrder`，父改子不更新；③`PlansPanel.vue:45-46` inject 默认值掩盖 provider 缺失，功能静默失效。 | **预期思路**：①改读 registry 单一权威；②改 computed/直接读 props；③inject 默认值改 fail-loud（缺失即显式报错）。<br>**实施风险**：①涉及呼吸链路回归；③需确认无「可选注入」的合法场景。<br>**尝试日志**：第一版尚未实际尝试（专项2 sliceC C-1/C-2/C-3）。 |
| p2       | a088-图层 id 字面量散落 7+ 处<br>`discover:20260911` | 公共底图/边界/港口 id 无常量，散落 `UnifiedMap.vue:290-327`、`LayerControlPanel.vue:23`、`mapStore.ts:14` 与 4 个业务页 layer-order 数组；`FloodAnalysisPage.vue:69` 定义 WATER_SURFACE_ID 同文件 :508 又裸写——改 id 需人肉扫多处，漏一处即面板按钮静默消失。<br>**对应文件**：`core/map/UnifiedMap.vue`、`stores/mapStore.ts`、`FloodAnalysisPage.vue` 等。 | **预期思路**：建 `shared/constants/layers.ts` 收口全部图层 key（含 base-image/boundary/ports），页面 layer-order 与面板默认值统一 import；guard 增图层 key 词表检查。<br>**实施风险**：触及 core/business ~15 文件回归面大；与 a066（key 命名空间，已闭环）为姊妹面勿混淆。<br>**尝试日志**：第一版尚未实际尝试（912 复核 a088）。 |
| p3       | a089-图层未挂载可观测性缺失<br>`discover:20260911` | 3D 非 hillshade GeoTIFF 静默跳过现仅 warn 留痕（a087 已升），图层面板/BLM 仍无从感知「图层没挂上」——用户以为图层存在实际缺失。<br>**对应文件**：`CesiumRenderer.ts` addGeoTIFFLayer、`BusinessLayerManager.ts` reapplyAll。 | **预期思路**：reapplyAll 汇总未挂载图层 id 列表上抛，图层面板对缺失图层标灰/提示。<br>**实施风险**：BLM 返回值契约变更需全调用方回归。<br>**尝试日志**：warn 留痕已做（a087）；完整方案第一版尚未实际尝试（912 复核 a089）。 |

## 业务层（业务逻辑 / Adapter / Store / 业务页特性）

| 问题等级 | 问题编号                                            | 问题描述及复现方法/对应文件                                                             | 解决记录                                                                                |
| -------- | --------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| p1       | b034-前后端类型同步/职责边界<br>`discover:20260802`                        | 无 OpenAPI/契约测试；zod 已落地为前端校验源，但前后端仍非单一事实源；adapter 与 <br>service 职责边界未文档化，业务规则是否前后端重复计算未界定。**对应文件**：<br>`package.json:37` zod；`types/schemas.ts`/`useApiRequest.ts`/`loadStatic.ts` <br>前端校验源；后端无 OpenAPI 文件。        | **预期思路**：方案A——用 zod schema 作单一事实源，由 schema 生成 <br>OpenAPI（zod-to-openapi）供后端对照；方案B——手写 OpenAPI + <br>契约测试（openapi-backend）；明确业务规则后端为权威。<br>**实施风险**：①OpenAPI 与 zod 双维护漂移风险（方案A 用生成器规避）；②工作量大（31 <br>端点）；③后端 Express 无 TypeScript，契约对齐靠人工。<br>**尝试日志**：第一版尚未实际尝试。    |
| p1       | b040-useAuth/useApiRequest模块级业务状态<br>`discover:20260802`            | 核心业务状态（user/token）为模块顶层单例可变变量，应进 store。**对应文件**：<br>`useAuth.ts:46` `const user: Ref<User\| null> = ref(readStoredUser())`；<br>`useApiRequest.ts:29` `const token: Ref<string> = ref('')`。            | **预期思路**：创建 `authStore`（Pinia）容纳 user/token/isAuthenticated/setToken/<br>clearToken；useAuth/useApiRequest 模块级仅保留纯函数（apiRequest 等）；所有调用方改读 <br>authStore。<br>**实施风险**：①useAuth 被多处调用（App.vue/各业务页），迁移需全量改读 store；<br>②useApiRequest 与 useAuth 的依赖关系（useAuth 解构 useApiRequest 的 token）需重构；<br>③HMR/测试隔离——模块级单例测试隔离困难正是迁移动机，但 store 需确保 reset 可测；<br>④isAuthenticated 是 computed 透传 token，迁入 store 后响应性需验证。<br>**尝试日志**：第一版尚未实际尝试。<br>**2026-09-12 范围扩展（专项2 sliceA/E 复核）**：同根问题另含 useFavorites 模块级 ref、<br>`pendingFavorite` 登出未清（跨账号残留提交）、模块级 watch 无 stop（HMR 重复注册）、<br>Pinia 未启 strict（state 直改无运行时护栏）、`lastBaseRenderer` 模块级单例；<br>全量证据清单已内联于本行（原件《全项目问题总清点》已收口归档至 .local/trash）。<br>**20260912 复核注记（912）**：部分收口——M-1 planSchema 载荷校验已闭环（b095）、FloodStatistics 绑 z.infer、bizCode 常量化（b100）；剩余 User/FavoriteItem/Plan 的 z.infer 派生与 OpenAPI 管道。  |
| p2       | b045-三业务模块结构不一致<br>`discover:20260803`                           | flood/forecast/site-selection 三模块目录结构不统一，新增模块无统一模板。**对应文件**：<br>flood 有 `constants/` 目录无 composables；forecast 有 `constants.ts` 有 composables；<br>site-selection 无 constants。  | **预期思路**：抽公共业务模块骨架（`constants/` + `composables/` + `adapter` 同构），<br>新模块按模板；现有三模块对齐补齐缺失层。<br>**实施风险**：①重构面大（三模块目录调整）；②site-selection 已确认设计豁免无 <br>adapter（a021 已解决，数据流契约 R5），强行补 adapter 违背既定决策；③回归风险。<br>**尝试日志**：第一版尚未实际尝试。**注意**：a021 闭环决议为"仅 flood 有 adapter"，<br>本条与 a021 有张力，需重新裁定范围。    |
| p2       | b065-floodStore快照混合读活状态<br>`discover:20260810`                     | `floodStore.ts:108-116` consumeState 恢复时混合读取活状态，快照与活状态边界不清（810 <br>副本副-04 取证）。   | **预期思路**：consumeState 与活状态读写分离，快照恢复走独立路径。<br>**实施风险**：登出/回跳恢复语义需回归验证。<br>**尝试日志**：第一版尚未实际尝试。                       |
| p2       | b089-业务图层注册/更新样板多处复制<br>`discover:20260903`                   | 「has()?updateData:register」同构样板散落五处：FloodAnalysisPage.renderFloodAreas/<br>renderAffectedFacilities、SiteSelectionPage.syncFacilityPoiLayer、useForecastLayer.<br>updateForecastLayer、useAnalysisLayer.createUpdateHandler（2026-09-03 取证）——<br>BLM 已有 register/updateData，但未收抽象，改一处需同步多处。                   | **预期思路**：BLM 或公共 composable 提供 applyOrUpdate(id, data, options) <br>单点（先例 useTerrainProfiles 收口）。<br>**实施风险**：各调用点注册参数化差异（options）需对齐。<br>**尝试日志**：第一版尚未实际尝试。            |
| p2       | b098-ForecastPage 快照跨卸载复活<br>`discover:20260912` | ForecastPage 卸载未清快照，内存快照跨卸载复活陈旧控件与图表。**对应文件**：`business/forecast/ForecastPage.vue:42`、`shared/composables/useProfileSnapshot.ts:16`（专项2 sliceA A-2 与指标 2.5 两处同项）。 | **预期思路**：卸载时 clear 快照（对齐 FloodAnalysisPage 的 unmount 守卫先例）。<br>**实施风险**：需确认快照恢复是否属有意设计（跨页返回保留）——若是，改为「标记来源页」而非全清。<br>**尝试日志**：第一版尚未实际尝试。 |
| p2       | b101-登出后 authRestored 恒 false，收藏失败恢复永久静默<br>`discover:20260912` | logout 置 `authRestored=false` 而 restoreAuth 仅 App 挂载时跑一次：SPA 内登出→重登（不刷新）后恒 false，useFavorites 把此后**所有**收藏拉取失败都当「恢复期」静默吞——L-3 修复的「恢复后显式可见」被架空。**对应文件**：`shared/composables/useAuth.ts`（logout 置位处）、`useFavorites.ts:64`（静默分支）。 | **预期思路**：登出改为「同步清态即视为已恢复」，或恢复期判定改用「restorePromise 在途」而非布尔标志。<br>**实施风险**：低；回归登出→登录→收藏失败提示路径。<br>**尝试日志**：第一版尚未实际尝试（ZCode 二审发现）。 |
| p3       | b102-useFavorites fetchInFlight 在途丢弃竞态<br>`discover:20260912` | `useFavorites.ts:59` `if (fetchInFlight) return` 直接丢弃而非排队：immediate 触发的旧登录态拉取在途时 restore 完成（token 空→非空），本次登录态重拉被丢弃；该在途请求随后失败则收藏静默为空，直到下次 user/token 变化。**对应文件**：`shared/composables/useFavorites.ts:59`。 | **预期思路**：在途时置 dirty 标记，finally 后补拉一次（单级重放）。<br>**实施风险**：极低。<br>**尝试日志**：第一版尚未实际尝试（ZCode 二审发现）。 |

## 组件层（UI 组件 / CSS / 面板 / 视图组件 / 前端打包）

| 问题等级 | 问题编号                                            | 问题描述及复现方法/对应文件                                                             | 解决记录                                                                                |
| -------- | --------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| p2       | c037-列表key/字段丢弃残留<br>`discover:20260810`                           | 坐标 (0,0) 哨兵已修（a059），列表渲染 key 与字段丢弃残留（810 副本副-15 取证）。        | **预期思路**：列表渲染 key 规范 + 字段透传审查。<br>**实施风险**：低。<br>**尝试日志**：第一版尚未实际尝试。                                        |
| p2       | c038-图例/瓦片缓存残留<br>`discover:20260810`                              | z-index/transition/图表色已收口（c035），图例与瓦片缓存策略残留（810 副本副-22 取证）。 | **预期思路**：图例 token 化 + 瓦片缓存策略（HTTP 缓存头）。<br>**实施风险**：瓦片缓存头需与 nginx 缓存策略协调。<br>**尝试日志**：第一版尚未实际尝试。                             |

## 后端层（后端代码 / API / 认证 / 服务 / 部署配置）

| 问题等级 | 问题编号                                            | 问题描述及复现方法/对应文件                                                             | 解决记录                                                                                |
| -------- | --------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| p2       | d051-缺统一入参校验层<br>`discover:20260801`                               | 参数校验散落各 controller，无统一中间件。**对应文件**：`backend/middleware/` 仅 <br>`auth.js`/`logSanitizer.js`，无 `validate.js`；`siteAnalysisController.js:35` <br>手写校验（均走 `BusinessError`+全局中间件，无裸 `res.status(400)`）。        | **预期思路**：新建 `backend/middleware/validate.js`（用 zod 或 ajv 校验 req.body/query/<br>params），挂路由级中间件；现有 controller 手写校验逐步迁移。<br>**实施风险**：①现有 controller 校验语义需逐一核对（避免新中间件与旧手写校验重复/冲突）<br>；②过渡期双轨；③后端无 TypeScript，zod 在 JS 侧需额外配置或用 ajv。<br>**尝试日志**：第一版尚未实际尝试。 |
| p3       | d054-公开路由白名单兜底确认<br>`discover:20260801`                         | 公开路由无统一输入白名单/validateQuery。**对应文件**：`backend/routes/*.js` 实测无 <br>`sanitize`/`whitelist`/`validateQuery`（仅 `validateWaterLevel` 单点校验 + <br>`logSanitizer` 日志脱敏）。     | **预期思路**：全公开路由（forecast/flood 只读端点）统一输入白名单中间件，<br>拒绝非预期字段。<br>**实施风险**：①白名单遗漏致合法功能失效；②需盘点所有公开路由的合法入参；③与 <br>d025（已解决的 forecast 路径遍历）`ALLOWED_INDICATORS` 白名单同源，可复用扩展。<br>**尝试日志**：第一版尚未实际尝试。         |
| p2       | d082-后端常量多处重复定义<br>`discover:20260810`                           | ErrorCode/包围盒/水位上限/riskLevel 常量前后端重复定义，易漂移（810 副本副-18 取证）。  | **预期思路**：抽共享常量模块，后端为权威源。<br>**实施风险**：前后端双仓常量同步机制需设计。<br>**尝试日志**：第一版尚未实际尝试。<br>**20260912 复核注记（912）**：部分收口——riskLevel 兜底走 RISK_LEVEL_BANDS（b095）、水位口径双侧常量 + constants-audit 守卫（b099）、bizCode 前端唯一表（b100）；剩余 route mode 枚举 4 份副本、预测年限前后端各自为政、token 占位字符串 3 种、riskLevelCode join 键完整迁移。                                            |
| p3       | d138-本地路由拓扑数据缺口（roads_noded/pgrouting）<br>`discover:20260912`  | 本地 v3_dev 无 `roads_noded` 表且容器无 pgrouting 扩展 → `route.e2e`「正常寻路」<br>本地 500（全量套件唯一红）；CI 侧已由 `backend/test/seed/ci-seed.sh` + <br>pgrouting 镜像闭环，仅本地开发环境缺数据。**对应文件**：`tools/roads/roads-noding.sql`；<br>`docker-compose.v3.yml`。        | **预期思路**：本地容器切 pgrouting 镜像（数据卷兼容需验证）后跑 roads-noding.sql<br>重建拓扑；或维持现状（本地跳过该用例，CI 已绿）。<br>**实施风险**：①容器重建涉用户开发库，需用户执行；②noding 流水线对 61 万段路网耗时。<br>**尝试日志**：CI 侧 20260912 已闭环（seed 夹具），本地待用户执行。        |
| p3       | d139-限流桶归属无语义化装饰器<br>`discover:20260911` | `@SkipThrottle` 手工散落 5 个控制器（auth 逐路由/flood/forecast/plans/health），新控制器忘挂即默认套 login 桶——d136 修急性出血，本条治理复发通道（策略只有注释没有执行机制）。<br>**对应文件**：`backend/src/modules/*/controllers/*.controller.ts`、`app.module.ts:25-27` 注释。 | **预期思路**：提供 `@ThrottleBucket('global')` 语义化装饰器或 Controller 级默认 global 桶、仅 auth 显式挑桶，把「新路由默认进哪个桶」变成代码而非约定。<br>**实施风险**：装饰器语义变更需全控制器盘点回归。<br>**尝试日志**：第一版尚未实际尝试（912 复核 d139）。 |
| p1       | d140-DB连接池无 error 监听，断库即全进程崩溃<br>`discover:20260912` | DB 重启/闪断/故障转移时 pg 空闲连接向 BoundPool 发 'error' 事件，db.service 无任何监听 → Node 按未处理 'error' 退出，全 API 瞬死（在途请求连 5xx 信封都发不出）。**复现**：nest 运行中 `docker stop` 数据库 → 进程即刻消失（ZCode 二审动态测试两次实锤，日志 `Unhandled 'error' event on BoundPool`）。**对应文件**：`backend/src/infra/db/db.service.ts:18`（new Pool 后零 pool.on）。 | **预期思路**：构造器补 `this.pool.on('error', (e) => logger.error('[db] pool error', e))`——idle client 错误降级为日志；每请求级致命错误已由 isFatalDbError 上抛（d135 修复），两层互不干扰。<br>**实施风险**：极低（纯监听不改查询路径）；生产 compose `restart: unless-stopped` 可自愈但掉在途请求。<br>**尝试日志**：第一版尚未实际尝试。 |
| p2       | d141-PostGIS 版本三方不一致<br>`discover:20260912` | CI 用 `pgrouting/pgrouting:16-3.5-3.8`（PostGIS 3.5），生产 compose 用服务器自建 `local/postgis-pgrouting:16-3.4`（构建游离 CI 外），本地开发 `postgis/postgis:16-3.4`（无 pgRouting）——route 域 SQL 在 3.5 上验证、3.4 上跑生产，存在函数行为差异风险；ci.yml 注释「库口径对齐 compose.v3」仅在用户/库名/端口层面成立。**对应文件**：`.github/workflows/ci.yml`（services 段）；`docker-compose.yml`；`docker-compose.v3.yml`。 | **预期思路**：生产自建镜像构建进 CI（tools/db/Dockerfile.pgrouting）或 CI 降到 3.4+pgrouting 对齐生产；本地容器切 pgrouting 镜像（联动 d138）。<br>**实施风险**：中——生产镜像重建需服务器配合；版本切换需真库回归。<br>**尝试日志**：第一版尚未实际尝试（ZCode 二审发现）。 |
| p3       | d142-build-push-images 不依赖 audit<br>`discover:20260912` | ci.yml build-push-images 的 needs 仅三个测试/static job：main 上 audit 真红（high 漏洞）时镜像照常构建推 GHCR——审计门禁实际只拦部署、不拦制品发布。**对应文件**：`.github/workflows/ci.yml`（build-push-images needs/if）。 | **预期思路**：needs 链加入 audit（配合 `always() && !failure() && !cancelled()` 避免 docs-only 过滤时误阻塞）。<br>**实施风险**：低。<br>**尝试日志**：第一版尚未实际尝试（ZCode 二审发现）。 |

## 暂未归类（文档 / 工程质量 / 测试覆盖 / 类型安全 / 架构守护 / 杂项）

| 问题等级 | 问题编号                                            | 问题描述及复现方法/对应文件                                                             | 解决记录                                                                                |
| -------- | --------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| p3       | z040-领先版本过激<br>`discover:20260801`                                 | 核心依赖大版本，磨合风险。**对应文件**：`package.json:64` `"vite":"^8.0.16"`、`:60` <br>`"typescript":"^6.0.3"`、`:68` `"vue-tsc":"^3.3.8"`；@vueuse 已现 `INVALID_ANNOTATION` <br>警告。 | **预期思路**：谨慎升级/锁版本，pin 到当前可用的稳定 patch 版本；评估 @vueuse <br>警告是否需降级。<br>**实施风险**：①降级可能引入其他不兼容；②锁版本后安全补丁需手动跟进；③vite 8 / ts 6 <br>为很新的大版本，生态插件（unplugin/vite-plugin-cesium）磨合风险持续。<br>**尝试日志**：第一版尚未实际尝试。  |
| p3       | z042-函数过长(指标恶化)<br>`discover:20260801`                             | 可读性最差的两处。**对应文件**：`CesiumRenderer.ts` 1877 行（P8 合并 4 <br>个假拆分文件后翻倍）；`OLRenderer.ts` 775 行；`floodAdapter.ts` 已改善至 222 行。       | **预期思路**：按职责拆分——CesiumRenderer 已拆出 CesiumEvents/CesiumViewportCulling/<br>CesiumLayerRegistrar/CesiumWaterSurface（z053 已解决），但 P8 合并 4 <br>个假拆分文件后反弹至 1877 行，需重新按职责拆（水面/图层/事件/裁剪进一步细分）；<br>OLRenderer 按图层类型拆（点/线/面/热力）。<br>**实施风险**：①拆分破坏类型（z065 已移除 @ts-nocheck，拆分需保持类型完整）；<br>②测试可能因模块路径变更失败；③需渐进拆分避免大爆炸式重构。<br>**尝试日志**：第一版尚未实际尝试。   |
| p2       | z064-无集成/E2E测试<br>`discover:20260802`                                 | 核心业务流程无跨模块流程测试，回归靠人工。**对应文件**：`package.json:10` <br>`"test":"cd frontend && vitest run"` 仅单元测试；`cypress`/`playwright` 0 命中；<br>`frontend/e2e`/`cypress`/`tests/e2e` 目录均不存在。        | **预期思路**：引入 Playwright 覆盖核心业务流程 E2E（登录→选址→洪涝分析→预测）；<br>或先补集成测试（API 层串联）。<br>**实施风险**：①E2E 维护成本高（UI 变更即失败）；②CI 时间显著增加；<br>③需测试环境（后端+前端+FastAPI flood-service 全启动）；④mock/api/online 三数据源模式 <br>E2E 路径不同。<br>**尝试日志**：第一版尚未实际尝试。 |
| p1       | z071-无路由守卫做store重置<br>`discover:20260802`                          | 路由切换时无显式 store 重置、无全局请求取消。**对应文件**：`router/index.ts` <br>beforeEach/afterEach/beforeResolve 0 命中（仅 createRouter + routes）；mapStore/<br>forecastState 不在 resetBusinessStores 中；useSiteAnalysisApi 无 AbortController。      | **预期思路**：添加 `router.beforeEach` 守卫，路由切换时重置非持久化业务 store + <br>取消在途请求（AbortController）。<br>**实施风险**：①重置范围/时机需精确——误清 mapStore 会导致底图/引擎状态丢失，<br>应只重置离开的业务页 store；②resetBusinessStores 现由登出触发（useAuth），<br>路由守卫需复用还是独立需设计；③useSiteAnalysisApi 已有实例级 AbortController（b021 <br>已解决），但路由级全局取消需统一信号；④首页/个人中心等非业务页不应触发业务 store <br>重置。<br>**尝试日志**：第一版尚未实际尝试。    |
| p1       | z072-错误上报SDK未接入<br>`discover:20260802`                              | 无错误上报，未捕获错误不可追溯。**对应文件**：`logger.ts` 全文实测已无 transport <br>钩子（emit 仅输出控制台，z020 曾实现的 `addLogTransport` 已被后续清理删除）；<br>`main.ts:73` 注释"logger 已预留 addLogTransport 钩子，Sentry <br>就绪后一行接入"——**注释与代码不符**（钩子已不存在）；`main.ts:78-86` window.onerror/<br>onunhandledrejection 仅 logger.error 无上报。  | **预期思路**：方案A——`logger.ts` 重新加 transport 数组 + `addLogTransport(fn)` 钩子，<br>main.ts 接 `@sentry/vue` 后 `addLogTransport(Sentry.captureMessage)`；方案B——直接 <br>main.ts 接 `@sentry/vue`，errorHandler 里 `Sentry.captureException(err)`，不动 logger。<br>**实施风险**：①引入 `@sentry/vue` 新依赖（包体积 +~30KB gzip）；②DSN <br>需环境变量配置（Sentry DSN/环境/release）；③采样率配置避免噪音；④方案A 的 transport <br>钩子曾被当死代码删除（z020→清理），重新引入需证明有调用方；⑤main.ts:73 <br>注释需同步修正。<br>**尝试日志**：第一版尚未实际尝试。**重要发现**：main.ts:73 注释引用的 addLogTransport <br>钩子在 logger.ts 中已不存在，属文档/注释与代码漂移。 |
| p2       | z104-createPersistedState仅内存快照<br>`discover:20260810`                 | createPersistedState 仅内存级快照，刷新即失（810 副本副-36 取证）。                     | **预期思路**：持久化到 localStorage（跨刷新恢复）。<br>**实施风险**：隐私模式/SSR 下 localStorage 不可用需防御。<br>**尝试日志**：第一版尚未实际尝试。                               |
| p2       | z132-eslint --fix 触发全量.vue 解析崩溃<br>`discover:20260828`             | **复现**：对仓库执行 eslint --fix（Windows 本机 2026-08-28 三次复现）：全部 .vue 报<br>"TSConfig does not include this file" 解析错误（38 文件全崩）；不带 --fix、npm run<br>lint、单文件 --fix-dry-run 均正常。**对应文件**：`eslint.config.js`、<br>`frontend/tsconfig.app.json`<br>**编号修正（2026-09-11）**：原编号 z127 与已解决台账 z127（白叠层 token 化，08-27 闭环）<br>撞号，按「首次分配」规则（已解决 z127 早于本条 discover）重编为 z132。 | **预期思路**：升级/钉住 typescript-eslint 与 vue-eslint-parser 验证；短期绕过——.vue<br>改动用手工排序（--fix-dry-run 导出期望输出对照），本机避免全量 --fix，CI 是否受<br>影响待验证。<br>**实施风险**：升级解析器影响面大，需全量 lint/typecheck 回归。<br>**尝试日志**：存量已手工清零（20260828）；缓存投毒假说已排除，失败与 --fix 强相关。 |
| p1       | z146-深链首屏「地图加载中」≈10s+ 不消<br>`discover:20260911` | 直链/刷新业务路由时「地图加载中…」长时间不消（报告口径 ≈10s；ZCode 本机 dev 实测 /forecast 约 15–17s 后才可达，最终能加载、非卡死）。**根因未定位**。**对应文件**：`business/site-selection/`、`core/map/`（地图初始化就绪时序）。 | **预期思路**：对比「首页进入」与「直链冷启」两条路径的地图就绪条件（排查仅首页路径会触发的 preload/首帧事件），把 loading 消除条件改为可观测就绪信号；补各阶段耗时打点。<br>**实施风险**：①就绪条件放宽可能掩盖真实失败；②需双引擎与多路由回归。<br>**尝试日志**：已实测复现（/forecast 15–17s 后可达），根因未定位。 |
| p2       | z147-前端性能债四项<br>`discover:20260911` | ①2D 拖图 154→32fps（热点图层未用 VectorImageLayer、拖动期未降级）；②3D 交互期未降载（相机 moveStart/moveEnd 未隐藏重实体）；③首进 3D ≈850KB 星空资源未懒加载；④ECharts 未全量切 merge 模式。**对应文件**：`core/map/renderers/OLRenderer.ts`、`CesiumRenderer.ts`、`dist/cesium/Assets`、`visualization/charts/`。 | **预期思路**：①VectorImageLayer + moveStart 降半径/模糊；②相机移动期隐藏淹没面/标签；③tycho2/IAU/approxHeights 运行时懒加载；④setOption merge。<br>**实施风险**：①视觉降级需口径确认；②③涉及 Cesium 资源加载时序。<br>**尝试日志**：第一版尚未实际尝试（实测代码零痕迹）。 |
| p2       | z148-数据与能力缺口五项<br>`discover:20260911` | ①海运网络（sea_ports/sea_routes 未建，**数据来源未确认**；红线：禁合成数据入库）；②railways 未建 noded 拓扑（≈9.5k 条仅原始表）；③海洋地形（GEBCO 测深未融合 + 涠洲岛误掩）；④信息栏增补（水深直方图/淹没体积/设施分类统计等后端已有字段未展示）；⑤剖面数据同源重采样（起终点离港 3.6–16.7km、GLO-30 与线上 ASTER 不同源、qz-mangrove 仅 1 点）。 | **预期思路**：①先确认数据源再动；②复用 roads-noding.sql；③GEBCO 融合+掩膜修正；⑤以 ports.json 重设起终点 + 线上同源 ASTER 重采样。<br>**实施风险**：①数据完整性红线；③量大；⑤需 DEM 与流水线复跑。<br>**尝试日志**：⑤ 已完成（2026-09-12）：4 条剖面贴港重定线 + 全量 DEM（filled_CGCS2000_int16）重采样，海平面基准（`tools/flood/rederive-terrain-profiles.py` 可复跑；钦州 0-8m/防城 0-15m/北海 0-10m/红树林 0-9m，41 点/条）；留尾：0.1m 真分辨率重算归 T-7（问题副本-2026-09-12 §四）。其余 ①-④ 未动。 |
| p2       | z149-部署链路残余<br>`discover:20260912` | ①部署仍耦合服务器 git 工作树（`ci.yml:478-487` 要求 `git pull --ff-only` + users.json 备份/还原 workaround）；②无镜像预拉并行与 GHCR tag 保留策略（部署轮询 90×20s=30min）。 | **预期思路**：①部署只依赖镜像 tag（服务器 compose 用 IMAGE_TAG 覆盖 + 预拉），脱离服务器 git 状态；②构建后并行预拉 + 轮询降到 15min + GHCR 只留最近 N 个 run tag。<br>**实施风险**：①改动部署链需真机验证；②tag 清理防误删在役版本。<br>**尝试日志**：第一版尚未实际尝试（CI-CD 报告 §1.5-3/§P2-5）。 |
| p2       | z150-测试体系脆性（过拟合与盲区）<br>`discover:20260912` | ①Cesium mock 六份内联复制（无共享 factory）；②白盒断言私有状态（`_cullLayers` 等）；③逐字节样式断言；④`setTimeout` 非确定性等待（UnifiedMap 32 处）；⑤页面级零测试（SiteSelection/RouteAnalysis/Profile/LayerControl）；⑥存量用例 `skipIf` 恒跳过（`types/__tests__/schemas.test.ts:33`）；⑦真库门控激活后 POI/xiaoqu 绑定用例仍自探测跳过。 | **预期思路**：①抽共享 cesiumMock factory；②③④改行为断言/假定时器/fixture；⑤补四条页面 smoke；⑥⑦把跳过改为显式 seed 或 fail-loud 标注。<br>**实施风险**：改造面大，需与覆盖率棘轮协同（防棘轮误判）。<br>**尝试日志**：第一版尚未实际尝试（CI-CD 报告 §2.3/2.5 + 任务清单 T4 组）。<br>**20260912 复核注记（912）**：补恒真断言锚点 T1-1 `plans.repository.spec.ts:87` / T1-2 `spatial.repository.spec.ts:342`（C4 激活真库的前置）；「测试依赖本机未入库 env」脆性实锤并已修（z143）。 |
| p2       | z151-文档/注释与代码漂移（机制退役未同步）<br>`discover:20260911` | Express/FastAPI/`flood-online`/「6 档」/已改名路径等幽灵声明散落：前端 ≈30 处注释（services 与 flood-analysis README、RouteAnalysisPage、api.ts、manifest.ts、FloodAnalysisPage 等）、根基文档（代码知识库大面积）、审查专项 1/5、tools README、配置注释（`.npmrc` 提 eslint-plugin-import、`.husky/pre-commit` glob、compose 头部「四服务」、Dockerfile 默认六域）。**根因**：机制退役后未同步且无门禁。 | **预期思路**：按层分批清理（代码注释 → tools README → 根基/专项文档），每批 grep 反查；易漂移域补自动化断言（routes-audit 先例）。<br>**实施风险**：面广量大，须防误改历史留档（复盘/历程类文档豁免）。<br>**尝试日志**：已修 3 份（API契约/核心流程/开发指南，`c2adac86`）；全量清单原件已收口归档（见《问题副本-2026-09-12-审查波归档与覆盖对账》）；2026-09-12 收口轮追加实例：App.vue:63 仍写「清 lastAnalysisResult 会话持久化」（该死状态已删）；根基文档回写欠账（isAuthenticated 双判据 / shared 新常量 / constants-audit 未写入，912 尾巴#6）。 |
| p2       | z152-台账与文档治理残余<br>`discover:20260911` | ①施工手册停更 09-04（自述「状态唯一数据源」但落后于 09-05 后事实）；②审查体系 00-附录 396 项全部暂标 C，metrics-tally 门禁只验证占位表自洽（**假绿**），附录缺 §1-3/5-7 与迁移路线图正文；③docs/README 索引残余悬空；④`docs/audits` 落盘规则（00-约定 §4）与实况不符，本批专项2 批次目录已从工作区消失（在回收站）；⑤面试稿与技术文档滞后（a066/RouteAnalysis/cruiser 等已修而文档仍写旧）。 | **预期思路**：①手册补 09-05 后状态或改「以台账为准」；②附录逐条复核回填或把 metrics-tally 降级为「占位检查」标注；③④统一落盘规则并恢复/重建 audits 批次；⑤面试稿随代码变更回填。<br>**实施风险**：②工作量大（396 项）；④落盘位置需用户裁定。<br>**尝试日志**：批次目录内容已由总清点保全（回收站可恢复）；其余未动。 |
| p2       | z153-CSP 强制化待决策<br>`discover:20260911` | 生产 CSP 仍 Report-Only 且**无 report-uri**（等于未在收集违规）；切强制有三步风险（unsafe-inline/unsafe-eval 为 Cesium 必要放开；漏配即生产白屏且回滚需重新部署）。 | **预期思路**：先加 report-uri 收集 1–2 周 → 依报告收紧 connect-src（天地图/高德域名）→ 再切强制。<br>**实施风险**：需业务/运维配合；回滚依赖重新部署。<br>**尝试日志**：待用户拍板（非纯技术改动）。 |
| p3       | z154-死代码/死数据/硬编码残余<br>`discover:20260802` | ①`readFloodArea()` 与 `floodArea.json` 死数据（`modules/flood/repositories/flood.repository.ts:104`；`tools/perf-bench/server-bench.mjs:22` 仍引用）；②城市名两处硬编码（`core/layout/AppLayout.vue:119-121/174-176` 未由 `CITY_CENTERS` 驱动）；③精选版 b023 金额语义文档化待办。 | **预期思路**：①删死方法与死数据并同步 bench 脚本；②改 `Object.keys(MAP_CONFIG.CITY_CENTERS)` 驱动；③b023 标注金额口径（估算/假设）。<br>**实施风险**：①bench 引用需同步改防断链；②注意 CITY_CENTERS（中文 key）与 CityKey（缩写）的有意双轨（桥为 CITY_CENTER_NAMES），勿误改。<br>**尝试日志**：第一版尚未实际尝试。 |
| p2       | z155-DEM/栅格源数据治理（唯一原件单点）<br>`discover:20260912` | DEM 全量原件（filled_CGCS2000_int16 等 4 级处理成果 + 原始 ASTER 6 幅）**仅存用户 Desktop 备援单点**；workspace 副本被清理进程反复删（`flood_engine.py:30-33` 记录）；栅格无服务化（ADR#5 静态分发，GeoServer 待动态需求）——剖面重派生/淹没模拟全依赖该单点。**对应文件**：`backend/algorithm-service/flood_engine.py:_resolve_dem_path`；`tools/dem-pipeline/*`；`backend/data/flood/dem/`（空）。 | **预期思路**：Desktop 原件树归档冷备一份（用户动作）；派生脚本已入 `tools/flood/`（可复跑）；workspace 副本视为缓存不存唯一副本；GeoServer 维持待立项。<br>**实施风险**：Desktop 单点丢失需重下 ASTER（地理空间数据云）；重建管线耗时段。<br>**尝试日志**：20260912 勘误记录在案——曾误用 cut 裁剪版判"贴港无数据"，切全量版后 P3 跑通（R4，见《问题副本-2026-09-12-审查波归档与覆盖对账》§四）。 |
| p3       | z156-VITE_DATA_SOURCE 僵尸变量 5 处<br>`discover:20260911` | 前端数据源切换变量代码已删（main.ts 2026-09-10 阶段 4），`docker-compose.yml:17`、`.env.production`、两份 `.env.example`、`.env.local` 仍在下发（根目录与 frontend 模板值还不同）——env 模板与代码契约漂移的标准案例，新人按 example 配置全然无效。<br>**对应文件**：`docker-compose.yml:17`、`frontend/.env.example:6`、根 `.env.example:7`。 | **预期思路**：全部删除并在 .env.example 加「已废弃勿再引入」注记。<br>**实施风险**：低（纯清理）。<br>**尝试日志**：第一版尚未实际尝试（912 复核 z156；z139 死配置清理的残余尾巴）。 |
| p3       | z157-「属性属于宿主对象」无 CI 检查<br>`discover:20260911` | 幽灵优化本尊（SSE 用 `as unknown as` 挂 scene 而非 scene.globe，四个月未生效，d134 已修）暴露的机制性防线缺失：往宿主对象挂不存在的属性无任何门禁拦截。<br>**对应文件**：`tools/v3-guard/`（可挂新检查）、`.dependency-cruiser.cjs`。 | **预期思路**：guard:v3 增静态断言（或 TS 强类型宿主收口），覆盖 as 强转挂属性模式。<br>**实施风险**：误报需调参；对既有合法 as 模式需白名单。<br>**尝试日志**：第一版尚未实际尝试（912 复核 z157）。 |
| p1       | z158-CI 流水线改造八项（任务清单阶段三）<br>`discover:20260912` | C1 拆 lint-and-build 巨石 job 为三并行 + C8 audit 补缓存；C2 消双跑（删 PR 触发或按 sha）；C3 pre-push 门禁对齐（补 format/lint/stylelint/cruise/types）；C4 激活真库测试（PostGIS service container，**前置=T1-1/T1-2 恒真断言修复，锚点见 z150 注记**）；C6 路径过滤保守默认；C7 "Make package public" 吞错改显式失败；C9 build 仅 main 执行。<br>**对应文件**：`.github/workflows/ci.yml`、`.husky/pre-push`。 | **预期思路**：按清单逐项独立提交验收；C4 首跑可能翻红（历史 pid 符号/SRID 事故正源于此盲区），红了修实现不许 skip 回避。<br>**实施风险**：CI 配置不可本地全验，需 push 观察首轮 run；部署 job 不受路径过滤影响（main 漏测比慢更糟）。<br>**尝试日志**：C5 覆盖率棘轮已落地（coverage-ratchet + baseline，剔除本条）；其余第一版尚未实际尝试（912 复核 z158）。**20260912 落地注记**：C1 拆 job/C2 消双跑/C3 pre-push 对齐/C4 真库激活（seed 夹具，首跑绿）/C6 路径过滤/C8 audit 缓存已由并行批次落地（`65def5ee` + `79a28540` 修 tools 依赖补装）；C7/C9 亦已由同批次落地（2026-09-12 收口轮 ci.yml 逐项核实），C1-C9 八项全闭环，待按原编号移入已解决台账。
| p3       | z160-coverage-ratchet 基线缺 schema 校验<br>`discover:20260912` | 基线 JSON 被手改丢指标键时 `baseline[m] - 0.5` 得 NaN、比较恒 false → 静默放行；`--update` 时 `Math.max(undefined, x)` 得 NaN 写出 null 静默损坏基线。**对应文件**：`scripts/coverage-ratchet.cjs`（检查与更新两段）。 | **预期思路**：读入后校验四键齐全且为 number，缺失即报错退出。<br>**实施风险**：零。<br>**尝试日志**：第一版尚未实际尝试（ZCode 二审发现）。 |
| p3       | z161-roads_noded 夹具 TRUNCATE 无行数护栏<br>`discover:20260912` | `backend/test/seed/roads-noded-fixture.sql` 无条件 `TRUNCATE roads_noded RESTART IDENTITY` 后灌 2 条合成边：「仅测试库、严禁灌 v3_dev」红线只是注释——误对本机 v3_dev（61 万段真路网）执行会先清空真表。**对应文件**：`backend/test/seed/roads-noded-fixture.sql`。 | **预期思路**：TRUNCATE 前加 DO 块断言（existing 行数 > 100 即 RAISE 拒绝）。<br>**实施风险**：零。<br>**尝试日志**：第一版尚未实际尝试（ZCode 二审发现）。 |
| p3       | z162-CesiumRenderer 无效非空断言<br>`discover:20260912` | `CesiumRenderer.ts:1145` `if (renderer.viewer!)` —— 非空断言写在真值判断里等于没写（`!` 无效果且误导读者）；同函数闭包在事件回调里 `renderer.viewer!.scene` 解构，依赖 destroyEvents 时序。**对应文件**：`core/map/renderers/CesiumRenderer.ts:1145` 及同函数闭包。 | **预期思路**：改 `if (renderer.viewer)`；闭包内改局部变量捕获。<br>**实施风险**：零。<br>**尝试日志**：第一版尚未实际尝试（ZCode 二审发现）。 |
| p2       | z163-guard:v3 七项 && 串联断链隐患（待拍板）<br>`discover:20260912` | `package.json:25` guard:v3 用 `&&` 串联七个子检查：任一子检查异常终止（非常规非零退出）会静默断链后续项 → 假绿。巡检自动化轮已建议改 `;` 或 npm-run-all --continue-on-error，等用户点头未改。**对应文件**：`package.json:25`。 | **预期思路**：改 `;` 串联 + 末尾汇总退出码，或 run-all continue-on-error（任一失败仍整体红）。<br>**实施风险**：低——CI/pre-push 失败语义不变。<br>**尝试日志**：待用户拍板（巡检自动化建单报告遗留决策#1）。 |
| p3       | z164-「淹没面积」统计口径全域 vs 陆域（待拍板）<br>`discover:20260912` | 报告/接口 floodArea 为数据全域真值（DB 列），显示几何已经 admin_boundary 陆域裁剪——数字与图不一致；是否统一裁陆域待用户决策（SQL 顺手可加）。**对应文件**：`flood.repository.ts` PICK_LEVEL_SQL。 | **预期思路**：拍板后统一口径（统计列改陆域或前端注明全域口径）。<br>**实施风险**：低；口径变更影响展示一致性需回归。<br>**尝试日志**：待用户拍板（浸没波 T-5 转记）。 |
| p3       | z165-服务器库设施高程回填待执行<br>`discover:20260912` | `tools/db/backfill-facility-elevation.sql`（幂等 UPDATE）本地 v3_dev 已执行 83/83，服务器库待随下次部署执行；未执行前服务器 SQL 侧 elevation 为 NULL（评估链路读 facilityPoints.json 不受影响，影响面=SQL 侧分析）。**对应文件**：`tools/db/backfill-facility-elevation.sql`。 | **预期思路**：下次部署后 `docker exec -i beibu-postgis psql -U postgres -d v3_dev -v ON_ERROR_STOP=1 < tools/db/backfill-facility-elevation.sql`。<br>**实施风险**：幂等可重跑。<br>**尝试日志**：待执行（浸没波 T-3 转记）。 |

---

## 统计（2026-09-12 收口轮对账后）

- **真实 open：53 项**（p0 × 0｜p1 × 8：a091/b034/b040/z071/z072/z146/z158/d140｜p2 × 27｜p3 × 18：a089/a092/a093/d054/d138/d139/d142/z040/z042/z154/z156/z157/z160/z161/z162/z164/z165/b102）。
- **按层级**：地图层 9（a064/a065/a078/a079/a088/a089/a091-a093）｜业务层 8（b034/b040/b045/b065/b089/b098/b101/b102）｜组件层 2（c037/c038）｜后端层 8（d051/d054/d082/d138/d139/d140/d141/d142）｜暂未归类 26（z040/z042/z064/z071/z072/z104/z132/z146-z158/z160-z165）。
- **2026-09-12 修正说明**：a066（layerCatalog key 命名空间）经复核确认已修（图层 key 已带 `site-*`/`flood-dem-hillshade`）→ 按原编号移入已解决问题.md；新增 13 项（a091-a093/b098/z146-z154）来自《问题副本-2026-09-12-全项目问题总清点》，其中 z151（文档/注释漂移）与 z152（台账/文档治理）为跨文档重复报告的**根因级合并**条目。
- **2026-09-12 基准重派生轮追加**：d138/z155 两项自《问题副本-2026-09-12-浸没基准重派生与全项目状态对账》转正（用户裁决）；z148-⑤（剖面重采样）本轮完成并在原条目留尾标注。当日浸没域闭环 8 个根因条目（R1-R8）按副本规则留在《问题副本-2026-09-12-审查波归档与覆盖对账》§四（收口轮原件已归档 .local/trash），不入正式编号。
- **2026-09-12 912 复核轮追加**：a088/a089/d139/z156/z157/z158 六项自《问题副本-2026-09-12-912复核汇总》（.local/agent-docs/912复核-2026-09-12/）转正——含审查修复波残余（图层字面量/可观测性/限流 helper）、架构排查（VITE_DATA_SOURCE）与 CI 改造清单；同轮 3 条闭环（z143 测试 env 解耦/b099 水位口径/b100 业务码收口）入已解决台账 2026-09-12 批次，并对 b034/d082/z150 增加进度注记、对 b095/a087 两处登记偏差做复核更正（详见该副本 §一）。
- **2026-09-12 收口轮追加**：11 项转正——d140（断库进程崩溃 p1，动态测试实锤）/d141（PostGIS 版本三方偏斜）/d142（audit 不拦制品）/b101（登出后恢复期语义被架空）/b102（收藏在途丢弃竞态）/z160（棘轮基线校验）/z161（夹具 TRUNCATE 护栏）/z162（无效非空断言）/z163（guard 串联断链，待拍板）/z164（淹没面积口径，待拍板）/z165（服务器高程回填），编号经两台账查重无冲突；z151 追加 App.vue:63 与根基文档回写两实例；z158 终态注记（C1-C9 全闭环）。同轮 2026-09-11~12 审查波 13 份报告 + 桌面 4 份报告收口删除，唯一归档承载=《问题副本-2026-09-12-审查波归档与覆盖对账》（覆盖对账矩阵见其 §一）。
