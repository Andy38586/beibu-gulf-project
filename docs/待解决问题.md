# 待解决问题（统一 open 项清单 · 2026-08-03 验证版 + 压力面试合并）

> **定位**：当前所有经真实代码取证确认**仍未闭环**的问题。修一个 → 从本文件删除，按**原编号**移入 `已解决问题.md`（状态由"在哪一文件"决定，解决即移走）。
> **编号规则**：按类型分节，四位数。`a`=地图层、`b`=业务层、`c`=组件层、`d`=后端层、`z`=暂未归类。**本文件内部顺序编号**；移入 `已解决问题.md` 时**保留原编号不重排**。
> **时间戳规则**：每条 `问题编号` 下方标注 `discover:YYYYMMDD`；移入 `已解决问题.md` 时补 `solve:YYYYMMDD`。
> **问题等级**：`p0` 致命/高危阻断｜`p1` 高（正确性/稳定性/安全非致命/面试短板）｜`p2` 中（待改进·低危 bug·语义不一致）｜`p3` 低（接受/硬化/文档/视觉 nits）。
> **溯源锚定**：问题源于代码、归属代码；每条以 `file:line` 证据直接锚定项目内位置，便于定位。不再标注问题源自哪份审查/审计文档（旧文档随专项收口会被删除，不可作长期溯源依据）。

---

## 2026-08-03 代码二次验证（溯源收口）

对全部候选 open 项逐条核对 `file:line` 与问题真实性，结果：

- **闭环（移出 open）**：`z033`（静态 fetch 已收口 `loadStatic`）、`b030`（真 DEM 流水线已落地 `tools/dem-pipeline`，`getDEM` 降级为无调用方钩子；残留"三维水面非真实高程演算"叙事并入 `z045`）。
- **降级（保留 open）**：`b025` 原 `Math.random()` 造假已不成立——`forecastEngine.js:115` 改 `seededRandom` 确定性种子、`:105` 顶部诚实标注"示意性合成数据"，欺骗性质疑解除，由 p0 降 p1；残留"算法非历史数据驱动"属 `z045` 叙事。
- **其余 open 项**行号/等级已就地更新（`a019`/`b027`/`b029`/`c021`/`z038`/`z042`/`z046`/`b034`/`b038`/`a027`/`b040`/`z048`/`z064`/`z071`/`z072` 等行号以 2026-08-03 实测为准），问题确认仍成立。

验证后真实 open 计数：**29 项**（二次验证版）。2026-08-03 下午两轮压力面试（面试官视角 + 用户自审）合并新增 **11 项**，并对 4 项同根因 open 项做了合并/升级（详见各条）。合并后真实 open：**31 项**（2026-08-04 复查闭环 9 项：a035/a036/a037/d053/d056/z073/z074/d069/z075）（2026-08-04 复查闭环 8 项：a035/a036/a037/d053/d056/z073/z074/d069）（2026-08-04 复查闭环 7 项：a035/a037/d053/d056/z073/z074/d069）（2026-08-04 复查闭环 6 项：a035/a037/d053/d056/z073/z074，详见已解决问题.md）。

---

## 地图层（图层生命周期 / 地图渲染 / 地图引擎集成 / 坐标系 / 空间数据性能 / 图层状态 / 地图交互）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | a019-Cesium视口裁剪BBox估算不准<br>`discover:20260801` | `CesiumRenderer.ts:406` `_getViewportBBox()` 委托；实际估算在 `CesiumViewportCulling.ts:30-48`（`halfRange = Math.min((height/111000)*1.5, 10)` 圆形/方形估算），非真实视口四角投影；`pitch≠-90` 倾斜视角下边缘 POI 被错误裁剪。OL 侧 a001（rbush 索引）已解决"有无裁剪"，但 Cesium 几何精度未覆盖。（行号以 2026-08-03 实测为准，原 475-494 已漂移） | 改为视口四角经纬度投影求 bbox。`|
| p2 | a020-WebGIS性能未实测+缺优化<br>`discover:20260801` | GeoJSON 大小 / Feature 数量 / 数据量均未实测（`README.md:26` 自承"数据规模有限"）；缺 WebWorker / 矢量瓦片 / 数据切片三大优化手段，优化无据可依。 | 先实测数据量基线，再按需引入 WebWorker / 矢量瓦片。`|
| p2 | a021-site-selection无专属adapter<br>`discover:20260730` | `useSiteAnalysisApi.ts:26` 走统一 `apiRequest` 边界（非直连 fetch），但全项目唯一无专属 adapter 抽象层（forecast/flood 有 `forecastAdapter`/`floodAdapter`）。 | 补 `siteAnalysisAdapter` 与业务 adapter 同构，或明确豁免。`|
| p1 | a027-Cesium缺terrainProvider真地形配置<br>`discover:20260802` | `CesiumRenderer.ts:64` `new Viewer(container, …)` 配置仅 UI 开关，全文 `terrainProvider`/`createWorldTerrain`/`EllipsoidTerrain` **0 命中**——默认 `EllipsoidTerrainProvider`（平滑椭球无高程）。已知降级：注释记录 quantized-mesh 因工具链缺失降级为 `dem_hillshade.png` 贴图（伪三维）。 | 接入真实 terrainProvider；或文档明确标注降级状态与上线前补齐。`|

---

## 业务层（业务逻辑 / Adapter / Store / 业务页特性）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | b025-预测热力图算法非历史数据驱动(诚实标注后降级)<br>`discover:20260801` | `forecastEngine.js:115` 现为 `seededRandom` 确定性种子（原 `Math.random()` 已移除，DISCONNECTED）；`:105` 顶部已诚实标注"示意性合成数据"。算法本质仍为线性外推+情景系数，**非"根据历史数据预测"**。欺骗性质疑解除（p0→p1），但 README:12"根据历史数据预测"叙事冲突属 `z045`。 | 面试诚实解释算法为合成示意；README 叙事改 `z045` 统一处理。`|
| p2 | b027-waterLevel传参姿势分裂<br>`discover:20260801` | `floodAdapter.ts:267`(`params:{waterLevel}`) 用 query 与 `:318`(`body: JSON.stringify({waterLevel})`) 用 body，同语义字段两套序列化/解析，前后端易漂移。（行号 2026-08-03 实测，原 198/202/244 已漂移） | 统一为 query 或 body 其一。`|
| p2 | b029-flood mock不响应水位(尾巴)<br>`discover:20260801` | `floodAdapter.ts:234-237` 仍 `mock 模式不响应水位参数（请求 ${waterLevel}m，固定返回 2.5m 档位）`（已加 warn 日志，诚实化≠修复）。（行号 2026-08-03 实测，原 256-259 已漂移） | mock 模式接真实水位函数或 UI 明确禁用滑块。`|
| p1 | b034-前后端类型同步/职责边界<br>`discover:20260802` | 无 OpenAPI/契约测试；zod 已在 `package.json:37` + `types/schemas.ts`/`useApiRequest.ts`/`loadStatic.ts` 落地为**前端**校验源，但**无 OpenAPI 文件、无契约测试**，前后端仍非单一事实源；adapter 与 service 职责边界未文档化，业务规则是否前后端重复计算未界定。 | 中期补 OpenAPI + 契约测试；明确业务规则后端为权威。`|
| p1 | b038-mapStore职责过载(465行跨6域+双机制+单槽回调)<br>`discover:20260802` | `mapStore.ts` 实测 **465 行**（原记 410，恶化），55 个顶层成员，跨 layerCatalog(:92)/analysisHandler(:98)/setLayerVisible(:377) 等 6 域。"万能 store"反模式加剧。衍生两处活债务：①**双图层控制路径共存**——旧 `toggleLayer`(:286) 与新 `BusinessLayerManager.setVisible` 并存，`:342-346` LIF-5 防御告警"新机制图层应通过 BLM 控制而非 toggleLayer"（迁移中）；②**`analysisHandler` 单槽回调**（:98 `ref(null)`、:134 覆盖式赋值、:153 单点调用），多组件订阅时后者覆盖前者，非发布订阅。 | 按业务域拆分：mapEngineStore/mapInteractionStore/analysisResultStore；双机制择一收敛（旧 toggleLayer 随迁移停用）；analysisHandler 改订阅数组或事件发射器。`|
| p1 | b040-useAuth/useApiRequest模块级业务状态<br>`discover:20260802` | `useAuth.ts:52` `user`(ref) 与 `useApiRequest.ts:27` `token`(ref) 均为模块顶层单例可变变量（行号 2026-08-03 实测，原 48-55/24 微漂移）。核心业务状态应进 authStore。 | 创建 authStore 容纳 user/token/isAuthenticated；模块级仅保留纯函数。`|
| p1 | b044-site-selection算法瓶颈(逐POI buffer+全量union)<br>`discover:20260803` | `siteAnalysisService.js:87-103` 每 POI 一 `turf.buffer` + 全量 `turf.union`（372 点场景）；`:44-47` 仅 `logger.debug('建议实现聚类或空间索引')` 代替实际优化。最大算法瓶颈，O(n²) 级。 | 引入 rbush 空间索引 / 聚类后 buffer；或网格化批量 union。`|
| p2 | b045-三业务模块结构不一致<br>`discover:20260803` | flood=有 `constants/` 目录 + 无 composables；forecast=有 `constants.ts` + 有 composables；site-selection=无 `constants`。同层三模块目录结构不统一，新增模块无统一模板。 | 抽公共业务模块骨架（constants + composables + adapter 同构），新模块按模板。`|

---

## 组件层（UI 组件 / CSS / 面板 / 视图组件 / 前端打包）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | c021-登录态UI未反映<br>`discover:20260801` | nav 配置已迁至 `App.vue:81`（`{ label:'个人中心', … disabled:false }` 静态数组，原 `BottomNavBar.vue:55-62` 已重构），恒显未随 `useAuth.user` 变化。（行号 2026-08-03 实测）⚠️ 复核：属"默认开放、动作层软校验"鉴权取向的体验取舍，非逻辑缺陷。 | 绑定 `user`，未登录隐藏或显"登录"（可选优化）。`|

---

## 后端层（后端代码 / API / 认证 / 服务 / 部署配置）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | d035-无HTTPS/HTTP2<br>`discover:20260730` | `nginx.conf:3` `listen 80` 无 `listen 443`/`ssl_certificate`，auth cookie 明文、安全头缺失；brotli 缺失属 p3。⚠️ 复核：`docker-entrypoint.sh` 已有条件 HTTPS（检测证书生成 https.conf），默认无证书仍走 80。用户决策：无证书暂缓。 | 配证书后 entrypoint 自动启用 TLS/HTTP2。`|
| p3 | d039-API无版本前缀<br>`discover:20260730` | `backend/app.js:123-129` 9 个路由全部裸 `/api/*`（无 `/api/v1/`）。用户决策：不加（单版本无演进需求）。 | 接受 / 暂缓。`|
| p2 | d051-缺统一入参校验层<br>`discover:20260801` | `backend/middleware/` 仅 `auth.js`/`logSanitizer.js`，**无 `validate.js`**；`siteAnalysisController.js:35` 手写校验，参数校验散落各 controller（均走 `BusinessError`+全局中间件，无裸 `res.status(400)`）。 | 建统一 `validate` 中间件。`|
| p3 | d054-公开路由白名单兜底确认<br>`discover:20260801` | `backend/routes/*.js` 全无 `sanitize`/`whitelist`/`validateQuery`；flood 侧 GET 参数遍历风险待统一确认（`validateWaterLevel` 有校验，其他 GET 遍历需排查）。 | 全公开路由统一输入白名单。`|
| p3 | d055-认证恢复逻辑微妙(尾巴)<br>`discover:20260801` | `useAuth.ts:97` 占位 token 启用 `isAuthenticated`；`restoreAuth` 始终调 `/auth/me`。⚠️ 复核：d038 注释明确"Cookie 为权威，成功后 setToken"——有意设计，p3 恰当。 | 边界用例补充测试（可选）。`|

---

## 暂未归类（文档 / 工程质量 / 测试覆盖 / 类型安全 / 架构守护 / 杂项）

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p2 | z038-mapStore关键链路单测覆盖待确认(尾巴)<br>`discover:20260801` | `mapStore.test.ts:131` 已补 `registerAnalysisHandler / setAnalysisResult` 覆盖；但 `setLayerVisible`（`mapStore.ts:377`）仍 0 覆盖。`reapplyAll` 在 `BusinessLayerManager.test.ts:147` 已有测试。 | 补 `mapStore.test.ts` 对 `setLayerVisible` 的覆盖即可。`|
| p3 | z040-领先版本过激<br>`discover:20260801` | `package.json:64` `"vite":"^8.0.16"`、`:60` `"typescript":"^6.0.3"`、`:68` `"vue-tsc":"^3.3.8"` 全为大版本，unplugin*/vite-plugin-cesium 磨合风险，@vueuse 已现 `INVALID_ANNOTATION` 警告。 | 谨慎升级，锁版本。`|
| p3 | z041-profileStore vs useAuth.user混淆<br>`discover:20260801` | `profileStore.ts`（选址方案选中态）与 `useAuth.ts:52` `user`（登录态）命名相近、职责不同，新人易混。 | 重命名 / 加注释区分。`|
| p3 | z042-函数过长(指标恶化)<br>`discover:20260801` | `floodAdapter.ts` 实测 **343 行**（原 264，恶化）、`CesiumRenderer.ts` **717 行**、`OLRenderer.ts` **732 行**，可读差。 | 拆分。`|
| p3 | z043-文档与代码同步漂移+维护风险<br>`discover:20260801` | `docs/开工前必读/项目根基.md` **1230 行**（精确），与代码同步成本极高，半年必漂移；改 Renderer / GCS / DEM 维护风险极高。 | 减负 / 文档拆分。`|
| p0 | z045-简历/README/代码叙事冲突(致命)<br>`discover:20260801` | 多处叙事与代码正面冲突：①`README.md:12`"根据历史数据预测"、`:13`"基于水位与地形的三维淹没模拟（Cesium 3D）" vs `forecastEngine.js:105`"示意性合成数据"、`floodAdapter.ts:333`"三维水面为预设水位档位可视化（非真实高程演算）"；②**`CesiumLayerRegistrar.ts:~276` 自承"降级为山体阴影贴图…贴在椭球面上…无真 z 值起伏（伪三维，非数字孪生级）"**——与简历"数字孪生"直接冲突；③**`HomePage.vue:38-56` 首页吞吐量图表写死假数组（钦州/北海/防城港 2019-2024，无 API 调用）**，展示层把 mock 当真实数据呈现；④README:165 自述"学习性质个人项目"却用于面试。面试官可当场拆穿。 | 改叙事：mock/模拟实现如实说明；删"预测/三维淹没/数字孪生"暗示；首页图表接真实接口或明确标注 mock；严禁写 PostGIS / GeoServer / 大规模空间分析。`|
| p2 | z046-核心renderer仍@ts-nocheck+313处类型逃生<br>`discover:20260801` | `CesiumRenderer.ts`/`OLRenderer.ts` **第 3 行** `// @ts-nocheck`（D-6 渐进迁移），`OLRenderer.ts:5` 记录"移除后 171 个 typecheck 错误"。全仓约 **318 处**类型逃生（`@ts-nocheck`/`:any`/`as any`/`@ts-ignore`，移除 `@ts-nocheck` 后实测 OLRenderer 171 + CesiumRenderer 147 个 typecheck 错误），类型安全靠排除两文件，`npm run typecheck` 现 0 错非真正覆盖。 | 渐进补类型后移除 `@ts-nocheck`；优先清两渲染器 + 高频 `any` 热点。`|
| p1 | z072-错误上报SDK未接入<br>`discover:20260802` | `logger.ts:21` `addLogTransport` 钩子预留；`main.ts:74` 仅注释"logger 已预留 addLogTransport 钩子…" + `:77-82` 全为 `// import * as Sentry from '@sentry/vue'` 注释，**SDK 未实际接入**。线上错误仍只落本地 console。（行号 2026-08-03 实测，原 main.ts:71 已漂移） | 引入 Sentry SDK，main.ts 初始化 + errorHandler 调用上报；带 release/version/environment。`|
| p1 | z048-链路关键节点缺乏观测点<br>`discover:20260802` | `useApiRequest.ts` 全文 `logger.` **0 命中**（219 行），无请求/响应日志（仅错误时记录）；floodAdapter 有 `logger.debug/warn` 但仅业务分支提示，无字段转换日志。故障时无法定位"数据在哪一跳丢失"。 | useApiRequest 增 dev 请求/响应日志；adapter 转换函数增 dev 转换日志。`|
| p2 | z064-无集成/E2E测试<br>`discover:20260802` | `package.json:10` `"test":"cd frontend && vitest run"`，仅单元测试；`cypress`/`playwright` **0 命中**，`frontend/e2e`/`cypress`/`tests/e2e` 目录均不存在。核心业务流程无跨模块流程测试，回归靠人工。 | 引入 Playwright 覆盖核心业务流程 E2E；或先补集成测试（API 层串联）。`|
| p1 | z071-无路由守卫做store重置<br>`discover:20260802` | `router/index.ts` `beforeEach`/`afterEach`/`beforeResolve` **0 命中**。路由切换时无显式 store 重置（mapStore/forecastState 不在 resetBusinessStores 中）；无全局请求取消（useSiteAnalysisApi 无 AbortController）。 | 添加 `router.beforeEach` 守卫，路由切换时重置非持久化 store、取消在途请求。`|
| p3 | z076-引擎切换强制pitch=-90(已知限制)<br>`discover:20260803` | `CesiumRenderer.ts:570` 引擎切换时 `const pitch = -90` 硬编码（注释：OL 无 pitch 概念），切换后丢失倾斜视角状态。已知实现取舍，面试需能解释。 | 文档标注限制；或切换时暂存/恢复 pitch 状态。`|
| p3 | z077-eslint 72 warning未清零(后端no-console为主)<br>`discover:20260803` | 全仓 `eslint .` 共 **72 warning / 0 error**（backend 50 + frontend 11 + root 级 config 11），绝大多数为 `no-console`（backend 日志未接 logger）。 | 后端日志统一走 `logger`；或 eslint 配置对 backend 放宽 `no-console`。`|

---

## 风险观察（非 bug · 面试 / 架构叙事）

- **Renderer 抽象 / GCS 布局过度设计嫌疑**：`MapRenderer` 277 + `OLRenderer` 607 + `CesiumRenderer` 1064 ≈ 1948 行仅服务于 2D↔3D 切换；GCS `useGCS.ts` 225 行 + 组件仅 4 业务页。面试需准备 ROI 辩护（对应面试报告架构章）。
- **依赖安全审计本机不可执行**：`npm audit` 在 npmmirror 镜像返回 `NOT_IMPLEMENTED`（d042 已记环境限制），上线前须切官方源跑。
- **性能优化无基线**：a020 已列，此处重申——没有数据量基线，任何"优化"都是空话。
- **压力面试暴露的根因聚类（2026-08-03）**：①凭据/AI痕迹类（`d056`+`d053`、`z073`）→ 安全与可信度；②伪三维/叙事类（`z045`+`a027`+`a037`）→ 简历诚信；③双渲染器类型与架构类（`z046`+`a036`+`b038`）→ 工程分；④性能资产类（`a035`+`a020`）→ 面试演示稳定性。

---

## 附：open 计数与状态（2026-08-03 验证版 + 压力面试合并）

### 总计

- **真实 open：40 项**（2026-08-03 代码二次验证闭环 `z033`/`b030`、`b025` 降级后 29 项；下午两轮压力面试合并新增 11 项并对 4 项同根因 open 项合并/升级）。
- **按等级**：p0 × 1（z045）｜p1 × 12（a019 / a027 / b025 / b034 / b038 / b040 / b044 / c021 / d035 / z048 / z071 / z072）｜p2 × 9（a020 / a021 / b027 / b029 / b045 / d051 / z038 / z046 / z064）｜p3 × 9（d039 / d054 / d055 / z040 / z041 / z042 / z043 / z076 / z077）。
- **按层级**：地图层 4（a019/a020/a021/a027）｜业务层 8（b025/b027/b029/b034/b038/b040/b044/b045）｜组件层 1（c021）｜后端层 5（d035/d039/d051/d054/d055）｜暂未归类 13（z038/z040/z041/z042/z043/z045/z046/z048/z064/z071/z072/z076/z077）。

### 已闭环（本次代码验证 · 2026-08-03）

> 以下项经代码二次验证确认问题已不再成立，从 open 移除；证据附后。如需归档请按原编号移入 `已解决问题.md`（补 `solve:20260803`）。

| 原编号 | 原等级 | 原 claim | 验证结论（证据） | 处置 |
|:---:|:---:|---|---|---|
| z033 | p2 | mapDataService.ts:28 / useBoundaryLayer.ts:49 直接 fetch 绕过 adapter | `mapDataService.ts:8,23` 已 `import { loadStatic }` + `loadStatic<unknown>(url)`；`useBoundaryLayer.ts:54` `// z032: 静态资源 fetch 收口 loadStatic`；两文件**无裸 fetch**（z032 已闭环）。DISCONNECTED | 移出 open（与 z032 重复闭环） |
| b030 | p2 | getDEM/三维淹没仍 mock | `tools/dem-pipeline/01~05` 真 DEM 流水线已落地；`floodAdapter.ts:330-336` `getDEM` 降级为"无调用方钩子"；`:333` 自承"三维水面为预设水位档位可视化（非真实高程演算）"→ 属 `z045` 叙事。DISCONNECTED | 移出 open；残留叙事并入 z045 |
| b025 | p0→p1 | forecastEngine.js:101-140 用 Math.random 造假 | `forecastEngine.js:115` `seededRandom` 确定性种子（Math.random 已移除）；`:105` 顶部诚实标注"示意性合成数据"。欺骗性质疑解除，降级 p1；算法非历史数据驱动属 z045 叙事。DISCONNECTED 局部 | 保留 open，降级 p1 + 改写描述 |

### 压力面试合并说明（2026-08-03 下午）

> 两轮压力面试（面试官视角报告 + 用户自审）暴露的问题，按"同根因直接合并"原则并入本清单：

- **合并进已有 open 项（不新增编号）**：`d056`（升 p3→p0，并入天地图密钥历史泄露）、`z045`（并入伪三维自承 `CesiumLayerRegistrar.ts:~276` + 首页假数据 `HomePage.vue:38-56`）、`z046`（并入 313 处类型逃生，升 p3→p2）、`b038`（并入双机制 `mapStore.ts:342-346` + 单槽 `analysisHandler`）。
- **已闭环不再新增**：`getDEM` 死代码（`floodAdapter.ts:330`）属 `b030` 已闭环范围（预留钩子无调用方）；GCS 拆 `core/layout`+`shared/layout` 为 `z054` 重构后既定布局（设计取舍，非缺陷）；`waterLevelStore` 仅 2 字段为合理 Pinia 模式（非缺陷）。
- **新增 11 项**（a035/a036/a037、b044/b045、d069、z073/z074/z075/z076/z077），编号接续全局最大空闲号，未与 `已解决问题.md` 碰撞。

### 编号体系与文档关系

- 本文件为唯一 open 清单，编号体系贯通（接续主清单 + 已解决最大编号）。
- 原有 open 项除 `b030`/`z033` 经本次验证闭环外，其余保留并就地更新行号/等级。
- 已闭环项按原编号移入 `已解决问题.md`（补 `solve:20260803`）。
