# 待解决问题

> **定位**：当前所有已知但尚未修复的**真实问题（bug / 待改进项）**。修一个删一个。
> **最后更新**：2026-07-28（整合 `审核报告-文档出入与重构进步`、`深度代码复核报告`、`问题清单复核总览` 三份源文档 + 既有双轨内容；对照 `开工前必读` 设计文档迁出有意设计项；**本轮再整合 `docs/reports/`（TS迁移进度、中高级问题收口）两报告：E32 确认已修复迁出、E10/E06 部分修复、E05 补 TS 进度、新增 E33**；**本轮再整合 `docs/分步审查/`（7 份深度审查 v2：前端架构 / TS_REVIEW / TS类型安全 / Vue工程 / WebGIS专项 / 工程化审查整合 / 面试官复核）：迁入新发现 E34/E35/E36/E37、修正 E05 的 vue-tsc 误测、登记 D06 状态冲突（详见第八节 §3）**）
> **重要更正（本轮）**：经对照 `开工前必读/项目根基.md` 等设计文档，A01/A02/A03/A04/E01/E16 均属**有意设计 / 已知限制 / 已知取舍 / 文档化行为**，并非 bug。设计文档甚至专立「反模式 6：把刻意简化当 bug 修」（`项目根基.md:1170`）警告此事。这些项已迁出 bug 表，归入「零、设计意图与已知约束（非缺陷）」。
> **说明**：
> - 本文件覆盖各源文档的全部问题项；按你指示执行 diff 收口：已覆盖源文档（`审核报告-文档出入与重构进步`、`问题清单复核总览`、`docs/reports/*`）已删除；未跟踪的新审查（`docs/分步审查/*`、`首屏分包分析.md`）已隔离至 `docs/_trash-2026-07-28/` 待你最终确认后清除。
> - 标 `OPEN` = 代码验证仍为未修复；标 `部分` = 已部分修复但未闭环；标 `待确认` = 源文档与当日早段自述存在状态冲突，需你核对代码实际。
> - 设计意图 / 已知约束 / 刻意简化（A01、A02、A03、A04、E01、E16、S01–S06）既不属 bug 也不属已修复，单列于「零节 / 第六节」，不计入待解决。
> - 已被代码验证确认修复的项见 `已解决问题.md`（含本轮从本文件迁出的 E28 / F01 / B05 / B07 / B08 / B11 / B13 / useFloodStore 残留 / D07）。
> **关联**：已修复问题见 `已解决问题.md`；API 稳定契约见 `开工前必读/API契约文档.md`；设计意图见 `开工前必读/项目根基.md`。

---

## 零、设计意图与已知约束（非缺陷，来自 `开工前必读`）

> 本节条目来自 `开工前必读/项目根基.md` 等设计文档明确声明的「不变规则 / 已知限制 / 已知取舍 / 文档化行为」。它们**不是 bug**，不应当作待修复项。保留在此仅作「面试解释 / 未来规划」参考，不计入待解决。

| # | 设计意图（原文定位） | 证据 | 原误列位置 |
|---|------|------|-----------|
| A01 | AppLayout 顶部城市按钮（钦州/北海/防城港）是固定结构，不在 slot 覆盖范围内；这是「当前限制」 | `项目根基.md:918`、`:996` 主动指向「待解决 A01」作为已知限制 | 原第一节架构问题 |
| A02 | 核心组件职责集中（如 mapStore 累积 analysisHandler 职责）是「已知的设计取舍」 | `项目根基.md:568`「已知的设计取舍（详见待解决 A02/A04）」 | 原第一节架构问题 |
| A03 | 双引擎是**路由级切换**（非同页面多引擎共存）；单页双引擎属远景目标，当前非设计 | `项目根基.md:33` 项目类型即「2D/3D 双引擎，路由级切换」 | 原第一节架构问题（原标设计取舍） |
| A04 | API 调用多方式并存是「已知的设计取舍」；且 `API契约文档.md` §1.3 记录 RESTful/GCS/混合三套响应格式是各子系统的有意约定 | `项目根基.md:568`、`API契约文档.md:55-57` | 原第一节架构问题 |
| E01 | `MapRenderer` 基类声明 3D-only 可选方法，OLRenderer 不实现、调用返回 `false + DEV warn` 是**文档化行为**（能力矩阵见 `项目根基.md:374-383`） | `项目根基.md:368`、`:1096`（CesiumRenderer 可不实现，返回 undefined + DEV warn） | 原第五节审计新增 |
| E16 | Cesium `Viewer` 单例、离开 3D 仅 `v-show` 隐藏不销毁，是「任何设备都单例，低端设备接受两个引擎同时占用内存的性能取舍」 | `项目根基.md:454` | 原第五节审计新增 |

> 注：`D06`（引擎切换丢数据）与 `D08`（三套响应格式）仍保留在待解决——因其存在**真实 bug 残留**（register 幂等守卫无 reapply、缺共享响应类型），但设计文档亦承认「切换不序列化图层数据」（`项目根基.md:457`）、「三格式是子系统有意约定」（`API契约文档.md` §1.3），属「部分有意设计 + 部分待修」。

## 一、架构问题

> A01 / A02 / A03 / A04 经对照 `开工前必读` 设计文档，确认属有意设计 / 已知限制 / 已知取舍，**已迁出至「零节」**，不计入本节的 bug。

| # | 问题 | 影响 | 代码验证（2026-07-28） |
|---|------|------|------|
| A05 | visualization 反向依赖 business（`useRadarChart → facilityConfig`） | 违反分层约束 §2.3 Rule 3，影响组件独立性 | ✅ OPEN：`useRadarChart.js:19`、`RadarScoreTooltip.vue:11` 仍 `import ... from '@/business/...'` |

## 二、后端安全与正确性

| # | 优先级 | 问题 | 文件 | 代码验证 |
|---|:-----:|------|------|----------|
| B01 | **P0** | `GET /api/markers/:id` 无归属校验（IDOR：任何已登录用户都能读取他人标注） | `controllers/markersController.js` | ✅ OPEN：`:18` `findById(req.params.id)` 后直接返回，未校验 `owner === req.user.id`（对比 `:60/:82` 的 update/delete 已校验） |
| B02 | **P1** | 注册 TOCTOU 竞态 → 重复用户名 | `controllers/authController.js` | OPEN（未验证修复） |
| B09 | P2 | ProfilePage `loadPlans()` 双触发（watch + onMounted 各触发一次） | `views/ProfilePage.vue` | OPEN |
| B10 | P2 | 浸没范围"向上取档"语义未在前端 UI 明示（请求 2.5m 实际返回 3.0m，非就近匹配） | `controllers/floodAnalysisController.js` | OPEN |
| B12 | P3 | scoring `Math.min` POI 量大时栈溢出 | `services/scoringService.js` | OPEN |

## 三、预测分析模块

| # | 问题 | 影响 | 备注 |
|---|------|------|------|
| C01 | 热力图色带未参数化（`addHeatmapLayer` 硬编码色带，业务层未传 `options.colorScale`） | 渲染器不应含业务语义 | 技术审计 C-03 / 技术债务 #11；预测诊断未覆盖此项 |
| C02 | 数据流断裂：`ForecastPanel.currentValue` 读 `dataCache` 但无写入 | 需重设计 | OPEN |
| C03 | `registerToggleable` 闭包累积：每次更新注册导致旧回调堆积 | 应只注册一次或先 remove | OPEN |
| C04 | 播放竞态 / 动画与图层同步 | 参考 `analysisSeq` 模式 | ⚠️ 已部分缓解：`ForecastControlPanel` 由 `setInterval` 推进 `currentTime` → 经 watch 触发图层更新（预测诊断 #1c）；**需运行时确认** |

## 四、工程实践

| # | 优先级 | 问题 | 代码验证 |
|---|:-----:|------|----------|
| D01 | **P0** | **测试覆盖率极低**（非"零测试"：已有 `spatialIndex`/`MapRenderer.interface`/`useChartBase`/`UnifiedMap` 4 个测试文件；但核心业务逻辑/Store/API 零测试） | 代码验证：4 测试文件存在；业务逻辑/Store/API 无测试 → 面试最大短板（代码质量评估 L2 复核：4/94 源文件比） |
| D03 | P1 | 状态管理重复（`siteSelectionState` 已用工厂；`floodState` 仍手写 `persisted*` ref，未泛型化/工厂化） | ✅ OPEN：`floodState.ts:37-39` 手写 `persistedWaterLevel/AffectedFacilities/TotalLoss` |
| D06 | P1 | **业务图层数据生命周期缺陷（确认 bug，含两层）**：① `BusinessLayerManager.register` 幂等守卫（`BusinessLayerManager.js:68-69` warn+return，无重应用路径），FloodAnalysisPage 切回 3D 时 `register` 被拒 → 洪水图层丢失；② 更深层：`register` 仅存 `{layerType, options}`，**`data` 从未持久化**，`updateData` 第 118 行 `if (!catalogEntry || !catalogEntry.visible) return` 在图层隐藏时直接丢弃新数据 → `setVisible(true)` 只调 `setVisibility` 但图层未创建且无数据可重建 → 空白；隐藏期数据刷新被吞 → 切回显示旧图 | ✅ OPEN：`BusinessLayerManager.js:66-88,100-127,145`（`_registry` 不存 data / `updateData` 隐藏 early-return / `setVisible` 直改 visible）。**设计意图注**：`项目根基.md:457` 承认引擎切换「图层数据本身不序列化」为有意限制，故本项属「部分有意设计 + 部分真实 bug（register 幂等守卫无 reapply，用户早段已重建 `reapplyAll`）」 |
| D08 | P1 | 三套响应格式并存（RESTful / GCS / 混合），新增接口应统一使用 RESTful | 设计权衡：`API契约文档.md` §1.3 记录三格式是各子系统的有意约定（RESTful 用 `res.ok`、GCS/混合用 `data.code===200`）；缺共享响应类型/契约属待改进项（中风险） |
| D09 | P2 | `layerCatalog` 用 `ref` 存数组（应 `shallowRef`，50 个图层注册触发 50 次重渲染） | ✅ OPEN：`mapStore.ts:83` 仍为 `ref([])`（同文件 `currentRenderer` 已用 `shallowRef`） |
| D11 | P2 | API 无版本号前缀 `/api/v1/` | OPEN（前端 `API_BASE` + 后端 `routes/*`） |
| E12 | P1 | `ErrorBoundary` 仅包 `RouterView`，地图渲染崩溃可能白屏（需确认 `UnifiedMap` 是否在 `ErrorBoundary` 内） | ⚠️ 低置信：`App.vue:107-111` 包 RouterView；**待确认覆盖边界**（主理人当日早段自述已包地图，源文档审核报告称未包，状态冲突见第八节） |

## 五、审计 / 面试评审新增问题（代码验证 2026-07-28）

> 以下条目来自 `技术审计报告`/`架构评审报告`/`改进清单-审计复核`/`面试准备`/`面试官独立压力测试评审`，已与最新代码交叉验证（独立源码核实见 `深度代码复核报告`）。
> 注：`E01`（胖接口返回 false）、`E16`（Cesium 不销毁）经对照 `开工前必读` 设计文档，确认分别为文档化行为 / 有意性能取舍，**已迁出至「零节」**，不计入本节 bug。

| # | 优先级 | 问题 | 代码验证（证据） |
|---|:-----:|------|------|
| E02 | P1 | **mapStore 双图层注册机制（迁移未完成债）**：`registerToggleable`（旧，把函数存进 Pinia state，反模式）与 `registerBusinessLayer`（新，只存元数据）并存；且 `handleBusinessLayerToggle` 访问 `entry.show!`，对后者为 `undefined` → 一旦被调用即抛 TypeError（当前被 LayerControlPanel 绕开，属潜在崩溃） | ✅ OPEN（已降级紧迫性）：`mapStore.ts:146-305`（含 `:197` 旧 / `:240` 新 / `handleBusinessLayerToggle`）；深度复核确认 `LayerControlPanel.vue:85` 绕开，当前不触发，但隐患代码仍在 |
| E03 | **P0** | **README 与代码脱节**：README 仍为"选址 + Express + JWT + 方案管理"，未提 forecast/flood 模块、mock 适配器、GCS 布局系统 | ✅ OPEN：`README.md:1-40`（标题"港口选址分析平台"，技术栈列 JWT/oxlint）；用户指示 README 待项目做完、上服务器前再重写 |
| E04 | **P0** | **mock/api 语义混乱 + api 分支空壳**：① `setDataSource('api')` 任何方法直接 `throw '真实 API 尚未接入'`；② flood 的 "mock" 分支 `getFloodAnalysis`/`getImpactAssessment` 实际 `apiRequest('/gcs/flood-areas?...')`、`apiRequest('/gcs/analysis/disaster')` —— 名为 mock 实为真实后端（与 forecastAdapter 读静态 JSON 的 mock 不一致）；③ `forecastAdapter` 的 `api` 分支同样全 `throw`。架构宣称「mock→api 零改动切换」在主 3D 模块未落地 | ✅ **部分修复**（2026-07-28 重建：floodAdapter mock 改读 `/data/*.json` 静态 fixture，E04② 已解；**api 分支仍 throw 属设计预留，未闭环**）：`forecastAdapter.ts:50,57`；原 `floodAdapter.ts:84,114,138,145` 已改 |
| E05 | 高 | **类型安全整体偏弱（两层）**：① `tsconfig.app.json` `checkJs:false` → 28 个 `.js`（含 OLRenderer/CesiumRenderer/MapRenderer）不受类型检查；② `eslint.config.js` 仅把 TS 当解析器，**未启用 `@typescript-eslint` 规则集**（无 `no-explicit-any` 门禁）。TS 迁移只保证「能编译」而非「类型安全」 | ✅ OPEN（部分推进）：`tsconfig.app.json:19` `checkJs:false`、`eslint.config.js` 未装 `@typescript-eslint`；据 `reports/TS迁移进度-2026-07-28.md`：逻辑文件口径 53%、剩 28 个 `.js`（全在类型网外）、已 typecheck 的 `.ts` 中约 9 处真实 `any`、含 unchecked `.js` 全仓约 53 any-like token；`reports/中高级问题收口` #2 已把 `useForecastRequest.js` rename 为 `.ts` 纳入检查（引用 0 改）。⚠️ **修正（TS_REVIEW 实测）**：`vue-tsc --noEmit` 实际 **138 错误 / 126 处隐式 any**（TS7006/7005/TS7053/TS7034，集中在 `.vue` SFC `<script setup>` 与测试文件，pre-existing 非本次改动引入）；此前「EXIT 0 / 全绿」为 v1 误测，已据 `分步审查/TS_REVIEW.md`（18:57）实测撤回。真问题优先级：先清零 138 错误（含 126 隐式 any）＞ 收编 21 个非保护 `.js` ＞ adapter 收口 ＞ `unknown` 收窄（见 E36/E37） |
| E06 | 高 | **CRS 运行时未强制业务边界**：`assertCRS` 已在 `OLRenderer`/`useApiRequest` 调用，但 `isInBeibuGulf`（北部湾 bbox）零调用，越界数据不拦截 | ✅ **部分修复**（`reports/中高级问题收口` #3）：`assertCRS` 经保守包装 `assertCrsSafe`/`validateResponseCrs` 接入 `OLRenderer` 4 处 + `useApiRequest` 返回处 1 处（`prod` 静默吞错、仅 `dev` 告警，绝不中断渲染/请求）；但 `isInBeibuGulf` 北部湾 bbox 仍零调用 → 越界数据不拦截，未完全闭环 |
| E07 | 中 | **floodState 未用持久化工厂**：仍手写 `persisted*` ref（`siteSelectionState` 已用 `createPersistedState`） | ✅ OPEN：`floodState.ts:37-39`；改进清单 #7（属 D03 子集）；`reports/中高级问题收口` #7 亦列为剩余待办（你此前故意保留，需确认是否接入工厂） |
| E08 | 中 | **魔法数字散落**：多处裸 `10000`(超时)/`24*60*60*1000`(缓存)，未抽常量模块（Cesium `IDLE_DESTROY_DELAY` 已集中，其余未集中） | ✅ OPEN：`mapDataService`/`floodAdapter`/`useBoundaryLayer`/`UnifiedMap.vue`；改进清单 #9；`reports/中高级问题收口` #9 细化为 `1000`(裁剪阈值)/`15s`(超时)/`bcrypt 10`/`JWT 7d` 抽 config |
| E09 | 低 | **裁剪集成路径无测试**：`OLRenderer` `_addCulledPointLayer`/`_refreshCulledLayer`/`moveend` 增量刷新无单测 | OPEN；改进清单 #10；`reports/中高级问题收口` #10 细化为「前端裁剪路径（>1000 features）补测试」 |
| E10 | 低 | **Controller 层零测试**：`backend/controllers/*` 无 test（forecastEngine/forecastService 已有） | ✅ **部分修复**：`reports/midhigh-cleanup-2026-07-28.md` 已补 `forecastController` 13 用例（全绿，主理人复跑 122 测试全过）；其余 controller 仍缺测。改进清单 #11 |
| E11 | 待验证 | **依赖安全未验证**：`npm audit` 在本机镜像返回 `NOT_IMPLEMENTED`，既不能证有也不能证无 | 需切官方 registry 验证；改进清单 #15 |
| E13 | 真实 bug | **SiteSelectionPage 直调渲染器 + 图层泄漏 + pointer-events 缺失**：① `renderer.removeLayer` 绕过 BusinessLayerManager（`:122/:156`）② `onUnmounted`（`:304`）仅 `stopBreathing`+清 timer，**未移除 `activeFacilityLayerKey` 图层** → 泄漏 ③ 全文件仅 `pointer-events:none`（`:375`），无 `:deep(.gcs-panel){pointer-events:auto}` → 面板不可交互 | ✅ OPEN：架构评审 #4 代码验证确认 |
| E14 | **P0** | **路由守卫鉴权依赖内存 token，与 HttpOnly Cookie 主通道脱节（功能级阻断）**：`isAuthenticated` 只看内存 `token.value !== ''`；`restoreAuth()`（设占位 token）在 `App.vue` `onMounted` 才跑 → 直接刷新/深链访问 `/profile`、`/site-selection`、`/flood-analysis` 等受保护路由时 `beforeEach` 先执行、`token` 仍空 → 被弹回登录页，即便 Cookie 有效；且 `restoreAuth` 有 `authRestored` 一次性保护，首次 `/auth/me` 失败即永不重试 | ✅ OPEN：`router/index.js:46-56`、`useAuth.ts:56-106`、`App.vue:96-100`；深度复核 F1 确认代码守卫与 `requiresAuth` **仍在**（与审核报告"代码已全公开"断言相反） |
| E15 | 高 | **useAuth 在组合式函数内无条件挂生命周期钩子（与 E14 同源）**：`useAuth()` 体内直接 `onMounted`/`onUnmounted`，在 `beforeEach` 守卫（无活跃组件实例）中调用会触发 Vue dev 警告且 `onMounted` 回调被忽略 → 多标签页 `storage` 同步监听永不注册 | ✅ OPEN：`useAuth.ts:197-210`、`router/index.js:48` |
| E17 | 高 | **Cesium 点图层 entity id 重复 `-undefined` 崩溃**：id 模板 `` `${id}-${item.id||item.name}` ``，要素无 `id` 且无 `name` 时全部得到 `${id}-undefined`，`viewer.entities.add` 对重复 id 直接抛错；浸没分析受影响设施/预测点图层含无 id 要素即崩 | ✅ OPEN：`CesiumRenderer.js:386-387` |
| E18 | 中 | **Cesium addGeoJsonLayer 给 `entity.properties.featureType` 赋值未判空**：feature 无 `properties` 时 `entity.properties` 为 `undefined` → `Cannot set properties of undefined` | ✅ OPEN：`CesiumRenderer.js:481-482` |
| E19 | 中 | **加载失败静默吞掉（仅超时提示）**：`loadData` 仅当错误含「超时」才显示 `loadError`，其它失败（如 4xx/网络异常）被静默吞 → 地图空加载无提示，用户不知数据没来 | ✅ OPEN：`UnifiedMap.vue:104-107` |
| E20 | 中 | **BusinessLayerManager.setVisible 绕过 Pinia action 直改 `catalogEntry.visible`**：与全项目「用 action 改状态」约定不一致，状态来源分裂 | ✅ OPEN：`BusinessLayerManager.js:145` |
| E21 | 中 | **错误处理靠子串匹配（中英混合、脆弱）**：① 中文子串 `'超时'`/`'格式'`/`error.message.includes('fetch')`（`UnifiedMap.vue:104`、`mapDataService.ts:56/92`、`useApiRequest.ts:167`）；② `useApiRequest` 用 `error instanceof TypeError && error.message.includes('fetch')` 且把 `AbortError` 一律算 TIMEOUT（用户主动取消也显示「超时」），本地化环境失效 | ✅ OPEN：上述文件 |
| E22 | 中 | **`vite.config.js` `build.target:'es2015'` 与 `browserslist`(Chrome 89+/Safari 14.1+) 矛盾**：产物过度降级、体积增大、无法用现代语法特性 | ✅ OPEN：`vite.config.js:52` |
| E23 | 中 | **Cesium `creditContainer: document.createElement('div')` 隐藏版权/坐标归属**：可能违反 Cesium attribution 许可要求 | ✅ OPEN：`CesiumRenderer.js:52` |
| E24 | 中 | **`OLRenderer.addHeatmapLayer` 未用 `normalizePoint`**：假设 `geometry.coordinates=[lng,lat]`，与 `addPointLayer` 不一致 → 对 `lon` 字段/异常坐标静默失效 | ✅ OPEN：`OLRenderer.js:357-403` |
| E25 | 低 | **`UnifiedMap.setupLayers` 直接读 `renderer._layers.has('boundary')` 私有字段**：破坏渲染器封装 | ✅ OPEN：`UnifiedMap.vue:180,187` |
| E26 | 低 | **`startBreathing` 的 RAF 循环引擎切换时不停止**：仅 `renderer.destroy` 才停，隐藏地图仍持续渲染，空转浪费 GPU/CPU | ✅ OPEN：`OLRenderer.js:528-557` / `CesiumRenderer.js:677-716` |
| E27 | 低 | **`withTimeout` 超时后不 abort 底层 promise**：加载在后台继续并最终写已卸载组件的 ref，后台无效工作 + 潜在竞态 | ✅ OPEN：`UnifiedMap.vue:54-60` |
| E29 | 低 | **`console.error` 生产路径无 `import.meta.env.DEV` 保护**：如 `mapDataService` 等生产环境仍打栈 | ✅ OPEN：`mapDataService.ts` 等 |
| E30 | 低 | **`App.vue` 重复 `provide('mapStore', …)`**：与 `UnifiedMap` 内再次 `provide` 重复（无害但冗余） | ✅ OPEN：`App.vue` + `UnifiedMap.vue` |
| E31 | P2 | **后端反向依赖前端静态目录（重组引入的新耦合）**：`backend/controllers/forecastController.js:16` 与 `backend/services/forecastService.js:7` 经 `../../frontend/public/data/forecast` 读取前端 mock 数据，后端与前端 public 目录形成跨边界耦合；前端目录一旦变动即 `ENOENT`（重组时曾因此 500，已临时修正） | ✅ OPEN：上述两文件（属 S05「部分 mock 数据」的延伸；建议统一数据源或后端自带数据目录） |
| E33 | 中 | **浸没分析 `AffectedFacility` 业务 DTO 字段名未统一 `lng/lat`**：受影响设施数据对象字段命名与 `crs.ts` `normalizePoint` 约定（`lng/lat` + 防御兼容）不一致（与 R17 通用层归一不同，此处是具体业务 DTO 字段）；统一改动有破坏后端 API 契约风险 | ✅ OPEN：`reports/中高级问题收口` #6（🟡 待定）；改动前需先对齐 `API契约文档.md` 与后端响应字段，避免破坏契约 |
| E34 | P2 | **`forecastAdapter.ts` 死代码（无运行时代码引用）**：全 `src` 无任何运行时代码 `import` 该 adapter（仅 `mock/forecast/README.md` 文档提及）；`ForecastPage` 直连 `/api/forecast`，从未走 Adapter，「mock→api 零改动切换」在预测模块未落地 | ✅ OPEN：`forecastAdapter.ts` 零引用（`分步审查/前端架构审查报告-2026-07-28.md` #4 代码验证确认）；与 E04 同属 Adapter 收口债 |
| E35 | P3 | **`CesiumRenderer` 直写外部单例私有 `_baseLayers`**：`cesiumViewerManager._baseLayers = {...}`（同 core 子系统内，封装弱，与 E25 同类越界访问私有） | ✅ OPEN：`CesiumRenderer.js:332`（`分步审查/前端架构审查报告-2026-07-28.md` #7） |
| E36 | 中（🟠） | **TS 类型收窄机会：`forecastState` 的 `unknown` 槽**：`dataCache`/`currentData` 仍为 `unknown`，未用 `ForecastSeries` 收窄（与 R04 的 TS 迁移同源，属迁移未完成） | ✅ OPEN：`forecastState.ts`（`分步审查/TS_REVIEW.md` F5a） |
| E37 | 低（🟡） | **TS 类型收窄：`mapStore.lastAnalysisResult` 的 `unknown` 槽**：来自 `sessionStorage` `JSON.parse`，恢复时无静态类型；关联已知设计取舍 A02/A04（`analysisHandler` 注入），降优先级，可加 `AnalysisResult` 守卫但非紧急 | ✅ OPEN：`mapStore.ts:56-64`（`分步审查/TS_REVIEW.md` F5b） |
## 六、刻意简化（非缺陷，来自设计文档反模式 6）

> `项目根基.md:1170` 专立「反模式 6：把刻意简化当 bug 修」——以下项经决策为**有意简化**（当前阶段），不是 bug，也不属于"已修复"。保留记录以便面试解释与未来规划对齐。

| # | 简化项 | 未来计划 |
|---|--------|---------|
| S01 | JSON 文件存储（无数据库） | 接 PostgreSQL + PostGIS |
| S02 | AppLayout 硬编码默认图表数据 | 接数据库后改为动态数据 |
| S03 | 浸没分析水面坐标硬编码兜底 | 替换为真实 DEM |
| S04 | 剖面分析面板为占位 | 功能开发 |
| S05 | 部分 mock 数据 | 接真实数据源（含 E31 后端反向依赖前端目录问题） |
| S06 | 没有 CI/CD | 加 GitHub Actions（**已加，见已解决问题 R15；此项可关闭**） |

## 七、深度复核登记（待处理项）

> 来源：`深度代码复核报告-2026-07-28.md`。本轮不采信上一轮审计自述，独立 grep/read + 子代理广度核实。

- **F01（新回归，P1）**：`WaterLevelProfilePanel.vue:238,242,256,261` 颜色常量被写成字符串 `'PROFILE_COLORS.safe'/'PROFILE_COLORS.water'`，ECharts 颜色失效。→ **已修复（见已解决问题），本轮从本文件迁出**。
- **E02 裁定修正**：由"OPEN（调用即抛）"降级为"隐患代码仍在、当前被规避"，保留于第五节（待清理隐患）。
- **审计报告三处硬伤（文档准确性，非代码 bug）**：① 审核报告 #3 称"代码已全公开"——实际路由守卫 + `requiresAuth` 仍在（F1，见 E14）；② 审核报告 #7 称 `noImplicitAny:false`——实际为 `true`，真问题是 `checkJs:false`（F2，见 E05）；③ 审核报告后端路径全写 `server/`——实际为 `backend/`（F3）。**需在删除审核报告前将这三处更正或标注作废，以免误导后续评审。**
- **文档/代码反向矛盾待拍板**：`docs/开工前必读/项目根基.md` §5.1 路由表写"公开（无 requiresAuth）"，而 `router/index.js` 实际有守卫 + `requiresAuth`。二选一：① 文档恢复 `requiresAuth: true` 匹配代码；② 若真要公开，去掉代码守卫。涉及 E14 设计取舍（深度复核第八节）。

## 八、本轮分类复核说明

> 本文件由三份源文档（`审核报告-文档出入与重构进步`、`深度代码复核报告`、`问题清单复核总览`）整合重写，并对照"状态标签 vs 所在桶"及"设计意图 vs bug"两次复核。

**1. 从本文件迁出至 `已解决问题.md`（源文档/旧文件标"已修复"却误留待解决）：**
- `E28` floodAdapter 顶层 `useApiRequest` 反模式 → 已修复
- `F01` WaterLevelProfilePanel 颜色常量带引号回归 → 已修复
- `B05` / `B07` / `B08` / `B11` / `B13` / `useFloodStore 残留` / `D07` → 旧待解决"已从清单移除"区，均标已修复/已统一，统一迁入

**2. 重新归类为"非缺陷"（对照 `开工前必读` 设计文档，本轮新增）：**
- `A01` AppLayout 城市按钮固定 / 顶部不可 slot 覆盖 → 已知限制（`项目根基.md:918/:996` 主动指向本文件 A01）
- `A02` 组件职责集中 / mapStore 累积职责 → 已知设计取舍（`项目根基.md:568`）
- `A03` 单页双引擎 → 非目标，路由级切换是有意设计（`项目根基.md:33`）
- `A04` API 多方式 / 三套响应格式 → 已知设计取舍（`项目根基.md:568`；`API契约文档.md` §1.3 记录三格式为子系统有意约定）
- `E01` 胖接口 / 3D 方法返回 false → 文档化行为（`项目根基.md:368/:1096`、能力矩阵 `:374-383`）
- `E16` Cesium 不销毁 / 双引擎常驻 → 有意性能取舍（`项目根基.md:454`）
- 以上 6 项迁入「零节：设计意图与已知约束（非缺陷）」。**`D06` / `D08` 保留在待解决**：其含真实 bug 残留（register 无 reapply、缺共享响应类型），但设计文档亦承认「切换不序列化数据」「三格式有意约定」，已加"部分有意设计"注。
- `S01`–`S06` 刻意简化 → 第六节（设计文档反模式 6 明确"不要把刻意简化当 bug 修"）。

**3. ⚠️ 待你确认的状态冲突（源文档标 OPEN，但主理人当日早段自述已完成）：**
- `E12` ErrorBoundary 包地图：审核报告称未包；主理人自述已包。
- `D06` BusinessLayerManager 缺 `reapplyAll`：审核报告称未做；主理人自述已重建（`App.vue:99-106` reapplyAll watcher + `BusinessLayerManager.reapplyAll`）；`分步审查/前端架构审查报告-2026-07-28.md`（18:19）代码验证亦称 D06 已修复。与本文件 D06 OPEN 标记冲突，待你核对代码实际后再决定是否迁 `已解决问题.md`。
- 以上两项当前**暂留待解决**（依源文档标签），待你核对代码实际状态后决定去留；若属实应迁 `已解决问题.md`。
- `E32` 硬编码 hex：经 `reports/TS迁移进度-2026-07-28.md` 代码验证**已确认修复**（硬编码 hex ~71 行全部收敛至 `colors.ts`，所有 `.vue`/`.js` 业务/渲染/图层代码已零 `#hex`，仅 `style.css`+`colors.ts` 保留为单一事实源；`vue-tsc --noEmit` EXIT 0）→ 已从本文件 bug 表移除，迁入 `已解决问题.md` R26。

**4. 源文档 diff（待解决/已解决 vs 三份源文档）：**
- 三份源文档的全部 OPEN / 已修复问题项均已落入本文件对应章节或 `已解决问题.md`，**无遗漏**。
- 源文档中"已对齐（避免误判）"类（store=8、adapters 存在、backend helmet 加固）属确认无误项，本就不是问题，不迁移属正确。
- 审计报告的"建议优先级"条目（README P0 / 双认证 / 路由表 / hex / floodAdapter / ErrorBoundary / tsconfig / reapplyAll）均已有对应落点（E03 / R16 / E14 / E32 / E04 / E12 / E05 / D06）。
- `分步审查/`（7 份 v2 深度审查）：其全部真实问题项（A05/E02/E13/E19/E20/E21/E26/E27/D03/E07/A02/A04）均已在主文件有对应 ID；**新增且已迁入本文件**的有 `E34`（forecastAdapter 死代码）、`E35`（CesiumRenderer 写 `_baseLayers` 私有）、`E36`（forecastState `unknown` 槽）、`E37`（mapStore `unknown` 槽）；`E05` 的 vue-tsc「EXIT 0」误测已据 `TS_REVIEW.md` 实测（138 错误/126 隐式 any）修正；`D06` 状态冲突已登记（见 §3）。
