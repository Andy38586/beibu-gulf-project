# 待解决问题

> **定位**: 当前所有已知但尚未修复的问题（含代码验证后仍为 open 的项）。修一个删一个。
> **最后更新**: 2026-07-28（两轮：① 全量文档复核+代码验证，去重并新增 E 系列；② 代码质量评估 H1-H7/M1-M11/L1-L9 归档，去重后 H1→D06、H2/L4→E04、M2→A04、M3→E02、L1/L9→E05、L2→D01、L3→E08，其余 H3-H7/M1/M4-M11/L5-L8 新增 E14-E30）
> **说明**:
> - D02/D04/D05/D10/B03/B04/B06 经代码验证已修复 → 已迁入 `已解决问题.md`（R15–R21）。
> - 原 D01"零测试"据实修正为"测试覆盖率极低"（已有 4 个测试文件，但核心逻辑/Store/API 零测试）。
> - "mock/api 运行时切换"原 P0-4 与审计新增"api 分支空壳"合并为 E04。
> - 旧审计/评审/诊断文档（技术债务清单、技术审计报告、架构评审报告、改进清单、预测诊断、面试准备、面试官压力测试等）**全部保留未删**，供 diff 比对。
> **关联**: 已修复的问题见 `已解决问题.md`；API 稳定契约见 `开工前必读/API契约文档.md`。

---

## 一、架构问题

| # | 问题 | 影响 | 代码验证（2026-07-28） |
|---|------|------|------|
| A01 | AppLayout 直接 import 三图表 + 硬编码钦州/北海/防城港按钮与吞吐量数组 | 新增业务无法通过 slot 替换顶部按钮组，需改 AppLayout | ✅ 仍 OPEN：`AppLayout.vue:30-32`(三图表) `:49-66`(写死吞吐量数据) `:96-99`(写死城市 NavButton) |
| A02 | 核心组件职责过重（UnifiedMap ~381 行；OLRenderer 604 行；CesiumRenderer ~872 行） | 维护困难，需拆分 | ✅ OPEN（注：文档曾误记 UnifiedMap 550+/OLRenderer 381，实际已漂移） |
| A03 | 引擎切换不支持同页面多引擎共存 | 当前业务不需要 | 维持（设计取舍） |
| A04 | API 调用三种方式并存（裸 fetch 4 处 + useApiRequest + 自定义），且三套行为不一致 | 维护成本、超时/错误处理不统一 | ✅ OPEN：`mapDataService.ts:20`（自有 fetch+超时）、`useApiRequest.ts`（统一层）、`forecastAdapter.ts:14-29`（裸 fetch 无超时/无 abort）、`floodAdapter.ts:34`、`useBoundaryLayer.js:42`；代码质量评估 M2 |
| A05 | visualization 反向依赖 business（`useRadarChart → facilityConfig`） | 违反分层约束 §2.3 Rule 3，影响组件独立性 | ✅ OPEN：`useRadarChart.js:19`、`RadarScoreTooltip.vue:11` 仍 `import ... from '@/business/...'` |

## 二、后端安全与正确性

| # | 优先级 | 问题 | 文件 | 代码验证 |
|---|:-----:|------|------|----------|
| B01 | **P0** | `GET /api/markers/:id` 无归属校验（IDOR：任何已登录用户都能读取他人标注） | `controllers/markersController.js` | ✅ OPEN：`:18` `findById(req.params.id)` 后直接返回，未校验 `owner === req.user.id`（对比 `:60/:82` 的 update/delete 已校验） |
| B02 | **P1** | 注册 TOCTOU 竞态 → 重复用户名 | `controllers/authController.js` | OPEN（未验证修复） |
| B09 | P2 | ProfilePage `loadPlans()` 双触发（watch + onMounted 各触发一次） | `views/ProfilePage.vue` | OPEN |
| B10 | P2 | 浸没范围"向上取档"语义未在前端 UI 明示（请求 2.5m 实际返回 3.0m，非就近匹配） | `controllers/floodAnalysisController.js` | OPEN |
| B12 | P3 | scoring `Math.min` POI 量大时栈溢出 | `services/scoringService.js` | OPEN |

## 三、预测分析模块

| # | 问题 | 影响 | 备注 |
|---|------|------|------|
| C01 | 热力图色带未参数化（`addHeatmapLayer` 硬编码色带，业务层未传 `options.colorScale`） | 渲染器不应含业务语义 | 技术审计 C-03 / 技术债务 #11；预测诊断未覆盖此项 |
| C02 | 数据流断裂：`ForecastPanel.currentValue` 读 `dataCache` 但无写入 | 需重设计 | OPEN |
| C03 | `registerToggleable` 闭包累积：每次更新注册导致旧回调堆积 | 应只注册一次或先 remove | OPEN |
| C04 | 播放竞态 / 动画与图层同步 | 参考 `analysisSeq` 模式 | ⚠️ 已部分缓解：`ForecastControlPanel` 由 `setInterval` 推进 `currentTime` → 经 watch 触发图层更新（预测诊断 #1c）；**需运行时确认** |

## 四、工程实践

| # | 优先级 | 问题 | 代码验证 |
|---|:-----:|------|----------|
| D01 | **P0** | **测试覆盖率极低**（非"零测试"：已有 `spatialIndex`/`MapRenderer.interface`/`useChartBase`/`UnifiedMap` 4 个测试文件；但核心业务逻辑/Store/API 零测试） | 代码验证：4 测试文件存在；业务逻辑/Store/API 无测试 → 面试最大短板（代码质量评估 L2 复核：4/94 源文件比） |
| D03 | P1 | 状态管理重复（`siteSelectionState` 已用工厂；`floodState` 仍手写 `persisted*` ref，未泛型化/工厂化） | ✅ OPEN：`floodState.ts:37-39` 手写 `persistedWaterLevel/AffectedFacilities/TotalLoss` |
| D06 | P1 | **业务图层数据生命周期缺陷（确认 bug，含两层，代码质量评估 H1 复核）**：① `BusinessLayerManager.register` 幂等守卫（`BusinessLayerManager.js:68-69` warn+return，无重应用路径），FloodAnalysisPage 切回 3D 时 `register` 被拒 → 洪水图层丢失；② 更深层：`register` 仅存 `{layerType, options}`，**`data` 从未持久化**，`updateData` 第 118 行 `if (!catalogEntry || !catalogEntry.visible) return` 在图层隐藏时直接丢弃新数据 → `setVisible(true)` 只调 `setVisibility` 但图层未创建且无数据可重建 → 空白；隐藏期数据刷新被吞 → 切回显示旧图 | ✅ OPEN：`BusinessLayerManager.js:66-88,100-127,145`（`_registry` 不存 data / `updateData` 隐藏 early-return / `setVisible` 直改 visible） |
| D08 | P1 | 三套响应格式并存（RESTful / GCS / 混合），新增接口应统一使用 RESTful | 设计权衡（架构评审软化为中风险）：缺共享响应类型/契约 |
| D09 | P2 | `layerCatalog` 用 `ref` 存数组（应 `shallowRef`，50 个图层注册触发 50 次重渲染） | ✅ OPEN：`mapStore.ts:83` 仍为 `ref([])`（同文件 `currentRenderer` 已用 `shallowRef`） |
| D11 | P2 | API 无版本号前缀 `/api/v1/` | OPEN（前端 `API_BASE` + 后端 `routes/*`） |
| E12 | P1 | `ErrorBoundary` 仅包 `RouterView`，地图渲染崩溃可能白屏（需确认 `UnifiedMap` 是否在 `ErrorBoundary` 内） | ⚠️ 低置信：App.vue:107-111 包 RouterView；待确认覆盖边界 |

## 五、刻意简化（不要当 bug 修）

| # | 简化项 | 未来计划 |
|---|--------|---------|
| S01 | JSON 文件存储（无数据库） | 接 PostgreSQL + PostGIS |
| S02 | AppLayout 硬编码默认图表数据 | 接数据库后改为动态数据 |
| S03 | 浸没分析水面坐标硬编码兜底 | 替换为真实 DEM |
| S04 | 剖面分析面板为占位 | 功能开发 |
| S05 | 部分 mock 数据 | 接真实数据源 |
| S06 | 没有 CI/CD | 加 GitHub Actions（**已加，见 R15；此项可关闭**） |

## 六、审计 / 面试评审新增问题（代码验证 2026-07-28）

> 以下条目在原 `待解决问题.md` 中无对应项，来自 `技术审计报告`/`架构评审报告`/`改进清单-审计复核`/`面试准备`/`面试官独立压力测试评审`，已与最新代码交叉验证。

| # | 优先级 | 问题 | 代码验证（证据） |
|---|:-----:|------|------|
| E01 | P1 | **MapRenderer 胖接口**：3D 专用方法（`addWaterSurface`/`updateWaterLevel`/`startBreathing` 等）声明在基类，`OLRenderer` 只能空实现 | ✅ OPEN：`MapRenderer.js:177-214`（默认 `console.warn` + `return false`），违反接口隔离原则 |
| E02 | P1 | **mapStore 双图层注册机制（迁移未完成债）**：`registerToggleable`（旧，把函数存进 Pinia state，反模式）与 `registerBusinessLayer`（新，只存元数据）并存；且 `handleBusinessLayerToggle` 访问 `entry.show!`，对后者为 `undefined` → 一旦被调用即抛 TypeError（当前被 LayerControlPanel 绕开，属潜在崩溃，代码质量评估 M3） | ✅ OPEN：`mapStore.ts:146-305`（含 `:197` 旧 / `:240` 新 / `handleBusinessLayerToggle`）；代码质量评估 M3 |
| E03 | **P0** | **README 与代码脱节**：README 仍为"选址 + Express + JWT + 方案管理"，未提 forecast/flood 模块、mock 适配器、GCS 布局系统 | ✅ OPEN：`README.md:1-40`（标题"港口选址分析平台"，技术栈列 JWT/oxlint） |
| E04 | **P0** | **mock/api 语义混乱 + api 分支空壳（代码质量评估 H2/L4 复核确认）**：① `setDataSource('api')` 任何方法直接 `throw '真实 API 尚未接入'`；② flood 的 "mock" 分支 `getFloodAnalysis`/`getImpactAssessment` 实际 `apiRequest('/gcs/flood-areas?...')`、`apiRequest('/gcs/analysis/disaster')` —— 名为 mock 实为真实后端（与 forecastAdapter 读静态 JSON 的 mock 不一致）；③ `forecastAdapter` 的 `api` 分支同样全 `throw`（L4）。架构宣称「mock→api 零改动切换」在主 3D 模块未落地 | ✅ OPEN：`forecastAdapter.ts:50,57` `floodAdapter.ts:84,114,138,145`；`floodAdapter.ts:87-139`（mock 实际打后端） |
| E05 | 高 | **类型安全整体偏弱（两层，代码质量评估 L1/L9 复核）**：① `tsconfig.app.json` `checkJs:false` → 31 个 `.js`（含 OLRenderer/CesiumRenderer/MapRenderer）不受类型检查；② `eslint.config.js` 仅把 TS 当解析器，**未启用 `@typescript-eslint` 规则集**（无 `no-explicit-any`），全仓 53 处 `any`/`as any`/`@ts-ignore`。TS 迁移目前只保证「能编译」而非「类型安全」 | ✅ OPEN：`tsconfig.app.json:19`、`eslint.config.js`、`src/**` 53 处 any；改进清单 #2 |
| E06 | 高 | **CRS 运行时未强制业务边界**：`assertCRS` 已在 `OLRenderer`/`useApiRequest` 调用，但 `isInBeibuGulf`（北部湾 bbox）零调用，越界数据不拦截 | ⚠️ 部分缓解：`crs.ts:102` 仅定义未调用；改进清单 #8 |
| E07 | 中 | **floodState 未用持久化工厂**：仍手写 `persisted*` ref（`siteSelectionState` 已用 `createPersistedState`） | ✅ OPEN：`floodState.ts:37-39`；改进清单 #7（属 D03 子集） |
| E08 | 中 | **魔法数字散落**：多处裸 `10000`(超时)/`24*60*60*1000`(缓存)，未抽常量模块（代码质量评估 L3 复核：Cesium `IDLE_DESTROY_DELAY` 已集中，其余未集中） | ✅ OPEN：`mapDataService`/`floodAdapter`/`useBoundaryLayer`/`UnifiedMap.vue`；改进清单 #9 |
| E09 | 低 | **裁剪集成路径无测试**：`OLRenderer` `_addCulledPointLayer`/`_refreshCulledLayer`/`moveend` 增量刷新无单测 | OPEN；改进清单 #10 |
| E10 | 低 | **Controller 层零测试**：`server/controllers/*` 无 test（forecastEngine/forecastService 已有） | OPEN；改进清单 #11 |
| E11 | 待验证 | **依赖安全未验证**：`npm audit` 在本机镜像返回 `NOT_IMPLEMENTED`，既不能证有也不能证无 | 需切官方 registry 验证；改进清单 #15 |
| E13 | 真实 bug | **SiteSelectionPage 直调渲染器 + 图层泄漏 + pointer-events 缺失**：① `renderer.removeLayer` 绕过 BusinessLayerManager（`:122/:156`）② `onUnmounted`（`:304`）仅 `stopBreathing`+清 timer，**未移除 `activeFacilityLayerKey` 图层** → 泄漏 ③ 全文件仅 `pointer-events:none`（`:375`），无 `:deep(.gcs-panel){pointer-events:auto}` → 面板不可交互 | ✅ OPEN：架构评审 #4 代码验证确认 |
| E14 | **P0** | **路由守卫鉴权依赖内存 token，与 HttpOnly Cookie 主通道脱节（功能级阻断）**：`isAuthenticated` 只看内存 `token.value !== ''`；`restoreAuth()`（设占位 token）在 `App.vue` `onMounted` 才跑 → 直接刷新/深链访问 `/profile`、`/site-selection`、`/flood-analysis` 等受保护路由时 `beforeEach` 先执行、`token` 仍空 → 被弹回登录页，即便 Cookie 有效；且 `restoreAuth` 有 `authRestored` 一次性保护，首次 `/auth/me` 失败即永不重试 | ✅ OPEN：`router/index.js:46-56`、`useAuth.ts:56-106`、`App.vue:96-100`；代码质量评估 H3 |
| E15 | 高 | **useAuth 在组合式函数内无条件挂生命周期钩子（与 E14 同源）**：`useAuth()` 体内直接 `onMounted`/`onUnmounted`，在 `beforeEach` 守卫（无活跃组件实例）中调用会触发 Vue dev 警告且 `onMounted` 回调被忽略 → 多标签页 `storage` 同步监听永不注册 | ✅ OPEN：`useAuth.ts:197-210`、`router/index.js:48`；代码质量评估 H4 |
| E16 | 高 | **UnifiedMap 卸载只销毁当前渲染器 → Cesium WebGL/Viewer 泄漏**：`onUnmounted` 仅 `currentRenderer.value.destroy()`；若当前是 2D(OL) 则 `CesiumRenderer.destroy()` 永不调用，`cesiumViewerManager` 单例 Viewer(~5.7MB+WebGL) 不释放；30s 空闲销毁定时器只在 `CesiumRenderer.destroy()` 内启动，OL 路径不触发 | ✅ OPEN：`UnifiedMap.vue:334-339`、`CesiumRenderer.js:839-870`；代码质量评估 H5 |
| E17 | 高 | **Cesium 点图层 entity id 重复 `-undefined` 崩溃**：id 模板 `` `${id}-${item.id||item.name}` ``，要素无 `id` 且无 `name` 时全部得到 `${id}-undefined`，`viewer.entities.add` 对重复 id 直接抛错；浸没分析受影响设施/预测点图层含无 id 要素即崩 | ✅ OPEN：`CesiumRenderer.js:386-387`；代码质量评估 H6 |
| E18 | 中 | **Cesium addGeoJsonLayer 给 `entity.properties.featureType` 赋值未判空**：feature 无 `properties` 时 `entity.properties` 为 `undefined` → `Cannot set properties of undefined` | ✅ OPEN：`CesiumRenderer.js:481-482`；代码质量评估 H7 |
| E19 | 中 | **加载失败静默吞掉（仅超时提示）**：`loadData` 仅当错误含「超时」才显示 `loadError`，其它失败（如 4xx/网络异常）被静默吞 → 地图空加载无提示，用户不知数据没来 | ✅ OPEN：`UnifiedMap.vue:104-107`；代码质量评估 M1 |
| E20 | 中 | **BusinessLayerManager.setVisible 绕过 Pinia action 直改 `catalogEntry.visible`**：与全项目「用 action 改状态」约定不一致，状态来源分裂 | ✅ OPEN：`BusinessLayerManager.js:145`；代码质量评估 M4 |
| E21 | 中 | **错误处理靠子串匹配（中英混合、脆弱）**：① 中文子串 `'超时'`/`'格式'`/`error.message.includes('fetch')`（`UnifiedMap.vue:104`、`mapDataService.ts:56/92`、`useApiRequest.ts:167`）；② `useApiRequest` 用 `error instanceof TypeError && error.message.includes('fetch')` 且把 `AbortError` 一律算 TIMEOUT（用户主动取消也显示「超时」），本地化环境失效 | ✅ OPEN：上述文件；代码质量评估 M5/L6 |
| E22 | 中 | **`vite.config.js` `build.target:'es2015'` 与 `browserslist`(Chrome 89+/Safari 14.1+) 矛盾**：产物过度降级、体积增大、无法用现代语法特性 | ✅ OPEN：`vite.config.js:52`；代码质量评估 M6 |
| E23 | 中 | **Cesium `creditContainer: document.createElement('div')` 隐藏版权/坐标归属**：可能违反 Cesium attribution 许可要求 | ✅ OPEN：`CesiumRenderer.js:52`；代码质量评估 M7 |
| E24 | 中 | **`OLRenderer.addHeatmapLayer` 未用 `normalizePoint`**：假设 `geometry.coordinates=[lng,lat]`，与 `addPointLayer` 不一致 → 对 `lon` 字段/异常坐标静默失效 | ✅ OPEN：`OLRenderer.js:357-403`；代码质量评估 M8 |
| E25 | 低 | **`UnifiedMap.setupLayers` 直接读 `renderer._layers.has('boundary')` 私有字段**：破坏渲染器封装 | ✅ OPEN：`UnifiedMap.vue:180,187`；代码质量评估 M9 |
| E26 | 低 | **`startBreathing` 的 RAF 循环引擎切换时不停止**：仅 `renderer.destroy` 才停，隐藏地图仍持续渲染，空转浪费 GPU/CPU | ✅ OPEN：`OLRenderer.js:528-557` / `CesiumRenderer.js:677-716`；代码质量评估 M10 |
| E27 | 低 | **`withTimeout` 超时后不 abort 底层 promise**：加载在后台继续并最终写已卸载组件的 ref，后台无效工作 + 潜在竞态 | ✅ OPEN：`UnifiedMap.vue:54-60`；代码质量评估 M11 |
| E28 | 低 | **`floodAdapter` 模块顶层 `const { apiRequest } = useApiRequest()`**：组合式函数在 setup 外调用（当前可用，反模式，一旦 `useApiRequest` 加生命周期钩子即崩） | ✅ OPEN：`floodAdapter.ts:34`（模块层）；代码质量评估 L5 |
| E29 | 低 | **`console.error` 生产路径无 `import.meta.env.DEV` 保护**：如 `mapDataService` 等生产环境仍打栈 | ✅ OPEN：`mapDataService.ts` 等；代码质量评估 L7 |
| E30 | 低 | **`App.vue` 重复 `provide('mapStore', …)`**：与 `UnifiedMap` 内再次 `provide` 重复（无害但冗余） | ✅ OPEN：`App.vue` + `UnifiedMap.vue`；代码质量评估 L8 |

---

## 已从清单移除（代码已修复）

| # | 原问题 | 修复标记 | 移除日期 |
|---|--------|---------|---------|
| D02 | ~~零 CI~~ | R15：`.github/workflows/ci.yml` 存在且阻断（无 continue-on-error） | 2026-07-28 |
| D04 | ~~双认证机制矛盾（Bearer + Cookie）~~ | R16：`useApiRequest` 仅 Cookie-only，无 Bearer 发送 | 2026-07-28 |
| D05 | ~~坐标字段命名不统一~~ | R17：`crs.ts` 归一化 + 防御性兼容层（架构评审误判修正） | 2026-07-28 |
| D10 | ~~无空间索引（rbush）~~ | R18：`spatialIndex.ts` 已实现 | 2026-07-28 |
| B03 | ~~选址分析入参错误返 500（应 400）~~ | R19：`siteAnalysisController` 改返 400/422 | 2026-07-28 |
| B04 | ~~业务错误以 200 {error} 返回~~ | R20：`siteAnalysisController` 改返 4xx | 2026-07-28 |
| B06 | ~~评分权重未校验~~ | R21：`siteAnalysisController` 校验 0-10 | 2026-07-28 |
| B05 | ~~标记字段名不一致：创建存 `note`，更新白名单用 `description`~~ | 已统一为 `note` | 2026-07-27 |
| B07 | ~~plans/markers ID 用 `Date.now()` → 并发碰撞~~ | FIX:P2-09 改用 `crypto.randomUUID()` | 2026-07-27 |
| B08 | ~~plans 先改内存缓存后写文件，写失败时缓存已脏~~ | FIX:P2-10 构造新数组，不原地修改 | 2026-07-27 |
| B11 | ~~认证策略不一致（`/api/gcs/*` 全裸）~~ | FIX:P2-11 已加 `router.use(authenticate)` | 2026-07-27 |
| B13 | ~~facilities 缓存永不过期~~ | FIX:P3-09 加 TTL 5 分钟 | 2026-07-27 |
| — | ~~`useFloodStore` / `@/stores/floodStore` 残留引用（3 处）~~ | 已改为 `useFloodState` / `@/stores/floodState` | 2026-07-27 |
| D07 | ~~`/api/forecast/*` 无认证保护与其他模块不一致~~ | 前端路由 `meta.requiresAuth` 改为不要求认证，与后端 forecast 路由无 authenticate 对齐；预测数据视为公开数据为稳定设计决策 | 2026-07-28 |
