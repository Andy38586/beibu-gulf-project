# 待解决问题（重组版 · 2026-07-30 · 类型分节 + a/b/c/d/z 编号）

> **定位**：当前所有经真实代码取证确认**仍未闭环**的问题。修一个 → 从本文件删除，按**原编号**移入 `已解决问题.md`（状态由"在哪一文件"决定，解决即移走）。
> **编号规则**：按类型分节，四位数。`a`=地图层、`b`=业务层、`c`=组件层、`d`=后端层、`z`=暂未归类。**本文件内部顺序编号**（a014→a015→...）；移入 `已解决问题.md` 时**保留原编号不重排**——已解决问题不要求编号连续，因为问题不可能按编号顺序解决。
> **时间戳规则**：每个条目 `问题编号` 下方必须标注 `discover:YYYYMMDD`（首次写入本文件的时间，可附 `, HH:MM` 精确时间）。移入 `已解决问题.md` 时在原 `discover` 下补 `solve:YYYYMMDD`。格式以 `discover:20260730` 为准（见各编号单元格）。
> **问题等级**：`p0` 致命/高危阻断｜`p1` 高（正确性/稳定性/安全非致命/面试短板）｜`p2` 中（待改进·低危 bug·语义不一致）｜`p3` 低（接受/硬化/文档/视觉 nits）。
> **来源整合**：整合原《待解决问题.md》（A01–A10 审计）、`排雷清单与待解决问题diff-2026-07-30.md`（B-R1–B-R7 全量覆盖）、`双文档交叉验证报告-2026-07-30.md`（新增 a018 Cesium §4.2 矛盾 / z028 console 残留 / b023 损失金额单位）、`首屏加载性能评估-2026-07-29.md`（PERF 系列）、`个人中心与登录面板-用户意图记录-2026-07-30.md`（PROF 系列）、`GCS命名规范与歧义排查-2026-07-30.md`（CGCS2000/GCS 残留；代码层残留小写类名 + GCSFeature 误称已于 2026-07-30 22:37 全部清零，归入 `已解决问题.md z018/z019`，无新增未闭环项）、`工程化收口方案-2026-07-30.md`（G-1~G-10 工程化缺口 / 密钥泄露，新增 b024 / d048 / z029-z034）。所有松散报告已于 2026-07-30 整合后移出 docs/ 主目录，内容以本文件为准。
> **说明**：设计意图/刻意简化类（原 A01–A04、E01、E16、D08、E14残留、S01–S06）经 2026-07-30 基准复核确认非缺陷，已迁入 `已解决问题.md`，不计入本文件。

---

## 地图层（图层生命周期 / 地图渲染 / 地图引擎集成）

> 以下条目均已逐条标注 `discover:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p2 | a014-设施POI绕过图层管理器<br>`discover:20260730` | 设施 POI 直调 `renderer.removeLayer`/`addPointLayer`/`registerToggleable` 绕过 `BusinessLayerManager`，双轨图层管理，`reapplyAll` 管不到；另 site-selection 全项目唯一无 adapter 直连后端（`useSiteAnalysisApi.ts:24`）。 | 统一经 `businessLayerManager` 注册；证据 `SiteSelectionPage.vue:124/139/146/158`。 |
| p2 | a015-热力图色带未参数化<br>`discover:20260730` | `addHeatmapLayer` 默认 `LAYER_DEFAULTS.heatmapGradient`，业务层从未传 `options.gradient`。 | 业务层传入 `gradient`/`colorScale`；证据 `OLRenderer.js:344`。 |
| p2 | a016-裁剪集成无单测<br>`discover:20260730` | 裁剪集成路径（`_addCulledPointLayer`/`_refreshCulledLayer`/`moveend` 增量刷新）无单测。 | 补 cull 单元测试。 |
| p3 | a017-z-index无刻度<br>`discover:20260730` | `z-index` 无 `--GCS-z-*` 刻度，`UnifiedMap.vue:426` 地图容器 `z-index:1` 把内部 `.map-loading(100)`/`.map-error(100)`/`.boundary-warning(90)` 封顶，地图加载/错误提示可能被面板压住。 | 建立 z-index 刻度令牌；证据 `UnifiedMap.vue:426`。 |

---

## 业务层（业务逻辑 / Adapter / Store / 业务页特性）

> 以下条目均已逐条标注 `discover:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | b019-flood mock忽略水位<br>`discover:20260730` | flood mock 完全忽略水位参数（`getFloodAnalysis(_waterLevel)` 参数 `_` 前缀显式未用），滑块与分析结果脱节，核心演示链路语义失真；`api` 分支直接 `throw`。 | `floodAdapter` 接入真实水位；证据 `floodAdapter.ts:184-213`/`:217-238`。 |
| p1 | b023-损失金额单位虚标<br>`discover:20260730` | `FloodAnalysisReportPanel.vue:45-50` `loss>=10000` 时 `(loss/10000).toFixed(1)+' 亿'`——除 10000 却标"亿"（1 亿=1e8，应为"万"或除 1e8）；同一值在 `AffectedFacilityListPanel.vue:43` 标"万"，两面板数据对不上，面试展示直接打脸。 | 统一单位换算常量（建议 `LOSS_UNIT_DIVISOR`），报告面板改成"万"或除 1e8；证据 `FloodAnalysisReportPanel.vue:45-50`、`AffectedFacilityListPanel.vue:43`。 |

---

## 组件层（UI 组件 / CSS / 面板 / 视图组件 / 前端打包）

> 以下条目均已逐条标注 `discover:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | c011-ElementPlus首屏110KB<br>`discover:20260730` | Element Plus 110KB gzip 进首屏（占 44%），`ui-vendor` 合并全站 El 组件并在首屏 `modulepreload`，落地页只用到 ElButton 却为全站买单；已核实为按需打包真实代价非 bug。 | 权衡换轻量自研组件或细分 `ui-vendor` 包；证据 `main.js:44-47`、`WaterLevelProfilePanel.vue`。 |
| p3 | c013-登录与个人中心嵌套<br>`discover:20260730` | `ProfilePage` 登录面板与个人中心嵌套（`LoginPanel` 嵌在 `GCSPanel` 顶部 25% + 收藏夹），用户要求改为"两张屏" `v-if` 互换；待拍板。 | 同一路由 `/profile` 内 `v-if` 切换登录面板/个人中心面板；证据 `ProfilePage.vue:265`/`:268`/`:271`。 |
| p3 | c016-CSS!important系统性<br>`discover:20260730` | `GCSPanel` 内联写死定位，`style.css:163` 与 `FloodAnalysisPage.vue:505` 只能靠 `!important` 反杀，每加变体多一组。 | 抽定位令牌，消除 `!important`。 |
| p3 | c017-重复样式块<br>`discover:20260730` | 小区列表行（`SiteSelectionPage.vue:382` vs `ProfilePage.vue:591`）、2 列按钮网格（3 份）、滑块 thumb（3 份）、`.info-row`（3 份）重复。 | 抽公共样式。 |
| p3 | c018-前端硬编码魔法值<br>`discover:20260730` | 港口名硬编码（`ForecastPage.vue:53`）、`BASE_YEAR/END_YEAR`（`ForecastControlPanel.vue:100`）、默认置信度 `0.8` 写死 5 处、水位刻度阈值（`WaterLevelProfilePanel.vue:79`）。 | 抽常量。 |
| p3 | c019-死代码<br>`discover:20260730` | `useFacilities.ts` 整文件无引用；`colors.ts` 的 `RISK_COLORS`/`CHART_COLORS` 从未 import；`RadarChart.vue:38` 死 `close` emit；`FloodAnalysisReportPanel.vue:63` `showReport` 恒真死分支；死 CSS 多处。 | 清理。 |
| p3 | c020-GCS Token合规缺口<br>`discover:20260730` | 三项合并：①半透明面板背景 `rgba(255,255,255,0.95)` 4 处绕过 `--GCS-bg-panel-translucent`（PortInfoPanel/FloodAnalysisReportPanel/WaterLevelProfilePanel/FloodAnalysisPage）；②散落字号 `13/15/11/10px` 脱离 GCS 字号令牌；③渲染层风险色散落组件未抽 `colors.ts`（非 flood 域，c015 仅覆盖 flood）。 | 全局收敛至 `--GCS-*` Token + `colors.ts`；证据 `排雷清单` Nits。 |

---

## 后端层（后端代码 / API / 认证 / 服务 / 部署配置）

> 以下条目均已逐条标注 `discover:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p1 | d035-无brotli/HTTP2<br>`discover:20260730` | 无 brotli/无 HTTP2：nginx 仅 gzip，`listen 80` 无 HTTPS/HTTP2，比 brotli 劣 15–20%。 | 启 `ngx_brotli` + TLS/HTTP2；证据 `nginx.conf`。 |
| p2 | d039-API无版本前缀<br>`discover:20260730` | API 无版本号前缀 `/api/v1/`。 | 加版本前缀；证据 `routes/*`、`apiRequest`。 |
| p2 | d040-Controller测试不全<br>`discover:20260730` | Controller 层测试不全（已补 `forecastController`，其余缺测）。 | 补 controllers 单测。 |
| p3 | d041-flood控制器手算评估<br>`discover:20260730` | flood 控制器内手算评估（`facility.value*damageRate`）且无 repository/缓存，与 forecast/site-analysis 分层不一致。 | 抽 `floodService` repository + 缓存。 |
| p3 | d042-依赖安全未审计<br>`discover:20260730` | 依赖安全未审计（`npm audit` 本机镜像 `NOT_IMPLEMENTED`）。 | 上线前官方源跑 `npm audit`。 |
| p3 | d043-无操作审计日志<br>`discover:20260730` | 成功操作无审计日志。 | 加操作审计日志；证据 `authController.js`。 |
| p3 | d044-users.json git历史<br>`discover:20260730` | git 历史 `users.json` 明文测试/探针账号（含 SQLi 探针）未清理；决策 A（`git filter-repo`）已批准，实施推迟。 | mirror 备份后 `git filter-repo` 清理。 |
| p3 | d045-userService无缓存<br>`discover:20260730` | `userService` 特意 `useCache:false`（`userService.js:10`），每个认证请求都读盘（auth 层 N+1）。 | 启用用户缓存。 |
| p3 | d047-scoringService双重循环<br>`discover:20260730` | `scoringService` 距离评分仍线性双重循环（O(n²)），rbush 空间索引已实现但近邻查询未用上。 | 接入 rbush 近邻替代全量遍历；证据 `排雷清单` 🟡 Suggestions。 |

---

## 暂未归类（文档 / 工程质量 / 测试覆盖 / 杂项）

> 以下条目均已逐条标注 `discover:20260730` 时间戳（详见各编号下方）。

| 问题等级 | 问题编号 | 问题内容以及影响 | 如何解决 |
|:---:|:---:|---|---|
| p2 | z020-Store/API测试覆盖<br>`discover:20260730` | 测试覆盖：Store/API 核心仍未全覆盖（测试文件由 4 增至 13，但核心 Store/API 仍缺），面试最大短板。 | 补 store/api 单测。 |
| p2 | z022-天地图外部依赖<br>`discover:20260730` | 天地图外部依赖不可控，底图延迟由 `tianditu.gov.cn` 决定。 | 后续同源代理/自托管瓦片。 |
| p3 | z023-README脱节<br>`discover:20260730` | README 与代码脱节（仍为"港口选址分析平台"+JWT+方案管理，未提 forecast/flood/mock 适配器/GCS）。 | 上服务器前重写 README。 |
| p3 | z026-mapStore unknown槽<br>`discover:20260730` | `mapStore.lastAnalysisResult` 的 `unknown` 槽来自 `sessionStorage` `JSON.parse`，无静态类型。 | 收敛类型；证据 `mapStore.ts`。 |
| p3 | z027-两张屏路由决策<br>`discover:20260730` | "两张屏"是同一路由 `/profile` 内 `v-if` 切换，还是拆独立 `/login` 路由——用户未拍板。 | 待用户决策。 |
| p3 | z030-无错误上报/监控<br>`discover:20260730` | `errorHandler.ts:4-5` 注释"后续可接入"但无上报/监控后端，生产错误无法聚合分析。 | 预留 `logger.transport`/reporter 钩子，未来接 Sentry 等；与 z029 联动。 |
| p2 | z033-mapDataService直接fetch<br>`discover:20260730` | `mapDataService.ts:28` / `useBoundaryLayer.ts:49` 直接 `fetch` 静态 GeoJSON，绕过 adapter 与统一 `errorHandler`/`logger` 边界，错误无统一处理。 | 方案待拍板：① 视为地图 infra 免 adapter，但错误走 errorHandler+logger 结构化；② 抽 `mapDataAdapter` 与业务 adapter 同构（为阶段二 GeoServer 演进铺路）；证据 `mapDataService.ts:28`/`useBoundaryLayer.ts:49`。 |
| p3 | z034-无useAsyncData封装<br>`discover:20260730` | 复杂页（Forecast/Flood/SiteSelection）各自维护 `loading/error/refresh/cancel`，重复造状态、易出竞态/取消 bug。 | 新增 `shared/composables/useAsyncData(fn,{immediate})` 封装 `data/loading/error/refresh/cancel(AbortSignal)`；仅复杂业务用，简单页保持 `onMounted`（D-4 克制）；证据 全仓无此 composable。 |

---

## 附：审计评分与修复优先级（2026-07-30）

- 独立代码审计整体 **69/100**（架构 78 / 代码质量 70 / 性能 72 / 可维护性 74 / 工程规范·安全 62）。扣分主因 d032（安全）、b019/a013/a014/d033 等（正确性/稳定性）；新增 a018（代码/文档对撞）进一步压制架构评分。
- TOP10 修复顺序（收益/成本）：**b019 → d033 → b023 → a014 → d036 → d038 → d037 → c012 → d034 → d035**。
- b023（损失金额单位虚标）已于 2026-07-30 双文档交叉验证中补入独立编号，原 A05 备忘归档。
