# 待解决问题（代码验证版 · 2026-07-29 复核 · 按影响分层）

> **定位**：当前所有经**真实代码取证**确认仍未闭环的问题（bug / 待改进 / 已知接受的限制）。修一个删一个。
> **方法**：每条均附 `file:line` 或实跑命令证据，结论不依赖任何旧报告记忆。本次对旧审查报告做了独立复验，发现多处以过时/不实结论，已在第七节与 `面试官视角.md` 标注。
> **本次调整（2026-07-29 重排序）**：原按"后端/前端/工程/刻意简化"平铺的章节，重排为**按影响等级的分层结构**（P2 重要 → 一般 OPEN → 低危接受/运维 → 暂缓/拍板 → 设计意图）。所有 2026-07-29 实施计划已修复的项已从本表删除，统一留痕于文末「附：已闭环项索引」，详情见 `已解决问题.md`。
> **关联**：已修复问题见 `已解决问题.md`；面试用审计与 diff 见 `面试官视角.md`；设计约束见 `开工前必读/`；TS 路线见 `TS学习路线.md`。

---

## 一、影响分层总览（速查）

| 层级 | 含义 | 项数 | 代表项 |
|:----:|------|:----:|--------|
| **A** | P2 重要 · 影响正确性 / 安全深度防御 / 架构 / 面试短板，应先处理 | 4 | B10、D01、E13①、安全验收测试空白 |
| **B** | 一般 OPEN · 待改进或低危待修 bug，有明确 fix 路径 | 10 | C01、D08、D11、E09、E10、E04、E27、E37、C04、E26 |
| **C** | 低危接受 / 运维 hardening · P3，可推迟 | 7 | E14 残留、E30、E11、注册无限流、审计日志、依赖 audit、users.json 6.6 |
| **D** | 用户拍板 / 暂缓项 | 1 | E03 |
| **E** | 设计意图与已知约束（非缺陷，仅面试解释 / 未来规划参考） | 12 | A01–A04、E01、E16、S01–S06 |

> 当前**真正未闭环且需跟踪的项共 22 条**（A4 + B10 + C7 + D1），设计意图类 E 不计入缺陷。

---

## 二、第一层 · P2 重要（应先处理）

| # | 优先级 | 问题 | 证据（2026-07-29 实跑） |
|---|:-----:|------|------------------------|
| B10 | P2 | 浸没范围"向上取档"语义未在前端 UI 明示（请求 2.5m 实际返回 3.0m，非就近） | `backend/controllers/floodAnalysisController.js:62`、`:209` 注释"向上取档（返回 >= 请求水位的最低档位）"；前端 UI 是否明示实际档位需运行时确认。 |
| D01 | 部分（面试最大短板） | 测试覆盖：Store/API 核心仍未全覆盖 | 测试文件由 4 增至 13（forecastController/forecastService/forecastEngine/forecastState/floodState/BusinessLayerManager/forecastAdapter/floodAdapter/UnifiedMap/MapRenderer.interface/useChartBase/spatialIndex）；核心 Store/API 仍未全覆盖 → 面试最大短板。 |
| E13 | ① OPEN（架构违规） | `SiteSelectionPage` 直调 `renderer.removeLayer`/`addPointLayer` 绕过 BusinessLayerManager（走老 `registerToggleable` 路径） | `frontend/src/business/site-selection/SiteSelectionPage.vue:122`（renderer.removeLayer）、`:137`（renderer.addPointLayer）、`:144`（registerToggleable）。② 图层泄漏已修（`:304-313` `onUnmounted` 真删+清 key）。 |
| — | P2（部分缓解） | 安全验收测试空白：floodAnalysis 等 controller 仍缺测 | markers IDOR 回归 5 例（阶段一）、auth 令牌失效用例（6.5）已补；关联 E10 / 6.8（用户拍板不实施）。 |

---

## 三、第二层 · 一般 OPEN（待改进 / 低危待修 bug）

| # | 状态 | 问题 | 证据（2026-07-29 实跑） |
|---|------|------|------------------------|
| C01 | OPEN | 热力图色带未参数化：`addHeatmapLayer` 硬编码 `LAYER_DEFAULTS.heatmapGradient`，业务层未传 `options.colorScale` | `frontend/src/core/map/renderers/OLRenderer.js:342` `gradient = LAYER_DEFAULTS.heatmapGradient`（默认；业务层调用未传 colorScale）。 |
| D08 | OPEN（设计有意） | 三套响应格式并存（RESTful/GCS/混合），缺共享响应类型 | `开工前必读/API契约文档.md` §1.3 记录三格式为子系统有意约定；属待改进（中风险）。 |
| D11 | OPEN | API 无版本号前缀 `/api/v1/` | 前端 `apiRequest` + 后端 `routes/*` 均无 `/v1`（仅 mock README 提过 `/api/v2`）。未深度复验，按源文档保留。 |
| E09 | OPEN | 裁剪集成路径无单测（`_addCulledPointLayer`/`_refreshCulledLayer`/`moveend` 增量刷新） | 无对应测试文件。 |
| E10 | 部分 | Controller 层测试不全 | 已补 `forecastController` 测试（全绿）；其余 controller 仍缺测。 |
| E04 | 部分解决 | flood mock 改读 `/data/*.json`；但 `api` 分支仍 `throw '真实 API 尚未接入'`（设计预留未闭环） | `floodAdapter.ts:37/202/229`；`:193/220/244/251` 仍 throw。 |
| E27 | OPEN（低） | `withTimeout` 超时后不 abort 底层 promise，后台继续写已卸载组件 ref | `frontend/src/core/map/UnifiedMap.vue:105` `withTimeout(loadPorts(),10000,...)`，底层 fetch 未 abort。 |
| E37 | OPEN（低） | `mapStore.lastAnalysisResult` 的 `unknown` 槽来自 `sessionStorage` `JSON.parse`，无静态类型 | `frontend/src/stores/mapStore.ts`（lastAnalysisResult: unknown），关联已知设计取舍 A02/A04。 |
| C04 | 部分缓解 | `ForecastControlPanel` 播放用 `setInterval` | `ForecastControlPanel.vue:151-167` 播放 `setInterval`+`stopPlayback`+`onUnmounted` 清理；动画/图层经 `currentTime` watch 同步。 |
| E26 | 已缓解 | 呼吸动画未停（RAF 泄漏） | `useMapControls.js:16-18` `stopBreathing` 代理到 renderer；`App.vue:60` 路由 watcher 每次切换调用 → RAF 已停。 |

---

## 四、第三层 · 低危接受 / 运维 hardening（P3，可推迟）

| # | 状态 | 问题 | 证据（2026-07-29 实跑） |
|---|------|------|------------------------|
| E14 残留 | P3（接受） | 前端无路由守卫：路由全公开，仅靠后端 `authenticate` 为真网关 | `frontend/src/router` 全目录 grep `beforeEach|requiresAuth` **无匹配**（App.vue 等无守卫）。原"内存 token 弹回登录"bug 因守卫移除已不存在；但缺前端深度防御，属低危接受项（见 `安全自检诊断报告` 维03/维19）。 |
| E30 | OPEN（无害） | `App.vue` 与 `UnifiedMap.vue` 重复 `provide('mapStore', …)` | `frontend/src/App.vue` 与 `frontend/src/core/map/UnifiedMap.vue` 均有 provide。 |
| E11 | 未验证 | 依赖安全未审计 | `npm audit` 在本机镜像返回 `NOT_IMPLEMENTED`，无法证有/无；需切官方 registry。 |
| — | P3 | 注册接口无限流 | `backend/app.js`。 |
| — | P3 | 成功操作无审计日志 | `authController.js`。 |
| — | P3 | 依赖未联网 audit（镜像 `NOT_IMPLEMENTED`） | 上线前官方源跑 `npm audit`。 |
| — | P3（推迟） | 上线前 `users.json` 测试账号 git 历史清理（6.6） | 工作区已清为 1 个 `webgis`；`git show HEAD:backend/data/users.json` 仍含 11 个历史测试/探针账号（含 SQLi 探针）。**决策 A（git filter-repo 清理）已批准，实施推迟至下一轮**（需先备 mirror 备份 + 回滚手段）。 |

---

## 五、第四层 · 用户拍板 / 暂缓项

| # | 状态 | 问题 | 证据 / 说明 |
|---|------|------|-------------|
| E03 | OPEN（决策暂缓） | README 与代码脱节（仍为"港口选址分析平台"+JWT+方案管理，未提 forecast/flood/mock 适配器/GCS） | `README.md` 标题/技术栈仍为旧描述；用户指示项目做完、上服务器前再重写。 |

---

## 六、设计意图与已知约束（非缺陷，来自 `开工前必读` / 反模式 6）

> 这些条目来自设计文档明确声明的「不变规则 / 已知限制 / 已知取舍 / 文档化行为」，不是 bug。保留仅供面试解释与未来规划参考。

### 6.1 设计意图（不变规则 / 取舍）

| # | 设计意图 | 证据 |
|---|----------|------|
| A01 | AppLayout 顶部城市按钮（钦州/北海/防城港）固定结构，不在 slot 覆盖范围内（已知限制） | `开工前必读/项目根基.md:918`、`:996` |
| A02 | 核心组件职责集中（如 mapStore 累积 analysisHandler 职责）是已知设计取舍 | `项目根基.md:568` |
| A03 | 双引擎是**路由级切换**（非同页多引擎共存），单页双引擎属远景 | `项目根基.md:33` |
| A04 | API 多方式并存是已知设计取舍；三套响应格式是各子系统有意约定 | `项目根基.md:568`、`开工前必读/API契约文档.md` §1.3 |
| E01 | `MapRenderer` 基类声明 3D-only 方法，OLRenderer 不实现返回 `false + DEV warn` 是文档化行为 | `项目根基.md:368`、`:1096`、能力矩阵 `:374-383` |
| E16 | Cesium `Viewer` 单例、离开 3D 仅 `v-show` 隐藏不销毁，是性能取舍 | `项目根基.md:454` |

### 6.2 刻意简化（未来计划）

| # | 简化项 | 未来计划 |
|---|--------|---------|
| S01 | JSON 文件存储（无数据库） | 接 PostgreSQL + PostGIS |
| S02 | AppLayout 硬编码默认图表数据 | 接数据库后改为动态 |
| S03 | 浸没分析水面坐标硬编码兜底 | 替换为真实 DEM |
| S04 | 剖面分析面板为占位 | 功能开发 |
| S05 | 部分 mock 数据 | 接真实数据源（含 E31 后端反向依赖前端目录，已解决） |
| S06 | 没有 CI/CD | 已加 GitHub Actions（见 `已解决问题.md` R15），可关闭 |

---

## 七、复核订正（旧报告不实 / 过时结论，勿再采信）

> 以下结论由 2026-07-29 实跑代码独立验证，**推翻了同日部分旧报告的声明**。完整论证见 `面试官视角.md`。

1. **E29「已修复」不实**：旧文档称 `mapDataService.ts` "grep console.error|console.warn 无匹配 → 生产路径 console.error 已移除"。实跑 grep **有 4 处 `console.error`（57/60/96/99）+ 1 处 `console.warn`（81，仅该处有 DEV 守卫）**。结论：E29 未解决，已重开（阶段二重闭环，详见文末索引 E29）。
2. **E05 路径错误**：旧文档称 `frontend/eslint.config.js:4,70` 启用 `typescript-eslint`。实际 `frontend/eslint.config.js` **不存在**；真实文件在**项目根** `eslint.config.js:4`（`import tseslint from 'typescript-eslint'`）、`:70`（`...tseslint.configs.recommended`）、`:105`（`@typescript-eslint/no-explicit-any:'off'`）。声明属实，路径写错。
3. **E18 行号过时**：文档写 `CesiumRenderer.js:481-482`，实跑该代码现位于 `:642`。
4. **墓碑代码诊断报告（2026-07-29）3 处 P1 结论复核**：
   - 声称 `backend/routes/forecast.js` 全接口无鉴权 → 实跑 `forecast.js:3,13` 确已 `import {authenticate}` + `router.use(authenticate)`。⚠️ **但此"修复"与设计文档直接冲突**：`开工前必读/API契约文档.md:860` 明确"`/api/forecast/*` 不需要认证…此为稳定设计决策"；`项目根基.md:482`/`912` 路由注释与设计决策一致；`已解决问题.md` D07 亦以"预测数据视为公开为稳定决策"结案。当前代码违反设计意图，**三处设计文档均未同步更新**。需决策：若 forecast 确需鉴权 → 回滚代码并同步更新设计文档；若应保持公开 → 移除 `forecast.js:13` 的 authenticate。**此项不得简单记为"已修复"**（6.1 决策 B 已闭环，详见 `已解决问题.md` D07，但设计文档同步仍未做）。
   - 声称 `users.json` 含 11 个测试/探针账号（含 SQLi 探针）→ 工作区文件实跑仅 **1 个 `webgis`**，**已清理**；但 `git show HEAD:backend/data/users.json` 仍含 **11 条**（含 `admin' OR '1'='1` 等 SQLi 探针账号），git 历史未清理（见第四层 6.6）。
   - 声称 `.gitignore` 路径写错导致 `users.json` 被 git 跟踪 → 实跑 `.gitignore:44-46` 正确忽略 `backend/data/users.json`+`plans.json`+`.env`，`git ls-files` 返回空（**未被跟踪**）。
   - 结论：墓碑报告 users.json/gitignore 两项**不得作为待解决问题**；forecast 鉴权项**定性需修正**（见上），为未闭环的设计冲突。
5. **E14 措辞修正**：旧文档标"已解决（设计变更）"，精确说是"通过移除前端路由守卫使原 token 弹回登录 bug 消失"，但引入"前端无守卫"的低危接受项（见第四层 E14 残留）。面试讲 E14 须同时讲清这两面，否则会被追问。

---

## 八、分类复核说明

- 本文件由旧 `待解决问题.md`、`已解决问题.md`、`问题验证报告`、`墓碑代码诊断`、`安全自检诊断报告`、`面试备战`、`面试压力测试`、`项目阅读索引`、`首屏分包分析` 整合重写，并经独立代码复验。
- 经复验从"旧待解决/旧报告 OPEN"迁出至 `已解决问题.md` 的项（代码证明确已修复）：B02、B09、B12、C02、D06、E02、E12、E14（设计变更，**残留低危项见 E14 残留**）、E15、E17、E19、E20、E22、E25、E29（**已重开，2026-07-29 阶段二重闭环**）、E32/F01、E33、E34、E35、D09、R15–R21、R26–R32 等，详见 `已解决问题.md`。
- 2026-07-29《待解决问题修复-2026-07-29》实施计划（阶段一~五 + 阶段六 6.1–6.7）已全部落地并经**真实代码复验**，以下项经代码取证确认已修复并迁入 `已解决问题.md`：**B01（P0 IDOR）、C03、E06、E08、E18、E21、E23、E24、E29、E31、令牌无吊销（R34/tokenVersion）、throughput_model（6.7）**。
- **B01（markers 水平越权）已于 2026-07-29 阶段一修复**（getOne 归属校验 + 5 例回归测试，见 `已解决问题.md` B01）。当前**无已知可利用的水平越权漏洞**；剩余项多为深度防御/运维 hardening（见第四层），及 E13①/6.8/6.9 等用户拍板或暂缓项。

---

## 九、渐进式迁移计划（原 `工程规范与性能参考.md` §10）

> 来源：原 `开工前必读/工程规范与性能参考.md`（v1）§10，2026-07-29 整合时迁至此。原则：**不要一次性改完整个项目，按文件类型分批**，每批一个 `style` 或 `refactor` commit 方便回滚。

| 批次 | 文件 | 动作 | 状态 | 预估 |
|:----:|------|------|:----:|:----:|
| 1 | `src/types/*.ts` | 统一类型命名（去 I/T 前缀），补充地理注释 | ✅ 已完成 | 30min |
| 2 | `src/core/map/renderers/*.ts` | 统一方法签名中的坐标参数名为 `lngLat` | ⏳ 待迁移（.js → .ts） | 30min |
| 3 | `src/stores/*.ts` | 统一 Setup Store 风格，消灭 `any` | ✅ 已完成 | 20min |
| 4 | `backend/services/*.js` | 引入 BusinessError 统一错误码 | ✅ 已完成 | 30min |
| 5 | 全部 `.vue` 文件 | Props 从对象语法迁移到泛型语法 | ✅ 已完成 | 1h |

> **2026-07-29 复核锚定**：批次 2（renderers `.js → .ts`）**仍 OPEN**——`core/map/` 整块（BusinessLayerManager / OLRenderer / CesiumRenderer / layerAdapters / useBusinessLayers / useMapRenderer 等）仍 `.js`，`tsconfig.app.json` 设 `allowJs:true` + `checkJs:false` 导致其被纳入编译但不参与类型检查（即"脱保区"）。这是当前 TS 收编（D-6）的最高优先级缺口，与批次 3/5 的"已完成"不冲突（store/.vue 层已真安全且 CI 兜底）。

---

## 附：已闭环项索引（详情见 `已解决问题.md`，本表仅留痕）

> 以下项均经 2026-07-29 真实代码取证确认已修复，已从上方待解决分层中删除，详情与 `file:line` 证据见 `已解决问题.md` 对应行。

| # | 原优先级 | 问题 | 已解决问题.md 定位 |
|---|:--------:|------|--------------------|
| B01 | P0（致命） | markers 水平 IDOR（GET /api/markers/:id 读他人标注） | 第一节 B01（getOne 归属校验 + 5 例回归） |
| C03 | — | registerLayer 重注册回调累加 | 第二节 C03（:158 改替换语义） |
| E06 | — | CRS 边界守卫零调用 | 第二节 E06（mapDataService getPorts 调 isInBeibuGulf） |
| E08 | — | 魔法数字散落（裸 10000 超时） | 第二节 E08（FETCH/LOAD/API_TIMEOUT_MS 全抽常量） |
| E18 | — | Cesium entity.properties 赋值未判空 | 第二节 E18（:642-643 判空） |
| E21 | — | AbortError 误判为超时 | 第二节 E21（:123 区分 aborted） |
| E23 | — | Cesium 隐藏版权 | 第二节 E23（移除 creditContainer） |
| E24 | — | 热力图未用 normalizePoint | 第二节 E24（addHeatmapLayer/updateHeatmapLayer 改用） |
| E29 | — | 生产路径 console.error 未移除 | 第二节 E29（4 处加 DEV 守卫） |
| E31 | — | 后端反向依赖前端静态目录 | 第二节 E31（7 JSON 迁 backend/data/forecast） |
| R34 | P2 | 令牌无吊销（logout/改密后旧 token 仍有效） | 第一节 R34（tokenVersion 机制） |
| 6.7 | — | tools/throughput_model.cjs 用途待确认 | 第四节 6.7（保留 + npm script + MAPE<2.3%） |

> 另：令牌无吊销、throughput_model 原列于第五节"已知接受的限制"，本次确认已闭环后同步从第五层移除，归入本索引。
