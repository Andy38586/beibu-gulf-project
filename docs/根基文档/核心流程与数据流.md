# 02 · 核心流程与数据流（Architecture Flow）

> **定位**：项目宪法第二篇——源码导读。回答"一个功能从用户点击到结果显示，中间发生什么"，每步标注文件与职责。
> **读者**：AI 优先、新开发者。
> **写作纪律**：只给"入口 → 状态 → 核心 → 渲染"链，不复制代码；写明"这层负责什么、不负责什么"。文件路径以当前代码为准，行号不写（会漂移）。

---

## 一、数据流总览

```
业务组件 → 取数入口（useApiRequest / loadStatic）
  ├─ flood         → floodAdapter       （services/adapters/）
  ├─ forecast      → useForecastRequest （业务 composable，事务/竞态管理）
  ├─ site-analysis → useSiteAnalysisApi （业务 composable，直连 useApiRequest + zod 校验）
  └─ 数据源: mock → loadStatic（public/data 契约桩）
             api   → fetch → Nest /nest-api/*
             online→ fetch → FastAPI /flood-online
后端：modules → controllers → services（createReadCache 等读文件缓存）→ data/*.json
```

**取数铁律**：请求唯一入口、信封唯一、边界 zod 校验、数据源归属统一管理（详见下文规则）。业务组件**不直接**判断数据源、不裸 fetch。

## 二、前端取数规则（R1-R5）

| 规则                | 内容                                                                                                                                                                                                                         | 违反后果                        |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| **R1 请求唯一入口** | API 一律走 `useApiRequest`；静态资源一律走 `loadStatic`。禁止裸 fetch/axios                                                                                                                                                  | 报错口径/重试/超时/解包全部失效 |
| **R1.1 跨服务例外** | FastAPI 返回裸 JSON（无信封）时，`apiRequest` 传 `envelope: false`，仍在统一入口内                                                                                                                                           | 同 R1                           |
| **R2 信封统一**     | 响应契约 `{ code, data }`，前端解包唯一走 `unwrapEnvelope`，禁止手写 `res.data`                                                                                                                                              | 解包逻辑重复、漂移              |
| **R3 静态资源**     | `loadStatic`（超时 / TTL / in-flight 去重 / LRU 上限）                                                                                                                                                                       | 缓存/超时/校验缺失              |
| **R4 数据源归属**   | 数据源模式由 `VITE_DATA_SOURCE` 构建期注入 + `floodAdapter` 模块级变量管理（原 `dataSourceConfig` 统一管理已随 816 简化——该配置仅 floodAdapter 一个使用方，`floodAdapter.ts:25` 注释为现状权威），业务组件不得直接判断数据源 | 数据源语义散落                  |
| **R5 取数统一收口** | 业务组件取数一律经 `useApiRequest`/`loadStatic` 统一入口；跨业务复用的取数封装放 `services/adapters/`（当前仅 floodAdapter）；业务专属取数逻辑（事务/竞态/校验）放业务模块 composable                                        | 中间层不一致、统一入口被绕过    |

## 三、后端响应规则（R6-R8）

| 规则              | 内容                                                                                                                   | 违反后果       |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------- | -------------- |
| **R6 成功出参**   | controller 一律 `sendSuccess(res, data)`，信封 `{ code, data }`                                                        | 前端解包失效   |
| **R7 错误出参**   | 错误抛 `BusinessError(code, message)` → 全局错误中间件统一映射 HTTP 状态码；禁止 controller 裸 `res.json`/`res.status` | 错误口径不一致 |
| **R8 读文件缓存** | 后端读 JSON 文件缓存统一 `createReadCache`（TTL + LRU + 上限），禁止自研 cache Map                                     | 缓存实现重复   |

## 四、业务主流程

### 4.1 选址分析（Site Selection）

```
用户点击「开始分析」
   │
   ├─ SiteAnalysisControlPanel.runAnalysis：防重复守卫 → 确认三态选择 → 组装参数
   │    （selectedKeys + typeSettings{defaultRadius, importance, selected}；前端不传 weights）
   ▼
useSiteAnalysisApi.analyze（无 adapter，直连统一入口）
   │   POST /api/site-analysis（需登录）· 竞态 signal · zod 校验
   ▼
Nest：controller 校验参数 → service 九步计算
   │   ① validateSelection → ② resolveRadiusSettings（importance→半径）
   │   ③ buildTypeCoverage（buffer + union，占核心耗时 95%+，已知瓶颈 b044）
   │   ④ intersectCoverages（两两求交）→ ⑤ RBush 空间索引
   │   ⑥ filterMatchedXiaoqu（BBox 粗筛 + 点在多边形内精确过滤）
   │   ⑦ rankXiaoqu（最近距离线性衰减评分，TOP 10）
   │   ⑧ facilityPoi：各选中类型经 extractValidPoi 的全量合法 POI（与覆盖计算同源）
   ▼
前端 handleResult：store 存结果 + BLM 注册 analysis-coverage / analysis-matched 图层；
   │   新结果到达即撤下上一次的设施 POI 图层与呼吸（防残留）
   ▼
雷达图 6 轴评分 + 小区列表分页 + 图层控制面板
```

**职责边界**：面板负责交互组装参数（不计算）；service 负责全部空间计算（不渲染）；页面负责图层注册与结果分发（不写算法）。图层一律经 BLM，禁止直接操作 renderer 图层。

**交互细节**：设施类型按钮为三态（默认→选中→3s 自动确认或点外部确认）；权重滑块只重置确认计时器、不触发请求；雷达图轴名点击可互斥显示对应 POI 图层。

**设施 POI 图层与呼吸（02 §4.1 交互契约）**：点击雷达轴名（如「医院」）→ 页面按 `facility-poi-<type>` 注册点图层（设施色，`FACILITY_COLORS_MAP` 单一事实源）+ 对该类型**全量 POI 多点呼吸**（`startBreathing(points, color)`，`MapRenderer` 双引擎公共能力，支持单点/多点与自定义色）；再点同一轴名 → 撤图层 + `stopBreathing`。

- `facilityPoi` 是**参与评分的全部 POI**，不是覆盖区内的子集：评分按最近设施距离线性衰减，覆盖区外的设施同样贡献得分（可能拉低），故必须随结果全量返回，否则地图上看到的点与评分依据不一致。

**评分语义（应然契约，审查比对基准——专项8 3.3/3.7 的标尺）**：

- 单因子得分 = 最近设施距离的线性衰减：`距离 ≥ 半径 → 0`，否则 `(1 - 距离/半径) × 100`（`linearDecay`）
- `breakdown[key]` = 单因子百分制得分（1 位小数）；`score` = 加权平均 `Σ(得分 × 权重) / Σ权重`（1 位小数），不是加权和
- 半径 = `importanceToRadius(defaultRadius, importance)`，系数表 1:0.4 / 2:0.7 / 3:1.0 / 4:1.5 / 5:2.2（默认权重：医院 1.2 / 小学 1.0 / 初中 1.0 / 公园 0.8 / 公交 0.6 / 商场 0.7）
- 语义边界：距离=0 → 满分 100；距离≥半径 → 0；权重 0 不贡献分子但**仍计入分母**（`totalWeight += weight` 对所有选中类型）；某类型无设施 → 该因子 0 分且权重仍计入分母（拉低总分，属设计语义）
- "无重叠区域"是**合法空结果**（`data` 内标记），不是错误信封（422）；结果不足 TOP 10 不补空位
- 关键历史教训：防御分支曾静默失效（`isEmpty?.()` 调用不存在的方法 / `||` 兜底被优先级绕过），评分语义因此失真——详见 5.6 不变量

### 4.2 预测分析（Forecast）

```
控件变化（指标 / 置信度 / 时间 / 播放）
   │   三值合并 watch + 300ms 防抖（拖动不逐次发请求，停手才发起）
   ▼
doForecastUpdate → startTransaction（新事务启动即取消旧请求，事务 ID 存 store）
   │   Promise.all 并发三个请求，共享 signal
   ├─ GET /forecast/timeseries        → 折线图（按月数据，12 点窗口截取）
   ├─ GET /forecast/indicator/:type   → 柱状图（cargo+container 两指标 × 三港）
   └─ GET /forecast/map               → 地图图层（heatmap / geojson）
   ▼
图表写页面本地 ref；图层经 BLM updateData（key: forecast-{indicator}，切换指标自动显隐）
```

**事务语义**：指标/时间/置信度任一变化 → 新事务 → 旧请求被 abort。播放模式 500ms/tick，每 tick 重置防抖。**当前行为是"取消旧请求重新发"**；"快速重放不丢动画"是期望改造项（未定，见台账方向）。

**已知边界**：cargo 为模型基线（无置信度滑块）、berth/traffic 为合成示意数据（诚实标注）；播放中 429 限流静默降级不弹 toast。

**预测数值自洽（应然契约，审查比对基准——专项8 3.4/3.8/3.12/4.1 的标尺）**：

- 确定性：同一输入必须同一输出——随机源必须显式种子（`seededRandom`），禁止 `Math.random` 直接参与业务数值
- 数值合理性：预测值不得为负、不得发散到荒谬值（10 年外推 2031 边界闭合）；置信度 0.8-1.2 对结果的影响单调且有界
- 双通道口径一致：模型产物（cargo）与引擎外推/合成（container 确定性趋势外推 + berth/traffic 合成示意——816-专项8 发现8 修订口径）在拼接点无跳变；年→月聚合口径统一（12 个月均值 vs 年值）
- 回测可复现：README 宣称的 MAPE ≤ 2.3% 必须能用回测脚本重跑复现，口径（窗口/公式）有文档
- 合成/模拟处必须诚实标注（UI/注释/README 三处一致）——叙事与行为不符是面试翻车点

### 4.3 浸没分析（Flood Analysis）

```
水位滑块（0-15m，step 0.1；刻度 0/2/10/15 可点击）
   │   100ms 防抖
   ├─ triggerFloodAnalysis  → floodAdapter → 数据源分流：
   │      api    → Nest：6 档（0/2/5/8/10/15）向上取档，返回 requested + actualWaterLevel
   │      online → FastAPI：预计算表 0.0-25.0 共 251 档，查表秒回，miss 才演算
   └─ triggerImpactAssessment → 受影响设施评估
   ▼
渲染三层：水面（3D-only）+ 淹没范围多边形（双引擎）+ 受影响设施点（双引擎）
```

**档位语义（向上取档）**：请求 2.5m 时 api 模式返回 3.0m 档数据——**故意为之**：宁可高估风险不可低估，前端通过 requested/actual 感知差异（当前仅日志，不弹警告）。

**真 DEM**：演算基于陆地真 DEM（填洼产物）；**海底 DEM 尚未获取**（当前海洋区域按海平面处理）——这是浸没分析真实性的最大短板，也是新选址分析的铺垫工作之一。

**淹没语义（应然契约，审查比对基准——专项8 3.5/3.6/4.5 的标尺）**：

- 水位 0 → **无淹没**（海面种子正确性的验证点）；淹没面积随水位**单调不减**（低水位面积 > 高水位面积 = 数据错）
- 档位取整语义：api 模式 6 档（0/2/5/8/10/15）**一律向上取档**（`find(档 ≥ 请求)`）——宁可高估风险；请求与返回差异经 `actualWaterLevel` 透传（前端至少可感知）；online 模式 251 档（0.0-25.0，0.1m 步）精确命中，miss 才触发演算
- 双通道一致：查表命中结果必须 = 兜底演算结果（同水位两条路径对照）；档位表边界（如请求 15.1）语义明确（拒绝或取 15，不能静默）
- 设施评估（受影响设施/损失）与淹没范围必须同源（同一水位档的演算产物），不得跨档混配

### 4.4 快照恢复（保留态）

```
业务页（选址/预测/浸没）→ 跳 /profile 前 saveState（内存快照）
   │   路由切换本身不重置任何 store；只有登出触发 resetStores
   ▼
返回业务页 → onMounted consumeState（一次性消费）→ 恢复控件状态与结果图层
   │   离开业务页但不走 /profile → clearState（清空）
   ▼
快照是内存级：同标签页路由导航之间存活，刷新/关闭即失（设计如此）
```

### 4.5 登录态期望

- **设计期望（已拍板，2026-08-29 收口实现）**：仅「收藏」功能要求登录（收藏数据存在服务端 `/api/favorites`）；地图浏览、选址分析、浸没分析均不要求登录态（分析端点为纯计算免鉴权）。

## 五、关键机制

### 5.1 Renderer 生命周期

- 渲染器实例**复用不销毁**；3D→2D 切换必须 `unmount()`（暂停渲染 + 空闲销毁）；30s 内切回由 mount 取消销毁。
- 能力缺失（如 2D 上调用水面）由调用方检查跳过，禁止空实现 stub。
- 引擎切换后图层由 BLM `reapplyAll` 按 registry 重建。

### 5.2 图层管理（BLM）

| 语义                | 行为                                                                                      |
| ------------------- | ----------------------------------------------------------------------------------------- |
| 注册即占位          | `register`：数据未就绪也可注册（visible 控制），重复 key 忽略                             |
| updateData          | 未注册则 throw；不可见只缓存；可见且无实例 → 补建，有 → 更新                              |
| registry vs catalog | **registry 是可见性唯一权威**（实例内 Map）；catalog 只是面板元数据（引擎切换会清空）     |
| reapplyAll          | 引擎切换后按 registry 重建全部图层，单层失败不拖垮整批                                    |
| 特殊图层            | 水面（不在 \_layers）经 adapter `setVisibility` 分派，禁止落入 pendingVisibility 失效路径 |

### 5.3 水面 vs 淹没多边形（容易混淆，明确区分）

|        | 水面（flood-water-surface）                                               | 淹没多边形（flood-area）                 |
| ------ | ------------------------------------------------------------------------- | ---------------------------------------- |
| 是什么 | **水平面片**，高度 = 当前水位                                             | **被淹区域**（DEM 演算出的不规则多边形） |
| 回答   | 水位涨到几米（三维位置）                                                  | 哪些地方被淹了（受灾范围）               |
| 渲染   | **仅 3D**（能力接口 Water3DCapability，Cesium 实现；2D 下注册但跳过创建） | 2D/3D 均可                               |
| 数据   | 水域坐标文件                                                              | 真 DEM 演算产物                          |

**注意**：水面是 3D-only 是设计；但 2D 下"图层控制面板有条目却无法控制"是已知交互缺陷（台账）。

### 5.4 GCS 布局三档

- 档位 1（≥960px）：桌面等比例布局，面板原位显示
- 档位 2（640-959px）：抽屉模式，面板收进 MobileDrawer，底部 nav 出现「菜单」键
- 档位 3（<640px）：抽屉紧凑，底部 nav 收敛为 3 键
- 断点判定在 `useGCS`（showPanels / showTopArea / navCompact），组件用 computed 消费，**禁止组件内重复判定**。

### 5.5 store 重置链

- 登出（user → null）→ `resetStores`：清业务快照 + 地图状态 + 业务 store（保留 mapType / baseLayerKey 等用户偏好）。
- 路由切换不触发重置；保留态走快照工厂（见 4.4）。
- store 职责单一；多 store 协调重置放应用层，不放"壳 store"。

### 5.6 数值与防御不变量（全局应然，专项8 的比对标尺）

> 审查/开发时以上述各流程语义 + 以下不变量为"应然"，比对代码"实然"。历史教训：防御分支静默失效比没有防御更危险（曾实锤 `isEmpty?.()` 调不存在方法、`||` 兜底被优先级绕过，均已在 45df38a 修复）。

1. **确定性**：同输入同输出；随机源必须显式种子；无对象遍历顺序依赖。
2. **防御可触发**：防御分支必须证明在真实输入下可达——`?.()` 调用的方法必须存在；`||`/`??` 兜底必须覆盖整个分母表达式（`a / (b || 1)` ≠ `a / b || 1`）；`Math.cos(π/2) = 6.12e-17 ≠ 0`，不能用 `|| 0` 兜底；`parseFloat(x) || 0` 会把坏数据伪装成 0（应显式处理）。
3. **粗筛保守**：rbush bbox 粗筛必须是精确结果的超集（宁大勿漏）；`kmToDegreeOffset` 需纬度修正且保守扩张；粗筛漏配 = 结果错误。
4. **缓存 = 重算**：缓存命中结果必须等于清缓存重算结果；key 必须覆盖全部参数维度（indicator/time/confidence/waterLevel），缺维度 = 串数据。
5. **NaN 禁传播**：计算链入口 `Number.isFinite` 守卫；展示层 `NaN → '--'` 兜底；任何路径不得把 NaN 静默算进结果。
6. **单位一致**：turf 距离米/面积平方米；buffer 半径千米（`units: 'kilometers'`）；展示换算（m²→km² 除 1e6）集中在展示层；常量带单位注释。
7. **浮点比较用 epsilon**：禁止 `score === 100` / `area === 0` 直接比较。
8. **空与错误分离**："业务空结果"（无重叠区域/无匹配）走合法 `data`，错误走 `error` 信封——前端两态 UI 语义不同。

## 六、新增业务模块（五步）

```
1. business/manifest.ts 加一条（路径/引擎/标题/导航/组件）
2. 建 business/<name>/ 模块目录（页面 + components + composables + constants）
3. 取数层：业务专属取数 composable 放 business/<name>/composables（经 useApiRequest 统一入口）；
   仅当取数逻辑跨业务复用才建 services/adapters/<name>Adapter.ts（规则 R5）
4. stores/<name>Store.ts（如需要跨页状态）
5. types/ 补充业务类型
```

路由 / 导航 / meta 由 manifest 自动生成，不需改 router/App.vue。

## 七、校验命令

```bash
npm run cruise          # R9/R10 架构契约（分层 + 入口收口，0 violation 门禁）
npm test                # 前后端测试（含数据流相关用例）
# 契约一致性（mock vs api）校验脚本已移除，靠人工保证（见台账）
```
