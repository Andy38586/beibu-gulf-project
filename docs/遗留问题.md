# 遗留问题

> 2026-08-09 复查：原 7 项遗留问题已闭环，新增 3 项未闭环（墓碑代码残留）。

## 一、DEM 渲染管线

1. **增量② 后端真洪涝** — ✅ 数据源为 DEM 派生已确认
   - `tools/dem-pipeline/04-generate-flood-data.py` 从 `dem_4326.tif` 派生生成 4 个 JSON（facilityPoints/floodArea/floodStatistics/terrainProfile）
   - `flood_engine.py` 从 DEM 读连通性淹没演算

2. **增量③ Cesium 真地形** — ✅ 真 3D 地形已实现
   - `CesiumRenderer.ts:_setupTerrain()` 使用 `CesiumTerrainProvider.fromUrl('/static/terrain/')` 加载真地形瓦片
   - `backend/static/terrain/layer.json` 存在，CTB 预切片 z0-12，heightmap-1.0 格式
   - 同时有 `_setupFullDem()` 全量 DEM mesh 备选方案
   - 通过 `setTerrainEnabled()` 提供开关切换

3. **静态资源位置** — ✅ 已统一走后端 static
   - DEM 产物：`backend/data/flood/dem/`（栅格）+ `backend/static/dem/`（可视化产物）
   - 地形瓦片：`backend/static/terrain/`
   - nginx 配置 `/static/` → `backend/static/`，`/data/` → `frontend/dist/data/`
   - 不在 frontend/public 下，已统一走后端托管

## 二、性能优化

4. **Phase 0 监控埋点** — ✅ PerfReporter 已落地
   - `shared/utils/perfReporter.ts` 完整实现（359行），采集 FCP/LCP/CLS/TTI/FPS/API 耗时/错误计数/Cesium 耗时
   - `main.ts:36` 调用了 `initPerfReporter()`
   - 生产默认开启（纯聚合开销 <0.1%），dev 额外存明细
   - 有配套测试 `perfReporter.test.ts`

5. **Phase 2 Cesium 预热** — ✅ 已实现
   - `renderers/index.ts:preloadCesium()` 使用 `requestIdleCallback` 空闲时段执行 `ensureCesiumLoaded()`
   - 预热 Cesium 脚本 + CesiumRenderer 模块 chunk
   - 与 createRenderer 共享 `_cesiumLoadPromise`，幂等防重复加载

6. **Phase 3 ECharts 增量 diff** — ✅ 统一使用 `replaceMerge: ['series']`
   - `useECharts.ts:92` 统一 `setOption(option, { notMerge: false, replaceMerge: ['series'], lazyUpdate: true })`
   - `useRadarChart.ts:186` 同样使用 `replaceMerge: ['series']`
   - `useChartBase.ts` 给 series 设置稳定 id，配合 replaceMerge 正确对位
   - 所有图表组件（LineChart/BarChart/RadarChart）均走同一路径

7. **Phase 4 DEM 预切片** — ✅ 已切片
   - `backend/static/terrain/layer.json` 存在，CTB 预切片 z0-12
   - 瓦片格式 `heightmap-1.0`，路径 `{z}/{x}/{y}.terrain`
   - `CesiumRenderer.ts` 通过 `CesiumTerrainProvider` 加载

## 三、墓碑代码残留（2026-08-09 新增，未闭环）

8. **`mapStore.map` + `setMap` 是 write-only 死状态**
   - `mapStore.ts:86` 定义 `map`，`:100` 定义 `setMap`，`:261/268` 导出
   - `UnifiedMap.vue:224` 唯一写端（`mapStore.setMap(...)`），**全库无任何读端**
   - `mapStore.map` 的 grep 零命中，`shallowRef<unknown>` 无类型收缩价值
   - 修函数时删了死函数，**修字段没删**

9. **`MapRendererKey` 还活在 provide 一侧，全库无 `inject`**
   - `useMapRenderer.ts:10` 定义，`UnifiedMap.vue:55` 仍 `provide(MapRendererKey, currentRenderer)`
   - `inject(MapRendererKey)` 的 grep **零命中**——业务组件已全部走 `mapStore.currentRenderer` / `useMapControls`
   - 删了消费端和工厂，provide 端残留（上一轮只删了死函数，没删 provide）

10. **选址持久化仍是两条通道**
    - `SiteSelectionPage.vue:90` 仍写 `mapStore.setAnalysisResult(result)` → sessionStorage `ANALYSIS_RESULT_STORAGE_KEY`
    - **但恢复只走 `siteSelectionPersisted.consumeState()`**（`SiteSelectionPage.vue:220`，`onMounted` 恢复路径）
    - 即 `lastAnalysisResult` 对应的 sessionStorage 通道**只写不读**（仅测试断言消费），与 `siteSelectionPersisted` 双通道并存

## 四、已闭环（旧文档已标注）

- 港口/行政区 2D 不显示 — ✅ commit 814a671 修复
- 渲染器类型注解豁免 — 已豁免，owner 终态决策
- 数据流契约分层规则 — 已由 dependency-cruiser 强制执行
