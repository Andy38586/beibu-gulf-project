# 问题副本-2026-08-10-专项审查（重建版）

> **定位**：2026-08-10 专项审查产物（39 条），2026-08-12 逐条代码取证归类完成。
> **重建说明**：本文件在 2026-08-12 文档清理时被误删（未入库无法从 git 恢复），
> 归类决议（第七节）为完整记录，原始条目（P0/P1/P2 描述）待按需补充。

## 一、P0（致命/高危阻断，2 项）

（原始条目描述已随误删丢失，归类结论见第七节；如需原始取证细节可依正式编号查已解决/待解决台账）

## 二、P1（高：正确性/稳定性/安全非致命，13 项）

（同上）

## 三、P2（中：待改进/低危 bug/语义不一致，重点 8 项）

（同上）

## 四、刻意设计（不转正，留档供面试话术）

（同上）

## 五、专项间矛盾记录（裁决用）

（同上）

## 六、面试压力测试审查（2026-08-10）核实结果

### 6.1 已修复（报告判定不再成立，全部闭环，明细见第七节归类决议）

### 6.2 仍属实（候选转正）

### 6.3 面试压力测试报告的核心结论（话术资产，不转正）

## 七、归类决议（2026-08-12，本副本 39 条全部闭环）

> 逐条代码取证归类：**已解决** = 已落地（对应正式编号）；**设计豁免** = 刻意设计/话术资产，不占名额；**转正 open** = 仍属实，已按各系列最大号后接续分配正式编号移入 `待解决问题.md`。

| 草稿编号 | 归类 | 去向 / 依据 |
| --- | --- | --- |
| 副-01 | 已解决 | **a050**（W4-01 合并：UnifiedMap 卸载生命周期收口） |
| 副-02 | 已解决 | **a051**（坐标字段别名归一） |
| 副-03 | 转正 open | **a064**（`CesiumRenderer.ts:906` DEV 门控仍在，取证属实） |
| 副-04 | 转正 open | **b065**（`floodStore.ts:108-116` consumeState 混合读活状态，取证属实） |
| 副-05 | 已解决 | **b060**（预测 store 快照/请求缓存收口） |
| 副-06 | 已解决 | **b060**（store action 化 + 严格模式） |
| 副-07 | 已解决 | **b060**（timeRange 已删、currentTime 单源、action 化收口，取证属实） |
| 副-08 | 已解决 | **a052**（渲染器监听/rAF 配对注销） |
| 副-09 | 已解决 | `renderers/index.ts:33-35` 已加 `.catch(() => {})`（预加载失败静默） |
| 副-10 | 已解决 | **c034**（雷达图重试定时器清理） |
| 副-11 | 已解决 | **b059**（预测请求链路收口 adapter/composable） |
| 副-12 | 已解决 | **b061**（shared 业务耦合清除） |
| 副-13 | 转正 open | **d080**（`plansController.js:6,10` 仍直连 plansRepo 无 service 层） |
| 副-14 | 并入 open | **z071**（组件直改 store，与路由守卫 store 重置同批次） |
| 副-15 | 部分解决 + 转正 | 坐标 (0,0)→skip 已解决（**a059**）；列表 key/字段丢弃残留 → **c037** |
| 副-16 | 转正 open | **d081**（`facility.ts:15` "米"注释 vs `siteAnalysisService.js:82` 公里解释） |
| 副-17 | 已解决 | P0-3 死状态清理批次（`cacheData` 零调用已删等） |
| 副-18 | 转正 open | **d082**（ErrorCode/包围盒/水位上限/riskLevel 常量不一致） |
| 副-19 | 已解决 | **z094**（online impact schema）+ **b062**（单断言/占位字段） |
| 副-20 | 转正 open | **a065**（CRS 散落无统一投影封装） |
| 副-21 | 未解决（open） | **z071**（已长期 open） |
| 副-22 | 部分解决 + 转正 | z-index/transition/图表色已收口（**c035**）；图例/瓦片缓存残留 → **c038** |
| 副-23 | 已解决 | **d074**（生产 CORS 禁 localhost + .env.example 对齐） |
| 副-24 | 已解决 | 高程基准 CLAMP_TO_GROUND（副-24 自证 + a050 批次验证） |
| 副-25 | 已解决 | 死状态清理（副-25 自证） |
| 副-26 | 设计豁免 | DEM 缺失属已知降级路径（hillshade PNG fallback + 251 档预计算表） |
| 副-27 | 已解决 | **z099**（CesiumRenderer 1245 行、any 50→26、`_setupFullDem` 已删） |
| 副-28 | 已解决 | cruise warn→error（副-28 自证） |
| 副-29 | 已解决 | husky 变量名 bug 修复（副-29 自证） |
| 副-30 | 证伪 | floodAdapter 纯透传不成立（副-30 自证） |
| 副-31 | 已解决 | 注释已改写为分层理由（副-31 自证） |
| 副-32 | 设计豁免 | 叙事已升级为"预计算 251 档 + online 可切"，面试话术资产 |
| 副-33 | 已解决 | **a054**（业务图层增量更新） |
| 副-34 | 转正 open | **a066**（layerCatalog key 无命名空间，取证属实） |
| 副-35 | 转正 open | **z103**（覆盖率阈值 15-25% 形同虚设） |
| 副-36 | 转正 open | **z104**（createPersistedState 仅内存级快照） |
| 副-37 | 已解决 | **z100**（关键模块 README 补齐，注册点已文档化） |
| 副-38 | 设计豁免 | 认证前端可伪造——面试话术留档（真鉴权在后端 Cookie） |
| 副-39 | 设计豁免 | Entity API 量级够用 + Primitive 迁移路径——话术留档 |
