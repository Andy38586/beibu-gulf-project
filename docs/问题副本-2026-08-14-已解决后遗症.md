# 问题副本 · 2026-08-14 已解决后遗症（审查批次）

> **定位**：2026-08-14 对《已解决问题.md》（主本，317 条）逐条审查后识别的**小尾巴 / 雷 / 台账勘误**清单（副本性质，规则见 `docs/待解决问题.md` 头部）。主本与精选版副本均保留原样。
> **产生方式**：5 分节并行审查（地图层/业务层/组件层/后端层/暂未归类），执行记录见 `docs/audits/2026-08-14-已解决台账审查/已解决台账审查-执行记录.md`。
> **标号体系**：沿用主台账原编号（唯一合法编号）；`discover` = 原问题发现日；转正主台账时沿用原编号体系分配（不追溯副本编号）。
> **审查结论**：真尾巴/雷 **41 项**（其中 3 项已承接进待解决台账、3 项人工动作、2 项仍 open）＋ 台账勘误 11 项 ＋ 系统级雷 3 项。

---

## 一、真尾巴（修复不彻底 / 需返工 / 留后手，转正候选）

| 编号 | discover | 等级 | 尾巴（证据） | 现状与建议处置 |
| --- | --- | --- | --- | --- |
| a020 | 20260801 | p3 | "浏览器三项(FCP/2D→3D 切换/557 小区渲染)待人工验收记录"——人工动作未完成，后遗症-08-10 曾误标 a024。 | **人工动作待执行**：补浏览器三项验收记录后闭环。 |
| a043 | 20260806 | p3 | 已解决档案称"onUnmounted `resizeObserver?.disconnect()` 配对注销"，但待解决 md 残留"未核实 onUnmounted disconnect"——两文档矛盾。 | **仍 open**：核实 UnifiedMap.vue onUnmounted 实际代码后统一口径。 |
| a042 | 20260806 | p2 | "直进3D白图(时序脆弱防御)"——防御式补丁（nextTick×2）掩盖根因，headless 实测不触发（被 route watch immediate 先置 '2d' 掩盖）。 | 真浏览器构造直进 3D 场景验证防御是否可靠；不可靠则从时序根治。 |
| a027 | 20260802 | p2 | terrain 加载"失败静默降级 hillshade 贴图"无告警；生产侧首次修复不彻底（服务器镜像 dist 旧，08-10 才 `--no-cache` 重建）。 | terrain 失败打可观测日志（并入 z095 批次）；部署清单加 dist 版本核对。 |
| b025 | 20260801 | p1 | `INDICATOR_SOURCE` 将 berth/traffic 硬编码 'mock'，"不受全局 VITE_DATA_SOURCE 切换影响"——修复仅诚实标注+角标，**未接线**，切 api 后指标仍合成 mock（半接入）。 | 指标源接入 VITE_DATA_SOURCE 统一驱动；禁止以注释替代接线。 |
| b058 | 20260811 | p3 | "仅补齐 FastAPI 缺失的 riskLevel 字段"——后端 FastAPI 补齐动作未见完成证据。 | 确认 flood-service 侧 riskLevel 已补齐，否则双模式仍有隐性不一致。 |
| c021 | 20260810 | p2 | "闭环（并入登录态期望改造）"——"仅收藏需登录"的登录态放开改造方向仅一句话，无承接编号、无设计决策摘要。 | 显式登记 open 实施项并给等级（"闭环≠未做"可追溯）。 |
| c029 | 20260810 | p3 | 空数据占位"未确认"退化为实现补丁；跳过项（PaginatedListPanel/RadarChart）只写"不改"无豁免结论。 | 解决记录补"已确认/已豁免/待补"三态结论。 |
| c031 | 20260810 | p3 | useAsyncData.ts 0 调用方，仅"保留 shared 待首个消费方接入"，无等待期策略。 | 登记待接入或删除死工具，勿以标注长期替代决策。 |
| c033 | 20260810 | p3 | 抽屉面板用 `height: auto !important` 硬覆盖解除布局链——与 c016"用 CSS 变量消除 !important"方向相反，且作用于 `.GCS-panel > *` 全部子元素。 | 补非抽屉模式误中验证 + 抽屉视觉断言测试。 |
| d044 | 20260802 | p2 | git-clean-history.sh 建好但"标注上线前手动执行；不可逆操作不自动跑"——明文探针账号仍留在 git 历史可读。 | **人工动作待执行**：与 d056 合并跟踪，上线前必须执行。 |
| d056 | 20260801 | p2 | "遗留人工动作（无法代码代劳）：天地图控制台轮换 key…git-clean-history.sh"（后遗症-08-10 误标 d038）。 | **人工动作待执行**：天地图 key 轮换 + git 历史清理，挂上线核验单。 |
| d073 | 20260806 | p2 | 21MB 预计算档位表"数据文件化"，交付方式（入库 vs 构建期生成）未明；"查表 miss 回退 LRU 动态演算兜底"——兜底恰是 d069/d072 曾崩溃路径。 | 明确数据文件交付方式；LRU 兜底 + 查表命中补测试。 |
| d066 | 20260802 | p2 | CI deploy job 依赖 `DEPLOY_WEBHOOK_URL`+`DEPLOY_WEBHOOK_TOKEN` secret 与服务器 webhook，无"首次真部署回执"证据。 | 补 CI deploy 首次真部署回执（与 d078 演练记录同级）。 |
| d077 | 20260811 | p2 | Dockerfile 装 `nginx-mod-http-brotli`（alpine community 仓库）"部署时构建验证"——依赖第三方仓库可用性且构建验证无证据。 | 部署演练记录登记真实构建/线上 brotli 生效核验。 |
| d055 | 20260801 | p3 | "边界用例补充测试留作可选项（记入副本）"与"闭环"标签冲突。 | 登记低危测试补充项或明确放弃，闭环结论不含"留作可选项"。 |
| z030/z032 | 20260730 | p1 | z030/z032 及 main.ts:73 注释宣称"transport 数组 + 预留 addLogTransport 钩子"，实读 logger.ts **无该钩子**（已被删）——台账与代码双漂移，后人接 Sentry 会接错。 | 已承接 **z072**（待解决 open）：按 z072 方案回写 z030/z032 + main.ts 注释。 |
| a034 | 20260730 | p3 | "后端改名 spatialQuery.js 暂未执行（命名变更影响面大，留作后续演进项）"。 | 登记为演进项或明确放弃。 |
| 渲染器测试注释 | 20260814 | p3 | z046 已移除渲染器源文件 @ts-nocheck，但 4 个测试文件注释仍称"带 @ts-nocheck"（waterSurface/geojson/heatmap/culling）。 | ✅ **已修复（2026-08-14 审查批次2，C-10）**：4 处注释修正为"运行时成员经断言暴露"，合并 z046。 |

## 二、台账勘误（质量门禁结论，随归档修订）

| 编号 | 问题（证据） | 修正动作 |
| --- | --- | --- |
| d048 vs d056 | d048（solve 08-01）"无真 key 入版本库；密钥未泄露"与 d056"天地图密钥 e4cef34…进过 7 个 commit"**直接矛盾**——d048 过早闭环。 | d048 修订为"当前工作树无真 key；历史已泄露见 d056"。 |
| b031 编号冲突 | b031(8.2) 与 b031-401软登录缺口 同号，条目内标"待统一修复"但无处置记录。 | 统一重编号（b031(8.2) → 后续空号）或显式归档。 |
| c024↔c018 | BASE_YEAR/END_YEAR：c018 称"收进 forecast/constants.ts（归零）"，c024 称"保留 shared（红线）"——同一文件两处结论冲突。 | 澄清实际存放位置，两处补指针。 |
| 后遗症文件错号 | 已解决后遗症-08-10：a024→应 a020；a025→应 a027；c015→应 c017；c019→应 c011；line24 b015→应 b032；d038→应 d056。 | 该文件已归档，勘误并入本副本与执行记录。 |
| z089/z036、z047/z078 | 同一原 z042 撞号：已解决台账标 z089（"z078 亦撞"），后遗症标 z036（"z047 亦撞"），renderers.d.ts 处置"保留 vs 删除"冲突。 | 建"z042→z089/z036"对照表，统一叙事。 |
| z042 跨文件复用 | 已解决 z089 是原 z042 撞号而来；待解决 z042 却是"函数过长"另一主题——同一编号两库并存且主题不同。 | z089 头注显式区分两条 z042。 |
| b022/b024→b031(8.2) | b022/b024 设"默认 mock"，b031(8.2) 推翻改"默认 api"——返工链未交叉引用。 | b022/b024 补注"已被 b031(8.2) 推翻"。 |
| b042→b057 | b042 建立 sessionStorage 版本迁移机制+5 回归测试，b057 整条删除通道——返工链未交叉引用，b042 白做。 | b042 补注"被 b057（08-10）废弃"，确认测试清理。 |
| z003 / z007 / z089 | z003 悬空引用"剩余残留见 z028"（z028 已闭环）；z007"见待解决问题.md a018"（a018 已闭环）；z089 自述"部分收口待后续批次"（实际已由 z046 收口）。 | 三处补注收口去向，消除悬空引用。 |
| 待解决 b044 双计数 | 原待解决 md 统计 p1 与 p2 各计 b044 一次。 | 已在 2026-08-14 待解决重整中修复。 |

## 三、系统级雷（建议专项承接）

| 雷 | 证据 | 建议 |
| --- | --- | --- |
| FastAPI flood-service 无测试门禁 | d072 自述"flood-service 无测试基础设施（venv 无 pytest/httpx），d069 修复零守护直接上线，回归只能靠用户实测暴露"——Express 112 全绿掩盖 Python 回归。 | ✅ **已部分缓解（2026-08-14 审查批次2/4）**：venv 已具备 pytest/TestClient（本批次实测 6+4 用例通过）；新增 test_real_engine.py 真演算回归（8-2，CI 无 DEM 自动 skip）+ test_main 参数同步（b027）。CI 门禁（d067 job 覆盖 Python 侧）仍为 v3 待办。 |
| 返工链不交叉引用纪律缺失 | b042→b057、b022/b024→b031(8.2)、d069→d072 三组"修复被后续修复推翻"均未两端标注。 | 入档质量门禁补第 5 条："本次修复是否推翻/废弃近期已解决条目？是 → 两端交叉引用"。 |
| CI/部署环境假设无验证 | d053/d067（外网可达）、d066（webhook/secret）、d077（alpine community 包）均为"假定可用"，无回执证据。 | 首次真执行回执入档（d078 演练同级）。 |

---

> 本副本为审查批次产物；转正/闭环后按 `docs/待解决问题.md` 副本规则处置。
