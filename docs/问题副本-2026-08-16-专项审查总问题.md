# 问题副本 · 2026-08-16 专项审查总问题（816 全量审查批次1 整合版）

> **定位**：816 批次（2026-08-16-全量审查批次1，9 份产出：8 专项 + 专项5 双窗口并行）全部发现整合——统一清单，供用户逐条裁决后转正 `docs/待解决问题.md`（分配正式编号，`discover`=2026-08-16；专项1 为 814 批次产出，`discover`=2026-08-14）。
> **统计（整合后）**：原始发现 **210 条**（专项1 18｜专项2 25｜专项3 19｜专项4 15｜专项5主 27｜专项5并 29｜专项6 29｜专项7 35｜专项8 13）→ 已在台账/负结果 **19 条**（见第一节，不重复立案）→ 并入既有台账/跨专项同根因合并 **21 条**（见第二节）→ **新立案 170 条**（P0×3［含专项5主发现1 审查期间已修］｜P1×12｜P2×67｜P3×88，等级以各专项副本统一定级为准；另 812 批次残余移交 3 条见四·五节：P2×1｜P3×2）。
> **裁决流程**：逐条裁决 → 转正主台账；原始证据链见 `docs/audits/816/*/专项问题副本/` 与 `实施日志/`；批次合并建议与台账对账见 `docs/audits/816/816问题副本/00-批次汇总.md`（M1-M14 合并族与本文件第二节对应）。
> **专项5 双窗口**：主窗口 27 条 / 并行窗口 29 条为同一专项并行产出，发现高度重叠，**裁决以合并去重后为准**（两份副本均保留归档）。
> **816 已修转正（2026-08-17）**：`03-P2P3-设计vs真bug判定.md` 判定的 109 条真 bug 中 105 条已修复（P2×52｜P3×53），按根因聚合登记入 `docs/已解决问题.md` **24 个正式编号**——a072（Q4 收口）/a073（渲染器生命周期）/a074（OL controls）/a075（渲染常量守卫）｜b081（store 纪律）/b082（面板双源）/b083（评分档位语义）/b084（预测交互）/b085（页面防御）｜c058（S7 颜色主题）/c059（S7 token）/c060（S7 布局）/c061（四态）/c062（死 provide）｜d104（档位数据）/d105（算法测试）/d106（日志部署）/d107（readiness）/d108（gzip）｜z120（工程卫生）/z121（构建产物）/z122（文档漂移）/z123（CI 门禁）/z124（死资产）/z125（类型契约）/z126（forecastAdapter 测试）；剩余 4 条登记不遗留决策（专项6 13 随 b034 裁决、专项5主 23 待提交动作、S7-41/42 待浏览器验收）。P0×3/P1×12 未动，保持待裁决。

---

## 一、去重对照（已在台账 / 已解决 / 负结果，不重复立案）

### 1.1 已在台账（复现确认，仍 open；转正时并案或沿用原编号）

| 原编号 | 台账关系 | 说明 |
| --- | --- | --- |
| 专项2 1-1（P1） | **b040**（open） | useAuth/useApiRequest 模块级业务状态确认仍存活（`useAuth.ts:46`、`useApiRequest.ts:30`），并入不重复立案 |
| 专项6 16（P2） | **b040** | 6.6 视角独立取证（user/token 模块级单例未进 Pinia），合并确认（M12） |
| 专项2 2-3（P2） | **b065**（open） | floodStore.consumeState 混合读活状态复现确认（两子 agent 同根因已合并） |
| 专项8 已在案 | **b065** | 子 agent C 独立取证复证（`floodStore.ts:108-120`） |
| 专项2 5-1（P3） | **c038**（open） | 底图瓦片无显式 HTTP 缓存头复现确认 |
| 专项6 17（P3） | **z118**（open） | core 层业务关键词兜底链残留确认（`LayerControlPanel.vue:69-93`），本批次补证 |
| 专项6 19（P3） | **z042**（open） | CesiumRenderer 1877→1593 行已部分回落（P8 合并假拆分后反弹），本体确认 |
| 专项6 14（P2） | **b045**（open） | 三业务模块子目录结构不一致确认 |
| 专项6 11（P2） | **b034 / d082** | 前后端类型/常量契约无自动跨端同步补充取证（gen-api-contract 不读 backend） |
| 专项5并 1-1（P1） | **z072**（open） | 错误上报完全缺失实锤确认（main.ts:75 过期注释仍在，logger 无 transport 钩子） |
| 专项5并 7-3（P3） | **z072** | main.ts:75 注释漂移同源，并案处置 |
| 专项5并 2-1（P2） | **d056**（代码侧已闭环） | 天地图 key 历史泄露：人工动作（控制台轮换 key + 域名白名单）未执行，挂人工核验单 |
| 专项5并 4-6（P2） | **z064**（open） | 无集成/E2E 测试确认真实未闭环（cypress/playwright 0 命中） |
| 专项5并 6-3（P2） | **b045**（open） | 三业务模块结构不一致确认（业务层目录同构视角） |
| 专项5并 6-5（P3）/ 7-4（P3） | **d082** | 后端 BBOX 内联字面量 + 水位上限 15 vs 25 注释矛盾，补充证据随 d082 处置 |
| 专项7 S7-35（P2） | **c052**（open） | 半透明叠层未 token 补证（暗色下与 `--GCS-overlay-tint` 脱钩），并入 |
| 专项7 S7-37（P2） | **c056**（open） | 底图无暗色变体补证（Cesium 3D 侧未闭环），并入 |
| 专项8 已在案 | **b044**（open） | 选址 O(n²) 复现 1.65s（同量级），未重复立案 |
| 专项4 补充取证 | **a064/a065/a066/c038/z105** | 本批次补充取证不重复立案（DEV 门控/CRS 散落/layerCatalog key 无命名空间/瓦片缓存/terrain） |
| 专项5主 交叉引用 | **z072/c057/z064/z038/b040/d051/d054/d082/d102/d053/d067/z040/z102/b044/z077** | 执行记录交叉引用台账确认，不计入本副本新立案 |

### 1.2 台账陈旧建议复核关闭（代码已解决/已覆盖，台账仍 open）

| 台账 | 本批次证据 | 建议 |
| --- | --- | --- |
| z116 provide/inject 无 injectionKey | 专项2 8-2；专项3 5.7；专项5并 7-5（`provideKeys.ts:29-39` 全 typed InjectionKey） | 复核关闭 |
| z117 zod 覆盖率未核对 | 专项3 7.2（17 处 data-consuming 调用点全部传 schema） | 复核关闭 |
| z038 setLayerVisible 覆盖 | 专项2 8-2；专项5并 7-5（`mapStore.test.ts:129-146` 已有用例） | 复核关闭 |
| d081 设施半径单位注释 | 专项5并 7-5（`facility.ts:15` 已改"公里"） | 复核关闭 |
| z077 后端 no-console | 专项5并 7-5（业务代码 console 已收敛仅剩 logger 内部） | 复核关闭 |
| d102 /flood-online 无限流 | 专项5主 台账对账；专项5并 7-5（nginx limit_req 已落地，工作树已修未提交） | 提交后复核关闭 |

### 1.3 豁免 / 证伪（负结果，防重复立案）

| 项 | 判定 | 依据 |
| --- | --- | --- |
| z071 无路由守卫做 store 重置 | **豁免**（设计：02 §4.4/§5.5 store 重置链由登出触发） | 专项2 全部子 agent 一致 |
| z065 渲染器 @ts-nocheck | **豁免**（现存 2 处均为 unplugin 生成文件） | 专项3 |
| 专项4 测量/绘制/3D Tiles/离线缓存等 11 项 | **豁免**（无实体/无需求/刻意取舍） | 专项4 三节 |
| 专项7 stylelint「CI 红灯 2 违规」 | **证伪**（主 agent 实测 exit 0 零违规） | 专项7 |
| 专项8 权重 0 计分母 / rankXiaoqu 确定性 | **通过**（代码与设计逐字一致，node 复现 45.5 分） | 专项8 |
| 专项7 双主题 key diff 12 项不对称 | **豁免**（radius/z-index 跨主题同值不复制，style.css:88-90 声明吻合） | 专项7 |

---

## 二、并入既有台账 / 跨专项同根因合并（21 条并入，不新建；裁决只立 1 条工单）

| 原编号 | 并入 | 说明 |
| --- | --- | --- |
| 专项5主 3（P1）+ 专项5并 6-1（P2）+ 专项6 8（P2） | **合并 1 条（M1）** | riskLevel 6 档阈值前后端双副本且行为已分叉（`floodAdapter.ts:52-63` vs `floodAnalysisController.js:26-33`；前端 `floodedKm2<=0` 条件与后端纯水位判定已漂移） |
| 专项5主 12（P2）+ 专项5并 6-2（P2）+ 专项6 22（P3） | **合并 1 条（M2）** | 根目录 `.tmp-p1.txt/.tmp-p2.txt` 残留：删除 + .gitignore 补 `.tmp-*` |
| 专项5主 13（P2）+ 专项6 5（P2） | **合并 1 条（M3）** | business/index.ts barrel 聚合导出多数无入口消费者 |
| 专项2 8-1（P3）+ 专项5并 6-6（P3） | **合并 1 条（M4）** | 死 provide MAP_STORE_KEY + core/README 过期引用（两副本已交叉引用，只立一条） |
| 专项1 7（P2）+ 专项3-0816-07（P2） | **合并 1 条（M5）** | affectedFacilities 同名不同义/不同型（`base.ts:55-56` 计数 vs `:83-84` 数组） |
| 专项5主 21（P3）+ 专项5并 5-2（P2） | **合并 1 条（M6）** | flood-service（FastAPI）无日志级别/轮转/结构化（print 直出） |
| 专项5主 15（P2）+ 专项5并 3-1（P3） | **合并 1 条（M7）** | dist 陈旧构建残留 + emptyOutDir:false chunk 堆积（z021 复发，18.2MB） |
| 专项5主 7（P2）+ 专项5并 5-1（P2） | **合并 1 条（M8）** | HEALTHCHECK 单维度（仅探后端 3000，nginx 前端面崩溃仍标 healthy） |
| 专项5主 4（P2）+ 专项5并 4-1/4-2/4-3（P2×3） | **合并 1 条（M9）** | 覆盖率门禁形同虚设（frontend 阈值 30% 过低 + backend 阈值未进 CI + 报告过期） |
| 专项5主 5（P2）+ 专项5并 2-2（P2） | **合并 1 条（M10）** | CSP 仅 Report-Only + unsafe-inline/unsafe-eval（XSS 纵深防御未生效） |
| 专项2 2-1（P2）+ 专项6 15（P2） | **合并 1 条（M11）** | 业务 composable 直写 store state 绕过 action（useForecastRequest/useSiteAnalysisApi） |
| 专项1 13（P3）+ 专项6 4（P2） | **合并 1 条（M13）** | ProfilePage 深路径导入业务 composable（`ProfilePage.vue:12`），走 business 桶入口 |
| 专项1 2（P0）+ 专项2 3-5（P3） | **2 条（M14，位置相同根因不同，可并 1 条治理）** | WaterLevelProfilePanel 卸载未取消在途请求（请求取消）vs 第三处 echarts.init（图表生命周期） |
| 专项1 14（P3）+ 专项2 2-2（P2） | **合并 1 条（取 P2 主体）** | siteSelectionStore.clearState 未复位 calculating（登出残留"分析中"） |
| 专项5主 14（P2）+ 专项5并 7-2（P3） | **合并 1 条** | visualization/ 与 views/ 无模块 README |
| 专项3-0816-08（P1） | **并入 b034 + z119** | 无类型同步机制（无 OpenAPI/生成脚本/契约测试/CI 对齐） |
| 专项4 转派项（P3） | **转派专项2** | AppLayout body class 副作用无清理钩子（`AppLayout.vue:63-70`，专项2 未单列，随本批裁决） |

---

## 三、新立案清单（170 条，按专项）

> 字段：原编号｜等级｜标题｜证据摘要｜整改建议。等级与证据以原副本为准（详见 `docs/audits/816/`）。原编号为批次工作编号，转正时由台账分配正式编号。

### 专项1 数据链（16 条，814 批次，discover:20260814）

| 原编号 | 等级 | 标题 | 证据摘要 | 整改建议 |
| --- | --- | --- | --- | --- |
| 专项1 发现1 | P0 | 正式数据路径存在标注"假数据/模拟"的静态业务数据 | `facilityPoints.json:11`、`floodStatistics.json:13`、`floodArea.json:9`，经 floodRepository 进入 `/api/flood/*` 正式响应 | 模拟数据移出正式目录或环境开关隔离；UI/文档/接口元数据三处显式标注 + 替换计划 |
| 专项1 发现2 | P0 | 组件卸载未取消在途请求（WaterLevelProfilePanel/HomePage/ProfilePage） | `WaterLevelProfilePanel.vue:122-127/304-313`（abortController 未 abort）、`HomePage.vue:41`、`ProfilePage.vue:34`（useOverviewCharts 无 signal） | onUnmounted 调 abort()；useOverviewCharts 加 AbortSignal 参数 |
| 专项1 发现3 | P2 | `/api/ports` 走 loadStatic 静态加载器，未走统一 API 入口 | `mapDataService.ts:14-27` 经 `loadStatic` + unwrapEnvelope | 迁移 useApiRequest（保留 schema 校验） |
| 专项1 发现4 | P1 | 坐标缺失回退 (0,0) 哨兵点，违反 CRS 约定 | `SiteSelectionPage.vue:165-166/176-177`、`PaginatedListPanel.vue:180-181` `?? 0`；对照 `crs.ts:14-15` 明确禁回退 | 缺失/非法坐标跳过或提示；adapter/schema 入口拦截 |
| 专项1 发现5 | P2 | API 无版本管理 | `app.js:140-145`、`routes/*.js` 均无 `/v1` 前缀；adapter 硬编码无版本路径 | 引入 `/api/v1` 或版本协商；前端版本号配置化 |
| 专项1 发现6 | P2 | 数据字典缺失，数据来源文档化不完整 | `API契约文档.md` 仅接口索引无字段字典；字段语义散落 types/* 与 data/*.json | 建立数据字典与 types/API 契约同步维护 |
| 专项1 发现7 | P2 | affectedFacilities 同名不同义（数量 vs 数组） | `base.ts:55-56`（number）vs `:83-84`（AffectedFacility[]）；ReportPanel 靠字段位置区分 | 计数侧改名 `affectedFacilityCount`（合并 M5） |
| 专项1 发现8 | P1 | 登出/重置未清理 BLM 图层注册表（跨登出残留） | `App.vue:50/59-74` resetStores 未调 removeAll；`mapStore.ts:152-155` 仅清 catalog | resetStores 调 `businessLayerManager.removeAll()` + 业务 composable 暴露 cancel |
| 专项1 发现9 | P1 | 选址雷达图硬编码快照数据作空态兜底（"假第一名"） | `radarSnapshot.ts:25-40` SNAPSHOT_XIAOQU（腾龙阁 85.2）；`SiteSelectionPage.vue:77-79` 空态回退；RadarChart EmptyState 不出现 | 移除快照回退改 null + EmptyState；如需示例显式标注"示例快照" |
| 专项1 发现10 | P2 | 洪涝风险等级字符串不一致（`无` vs `无风险`） | `floodAnalysisController.js:71`、`floodService.js:17` 返 `'无'`；deriveRiskLevel/floodAdapter 默认 `'无风险'`；colors.ts 仅 `'无风险'` 键 | 统一为 `'无风险'` |
| 专项1 发现11 | P2 | 孤儿路由 `/forecast/:portId` 未登记且前端零消费 | `routes/forecast.js:19` 自述"前端零消费；保留作兼容端点"；API契约文档 §5 未列 | 补入契约注明兼容用途，或删除 |
| 专项1 发现12 | P3 | flood-service 文档参数名仍写 `level`，实际为 `waterLevel` | `main.py:6/18` vs `:123`；`backend/data/README.md:93` | 同步文档参数名 |
| 专项1 发现15 | P1 | 浸没收藏保存链路丢失业务类型与洪涝字段 | `PaginatedListPanel.vue:120-137` 只传 `createPlan(name,{})` 无 businessType:'flood'；`:175-184` 丢弃 type/loss；`PlansPanel.vue:101-108` 非 flood 走选址加载 | 保存时按 planType 设 businessType 并透传洪涝字段 |
| 专项1 发现16 | P3 | WaterLevelProfilePanel 组件内直接调用 apiRequest（未按 R5 收口） | `WaterLevelProfilePanel.vue:20/62/122-127` 直请求 `/flood/terrain-profiles` | 抽 useTerrainProfiles 或入 floodAdapter |
| 专项1 发现17 | P3 | floodAdapter._onlineLevelCache 无 TTL，仅 FIFO 淘汰 | `floodAdapter.ts:31-33`（MAX 64）、`:149-154` | 加 TTL 或数据源版本校验 |
| 专项1 发现18 | P3 | 缺失时间返回 null，前端 `\|\| 0` 混淆"无数据"与"0" | `forecastService.js:196-200`；`useForecastComparison.ts:79` `p?.qinzhou?.value \|\| 0` | 保留 null 展示"暂无数据" |

### 专项2 生命周期（20 条，discover:20260816）

| 原编号 | 等级 | 标题 | 证据摘要 | 整改建议 |
| --- | --- | --- | --- | --- |
| 专项2 1-2 | P3 | useGCS 模块级可变量数超"单文件≤2"建议 | `useGCS.ts:68-72` 5 个模块级可变量 | 可接受（GCS 单例语义）；resizeTimer/listenerRegistered 收进守卫对象 |
| 专项2 1-3 | P3 | floodAdapter/forecastAdapter 模块顶层调用 useApiRequest | `floodAdapter.ts:29`、`forecastAdapter.ts:17`、`useAuth.ts:47` | 与 b040 同批收口至实例级（b008 曾修 floodAdapter，需复核复发） |
| 专项2 2-1 | P2 | 请求进行态被请求 composable 绕过 action 直改 store | `useForecastRequest.ts:22/49/72`；`useSiteAnalysisApi.ts:23-24,33,69,72` storeToRefs 直改 | 两 store 补 action，composable 禁直改状态字段（合并 M11） |
| 专项2 2-2 | P2 | siteSelectionStore.clearState 未复位 calculating（只 set 不 clear） | `siteSelectionStore.ts:65-73` 独缺 `calculating.value=false`（:26 定义） | clearState 补复位（对齐 b078 模式）（合并专项1 14） |
| 专项2 2-4 | P3 | ADR 7「siteSelectionStore 去 Pinia」与代码不符（设计漂移） | `siteSelectionStore.ts:24` defineStore(Pinia)；对照 `开发指南与决策.md:307` ADR 7 | 更新 03 ADR 7 与 b045 注记 |
| 专项2 2-5 | P3 | 选址雷达数据本地 ref 与 store 双份持有 | `SiteSelectionPage.vue:41-46/96-100` + `AppLayout.vue:49` 双通道双写 | 单侧读取或注释双通道为有意设计（commit 2811035） |
| 专项2 3-1 | P3 | CesiumViewerManager.destroy() 重复调度 30s 空闲定时器 | `CesiumRenderer.ts:217-224` destroy→unmount→_startIdleDestroyTimer | destroy 末尾补 `_clearIdleDestroyTimer()` |
| 专项2 3-2 | P3 | OLRenderer.destroy() 未清空 baseLayers 数组引用 | `OLRenderer.ts:811-842` dispose 后仍持 TileLayer 引用 | destroy 末尾 `baseLayers.image=[]; vector=[]` |
| 专项2 3-3 | P3 | 闲置销毁后旧 CesiumRenderer 被跳过 destroy()、this.viewer 悬空 | `UnifiedMap.vue:164-171` + `CesiumRenderer.ts:800`（注释自述防抛错） | 丢弃路径补置 null |
| 专项2 3-4 | P3 | ensureCesiumLoaded 失败路径 script 节点残留 DOM | `renderers/index.ts:68-79` onerror 未移除 `<script>` | onerror 时 `script.remove()` |
| 专项2 3-6 | P3 | ForecastControlPanel.startPlayback() 无「先清再建」防重守卫 | `ForecastControlPanel.vue:173-187` setInterval 未先 clearInterval | 开头 `if (playbackTimer) return` 或先 clear |
| 专项2 4-1 | P2 | 水位滑块值「store + 本地 ref」双源，手动 watch 同步 | `WaterLevelProfilePanel.vue:69/84-89/92-100/342-353` | 改可写 computed v-model，删本地 ref 与 watch |
| 专项2 4-2 | P2 | 预测「当前指标」双源，快照恢复后按钮视觉与 store 脱钩 | `ForecastControlPanel.vue:39-41/52/104` vs `forecastStore.ts:98-107` restoreState | btnStates.selected 改 computed 派生 |
| 专项2 4-3 | P2 | 选址分析结果「页面本地 ref + store」双持，双写同步 | `SiteSelectionPage.vue:41-46/87-100/224-232` + `AppLayout.vue:48-54,138-139` | store 唯一来源（computed 透传）或反之 |
| 专项2 4-4 | P3 | timeRange 死/重复状态（currentTime 与 timeRange.current 双持） | `forecastStore.ts:30-34/117/155` 全仓业务 0 消费 | 删除 timeRange 字段，派生 getter |
| 专项2 4-5 | P3 | SiteAnalysisControlPanel 设施三态选择面板内聚，靠 emit+expose 间接同步 | `SiteAnalysisControlPanel.vue:41-44/160-165/207-229` + `SiteSelectionPage.vue:203,237-239` | 提升父页/store 单一来源 |
| 专项2 7-1 | P3 | async onMounted 无 try/catch（恢复路径未兜底） | `FloodAnalysisPage.vue:186-224`（BLM updateData 对未注册图层 throw） | 恢复路径外包 try/catch |
| 专项2 7-2 | P3 | 浮动 Promise（void import() 未 catch） | `renderers/index.ts:31` void import('./CesiumRenderer') 外层 catch 不兜此 | 补 `.catch(()=>{})` 或 await |
| 专项2 7-3 | P3 | void 调用的 async 图层更新器无内部 catch | `SiteSelectionPage.vue:91`；`useAnalysisLayer.ts:132` try/finally 无 catch | setAnalysisResult 内补 catch 或调用点 `.catch()` |
| 专项2 8-1 | P3 | 死 provide MAP_STORE_KEY + 陈旧文档 | `App.vue:46` provide 无 inject 消费方；`core/README.md:60` 引用已删 useLayerManager；provideKeys.ts:6 注释"4 个"实 5 个 | 删除死 provide；同步 README/计数注释（合并 M4） |

### 专项3 TS 类型（17 条，discover:20260816）

| 原编号 | 等级 | 标题 | 证据摘要 | 整改建议 |
| --- | --- | --- | --- | --- |
| 专项3-0816-01 | P2 | CesiumRenderer 唯一裸 `as any` 无注释 | `CesiumRenderer.ts:1289`（SingleTileImageryProvider 构造选项整对象断言） | 结构化精确接口断言 + 注释 + 退出计划 |
| 专项3-0816-02 | P3 | forEach 回调显式 `any` 参数 | `CesiumRenderer.ts:1104-1107`（有运行期注释无退出计划） | 定义最小 CesiumEntityLike 接口替代 |
| 专项3-0816-03 | P1 | Plan.savedXiaoqu 类型必填 vs 运行时缺省（类型虚言） | `plan.ts:14` 必填 vs `schemas.ts:191-192` optional + plans.json:118/162 缺省 | 类型改可选对齐 schema，或补全数据改必填 |
| 专项3-0816-04 | P2 | Plan.weights 后端持久化、前端类型/schema 均未声明 | `plans.json:15/21`、`plansController.js:62`；plan.ts/schemas.ts 无 weights | planSchema 补 nullable optional + Plan 补 `weights?` |
| 专项3-0816-05 | P2 | ForecastPoint.confidence 被 schema 静默剥除 | `berth.json:493` 带 confidence；`forecast.ts:17` 无字段；schemas.ts:90-94 默认剥除 | 补可选 confidence 或注明剥除行为 |
| 专项3-0816-06 | P2 | riskLevel 类型必填但 schema 未守契约 | `base.ts:67` 必填；`schemas.ts:34-39` loose record 不强制 | floodFeatureSchema 补 `riskLevel: z.string()` |
| 专项3-0816-09 | P2 | UnifiedMap 双重断言 `as unknown as Port` | `UnifiedMap.vue:280`（注释承认无法判别收窄） | 自定义类型谓词替代双重断言 |
| 专项3-0816-10 | P2 | GeoPoint 同名类型两处独立定义 | `crs.ts:11`（泛型 CRS）vs `base.ts:14`（lng/lat） | 收敛单一 GeoPoint |
| 专项3-0816-11 | P2 | rbush 类型依赖 ol 传递依赖，未显式声明 @types | `package.json:34`；`spatialIndex.ts:6-7` import type from 'rbush' | @types/rbush 显式加入 devDependencies |
| 专项3-0816-12 | P3 | env.d.ts `declare module '*.vue'` 用 any 弱化 SFC 类型 | `env.d.ts:3-6` DefineComponent&lt;{},{},any&gt; | 评估删除或收敛泛型 |
| 专项3-0816-13 | P2 | 13 个导出 composable/工厂无显式返回类型标注 | useForecastRequest.ts:12、useAuth.ts:120、usePlans.ts:15 等 13 处 | 补显式返回类型接口 |
| 专项3-0816-14 | P3 | ForecastMapData.reliability 接口/schema 双源分歧 | `forecast.ts:52` 声明 vs `schemas.ts:141-145` looseObject 透传 | schema 补 `reliability: z.number().optional()` |
| 专项3-0816-15 | P3 | PortInfoPanel props 弱类型 | `PortInfoPanel.vue:8` Record&lt;string,unknown&gt;，模板直访 .name/.lng/.lat | 定义 Port 窄接口 + 父组件守卫 |
| 专项3-0816-16 | P3 | UnifiedMap 事件名 camelCase 偏离约定 | `UnifiedMap.vue:34` typeChange（约定 kebab-case） | 改 `type-change` |
| 专项3-0816-17 | P3 | terrainProfile startPoint/endPoint 未入本地接口 | `terrainProfile.json:17-24`；`WaterLevelProfilePanel.vue:54-60` 本地接口未声明 | 补可选字段或注释不消费 |
| 专项3-0816-18 | P3 | allowJs + checkJs:false 配置半开缺口 | `tsconfig.app.json:18-19`（src 当前 0 个 .js，moot） | 保持零 .js 或开启 checkJs |
| 专项3-0816-19 | P3 | 根 jsconfig 别名指向不存在目录 | `jsconfig.json:3-5` 根目录无 src/ | 删除或对齐 frontend/src |

### 专项4 WebGIS（15 条，discover:20260816）

| 原编号 | 等级 | 标题 | 证据摘要 | 整改建议 |
| --- | --- | --- | --- | --- |
| 专项4 1.3 | P1 | CesiumRenderer._doFlyTo 兜底死代码 `?? 0`（量纲伪装） | `CesiumRenderer.ts:560-562` lng 缺失伪装 0°、lat 缺失 NaN 崩溃（正常路径 _normalizeFlyToTarget 已保证） | 入口归一化 + 缺失显式 return，删 `?? 0` |
| 专项4 3.1 | P2 | 渲染器接口契约无类型强制且契约测试未覆盖真实实现 | `renderers/index.ts:93,104` as unknown as MapRenderer；OL/Cesium 未 implements；接口测试仅 MockRenderer | 补 implements/satisfies 断言；updateGeoJsonLayer 决策入接口 |
| 专项4 3.2 | P2 | 业务层引擎类型判断（getType 分支）违反适配器边界 | `FloodAnalysisPage.vue:155/34-39`；waterSurface 已有 isWater3DCapable 能力守卫 | 收敛 adapter 能力守卫，业务页删 getType 分支 |
| 专项4 4.1 | P2 | Cesium 卸载=unmount 非真正销毁，非根级挂载边界未硬化 | `CesiumRenderer.ts:779-801/217-224`；`UnifiedMap.vue:496-499`；destroy 未清 _geoJsonTokens | 文档化非根级需 destroyViewer；destroy 补 tokens.clear() |
| 专项4 5.1 | P2 | Cesium 视口裁剪索引坐标系与工具声明不一致（度 vs 3857） | `spatialIndex.ts:2-3` 声明 3857；`CesiumRenderer.ts:980-996/1474` 用度 | 统一 fromLonLat 转 3857 或文档化双坐标系 |
| 专项4 1.4 | P2 | hillshade 投影 bbox 与缩放限位硬编码在渲染器内部 | `CesiumRenderer.ts:1285`（106.97..110.07/20.93..23.07）与 BEIBU_GULF_BBOX 不同源；`:335-336`；`OLRenderer.ts:121-124` | 提配置常量对齐业务 bbox；缩放限位入 MAP_CONFIG |
| 专项4 2.1/3.4 | P2 | 引擎不支持的图层"有条目却无法控制"，无降级提示 | `layerAdapters.ts:97-100,175-180,213-216` 能力守卫 warn 跳过；`BusinessLayerManager.ts:288` 静默 drop；面板不感知能力 | BLM 上报错误回调 toast + 面板置灰 |
| 专项4 8.3 | P2 | OL 默认控件全开且不可配置，2D/3D 控件不对称 | `OLRenderer.ts:126-130` 未传 controls（默认全开）；Cesium 侧显式禁用 8 个 widget | OL 显式传 controls 配置；useMapControls 更名或补职责注释 |
| 专项4 2.5 | P3 | Cesium 渲染器无图层 z-order 控制 | CesiumRenderer 全文件无 zIndex；OL 侧 _resolveLayerZIndex + setZIndex | 文档标注 Cesium 无显式 zIndex 能力 |
| 专项4 5.3 | P3 | 聚合距离不随 zoom 自适应，无几何简化 | `OLRenderer.ts:303` distance 固定 40px；`:45` 半径固定；全库无 simplify | cluster distance 随 zoom 分级或文档化取舍 |
| 专项4 7.1 | P3 | OL pointer-move 事件无节流 | `OLRenderer.ts:225-231` 每帧 raw emit；对照 moveend/camera-changed 已 300ms 防抖 | 加 rAF/时间节流（当前无订阅方） |
| 专项4 6.3 | P3 | Cesium 点标注 label 黑白样式硬编码 | `CesiumRenderer.ts:1026-1028` Color.BLACK/WHITE；同文件其他已走 LAYER_DEFAULTS | label 色收口 LAYER_DEFAULTS |
| 专项4 3.5 | P3 | CesiumViewerManager 单例无并发原子保护 | `CesiumRenderer.ts:58-234` 模块级单例无占用锁；`App.vue:185` 单挂载约定 | 文档化单挂载前提；多地图时加占用锁 |
| 专项4 8.4 | P3 | UnifiedMap 卸载未显式置空容器 DOM ref | `UnifiedMap.vue:469-516`（ol/cesiumContainerRef 未置空） | onUnmounted 尾部显式置 null |
| 专项4 1.2 | P3 | GeoJSON 加载依赖规范默认 4326，未显式声明 CRS | `OLRenderer.ts:518` readFeatures 输入侧依赖默认；业务 GeoJSON 无 CRS 元数据 | 注释/断言显式声明 4326 约定 |

### 专项5 工程化（主窗口）（27 条，discover:20260816）

| 原编号 | 等级 | 标题 | 证据摘要 | 整改建议 |
| --- | --- | --- | --- | --- |
| 专项5主 发现1 | P0（已修） | `npm run build` 被 vue-tsc 模板类型错误硬阻断（审查期间 11:13 工作树修复） | `LayerControlPanel.vue:119` 调 getLayerIcon 缺 layerType 字段 → TS2339；package.json:8 typecheck 前置 | 已由工作树补 `layerType` 修复；合入前干净环境复跑 typecheck/build 终裁 |
| 专项5主 发现2 | P1 | flood-service（FastAPI）测试未接入 CI | `ci.yml` 4 job 全仓 pytest 0 命中；test_main.py 9 用例 + test_real_engine.py 4 用例仅本地可跑；无 pyproject.toml | ci.yml 加 pytest job；补 pyproject.toml testpaths/pythonpath |
| 专项5主 发现3 | P1 | riskLevel 6 档阈值业务逻辑前后端双副本 | `floodAdapter.ts:56-63` vs `floodAnalysisController.js:26-33` 同表双实现 | FastAPI 直接返回 riskLevel 或抽共享阈值表（合并 M1） |
| 专项5主 发现4 | P2 | 覆盖率门禁形同虚设（阈值过低 + backend 未进 CI） | vitest.config.js:34-38（30/25/18/30）；实测 39.87%/29.87%/26.2%；ci.yml:137-140 无 --coverage | 阈值上调 + backend 补 coverage-v8 进 CI（合并 M9） |
| 专项5主 发现5 | P2 | CSP 仅 Report-Only + unsafe-inline/unsafe-eval | `nginx.conf:35` 探测模式；全站无强制 CSP 头 | 按收紧路径推进：上报→去 unsafe-eval→白名单→转强制（合并 M10） |
| 专项5主 发现6 | P2 | ForecastPage 数据刷新无 loading 视觉反馈（三态缺口） | `ForecastPage.vue:94/136/141-147` 注释自认不绑 UI | 绑定 isRequesting 渲染 ChartLoading/spinner |
| 专项5主 发现7 | P2 | readiness 健康检查仅探数据目录可读，单维度过弱 | `app.js:46-57` 仅 checkDataDirReadable；不探写路径/DEM volume/flood-service | 扩展多维检查 `{dataDir,dataWritable,demVolume,floodReachable}`（合并 M8） |
| 专项5主 发现8 | P2 | 回滚依赖服务器本地历史 tag 镜像，无远端 registry 兜底 | `rollback.sh:15` `--pull missing`；无 registry | 明确报错与重建指引；禁 `prune -a`；可选推私有 registry |
| 专项5主 发现9 | P2 | 容器内 nginx 仍以 root 运行 | `docker-entrypoint.sh:117` nginx 以 PID1(root) 拉起（Dockerfile:52 自认） | nginx worker 降权（`user nodeapp;`） |
| 专项5主 发现10 | P2 | `.husky/` 被 .gitignore 排除，fresh clone 丢失本地门禁 | `.gitignore:62`；git ls-files 确认 hooks 未跟踪 | .husky/ 入版本库（官方推荐做法） |
| 专项5主 发现11 | P2 | README.md 硬编码"416 用例"违反"文档不写易过期数字"红线 | `README.md:98`；01 六红线 | 删除数字或改指针表述 |
| 专项5主 发现12 | P2 | `.tmp-p1.txt`/`.tmp-p2.txt` 未跟踪且未被 .gitignore 覆盖 | 仓库根 `??` 两文件（11:02 写入） | 删除 + .gitignore 补 `.tmp-*`（合并 M2） |
| 专项5主 发现13 | P2 | business/index.ts barrel re-export 无消费者 | `business/index.ts` 5 项中 4 项无 `from '@/business'` 导入 | 删无消费者导出或全量收口（合并 M3） |
| 专项5主 发现14 | P2 | visualization/ 与 views/ 分层无 README | frontend/src 各子模块均有 README，唯独这两层无 | 补 visualization/README.md + views/README.md（合并专项5并 7-2） |
| 专项5主 发现15 | P2 | dist 陈旧构建残留 + emptyOutDir:false chunk 堆积 | `vite.config.js:75`；dist/assets/js 实测 index-* 4 份、CesiumRenderer-* 4 份等 | 恢复 emptyOutDir:true + 清理；部署以 CI fresh 产物为准（合并 M7） |
| 专项5主 发现16 | P3 | 资源加载错误（script/link/img）无捕获监听 | `main.ts:79-87` 仅 onerror/onunhandledrejection；无 capture error 监听 | 补 `addEventListener('error', handler, true)`（配合 z072） |
| 专项5主 发现17 | P3 | loadStatic 无重试（与 apiRequest 不一致） | `loadStatic.ts:75-101` 单次 fetch；apiRequest GET 有 MAX_RETRIES=3 | loadStatic GET 引轻量退避重试 |
| 专项5主 发现18 | P3 | 首屏 openlayers chunk 偏大（157.3KB gzip 单项） | dist openlayers-X_qIjX8I.js 549.9KB raw；首屏 gzip ≈330KB | build:analyze 分析 + OL 按需引入 + 清理陈旧 chunk |
| 专项5主 发现19 | P3 | nginx gzip_types 不含 image/*（与 brotli 不对称） | `nginx.conf:122` vs `:130` | gzip_types 补 image/svg+xml |
| 专项5主 发现20 | P3 | 03 性能基线表体积口径过期（floodArea.json 3.0MB vs 实测 1.35MB） | `开发指南与决策.md:139` vs 实测 1381KB | 更新基线表或改指针表述 |
| 专项5主 发现21 | P3 | flood-service 日志无级别/轮转，仅 uvicorn stdout | main.py 无 logging 配置；Dockerfile CMD stdout | 接受现状或加 uvicorn logging 配置（合并 M6） |
| 专项5主 发现22 | P3 | CI 未归档 build 产物（可追溯性缺失） | ci.yml 无 upload-artifact | lint-and-build 加 upload-artifact 留档 |
| 专项5主 发现23 | P3 | types:gen 脚本与 generated 产物未跟踪（WIP 交付状态） | `git status` 显示 `?? scripts/gen-api-contract.cjs`、`?? types/generated/` | 合入前纳入版本控制或明确为一次性工具 |
| 专项5主 发现24 | P3 | 多环境变量映射表不在库（仅 .env.example 契约层） | git ls-files env 类仅 .env.example×3 | 可选补多环境变量映射表文档 |
| 专项5主 发现25 | P3 | 后端 controller 层跨层集成测试偏少 | `forecastRouter.test.js:11-19` mock 整个 controller；唯一真装配 app.test.js:57-87 | 推广 listen(0) 模式 1-2 个真实装配冒烟 |
| 专项5主 发现26 | P3 | 三处手写 debounce 未统一 | frontend/src 3 处业务自实现；无 lodash | 抽 shared/utils/debounce.ts 单实现 |
| 专项5主 发现27 | P3 | 文档与代码漂移（core/README navConfig"已删"实仍活跃；pre-commit 空守卫残留） | `core/README.md:105` vs `navConfig.ts` 被引用；.husky/pre-commit PROTECTED="" | 同步 README；移除空 guard |

### 专项5 工程化（并行窗口）（10 条，discover:20260816；与主窗口重叠项见第二节合并）

| 原编号 | 等级 | 标题 | 证据摘要 | 整改建议 |
| --- | --- | --- | --- | --- |
| 专项5并 1-2 | P2 | 错误技术细节上屏（error.message 原文渲染） | `PlansPanel.vue:64,89,182,317`；`errorHandler.ts:34,36` 不回退 fallback | showError 统一出口 + message 无害化 |
| 专项5并 1-3 | P2 | 四态覆盖残缺（loading/error/empty 缺失与误判） | `SiteSelectionPage.vue:71` 计算中误显空；ReportPanel 未加载渲染 0/无；BarChart/LineChart 加载被误判为空 | 补 loading/error/empty 独立分支 + 重试 |
| 专项5并 1-4 | P3 | 同一错误双出口重复弹窗 | `useSiteAnalysisApi.ts:68-69` + `SiteSelectionPage.vue:61-62` 两次 showError（:151 注释自称避免未生效） | 收敛单一出口 |
| 专项5并 3-2 | P3 | DebugToggle/GCSDebugOverlay 代码进生产包 | `AppLayout.vue:22-23` 静态 import；`:59` DEV 门控；生产 chunk 含 debug 字符串 | 改动态 import 仅 DEV 引用 |
| 专项5并 4-4 | P2 | 「fix/refactor 必须附测试」约定未执行、无执行闸门 | `开发指南与决策.md:102` 约定；git log -40 中 14 个 fix 仅 ~36% 带测试；lint-staged 只跑 prettier | CI/lint-staged 加"改 src 必须含测试"强校验 |
| 专项5并 4-5 | P3 | forecastAdapter 无专属测试、函数 0% 覆盖 | `forecastAdapter.ts` 无 __tests__；coverage 报告 0% | 补 1 个单测或显式断言委派 |
| 专项5并 5-3 | P3 | Dockerfile 用 npm install 而非 npm ci（构建不可复现） | `Dockerfile:25-26`（注释自述 npm ci EUSAGE） | 重建完整 lock 后恢复 npm ci |
| 专项5并 5-4 | P3 | flood-service 镜像以 root 运行且无 HEALTHCHECK | `backend/flood-service/Dockerfile:5` 无 USER/HEALTHCHECK；compose 无 healthcheck | 加 useradd + USER + HEALTHCHECK（/health 已有） |
| 专项5并 6-4 | P3 | types/components/{dock,panel}.ts 5 个 export 全部未消费 | `dock.ts:5-20`、`panel.ts:7-40` 全仓无 import；NavItem 另由 navConfig.ts 定义 | 删除两废弃类型文件 + 移除 re-export |
| 专项5并 7-1 | P3 | lint-staged 仅跑 prettier 未跑 eslint | package.json lint-staged 只 `prettier --write`；与 03 §1.6 不符 | 增补 `eslint --fix` |

### 专项6 架构耦合（19 条，discover:20260816）

| 原编号 | 等级 | 标题 | 证据摘要 | 整改建议 |
| --- | --- | --- | --- | --- |
| 专项6 发现1 | P1 | core 布局基座直连业务 store + 业务雷达快照进 visualization 资产层 | `AppLayout.vue:17/48-54/131-140` useSiteSelectionStore；`radarSnapshot.ts:1-32` 业务快照置于 L4 且被 core 消费 | 布局基座去业务化（slot 注入）；radarSnapshot 迁 business 层 |
| 专项6 发现2 | P1 | core 渲染器按业务图层 key 硬编码 z-index 判定 | `OLRenderer.ts:109-116` `flood-area/flood-facilities/forecast-` 业务 key | z-index 由注册方经 LayerOptions 显式传入 |
| 专项6 发现3 | P2 | 业务模块无 per-module 入口，组件深路径 import 成体系 | 三业务页直引 AppLayout/GCSPanel/LayerControlPanel/useSliderFocus；`business/index.ts:1` 注释自认不 re-export | 立法二选一：补 index 聚合收口 或 01 §五.10 增补例外 + cruise 规则 |
| 专项6 发现6 | P2 | visualization 层无入口文件、无 README，12 处深路径消费 | `from '@/visualization/` 12 处全深路径；core 直依赖 visualization 内部 | 建 index.ts + README；或与发现3 一并豁免立法 |
| 专项6 发现7 | P2 | core↔services 模块粒度回边（services 深引 core/config/map 叶子） | `mapDataService.ts:1-2` 深导入；`.dependency-cruiser.cjs:96-104` pathNot 放行单叶子 | DATA_PATHS 上提 shared/config 消除回边 |
| 专项6 发现9 | P2 | shared 常量层携带业务数据（港口名/设施类型 key/业务配色） | `shared/constants/forecast.ts:7-15` PORT_NAMES/PORT_KEYS/BASE_YEAR；`colors.ts:14-36` FACILITY_COLORS_MAP/LAYER_FILL_* | 业务常量下沉 business/types 或显式设计裁决豁免 |
| 专项6 发现10 | P2 | 路由跳转目标字符串散落且无路由级统一守卫 | `App.vue:83`、`GCSModal.vue:46`、`PaginatedListPanel.vue:107`、`errorHandler.ts:65`、`PlansPanel.vue:109,162` 硬编码 | 跳转目标自 manifest 派生；beforeEach 统一登录引导（z071 同批） |
| 专项6 发现12 | P2 | backend 死资产：site-analysis-bench.mjs 与 floodLevelsStore.js | bench 0 引用且头注释指向不存在文件；floodLevelsStore 仅测试 mock 引用 | bench 迁 tools/perf-bench/ 或删；floodLevelsStore 确认后删/归档 |
| 专项6 发现13 | P2 | types/schemas.ts zod 运行时 schema 违反「types 零运行时依赖」铁律 | `schemas.ts:5` import zod（308 行运行时 schema）；01 §三.1/§四 冲突 | 设计裁决：迁移 shared 或 01 增补 schema 例外 |
| 专项6 发现18 | P3 | GCSDebugOverlay.vue 734 行（dev-only 多职责） | `GCSDebugOverlay.vue`（网格+性能+QA 色）；`AppLayout.vue:59` DEV 门控 | 按职责拆分子组件或保留并注明 |
| 专项6 发现20 | P3 | core/README 与 cruise 规则引用已合并消失的渲染器文件 | `core/README.md:39-40`、`.dependency-cruiser.cjs:48-49` 引用 CesiumEvents.ts 等 4 个不存在文件 | 同步清理 README 与 cruise pathNot |
| 专项6 发现21 | P3 | visualization 两级 composables 目录并存 | `visualization/composables/useECharts.ts` vs `charts/composables/useChartBase.ts、useRadarChart.ts` | 统一一级 composables |
| 专项6 发现23 | P3 | types/generated/api-contract.json 生成产物混入源码区且未忽略 | `api-contract.json` 8534B 未跟踪未 gitignore | gitignore types/generated/ 或独立目录 |
| 专项6 发现24 | P3 | floodAdapter 数据源模式简化与 02 R4 设计文档漂移 | `floodAdapter.ts:25` 注释；02 §二 R4 仍写 dataSourceConfig 统一管理（全仓仅此注释 1 处命中） | 02 R4 改写或恢复统一收口（转专项1 裁决） |
| 专项6 发现25 | P3 | spatialIndex 前后端同名不同义 | backend `utils/spatialIndex.js`（POI 查询）vs frontend `shared/utils/spatialIndex.ts`（视口裁剪） | 重命名其一（如 backend spatialQuery.js）或注释互指 |
| 专项6 发现26 | P3 | PlanSaveModal 伪 shared 雏形（单消费者） | `shared/components/PlanSaveModal.vue` 仅 PlansPanel.vue:17,310 消费 | 随 PlansPanel 归位或确认第二消费方 |
| 专项6 发现27 | P3 | useGCS.js 后缀 import（命名风格不一致） | `PaginatedListPanel.vue:14`、`PanelTitle.vue:5` import `@/shared/layout/useGCS.js`（实为 .ts） | 去掉 .js 后缀 |
| 专项6 发现28 | P3 | cruise 未入本地 git 钩子 | `.husky/pre-push` 仅 typecheck；cruise 仅 CI 执行 | pre-push 追加 `npm run cruise` 或文档化 |
| 专项6 发现29 | P3 | .pytest_cache/ 未纳入 .gitignore（根目录与 flood-service 各一处） | git status `?? .pytest_cache/`×2；.gitignore 仅 __pycache__/*.pyc | .gitignore 补 `.pytest_cache/` |

### 专项7 设计 Token（33 条，discover:20260816；编号承接 S7-31 之后）

| 原编号 | 等级 | 标题 | 证据摘要 | 整改建议 |
| --- | --- | --- | --- | --- |
| S7-32 | P1 | token 变更 SOP 引用的统计命令与实体文件漂移 | `开发指南与决策.md:248,249` 写 `node tools/token-stats.cjs`，实体为 `token-stats.mjs`；实测 Cannot find module | 统一为 .mjs；package.json 加 token-stats script |
| S7-33 | P1 | token-stats.mjs 死 token 检测假阴性 bug（实测虚报 0%，真实 16.7%） | `token-stats.mjs:7` 绝对 SRC vs `:29` 相对 defLoc vs `:45` 绝对比较恒不等 → 定义行永计为引用 | 修 :45 路径基准（path.resolve 统一）；补用例 |
| S7-34 | P2 | chart 多分类系列色板缺失，回退 ECharts 默认色板 | `useChartBase.ts:98-104` 无顶层 color；CHART_COLORS 无 c1-c6 系列色 | 增 seriesPalette{light,dark} ≥6 色 + 顶层注入 |
| S7-36 | P2 | prefers-color-scheme 仅读初始值、无 change 监听 | `useTheme.ts:28` 仅初始化 matches；全仓 matchMedia 仅此一处 | matchMedia change 监听（无手动偏好时跟随） |
| S7-38 | P2 | Element Plus 组件变量未映射到 GCS token（零 `--el-*` 覆写） | main.ts:3 dark css-vars；grep `--el-*` 0 命中 | 建 EP 覆写文件映射 --el-color-primary 等 |
| S7-39 | P2 | 亮色语义色 success/warning/danger 白底文本对比度不达 WCAG AA | node 实测 2.27:1 / 2.19:1 / 2.90:1（<4.5） | 语义色文字用 -bg 深色文字或亮色专属加深变体 |
| S7-40 | P2 | z-index 硬编码散落（5/50/55/60），未走 `--GCS-z-*` 刻度 | `App.vue:212`、`AppLayout.vue:210/260`、`PortInfoPanel.vue:45`、`BottomNavBar.vue:93` | 收敛 --GCS-z-*；壳层/nav 补刻度档 |
| S7-41 | P2 | PortInfoPanel 手搓浮动面板，绕过 GCS 布局引擎 | `PortInfoPanel.vue:41-44` absolute + calc 手算；HomePage.vue:58 直置未套 GCSPanel | 改 GCSPanel（anchor + 整数 w/h） |
| S7-42 | P2 | 三处近同「指标/图层切换按钮网格」样式重复实现 | LayerControlPanel.vue:138-192 / SiteAnalysisControlPanel.vue:307-371 / ForecastControlPanel.vue:293-337 | 抽取 ControlButtonGrid 统一 |
| S7-43 | P2 | 自绘按钮/滑块 focus 环被 outline:none 去除且无替代 | `GCSButton.vue:77`；滑块 ForecastControlPanel:396/481、SiteAnalysisControlPanel:399、LoginPanel:222 | 删 outline:none 或补 :focus-visible 环 |
| S7-44 | P2 | 表单控件来源混用 + 滑块尺寸三套（视觉分裂） | ElSelect/ElSlider vs 自绘 .conf-slider/.factor-slider/.form-input；拇指 14/16/18px、轨道 4/6px | 统一控件来源；尺寸收敛单一 token |
| S7-45 | P2 | 死 token 8/48 = 16.7%，远超 ≤5% 阈值 | 8 个 `--GCS-*`（bg-canvas/border-hover/color-primary-active/color-primary-bg/radius-xl/shadow-lg/text-placeholder/z-panel）零引用 | 删除 8 个死 token（删前 grep 复核） |
| S7-46 | P3 | FloodAnalysisPage 业务层硬编码 markerColor 字面量 | `FloodAnalysisPage.vue:402` '#F56C6C'；同值已在 colors.ts:12 | 引用 FLOOD_RISK_COLORS |
| S7-47 | P3 | 缺 `--GCS-text-disabled` token，禁用态裸 opacity 0.5/0.6 硬凑 | style.css:37-42 无 disabled；LoginPanel:258-259、GCSButton:94-96、SiteAnalysisControlPanel:454-456、PlansPanel:452-453 | 补双主题 token，改 color 写法 |
| S7-48 | P3 | 同值异名 token（text-regular/primary 近同值；text-muted/placeholder 同值；#ffffff 三处） | style.css:37-42、:27/:29/:42 | regular 并入 primary；placeholder 派生或删；#ffffff 收敛引用 |
| S7-49 | P3 | useRadarChart splitArea 写死 rgba 白叠层数组 | `useRadarChart.ts:186-188` isDark 分支硬编码 | 收敛 CHART_COLORS radar splitArea 叠层对 |
| S7-50 | P3 | 洪涝图层透明度/风险图例色未做暗色适配 | `FloodAnalysisPage.vue:544` opacity:0.7；FLOOD_RISK fill alpha 恒 0.3-0.5 | 透明度入渲染层常量表；图例暗色补档 |
| S7-51 | P3 | 魔法间距/刻度字面量散落 | PlansPanel.vue:429/435、UserInfoCard.vue:36、WaterLevelProfilePanel.vue:375、UnifiedMap.vue:602/603（5/6/10/12px） | 归 GAP/PANEL_SPACING 刻度或 v-bind(cell*) |
| S7-52 | P3 | 面板/按钮组间距用逐子 margin 而非容器 gap | `GCSModal.vue:152,156`；`RadarChart.vue:187-188` | 容器 flex gap + padding |
| S7-53 | P3 | 负间距 hack 未注释 | `RadarChart.vue:220` margin-bottom:-20px 无注释 | 改定位/transform 或补注释 |
| S7-54 | P3 | 非 token 圆角散落（2px/4px，不在 --GCS-radius-* 档位） | PaginatedListPanel.vue:374、PlansPanel.vue:437（4px）；ForecastControlPanel:395、SiteAnalysisControlPanel:398（2px） | 归档位或补 4px 档 token |
| S7-55 | P3 | PortInfoPanel 圆角用非 token calc | `PortInfoPanel.vue:47` calc(1.25*v-bind(unitPx)) ≈10px | 改 var(--GCS-radius-md) 或对齐档位 |
| S7-56 | P3 | PortInfoPanel 写死 box-shadow（不随暗色档切换） | `PortInfoPanel.vue:50` rgba 手写（注释说明响应式取舍） | 暗色补档或登记豁免 |
| S7-57 | P3 | 字号越档（11px/10px）且面板标题 16px 字面量散落 | RadarChart.vue:209、WaterLevelProfilePanel.vue:436（11px）；PlansPanel.vue:399（10px）；标题 16px 三处 | 归 --GCS-font-size-* 档；标题用 PanelTitle/token |
| S7-58 | P3 | 固定 px 控件宽（非 GCS 网格倍数） | `WaterLevelProfilePanel.vue:394` width:160px | calc(2*var(--GCS-cell)) 或 v-bind(cell) |
| S7-59 | P3 | 动画时长/缓动无 token 体系 | 全库无 --GCS-duration-*；时长散落 GCSButton.vue:81、style.css:284/288、PlansPanel.vue:391、RadarChart.vue:193 | 定义 duration/ease token 收敛档位 |
| S7-60 | P3 | 手写圆形播放按钮（GCSButton 规格外重复） | `ForecastControlPanel.vue:272-275` .act-btn | 抽共享 FlatButton 或统一样式 |
| S7-61 | P3 | 面板 offset-y 用小数 Cell（1.25/5.5） | HomePage/ProfilePage/ForecastPage/SiteSelectionPage/FloodAnalysisPage/AppLayout 多处 `:offset-y="1.25"/"5.5"` | 整数 offset 或 panelPosition + PANEL_SPACING |
| S7-62 | P3 | `color: var(--GCS-bg-panel)` 作前景文字色（语义 token 误用） | ForecastControlPanel.vue:386/425、SiteAnalysisControlPanel.vue:426/446、PlansPanel.vue:474、GCSButton.vue:79 | 改 var(--GCS-text-inverse) |
| S7-63 | P3 | HomePage `:deep(.info-panel)` 与真实 `.port-info-panel` 类名漂移（死选择器） | `HomePage.vue:69` vs `PortInfoPanel.vue:18/40` | 改 `.home-page :deep(.port-info-panel)` |
| S7-64 | P3 | 全站图标体系为 emoji（29+ 处），非 icon 组件/SVG | App.vue:96、PortInfoPanel:21-33、LayerControlPanel:69-89、ForecastControlPanel:33-36、BottomNavBar:83 等 | 收敛统一 icon 组件（SVG + currentColor） |
| S7-65 | P3 | 滚动条样式仅抽屉一处，全站不统一 | style.css:256-258 仅 .GCS-drawer__body；其余滚动容器默认滚动条 | 全局滚动条 token 化（双主题） |
| S7-66 | P3 | rename→别名映射注释缺失，废弃 token 未触发清理 | 全 --GCS- 定义区无"旧名→新名"映射块；radius-xl/z-panel 定义后从未消费 | 先删 S7-45 死 token；补改名映射注释 |

### 专项8 算法与结果正确性（13 条，discover:20260816）

| 原编号 | 等级 | 标题 | 证据摘要 | 整改建议 |
| --- | --- | --- | --- | --- |
| 专项8 发现1 | P2 | 无候选衰减分支对自定义 decayFn 不保证「0 分」语义 | `scoringService.js:115` 无候选返 `decayFn(max,max)`（指数衰减 ≈36.8 分非 0）；生产固定 linearDecay 不触发 | :104/:115 无候选分支直接 return 0；补自定义 decayFn + 零设施用例 |
| 专项8 发现2 | P2 | 时间轴月份滑块步数 off-by-one，右端越界到 2032-01 | `ForecastControlPanel.vue:125` maxSteps=(2031-2021+1)*12=132；`stepToTime(132)='2032-01'`（node 复现）；年模式 10 正常闭合 | 月模式 maxSteps 改 `(END_YEAR-BASE_YEAR)*12+11` 或钳制；补边界用例 |
| 专项8 发现3 | P2 | 地图图层缓存命中路径未做事务校验，竞态守卫覆盖不全 | `useForecastLayer.ts:165-169` 缓存命中即 updateData 无 isTransactionValid；对照 useForecastTimeseries.ts:51-54 | 缓存命中分支补事务校验；补连点+缓存用例 |
| 专项8 发现4 | P2 | 预计算档位表 0.0 档 floodedKm2 与 features 自相矛盾（水位 0 声称淹没 6.87 km²） | `flood_levels.json.gz` 键"0.0"={featureCount:0,floodedKm2:6.87}；main.py:134-136 响应层补救（8-6 历史缺陷） | precompute_levels.py 源头修正 0 档 + 删响应层特判 + 校验测试 |
| 专项8 发现5 | P3 | 双通道简化容差不一致（查表 300m vs 兜底演算 180m） | `precompute_levels.py:41` SIMPLIFY_TOL=300 vs `flood_engine.py:109` 默认 180；main.py:150 miss 兜底 | 统一 simplify_tol 或注释声明有意设计 |
| 专项8 发现6 | P2 | api 模式超档（15<水位≤25）静默返回「无淹没」，风险语义反转且缺 actualWaterLevel | `floodAnalysisController.js:7-8` MAX=25、`:51` 6 档上限 15、`:68-73` 超档返 `{riskLevel:'无',features:[]}`；三端点超档行为互不一致 | 15<m≤25 返 15 档或显式 INVALID_PARAMS；统一三端点 + actualWaterLevel 回显 |
| 专项8 发现7 | P3 | TOP_N=10 无取值依据注释 | `siteAnalysisService.js:7`（同文件其他参数均有注释） | 补业务依据注释 |
| 专项8 发现8 | P2 | README/设计文档将 container 归入「吞吐量模型产物」，实际走趋势外推引擎 | `README.md:12`；`核心流程与数据流.md:102`；实然 `forecastService.js:18` MODEL_INDICATORS 仅 cargo，container 落 computeForecast（引擎自述模拟实现） | README/02 §4.2 改口径「确定性趋势外推」或为 container 接模型 |
| 专项8 发现9 | P3 | O(n²) 退化预警阈值（>1000）高于当前单类上限（372），预警永不触发 | `siteAnalysisService.js:40`；node 实测单类最大 bus_station 372；b044 已定性可接受 | 阈值与性能基线挂钩 + 单位注释（未重复立案） |
| 专项8 发现10 | P2 | 评分核心 NaN/坏坐标防御分支无对应触发测试 | 防御分支实然生效（scoringService.js:60-64/152-155/:11）但 9 个 it 全未触发 | 补 3 个触发用例（linearDecay NaN/坏设施 skip/坏小区 0 分） |
| 专项8 发现11 | P2 | 水位档位边界（25.0 上界 / >15 表外档回退）无测试 | test_main.py 水位仅 3.5/3.4/5/15/7.2/0.0；test_real_engine.py 仅 0/2/5/10 | 补表外档 miss 回退不 500、上界 25.0 命中两用例 |
| 专项8 发现12 | P2 | 加权平均与「权重 0 计入分母」设计语义零断言 | 应然 `核心流程与数据流.md:74-76`；实然 scoringService.js:145-167；测试从未断言合并 score 数值（主 agent 裁定代码与设计一致，缺口在测试） | 补 1 用例锁加权平均公式固定期望值 |
| 专项8 发现13 | P3 | spatialIndex「粗筛 ⊇ 精确集」属性测试仅 4 个固定点，无随机广域采样 | `spatialIndex.test.js:196-226` 仅 4 手工点；性能用例 1000 随机点只断言 candidateRatio | 补随机化属性测试（N 组随机点/多边形，断言 ⊇ 逐点精确） |

---

## 四、批次流转说明

- **专项1**：814 批次（2026-08-14）产出，随目录重组并入 `docs/audits/816/专项1-数据链审查/`；`discover=2026-08-14`，其余专项 `discover=2026-08-16`。
- **专项5 双窗口**：主窗口（27 条）与并行窗口（29 条）为同一专项并行产出，等级分布与发现高度重叠（M1/M2/M3/M6/M7/M8/M9/M10 及 README、台账陈旧项），**裁决以合并去重后为准**；两份问题副本均保留归档。
- **审查期间已修**：专项5主 发现1（build 阻断）已于审查期间由工作树更新修复（11:13:57），需合入前干净环境终裁复核。
- **编号承接**：专项7 编号 S7-32 ~ S7-66 承接批次序列（S7-31 之后）；其余专项为批次内序号。
- **台账对账 / 复验清单**：见 `docs/audits/816/816问题副本/00-批次汇总.md` §3、§6。

---

## 四·五、历史批次流转（812 批次移交，2026-08-16）

> 812 批次裁决副本（`docs/问题副本-2026-08-12-专项审查总问题.md`）经 2026-08-16 逐条对账：
> 103 条新立案中 **101 条代码侧已闭环**（对应已解决台账编号见 812 副本 §三 各表，编号体系延续）；
> 以下为对账后**仍 open / 未转正 / 未登记**的残余条目，随 812 副本删除移交本批次继续裁决。
> 812 批次其余内容（去重对照/并入台账/后遗症批次/早期归档）均已由台账与各专项执行记录承接，历史描述存于 git 与桌面 audits 备份。

| 812 原编号 | 等级 | 标题（原文） | 证据摘要（原文） | 整改建议（原文） | 812→816 状态 |
| --- | --- | --- | --- | --- | --- |
| 专项5 副-20/21 | P2 | 死代码无全量扫描 | ts-prune 未装（抽样均被消费） | CI 加 ts-prune | **移交 open**：816 未承接，随本批裁决 |
| 专项7 S7-30 | P3 | 亮色主按钮白字对比度不足 2.78:1 | `--GCS-color-primary:#409eff` + `--GCS-text-inverse:#fff`，实算 2.78:1（<3:1 大文本阈值）；消费点 LoginPanel:195-196 等（暗色侧 6.67:1 达标） | 提亮 primary 至 ≥3:1 或登记品牌豁免注释；改前走 S7-10 SOP | **代码已处置未登记**：style.css:12 已登记豁免注释，台账未登记，移交补登记 |
| 专项7 S7-31 | P3 | JS 兜底色 '#666' 硬编码 | `RadarScoreTooltip.vue:33`、`useRadarChart.ts:93` `FACILITY_COLORS_MAP[key] \|\| '#666'` | colors.ts 增 `FACILITY_FALLBACK` 常量引用；补注释 | **代码已处置未登记**：colors.ts:24 `FACILITY_FALLBACK_COLOR` 已收口，台账未登记，移交补登记 |
| 专项3 3.3 | P3 | 内部函数无返回类型 111 处 | frontend/src（抽样） | 可接受或顺手修，需人工抽样判定 | 移交裁决（P3 可接受类） |
| 专项3 3.4 | P3 | composable 无显式返回类型 | 23 个 composable 中 23 个未见显式返回类型声明（as const 也算安全） | 可接受或顺手修 | 移交裁决（816 专项3-0816-13 已重立案 13 处，可并入） |
| 专项3 5.4 | P3 | store 泛型缺口 | 10 个 store 中 9 个未见泛型形式（需人工确认） | 可接受或顺手修 | 移交裁决（P3 可接受类） |
| 专项3 5.6 | P3 | ref(null) 无泛型 3 处 | 推断为 any 型 ref，应 `ref<T>(null)` | 可接受或顺手修 | 移交裁决（P3 可接受类；816 复核实测全仓 ref(null) 均带显式泛型） |
| 专项3 6.5 | P3 | interface/type 选择一致性 | interface 160 个 / type 47 个 | 人工确认选择一致性 | 移交裁决（P3 可接受类） |
| 专项3 7.3 | P3 | @types 缺失需人工确认 | 依赖 43 个，机械仅能列清单 | 人工确认 @types 覆盖 | 移交裁决（816 专项3-0816-11 rbush 类型已重立案，可并入） |

> 812 批次其他对账结论（供追溯）：P1-1 取数入口 → 已收口 apiRequest+schema+signal（c048 同批）；F-1 水位双源 → 816 专项2 4-1 重新立案承接；F-7 dataCache → forecastStore.ts:49 已移除注释；专项4 待裁决-1 → renderer.ts:20 注释已同步 a059；副-04 三态 → c027/c036 已做、816 专项5并 1-3 重新立案承接；副-07 CORS → d074 已处置；副-11 dist 阈值 → 816 M7 重新立案承接；副-28 PII → logSanitizer SENSITIVE_KEYS 已含 phone/email；S7-07 死 token → 816 S7-33 实锤假阴性重新立案承接；S7-11/S7-14/S7-16/S7-25 → 均已在代码侧解决（绑定注释/出处注释/shadow-sm token/ci.yml z101）。

---

## 五、优先处理建议

- **P0（上线阻断，2 条）**：专项1 发现1（假数据入正式路径，三文件实锤）；专项1 发现2（卸载未取消在途请求）。另专项5主 发现1（build 阻断）已修，合入前终裁。
- **P1 高价值（建议本批）**：M1 riskLevel 三处合并（行为已分叉，实锤漂移）；专项5主 发现2（flood-service 测试进 CI）；专项7 S7-32/S7-33（token 治理工具链假防护，验收跑不通 + 假阴性）；专项3-0816-03（savedXiaoqu 类型虚言）；专项4 1.3（`?? 0` 量纲伪装）；专项1 发现4/8/9/15（哨兵点/登出残留/假第一名/收藏链路丢失）；专项6 发现1/2（core 业务污染 P1×2）；专项2 1-1 与 b040 并案处置。
- **台账维护（零代码）**：1.2 节 6 项复核关闭（z116/z117/z038/d081/z077/d102）；d056 人工动作（key 轮换 + 域名白名单）挂人工核验单。
- **审查资产自身（直接修文档，不进主台账）**：专项6 发现20（core/README 与 cruise 引用已消失文件）、专项5主 发现27（navConfig 漂移）、专项2 2-4（ADR 7 漂移）、专项6 发现24（02 R4 漂移）、专项5主 发现23（types:gen WIP 未提交）。

---

*本文件为 816 批次用户裁决副本（按 `docs/待解决问题.md` 副本规则②命名落位）；批次内工作副本与执行记录保留于 `docs/audits/816/`，批次汇总见 `docs/audits/816/816问题副本/00-批次汇总.md`。裁决转正后本文件作为批次归档留存。*
