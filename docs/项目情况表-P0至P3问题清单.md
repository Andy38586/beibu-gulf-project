# 项目情况表（P0–P3 问题清单）

> **日期**：2026-07-23
> **检测方式**：真实代码通读（前端 74 文件 + 后端 21 文件，关键条目全部人工复核行号）+ 全项目运行验证
> **分级定义**：
> - **P0**：阻断核心功能 / 数据损坏 / 安全越权
> - **P1**：功能缺陷（功能存在但行为错误）
> - **P2**：边界、时序、一致性问题（特定场景下出错）
> - **P3**：代码质量 / 技术债务（不影响正确性）

---

## 一、项目总体状况速览

| 维度 | 状况 |
|---|---|
| 运行状态 | ✅ 前后端均可正常启动；后端 :3000、前端 :5173、API 代理链路正常（本次实测） |
| 生产构建 | ⚠️ 模块转译全通过；本次在当前沙箱中因环境拦截文件删除导致清空 dist 失败（环境问题，非项目问题） |
| 架构健康度 | 良好。历史两大 P0（渲染器接口缺失、core→business 反向依赖）已修复 |
| 当前最大隐患 | 图表组件 props 更新链断裂（P0-01），预测分析模块的直接前置阻断 |
| 预测分析模块 | 设计文档完成度 ~85%，未实现；落地前有 8 处文档与代码偏差需校准（详见《预测分析路由可行性报告》） |

---

## 二、P0 问题（2 个）

| 编号 | 位置 | 问题 | 证据与后果 | 修复建议 |
|---|---|---|---|---|
| P0-01 | `src/visualization/charts/composables/useChartBase.js:9-44` + `useECharts.js:89` | **LineChart/BarChart 在 props 变化后永不更新**。`baseOption` 是 setup 时求值的一次性普通对象，`getOption: () => baseOption` 永远返回旧快照；watch 虽触发 `updateChart`，`setOption` 用的仍是初始数据 | 任何动态数据场景图表全哑。当前未暴露仅因 AppLayout.vue:48-66 喂静态数据；**预测分析的核心交互（时间轴→图表联动）全部依赖 props 驱动，此 bug 为该模块的阻断级前置**。RadarChart 不受影响（useRadarChart.js:42-136 每次现取 props） | 将 `baseOption` 改为 `computed` 或在 `getOption` 函数体内现取 `props.xData/props.series/props.title` 构建 option，约 20 行 |
| P0-02 | `server/routes/markers.js:8-9` + `controllers/markersController.js` + `repositories/markersRepository.js:62` | **标记接口越权**：GET `/api/markers` 匿名返回全量标记；PUT/DELETE 仅校验登录、不校验归属，任何登录用户可改/删他人标记；更新白名单还包含 `'userId'`，可篡改标记归属 | 越权读写 + 数据可被任意篡改。属安全漏洞 | 写操作校验 `marker.userId === req.user.id`；GET 按用户过滤或脱敏；白名单移除 `userId` |

---

## 三、P1 问题（14 个）

| 编号 | 位置 | 问题 | 后果 |
|---|---|---|---|
| P1-01 | `src/shared/components/PaginatedListPanel.vue:370-373` vs `ErrorPopup.vue:21,48` | 未登录收藏的"去登录"弹窗**永不渲染**：使用了 `<ErrorPopup v-if="showLoginPopup" mode="login">` 但未传 `:visible`，而 ErrorPopup 内部用 `v-if="visible"`（默认 false）控制渲染。**07-23 实施日志中的 P3-2 修复实际是无效修复** | 未登录点收藏 → 用户得不到任何反馈，体验断点仍在 |
| P1-02 | `src/shared/composables/usePlans.ts:47-53, 80-86` | `createPlan`/`updatePlan` 中 `return apiRequest(...)` 前无 `await`，`finally` 在 Promise 创建后即执行，`saving`/`updating` 在请求完成前就被复位 | 防重复提交和加载态失效，用户可连点造成重复请求 |
| P1-03 | `src/views/ProfilePage.vue:155` + `usePlans.ts:77` | 重命名浸没分析方案必失败：`updatePlan(id, name, plan.typeSettings)`，flood 方案无 `typeSettings`（loadFloodPlan:128-134 可证），`Object.entries(undefined)` 抛 TypeError | flood 方案永远无法重命名，只提示"重命名失败" |
| P1-04 | `src/views/ProfilePage.vue:322` | 重命名弹窗初始名用错变量：`:initial-name="editingPlan?.name"` 用的是 inject 的 `editingPlan`（选址页"加载方案"时设置），应为本地 `editingNamePlan`（行 54、141-145） | 弹窗初始名为空或显示成另一个方案的名字 |
| P1-05 | `src/business/site-selection/SiteSelectionPage.vue:222,236-240` + `80-93` | 从个人中心恢复选址状态时丢失 `selectedTypes`：restoreState 先恢复它，随后 `handleResult({matchedXiaoqu})` 内 `selectedTypes.value = result.selectedTypes || []` 将其重置为空数组；且 `facilityPoi` 未入保存字段，恢复后也为 `{}` | 恢复后雷达图维度全丢、设施 POI 图层无法再展示 |
| P1-06 | `server/controllers/authController.js:22-27` | 注册存在 TOCTOU 竞态：`userExists` 检查在写锁之外，两个并发同名注册都通过检查后各自入锁写入 | users.json 出现重复用户名；`findByUsername` 只命中第一个，**第二个账号永远无法登录**（数据损坏，触发条件为并发同名注册，故列 P1 顶格） |
| P1-07 | `server/controllers/markersController.js:31` vs `repositories/markersRepository.js:62` | 标记字段名不一致：创建时存 `note`，更新白名单却是 `'description'` | 前端 PUT `{note}` 被白名单静默丢弃，更新"成功"但没改任何内容 |
| P1-08 | `server/services/siteAnalysisService.js:19` + `controllers/siteAnalysisController.js` | 选址分析对用户输入错误返回 500：`typeSettings[key]` 缺 key 时 `setting.defaultRadius` 抛 TypeError；`defaultRadius` 非数字 → NaN 半径；`selectedKeys` 传非数组同样 500 | 客户端参数错误应返回 400，全部变成 500 服务器错误，前端无法区分"自己传错"与"服务器挂了" |
| P1-09 | `server/controllers/siteAnalysisController.js:43` | 业务错误以 HTTP 200 + `{error}` 返回：无条件 `res.json(result)`，即使 `result.error` 非空 | 与全站 4xx+`{error}` 风格冲突，前端无法用状态码判断失败；axios 类客户端甚至不会进 catch |
| P1-10 | `src/core/map/renderers/CesiumRenderer.js:599-601` | `_doFlyTo` 的 `duration: 1000` 单位错误：Cesium 的 duration 单位是**秒**（同文件 697 行用 `3.0` 可证作者知情），此处带入了 OL 的毫秒习惯 | 任何 `flyTo({lng,lat})` 飞行动画长达 1000 秒（16.6 分钟），用户视角看似"卡死"且飞行期间相机被占用 |
| P1-11 | `src/core/map/renderers/CesiumRenderer.js:527-538` | `addGeoJsonLayer` 只处理 `entity.polygon`，无 billboard/point 分支，`markerColor`/`markerSize` 选项被完全忽略 | 3D 下浸没分析的受影响设施点（FloodAnalysisPage.vue:386-390 传了红色 10px 标记）渲染为默认图钉，2D/3D 视觉不一致 |
| P1-12 | `src/core/map/renderers/CesiumRenderer.js:302-312, 898-911` | `camera.changed` 监听器从不移除：destroy 将 `this.viewer = null` 但 Viewer 单例上的旧监听器仍在 | 离开 3D 再进入后拖动相机，旧闭包防抖 300ms 后访问 `null.scene` 抛 TypeError；监听器随进出次数累积（内存泄漏 + 错误刷屏） |
| P1-13 | `src/shared/components/LoginPanel.vue:185-191`（对照 119-125） | 已登录分支**没有渲染退出登录按钮**，`handleLogout` 是死代码，全站无任何登出途径 | 用户登录后无法退出（只能清浏览器数据） |
| P1-14 | `src/shared/components/LoginPanel.vue:52-57, 100-104` | `escapePassword()` 把密码中的 `&<>"'` 转 HTML 实体后再提交 | 密码被静默改写（`Pass&1`→`Pass&amp;1`）再哈希存储；防 XSS 用错层（凭据走 JSON，不经 HTML 渲染），与任何不做同样转义的客户端互不兼容 |

---

## 四、P2 问题（11 个）

| 编号 | 位置 | 问题 | 后果 |
|---|---|---|---|
| P2-01 | `src/business/flood-analysis/FloodAnalysisPage.vue:467` vs `500,517,546` | CSS 类名不匹配：模板根节点是 `gcs-analysis-page`，scoped 样式全写 `.flood-analysis-page` | 三条规则全部失效，尤其是为 Cesium 性能准备的 `backdrop-filter: none !important` 成了死代码，3D 页仍背着 backdrop-filter 的 WebGL 性能开销 |
| P2-02 | `src/business/flood-analysis/FloodAnalysisPage.vue:231-235, 244-309` | 防抖后的分析请求无乱序防护：快速拖水位滑块产生多个在途请求，旧响应后到会覆盖新数据 | 报告/地图短暂显示与当前水位不符的结果（竞态） |
| P2-03 | `src/views/ProfilePage.vue:128-134` + `stores/floodState.js:30` | `loadFloodPlan` 未传 `floodRiskLevel`，`saveState` 无条件赋值 `undefined`，覆盖默认值 `'无风险'` | flood 方案恢复后风险等级显示为空白/undefined |
| P2-04 | `src/views/ProfilePage.vue:187-205` | `watch(user, {immediate:true})` 与 `onMounted` 在已登录进入页面时各触发一次 `loadPlans()` | 每次进入个人中心并发两次 `GET /plans` |
| P2-05 | `src/shared/composables/useAuth.ts:113-114` | 多标签页登录态不同步：其他标签页登录时只写 `user`，未 `setToken` | `user` 有值但 `isAuthenticated` 为 false，依赖后者的判断（如 PaginatedListPanel:83）误判未登录 |
| P2-06 | `src/shared/composables/useAuth.ts`（logout） | 退出登录不清理 Pinia 业务状态（siteSelectionState/floodState 等） | 同设备下一用户可能看到上一用户保存的分析状态（隐私） |
| P2-07 | `server/controllers/floodAnalysisController.js`（实测验证） | 淹没范围"就近匹配"语义不透明：请求 `waterLevel=2.5` 实际返回 5m 淹没区且响应里未告知不匹配；灾害评估又用请求水位而非匹配区间水位过滤设施 | 地图上的淹没区（5m）与设施损失（2.5m）口径不一致，用户无感知（本次 curl 实测复现） |
| P2-08 | `server/services/scoringService.js:36` | 权重未校验：`weights[key] ?? 1`，可传负数/字符串/NaN | 综合分为 NaN 或负数权重扭曲排序，且以 200 返回 |
| P2-09 | `server/repositories/plansRepository.js:56` + `markersRepository.js:52` | ID 用 `Date.now().toString()`，同毫秒并发创建产生同 id | id 碰撞导致数据互相覆盖（users 已改用 `crypto.randomUUID`，这两处没跟上） |
| P2-10 | `server/repositories/plansRepository.js:62,118-124` | 先改内存缓存、后写文件：`fs.writeFile` 失败时缓存已脏 | 后续读取返回未落盘数据，缓存与磁盘不一致 |
| P2-11 | `server/routes/gcs.js`（全文无 authenticate）+ `controllers/authController.js:38,72` | 认证策略不一致：`/api/gcs/*` 完全无认证（其他数据接口全要登录）；登录/注册在已启用 HttpOnly Cookie 后仍在响应体返回 token | 安全策略口径混乱；token 出现在 JSON 里削弱了 Cookie 方案的 XSS 防护意义 |

---

## 五、P3 问题（15 个）

| 编号 | 位置 | 问题 |
|---|---|---|
| P3-01 | `src/core/map/renderers/OLRenderer.js:104` vs `UnifiedMap.vue:249-253` | 经纬度字段双轨制：`addPointLayer` 读 `item.lon`，`flyTo`/`startBreathing` 用 `lng`，业务层被迫到处做 lon/lng 互转（SiteSelectionPage:125-129,162-167）。当前靠 properties 里恰好有 `lon` 才没炸，属隐性陷阱 |
| P3-02 | `src/business/flood-analysis/FloodAnalysisPage.vue:78-86` | 水面多边形是硬编码示例坐标（注释自承"实际应从 floodArea.json 加载"），水面与真实淹没数据永远对不上 |
| P3-03 | `src/business/site-selection/SiteSelectionPage.vue:67-68` | `showErrorPopup`/`errorMessage` 声明后从不赋值，错误弹窗是死代码 |
| P3-04 | `src/business/site-selection/SiteSelectionPage.vue:280-289` | `tryZoom` 递归 setTimeout（最长 5s）未在 onUnmounted 清理，组件销毁后仍可能执行 zoomToCity |
| P3-05 | `src/core/layout/useGCS.js` | 每次调用创建独立状态 + 独立 window resize 监听，N 个组件 = N 个监听器；应改模块级单例（属浪费非泄漏） |
| P3-06 | `src/services/mapDataService.js` | 并发首访无 Promise 级去重（两个并发调用各发一次请求）；缓存永不过期无失效机制 |
| P3-07 | `src/core/config/map.js:2` | 天地图 KEY 硬编码 fallback 入库 |
| P3-08 | `server/services/scoringService.js:15` | `Math.min(...points.map())` 展开大数组，POI 量大时有栈溢出风险，应改 reduce |
| P3-09 | `server/repositories/facilitiesRepository.js` | 缓存永不过期，`invalidateCache` 无人调用，改数据文件必须重启服务 |
| P3-10 | `server/services/importanceMapping.js:9` | importance 为小数（如 2.5）时落不到表项，静默取 1，无警告 |
| P3-11 | 多处 | 生产环境 console 调试输出：FloodAnalysisPage.vue:232,246,254,261,282,289,296、RadarChart.vue:124、SiteSelectionPage.vue:81,159-160 等 |
| P3-12 | `src/router/index.js:43-56` | 路由守卫依赖 localStorage 的 `beibu-gulf-user` 判登录，但所有路由均未声明 `requiresAuth`，守卫实际不生效（死代码） |
| P3-13 | `src/core/map/renderers/CesiumRenderer.js:332, 898` | `destroy()` 重复定义两次，后者覆盖前者，332 行版本为死代码 |
| P3-14 | `src/shared/components/PlanSaveModal.vue` | `emit('error')` 未在 `defineEmits` 中声明 |
| P3-15 | `src/business/flood-analysis/components/AffectedFacilityListPanel.vue` | `formatLoss` 对 undefined 的 loss 缺防御 |

---

## 六、统计

| 级别 | 数量 | 与预测分析路由的关系 |
|---|---|---|
| P0 | 2 | P0-01 是预测模块的**直接前置阻断**；P0-02 为独立安全问题 |
| P1 | 14 | P1-01 在实施日志中被标记"已修复"但实际无效，建议回归；P1-10/11/12 集中在 CesiumRenderer |
| P2 | 11 | P2-02（请求乱序）的模式会被预测时间轴播放放大，实现时需前置规避 |
| P3 | 15 | P3-01（lon/lng 双轨）直接影响预测数据文件的字段设计 |

**建议修复顺序**：P0-01（预测前置，已有专项实施文档 `修复实施文档-P0-01图表props更新失效.md`）→ P1-01/02/03/04/13（均为小改动、用户可感知）→ P0-02 + P1-06/07（后端安全与数据正确性）→ P1-10/11/12（Cesium 三件套，可一次顺修）→ P1-08/09 + P2（健壮性）→ P3 随重构顺带清理。

---

## 七、覆盖率补扫记录（2026-07-23 第二轮）

第一轮的盲区文件经补扫后新增 P1-10 ~ P1-14、P3-13 ~ P3-15。补扫范围与结论：

| 补扫文件 | 结论 |
|---|---|
| `CesiumRenderer.js`（927 行，全读） | 发现 P1-10、P1-11、P1-12、P3-13 |
| 3 个浸没子面板（AffectedFacilityListPanel / FloodAnalysisReportPanel / WaterLevelProfilePanel） | 仅 P3-15，无 P0/P1 |
| LoginPanel.vue | 发现 P1-13、P1-14 |
| PlanSaveModal / PanelTitle / PortInfoPanel / RadarScoreTooltip / GcsPanel / GcsInspectionOverlay / NavButton / GcsButton / renderers/index.js / main.js / index.html | 仅 P3-14，其余无 P0/P1 |

**两轮合计覆盖**：前端 74 个源文件全部至少读过一遍（关键文件人工复核行号），后端 21 个源文件全读。

**仍明确未覆盖的区域**（诚实声明，后续可再扫）：

1. **数据文件内容校验**：`server/data/*.json`、`public/data/*.json` 的坐标有效性、字段完整性未做全量校验（只抽查了 ports.json、flood 系列）
2. **依赖层**：`npm audit` 漏洞、依赖版本漂移（如 vue-router ^5.1.0 是较新的主版本）未评估
3. **配置文件**：`eslint.config.js`、`jsconfig.json`、README 提到但根目录不存在的 `vercel.json`（文档与仓库不一致）
4. **运行时动态行为**：本轮以静态分析 + 接口实测为主，未做浏览器端自动化 E2E（如 Playwright 全流程回归）
5. **测试代码本身**：`__tests__` 下的测试是否存在假绿未逐一验证
