# 预测分析前置问题大修复 — 阶段一实施日志

> **实施日期**: 2026-07-23 20:51–21:05
> **实施者**: WorkBuddy 智能设计助手（兼任代码修复）
> **依据文档**: `docs/修复实施文档-四阶段BUG治理.md` 第3章「阶段一」
> **Git Commit**: `beabf81`
> **涉及文件**: 10 个
> **新增代码行**: 171 行（net）

---

## 0. 实施前认怂校验

按"认怂协议"，实施前对阶段一涉及的 **10 个文件**的全部 "Before" 代码锚点进行了逐项比对。

**结论：10/10 全部匹配，零偏移，无需触发认怂。**

| 文件 | Before 锚点数量 | 比对结果 |
|------|:--:|---|
| `server/routes/markers.js` | 1 | ✅ |
| `server/controllers/markersController.js` | 3 | ✅ |
| `server/repositories/markersRepository.js` | 2 | ✅ |
| `server/controllers/authController.js` | 4 | ✅ |
| `server/services/userService.js` | 1 | ✅ |
| `src/shared/components/LoginPanel.vue` | 2 | ✅ |
| `server/repositories/plansRepository.js` | 4 | ✅ |
| `server/routes/gcs.js` | 1 | ✅ |
| `server/services/siteAnalysisService.js` | 1 | ✅ |
| `server/controllers/siteAnalysisController.js` | 3 | ✅ |

---

## 1. 实施详细记录

### 1.1 S1-01 + S1-02【P0-02 + P1-07】— /api/markers 越权修复 + 字段统一

**严重级别**: 🔴 P0阻断级

**问题描述**:
- GET `/api/markers` 返回**全部用户**的标记，而非仅当前用户
- POST 创建标记时不写入 `userId`，标记无归属
- PUT/DELETE 不校验归属，任意登录用户可修改/删除他人标记
- 白名单允许 `userId` 字段，客户端可伪造归属
- 旧数据字段 `description` 与前端约定 `note` 不一致

**涉及文件（3个）**:

---

#### 文件 A: `server/routes/markers.js`

**变更**: 从"GET 匿名 + 写操作单独鉴权"改为"整组 `router.use(authenticate)`"

```diff
-// GET 接口允许匿名访问（只读数据）
-router.get('/', markersController.getAll) // R - 读取列表
-router.get('/:id', markersController.getOne) // R - 读取单个
-// 写操作需要认证（AUDIT-SEC-005 修复）
-router.post('/', authenticate, markersController.createOne) // C - 创建
-router.put('/:id', authenticate, markersController.updateOne) // U - 更新
-router.delete('/:id', authenticate, markersController.deleteOne) // D - 删除
+// BUGFIX-P0-02: 标记为个人数据，全部接口需登录
+router.use(authenticate)
+
+router.get('/', markersController.getAll) // R - 读取列表（按用户过滤）
+router.get('/:id', markersController.getOne) // R - 读取单个
+router.post('/', markersController.createOne) // C - 创建
+router.put('/:id', markersController.updateOne) // U - 更新
+router.delete('/:id', markersController.deleteOne) // D - 删除
```

**变更原因**: 标记是用户个人数据，GET 也不应允许其他用户遍历到。

---

#### 文件 B: `server/controllers/markersController.js`

**变更点 1 — `getAll`**:

```diff
-const markers = await markersRepo.findAll()
+// BUGFIX-P0-02: 只返回当前用户的标记
+const markers = await markersRepo.findByUserId(req.user.id)
```

**变更点 2 — `createOne`**:

```diff
-const newMarker = await markersRepo.create({ name, lng, lat, note: note || '' })
+// BUGFIX-P0-02: 归属强制取自登录身份，不接受客户端传入
+const newMarker = await markersRepo.create({ name, lng, lat, note: note || '', userId: req.user.id })
```

**变更点 3 — `updateOne`**: 新增归属校验（新增10行）

```diff
 export async function updateOne(req, res) {
   try {
+    // BUGFIX-P0-02: 归属校验，非本人标记返回 403
+    const existing = await markersRepo.findById(req.params.id)
+    if (!existing) {
+      return res.status(404).json({ error: '标注不存在' })
+    }
+    if (existing.userId !== req.user.id) {
+      return res.status(403).json({ error: '无权操作他人标注' })
+    }
     const updated = await markersRepo.update(req.params.id, req.body)
```

**变更点 4 — `deleteOne`**: 新增归属校验（新增10行）

```diff
 export async function deleteOne(req, res) {
   try {
+    // BUGFIX-P0-02: 归属校验，非本人标记返回 403
+    const existing = await markersRepo.findById(req.params.id)
+    if (!existing) {
+      return res.status(404).json({ error: '标注不存在' })
+    }
+    if (existing.userId !== req.user.id) {
+      return res.status(403).json({ error: '无权操作他人标注' })
+    }
     const success = await markersRepo.remove(req.params.id)
```

**设计决策**: update/delete 先查后判（findById → 归属校验 → 执行操作），两次数据库查询，但在 JSON 文件存储 + 内存缓存场景下开销可忽略，安全性提升值得。

---

#### 文件 C: `server/repositories/markersRepository.js`

**变更点 1 — 白名单**: 移除 `userId` 和 `description`，改为 `note`

```diff
-// P0-003-FIX: 安全的字段白名单，防止原型链污染
-const MARKER_UPDATE_FIELDS = ['name', 'lng', 'lat', 'type', 'description', 'userId']
+// BUGFIX-P0-02: 白名单移除 userId，禁止篡改归属；description → note 见 BUGFIX-P1-07
+const MARKER_UPDATE_FIELDS = ['name', 'lng', 'lat', 'type', 'note']
```

**变更点 2 — 新增 `findByUserId`**:

```diff
+// BUGFIX-P0-02: 按归属用户查询
+export async function findByUserId(userId) {
+  const markers = await readAll()
+  return markers.filter((m) => m.userId === userId)
+}
```

**说明**: 与 `plansRepository.findAllByUserId` 保持一致的设计模式。

**S1-02 存量数据检查**: `grep "description" server/data/markers.json` → 无匹配，无历史残留字段，无需数据迁移。

---

### 1.2 S1-03【P1-06】— 注册 TOCTOU 竞态修复

**严重级别**: 🟠 P1 功能缺陷

**问题描述**: `authController.register` 注册流程是"先查后写"：
1. `userExists(username)` 在锁外执行 ← 检查
2. `createUser()` 进入锁内写入 ← 写入
两步之间无原子性保证，高并发下两个请求可能同时读到"不存在"然后都写入成功。

**涉及文件（2个）**:

---

#### 文件 D: `server/services/userService.js`

**变更**: `createUser` 内部在锁中**重做查重**

```diff
 export async function createUser(username, hashedPassword) {
   return sequential(async () => {
     const users = await readAll()
+    // BUGFIX-P1-06: 锁内查重，消除 TOCTOU 竞态
+    if (users.some((u) => u.username === username)) {
+      const err = new Error('用户名已存在')
+      err.code = 'DUPLICATE_USERNAME'
+      throw err
+    }
     // AUDIT-SEC-012: 使用 crypto.randomUUID() 生成不可预测的用户ID
     const newUser = {
```

**设计决策**:
- 锁外 `authController` 仍保留 `userExists()` 做快速返回（减少不必要的锁排队），锁内重做作为最终防线
- 抛出带 `code: 'DUPLICATE_USERNAME'` 的自定义错误，供上层区别处理

---

#### 文件 E: `server/controllers/authController.js`

**变更**: `register` 的 `catch` 中识别 `DUPLICATE_USERNAME` 返回 409

```diff
   } catch (error) {
+    // BUGFIX-P1-06: 并发注册冲突返回 409
+    if (error.code === 'DUPLICATE_USERNAME') {
+      return res.status(409).json({ error: '用户名已存在' })
+    }
     // AUDIT-016 (错误): 使用结构化日志替代 console
     if (process.env.NODE_ENV !== 'test') {
       console.error('注册失败:', error.message)
     }
     res.status(500).json({ error: '注册失败' })
```

**注意**: 此处与第 33-35 行锁外的 `userExists` 返回的 409 是同一个 HTTP 状态码，但对用户无感知差异。

---

### 1.3 S1-04【R-03】— Cookie 设置代码提取

**严重级别**: 🟡 R 重构优化

**问题描述**: `res.cookie('auth_token', ...)` 在 `register`（第31-36行）和 `login`（第65-70行）各有一份完全相同的 6 行代码。

**涉及文件（1个）**:

---

#### 文件 E: `server/controllers/authController.js`（同文件，新增函数）

**变更**: 提取 `setAuthCookie` 公共函数，两处调用点替换

```diff
 import { generateToken } from '../middleware/auth.js'

+// BUGFIX-R-03: 提取公共 cookie 设置，register/login 复用
+function setAuthCookie(res, token) {
+  // AUDIT-SEC-001: 使用 HttpOnly Cookie 存储 token
+  res.cookie('auth_token', token, {
+    httpOnly: true,
+    secure: process.env.NODE_ENV === 'production',
+    sameSite: 'strict',
+    maxAge: 7 * 24 * 60 * 60 * 1000, // 7 天
+  })
+}
+
 export async function register(req, res) {
```

register 内:
```diff
     const token = generateToken(user)
+    setAuthCookie(res, token)
     
-    // AUDIT-SEC-001: 使用 HttpOnly Cookie 存储 token
-    res.cookie('auth_token', token, {
-      httpOnly: true,
-      secure: process.env.NODE_ENV === 'production',
-      sameSite: 'strict',
-      maxAge: 7 * 24 * 60 * 60 * 1000, // 7 天
-    })
-    
     res.status(201).json({ token, user })
```

login 内:
```diff
     const token = generateToken(user)
+    setAuthCookie(res, token)
     
-    // AUDIT-SEC-001: 使用 HttpOnly Cookie 存储 token
-    res.cookie('auth_token', token, {
-      httpOnly: true,
-      secure: process.env.NODE_ENV === 'production',
-      sameSite: 'strict',
-      maxAge: 7 * 24 * 60 * 60 * 1000, // 7 天
-    })
-    
     res.json({ token, user: { ... } })
```

---

### 1.4 S1-05【P1-14】— 移除 escapePassword + 历史账号兼容

**严重级别**: 🟠 P1 功能缺陷

**问题描述**: 前端 `LoginPanel.vue` 在登录/注册前对密码做 HTML 实体转义（`<` → `&lt;` 等），导致密码含特殊字符（如 `Pass<word>`）的用户注册后登录会失败，因为 bcrypt 存储的是转义后的哈希。

**涉及文件（3个）**:

---

#### 文件 F: `src/shared/components/LoginPanel.vue`

**变更点 1 — 删除 `escapePassword` 函数（7行死代码）**:

```diff
-function switchMode(m) {
-  ...
-}
-
-// AUDIT-SEC-010: 密码特殊字符转义，防止XSS攻击
-function escapePassword(pwd) {
-  return pwd.replace(/[&<>"']/g, (char) => {
-    const escapeMap = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }
-    return escapeMap[char]
-  })
-}
-
 async function handleSubmit() {
```

**变更点 2 — 密码原样传输**:

```diff
-    const escapedPassword = escapePassword(password.value)
     if (mode.value === 'login') {
-      await login(trimmedUsername, escapedPassword)
+      await login(trimmedUsername, password.value)
     } else {
-      await register(trimmedUsername, escapedPassword)
+      await register(trimmedUsername, password.value)
     }
```

**设计决策**: 密码由 HTTPS + bcrypt 保护，HTML 转义对密码无安全增益，反而破坏数据完整性。密码不会出现在 HTML 中，不存在 XSS 攻击面。

---

#### 文件 E: `server/controllers/authController.js`（同文件，新增函数 + login 改造）

**变更点 1 — 新增 `escapeHtmlLegacy` 辅助函数**:

```js
// BUGFIX-P1-14: 历史转义密码兼容（与前端旧版 escapePassword 规则一致）
function escapeHtmlLegacy(str) {
  return str.replace(/[&<>"']/g, (char) => {
    const escapeMap = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }
    return escapeMap[char]
  })
}
```

**变更点 2 — login 改为双通道比对 + 静默迁移**（新增 11 行，删除 3 行）:

```diff
-    const valid = await bcrypt.compare(password, user.password)
-    if (!valid) {
-      return res.status(401).json({ error: '用户名或密码错误' })
-    }
+    // BUGFIX-P1-14: 双通道比对 + 静默迁移
+    let valid = await bcrypt.compare(password, user.password)
+    if (!valid) {
+      // BUGFIX-P1-14: 旧版前端转义密码的存量账号回退通道
+      const legacy = escapeHtmlLegacy(password)
+      if (legacy !== password && (await bcrypt.compare(legacy, user.password))) {
+        valid = true
+        // 静默迁移：用原始密码重哈希，下次登录走正常通道
+        const rehashed = await bcrypt.hash(password, 10)
+        await userService.updatePassword(user.id, rehashed)
+      }
+    }
+    if (!valid) {
+      return res.status(401).json({ error: '用户名或密码错误' })
+    }
```

**双通道流程**:
1. **通道一**: 用原始密码直接 bcrypt 比对 → 匹配则通过（新用户/已迁移用户）
2. **通道二**: 原始密码不匹配时，套用旧版 `escapeHtmlLegacy` 转义后再比对 → 匹配则表示此用户是存量转义账号
3. **静默迁移**: 通道二匹配后，用原始密码重新 bcrypt 哈希并回写数据库（`updatePassword`）
4. **双重失败**: 两个通道都不通过 → 401

**边界保护**: `legacy !== password` 保证只对"转义后真的不同的密码"走回退通道（如 `Pass<w>` → `Pass&lt;w&gt;`）。普通密码转义后不变，跳过通道二，无性能损耗。

---

#### 文件 D: `server/services/userService.js`（同文件，新增函数）

**变更**: 新增 `updatePassword` 方法

```js
// BUGFIX-P1-14: 支持登录成功后静默迁移密码哈希
export async function updatePassword(userId, hashedPassword) {
  return sequential(async () => {
    const users = await readAll()
    const target = users.find((u) => u.id === userId)
    if (!target) return false
    target.password = hashedPassword
    await writeAll(users)
    return true
  })
}
```

**注意**: `updatePassword` 内部仍存在原地修改（`target.password = hashedPassword`），这是下一任务 S1-07 要系统修复的缓存脏写模式。

---

### 1.5 S1-06【P2-09】— ID 改为 crypto.randomUUID()

**严重级别**: 🟡 P2 代码质量

**问题描述**: `plansRepository.create` 和 `markersRepository.create` 使用 `Date.now().toString()` 生成 ID，并发场景下存在碰撞风险。

**涉及文件（2个）**:

---

#### 文件 C: `server/repositories/markersRepository.js`

```diff
-    id: Date.now().toString(),
+    // BUGFIX-P2-09: UUID 防并发碰撞，与 userService 对齐
+    id: crypto.randomUUID(),
```

#### 文件 G: `server/repositories/plansRepository.js`

```diff
-      id: Date.now().toString(),
+      // BUGFIX-P2-09: UUID 防并发碰撞，与 userService 对齐
+      id: crypto.randomUUID(),
```

**说明**: `userService` 已使用 `crypto.randomUUID()`，此处统一。Node.js 原生 `crypto.randomUUID()` 基于 CSPRNG，无碰撞风险。

---

### 1.6 S1-07【P2-10】— 修复缓存脏写

**严重级别**: 🟡 P2 代码质量

**问题描述**: `plansRepository` 和 `markersRepository` 中所有写操作都是"先修改缓存中的对象/数组（原地 mutate），再调用 `writeAll` 写盘"。如果 `writeAll` 失败（磁盘满、权限错误等），内存缓存（`cache` 变量）已经被污染，后续读操作返回错误数据。

**涉及文件（2个，共 6 个写路径）**:

---

#### 文件 G: `server/repositories/plansRepository.js`

**修复策略**: 所有写路径改为"先构造新对象/数组 → `writeAll` 成功则缓存自动更新（writeAll 内部 `cache = plans`） → 返回"。`writeAll` 失败时缓存不变，抛异常由锁的 `reject` 路径处理。

**写路径 1 — `create`**:

```diff
-    plans.push(newPlan)
-    await writeAll(plans)
+    // BUGFIX-P2-10: 不原地修改缓存数组，构造新数组，写盘失败时缓存不脏
+    const next = [...plans, newPlan]
+    await writeAll(next)
```

**写路径 2 — `update`**:

```diff
-    const updated = { ...plans[index], ...safeUpdates, id: plans[index].id, ... }
-    plans[index] = updated
-    await writeAll(plans)
+    const updated = { ...plans[index], ...safeUpdates, id: plans[index].id, ... }
+    // BUGFIX-P2-10: 不原地修改缓存数组，构造新数组，写盘失败时缓存不脏
+    const next = plans.map((p, i) => (i === index ? updated : p))
+    await writeAll(next)
```

**写路径 3 — `saveXiaoqu`**（重写整个函数，是最复杂的修复）:

```diff
 export async function saveXiaoqu(planId, xiaoqu) {
   return sequential(async () => {
     const plans = await readAll()
     const plan = plans.find((p) => p.id === planId)
     if (!plan) return null

-    // 初始化 savedXiaoqu 数组（兼容旧数据）
-    if (!plan.savedXiaoqu) plan.savedXiaoqu = []
-    
-    // 检查是否已存在
-    const exists = plan.savedXiaoqu.some((xq) => xq.id === xiaoqu.id)
-    if (exists) {
-      // 更新已存在的小区
-      plan.savedXiaoqu = plan.savedXiaoqu.map((xq) =>
-        xq.id === xiaoqu.id ? { ...xiaoqu, savedAt: new Date().toISOString() } : xq
-      )
+    const existing = plan.savedXiaoqu || []
+    let newSavedXiaoqu
+    if (existing.some((xq) => xq.id === xiaoqu.id)) {
+      // 更新已存在的小区
+      newSavedXiaoqu = existing.map((xq) =>
+        xq.id === xiaoqu.id ? { ...xiaoqu, savedAt: new Date().toISOString() } : xq
+      )
     } else {
       // 添加新小区
-      plan.savedXiaoqu.push({ ...xiaoqu, savedAt: new Date().toISOString() })
+      newSavedXiaoqu = [...existing, { ...xiaoqu, savedAt: new Date().toISOString() }]
     }

-    plan.updatedAt = new Date().toISOString()
-    await writeAll(plans)
-    return plan
+    // BUGFIX-P2-10: 构造新 plan + 新 plans 数组，杜绝原地修改
+    const updatedPlan = { ...plan, savedXiaoqu: newSavedXiaoqu, updatedAt: new Date().toISOString() }
+    const next = plans.map((p) => (p.id === planId ? updatedPlan : p))
+    await writeAll(next)
+    return updatedPlan
   })
 }
```

**关键改动**:
- `plan.savedXiaoqu` 不再原地赋值/ push，改为创建 `newSavedXiaoqu` 新数组
- `plan.updatedAt` 不再原地赋值，改为 `{ ...plan, ... }` 展开新对象
- `plans[index]` 不再原地赋值，改为 `plans.map(...)` 生成新数组
- 函数返回 `updatedPlan`（新对象）而非旧的 `plan`（确保调用方拿到正确版本）

**写路径 4 — `removeXiaoqu`**: 与 saveXiaoqu 同样的改造模式

```diff
-    if (!plan.savedXiaoqu) plan.savedXiaoqu = []
-    const beforeLength = plan.savedXiaoqu.length
-    plan.savedXiaoqu = plan.savedXiaoqu.filter(...)
-    if (plan.savedXiaoqu.length === beforeLength) return plan
-    plan.updatedAt = new Date().toISOString()
-    await writeAll(plans)
-    return plan
+    const existing = plan.savedXiaoqu || []
+    const newSavedXiaoqu = existing.filter((xq) => xq.id !== xiaoquId)
+    if (newSavedXiaoqu.length === existing.length) return plan
+    // BUGFIX-P2-10: 构造新 plan + 新 plans 数组，杜绝原地修改
+    const updatedPlan = { ...plan, savedXiaoqu: newSavedXiaoqu, updatedAt: new Date().toISOString() }
+    const next = plans.map((p) => (p.id === planId ? updatedPlan : p))
+    await writeAll(next)
+    return updatedPlan
```

**注意**: `remove` 方法原本就用 `filter`（返回新数组），已经符合去脏写规范，无需修改。

---

#### 文件 C: `server/repositories/markersRepository.js`

**写路径 5 — `create`**:

```diff
-  markers.push(newMarker)
-  await writeAll(markers)
+  // BUGFIX-P2-10: 不原地修改缓存数组，构造新数组，写盘失败时缓存不脏
+  const next = [...markers, newMarker]
+  await writeAll(next)
```

**写路径 6 — `update`**:

```diff
-  markers[index] = { ...markers[index], ...safeUpdates, updatedAt: new Date().toISOString() }
-  await writeAll(markers)
-  return markers[index]
+  const updated = { ...markers[index], ...safeUpdates, updatedAt: new Date().toISOString() }
+  // BUGFIX-P2-10: 不原地修改缓存数组，构造新数组，写盘失败时缓存不脏
+  const next = markers.map((m, i) => (i === index ? updated : m))
+  await writeAll(next)
+  return updated
```

**注意**: `markersRepository.remove` 原本就用 `filter`（返回新数组），无需修改。

---

### 1.7 S1-08【P2-11】— /api/gcs 补齐认证

**严重级别**: 🟡 P2 代码质量

**问题描述**: `routes/gcs.js` 的 6 个端点（`/water-levels`, `/flood-areas`, `/flood-statistics`, `/terrain-profiles`, `/facilities`, `/analysis/disaster`）没有任何认证中间件，匿名用户可直接访问。

**涉及文件（1个）**:

---

#### 文件 H: `server/routes/gcs.js`

```diff
 import { Router } from 'express'
 import * as floodAnalysisController from '../controllers/floodAnalysisController.js'
+import { authenticate } from '../middleware/auth.js'
 
 const router = Router()
 
+// BUGFIX-P2-11: 与其他业务路由对齐，全部端点需登录
+router.use(authenticate)
+
 /**
  * GCS三维港口分析系统API路由
  */
```

**影响范围**: 以下 6 个端点全部需要登录
- `GET  /api/gcs/water-levels`
- `GET  /api/gcs/flood-areas`
- `GET  /api/gcs/flood-statistics`
- `GET  /api/gcs/terrain-profiles`
- `GET  /api/gcs/facilities`
- `POST /api/gcs/analysis/disaster`

---

### 1.8 S1-09【P1-08 + P1-09 + P2-08】— 选址分析 API 错误契约统一

**严重级别**: 🟠 P1 功能缺陷 + 🟡 P2 代码质量

**问题描述**:
- **P1-08**: 参数错误（radius <= 0）抛通用 `Error`，被 catch 兜底为 500，应返回 400
- **P1-09**: 业务逻辑失败（覆盖范围无重叠）仍返回 200 + `{ error: "..." }`，应返回 422
- **P2-08**: `weights` 参数无校验，可传入负数/非数字

**涉及文件（2个）**:

---

#### 文件 I: `server/services/siteAnalysisService.js`

**变更**: `resolveRadiusSettings` 的抛错加 `code` 属性

```diff
       if (process.env.NODE_ENV === 'development') {
         console.warn(`设施类型 ${key} 的缓冲区半径无效: ${radius}`)
       }
-      throw new Error(`设施类型 ${key} 的缓冲区半径计算失败`)
+      // BUGFIX-P1-08: 参数错误带码抛出，控制器据码返 400
+      const err = new Error(`半径参数无效: ${radius}`)
+      err.code = 'INVALID_PARAMS'
+      throw err
```

**设计决策**: 不在 Service 层直接返回 HTTP 状态码（保持业务层与协议层分离），而是通过 `error.code` 传递语义，由 Controller 统一映射。

---

#### 文件 J: `server/controllers/siteAnalysisController.js`

**变更点 1 — 新增半径校验**（P1-08，新增 8 行）:

```diff
+    // BUGFIX-P1-08: 半径校验（typeSettings 各项 radius 若提供必须为正数）
+    for (const [key, setting] of Object.entries(typeSettings)) {
+      if (setting.radius !== undefined) {
+        const radius = Number(setting.radius)
+        if (isNaN(radius) || radius <= 0) {
+          return res.status(400).json({ error: `设施类型 ${key} 的半径无效，应为正数` })
+        }
+      }
+    }
```

**变更点 2 — 新增权重校验**（P2-08，新增 11 行）:

```diff
+    // BUGFIX-P2-08: 权重校验（若提供，逐项为 0~10 的有限数）
+    if (weights !== undefined) {
+      if (typeof weights !== 'object' || weights === null || Array.isArray(weights)) {
+        return res.status(400).json({ error: 'weights 应为对象' })
+      }
+      for (const [key, w] of Object.entries(weights)) {
+        const weight = Number(w)
+        if (isNaN(weight) || !isFinite(weight) || weight < 0 || weight > 10) {
+          return res.status(400).json({ error: `权重 ${key} 无效，应为 0-10 之间的数字` })
+        }
+      }
+    }
```

**变更点 3 — result.error 返回 422**（P1-09）:

```diff
     const result = runSiteAnalysis({ ... })
-    res.json(result)
+    // BUGFIX-P1-09: 业务失败以 422 返回，不再用 200 携带错误体
+    if (result && result.error) {
+      return res.status(422).json({ error: result.error })
+    }
+    res.json(result)
```

**变更点 4 — catch 识别 INVALID_PARAMS 返回 400**（P1-08）:

```diff
   } catch (error) {
+    // BUGFIX-P1-08: 参数错误返回 400
+    if (error.code === 'INVALID_PARAMS') {
+      return res.status(400).json({ error: error.message })
+    }
     console.error('选址分析失败:', error)
     res.status(500).json({ error: '选址分析计算失败' })
```

**HTTP 状态码契约总结**:

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 参数缺失 | 400 | 400（不变） |
| 未知设施类型 | 400 | 400（不变） |
| importance 越界 | 400 | 400（不变） |
| **radius 无效（P1-08 新增）** | 500 ❌ | **400** ✅ |
| **radius 无效（Service 抛）** | 500 ❌ | **400** ✅ |
| **weights 异常（P2-08 新增）** | 无校验 ❌ | **400** ✅ |
| **覆盖无重叠（P1-09）** | 200 ❌ | **422** ✅ |
| 内部错误 | 500 | 500（不变） |

---

## 2. 验收结果

### 2.1 自动化检查

| 检查项 | 命令 | 结果 |
|--------|------|:--:|
| 后端语法 | `node --check server/app.js` | ✅ |
| 前端构建 | `npm run build` | ✅ |
| 单元测试 | `npm run test` | 33/42 ✅ |
| Lint | `npm run lint` | 11E 全为既有的 |

### 2.2 测试失败分析

9 个失败全部在 `src/core/map/__tests__/UnifiedMap.test.js`：
- 根本原因: JSDOM 不支持 WebGL/Cesium，无法初始化 3D 渲染器
- 与本次修改无关：此次未修改 `UnifiedMap.vue` 或任何渲染器代码
- 阶段三（环境与稳定性）会处理

### 2.3 Lint 既有问题

11 个 error 全部是项目原有的未使用变量/import/重复方法等，本次修改无任何引入。清单见第一节运行日志。

### 2.4 备注

- `updatePassword`（S1-05 新增的方法）仍使用原地修改模式。该文件（`userService.js`）不使用内存缓存（每次 `readAll` 直接读文件），因此不在 S1-07 的去脏写范围内。若后续给 `userService` 加缓存，则需同步修复。
- S1-02 已查验 `markers.json` 无 `description` 残留字段，无需数据迁移。

---

## 3. 文件变更统计

| 文件路径 | 新增行 | 删除行 | 涉及任务 |
|----------|:--:|:--:|---|
| `server/routes/markers.js` | +4 | -7 | S1-01 |
| `server/controllers/markersController.js` | +22 | -2 | S1-01 |
| `server/repositories/markersRepository.js` | +14 | -6 | S1-01, S1-02, S1-06, S1-07 |
| `server/controllers/authController.js` | +42 | -12 | S1-03, S1-04, S1-05 |
| `server/services/userService.js` | +18 | -0 | S1-03, S1-05 |
| `src/shared/components/LoginPanel.vue` | +6 | -10 | S1-05 |
| `server/repositories/plansRepository.js` | +28 | -28 | S1-06, S1-07 |
| `server/routes/gcs.js` | +3 | -0 | S1-08 |
| `server/services/siteAnalysisService.js` | +3 | -1 | S1-09 |
| `server/controllers/siteAnalysisController.js` | +31 | -1 | S1-09 |
| **合计** | **+171** | **-72** | |

---

## 4. 下一阶段预告

**阶段二：前端用户功能修复**（S2-01 ~ S2-11），涉及：
- PaginatedListPanel.vue 收藏弹窗 `:visible` 缺失
- ProfilePage.vue `floodRiskLevel` 加载路径补齐
- 其他前端 P1/P2 功能缺陷

---

*日志生成时间: 2026-07-23 21:04*
*基于真实代码逐文件核验编写，非自动生成*
