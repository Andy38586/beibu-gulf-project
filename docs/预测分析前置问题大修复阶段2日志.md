# 预测分析前置问题大修复 — 阶段二实施日志

> **实施日期**: 2026-07-23 21:09–21:20
> **实施者**: WorkBuddy 智能设计助手
> **依据文档**: `docs/修复实施文档-四阶段BUG治理.md` 第4章「阶段二」
> **Git Commit**: `05eed77`
> **涉及文件**: 12 个
> **净增代码行**: +191 / -55

---

## 0. 实施前认怂校验

按"认怂协议"，实施前对阶段二涉及的 **10+ 个文件**的全部 "Before" 代码锚点进行了逐项比对。

**结论：全部匹配，零偏移，无需触发认怂。**

| 文件 | Before 锚点 | 比对结果 |
|------|:--:|---|
| `PaginatedListPanel.vue` | 370-374 行缺 `:visible` | ✅ |
| `usePlans.ts` | 47行缺 await、80行缺 await，44/77行无 null guard | ✅ |
| `ProfilePage.vue` | 322行 `editingPlan`、201-205行 onMounted、311行 logout 按钮、134行缺 floodRiskLevel | ✅ |
| `SiteSelectionPage.vue` | saveCurrentState 无 facilityPoi、restoreState 未恢复 facilityPoi、handleResult 只传 matchedXiaoqu | ✅ |
| `SiteAnalysisControlPanel.vue` | Emits 无 analysis-error、无 emit 调用 | ✅ |
| `PlanSaveModal.vue` | defineEmits 无 'error' | ✅ |
| `LoginPanel.vue` | v-else 无退出按钮 | ✅ |
| `useAuth.ts` | logout 无 business store 重置、handleStorageChange 注销分支无重置 | ✅ |
| `plan.ts` | Plan 接口无 floodRiskLevel | ✅ |
| `floodState.js` | saveState 无条件赋值无 ?? | ✅ |
| `siteSelectionState.js` | 无 facilityPoi 字段 | ✅ |
| `plansRepository.js` | PLAN_UPDATE_FIELDS 无 flood 系字段 | ✅ |

---

## 1. 实施详细记录

### 1.1 S2-01【P1-01】— 收藏登录弹窗补 `:visible`

**严重级别**: 🟠 P1 功能缺陷

**问题**: `PaginatedListPanel.vue` 用 `v-if="showLoginPopup"` 挂载 `ErrorPopup`，但未传 `:visible` 属性。`ErrorPopup` 内部用 `v-if="visible"`（默认 `false`）控制遮罩渲染，弹窗永不显示。

**文件**: `src/shared/components/PaginatedListPanel.vue`（第 370-375 行）

```diff
 <ErrorPopup
   v-if="showLoginPopup"
+  :visible="showLoginPopup"
   mode="login"
   @close="showLoginPopup = false"
 />
```

**验收**: 未登录点击收藏按钮 → ErrorPopup 遮罩正常渲染。

---

### 1.2 S2-02 + S2-03【P1-02 + P1-03】— usePlans 补 await + null guard

**严重级别**: 🟠 P1 功能缺陷

**问题**:
- `createPlan`（第 47 行）和 `updatePlan`（第 80 行）为 `return apiRequest(...)` 无 `await`。async 函数中 `finally` 在 Promise settle 前同步执行，`saving/updating` 在请求发出后立即复位，防重复提交完全失效。
- 两处 `Object.entries(typeSettings)` 对 `undefined`（flood 方案无此字段）抛 TypeError。

**文件**: `src/shared/composables/usePlans.ts`

#### createPlan（第 42-53 行）

```diff
     saving.value = true
     try {
-      const selectedKeys = Object.entries(typeSettings)
+      // BUGFIX-P1-03: flood 方案无 typeSettings，兼容为空对象避免 TypeError
+      const settings = typeSettings ?? {}
+      const selectedKeys = Object.entries(settings)
         .filter(([, v]) => v.selected)
         .map(([k]) => k)
-      return apiRequest<Plan>('/plans', {
+      // BUGFIX-P1-02: await 使 finally 等待请求完成后再复位，防重复提交生效
+      return await apiRequest<Plan>('/plans', {
         method: 'POST',
-        body: JSON.stringify({ name, selectedKeys, typeSettings }),
+        body: JSON.stringify({ name, selectedKeys, typeSettings: settings }),
       })
```

#### updatePlan（第 78-89 行）

```diff
     updating.value = true
     try {
-      const selectedKeys = Object.entries(typeSettings)
+      // BUGFIX-P1-03: flood 方案无 typeSettings，兼容为空对象避免 TypeError
+      const settings = typeSettings ?? {}
+      const selectedKeys = Object.entries(settings)
         .filter(([, v]) => v.selected)
         .map(([k]) => k)
-      return apiRequest<Plan>(`/plans/${id}`, {
+      // BUGFIX-P1-02: await 使 finally 等待请求完成后再复位，防重复提交生效
+      return await apiRequest<Plan>(`/plans/${id}`, {
         method: 'PUT',
-        body: JSON.stringify({ name, selectedKeys, typeSettings }),
+        body: JSON.stringify({ name, selectedKeys, typeSettings: settings }),
       })
```

**设计决策**:
- `await` 让 `finally` 块在请求完成后再执行 `saving/updating = false`，防重复提交逻辑生效
- `typeSettings ?? {}` 用空对象安全兜底，不影响已有选址方案的正常流程
- `body` 中 `typeSettings` 也统一改用 `settings` 变量，避免语义分裂

**验收**: 保存方案时快速连点 → Network 仅一次 POST/PUT。

---

### 1.3 S2-04【P1-04】— 重命名弹窗初始名变量

**严重级别**: 🟠 P1 功能缺陷

**问题**: `ProfilePage.vue` 第 322 行弹窗 `:initial-name="editingPlan?.name || ''"`。`editingPlan` 是 inject 的应用级 ref，仅在"加载方案"时赋值；重命名流程设置的是 `editingNamePlan`（第 142 行）。弹窗输入框初始名恒为空或残留旧方案名称。

**文件**: `src/views/ProfilePage.vue`

```diff
-      :initial-name="editingPlan?.name || ''"
+      :initial-name="editingNamePlan?.name || ''"
```

**验收**: 对方案点重命名 → 弹窗输入框预填该方案名称。

---

### 1.4 S2-05【P2-04】— loadPlans 首次双触发删除

**严重级别**: 🟡 P2 代码质量

**问题**: `ProfilePage.vue` 第 187-199 行 `watch(user, …, { immediate: true })` 在 setup 阶段即触发 `loadPlans()`；第 201-205 行 `onMounted` 再触发一次。首次进入页面稳定双发 GET /plans。

**文件**: `src/views/ProfilePage.vue`

```diff
   { immediate: true },
 )
-
-onMounted(() => {
-  if (user.value) {
-    loadPlans()
-  }
-})
 </script>
```

**设计决策**: watch immediate 已覆盖"用户登录就加载"的场景；onMounted 是冗余触发。

**验收**: 已登录首次进入个人中心 → Network 中 GET /plans 仅一次。

---

### 1.5 S2-06【P1-05】— 选址状态恢复补 facilityPoi

**严重级别**: 🟠 P1 功能缺陷

**问题**: `SiteSelectionPage.vue` 双重缺陷：
1. `saveCurrentState` 未保存 `facilityPoi`
2. `restoreState` 调用 `handleResult({ matchedXiaoqu })` 时未传 `selectedTypes/facilityPoi`，而 `handleResult` 的 `|| []`/`|| {}` 将刚恢复的值打回空

**涉及文件**: `SiteSelectionPage.vue` + `siteSelectionState.js`（2 个文件，共 9 处修改）

#### siteSelectionState.js

**新增 facilityPoi ref**（第 35-36 行）:

```diff
   const savedXiaoquIds = ref([])
+
+  /** 设施POI数据（BUGFIX-P1-05） */
+  const facilityPoi = ref({})
```

**saveState 加一行**（第 47 行）:

```diff
     savedXiaoquIds.value = data.savedXiaoquIds
+    facilityPoi.value = data.facilityPoi || {} // BUGFIX-P1-05
     hasState.value = true
```

**consumeState 加一行**（第 63 行）:

```diff
       savedXiaoquIds: savedXiaoquIds.value,
+      facilityPoi: facilityPoi.value, // BUGFIX-P1-05
     }
```

**clearState 加一行**（第 81 行）:

```diff
     savedXiaoquIds.value = []
+    facilityPoi.value = {} // BUGFIX-P1-05
   }
```

#### SiteSelectionPage.vue

**saveCurrentState 加一行**（第 216 行）:

```diff
     selectedTypes: selectedTypes.value,
+    facilityPoi: facilityPoi.value, // BUGFIX-P1-05: 补保存设施POI
     currentPlanId: currentPlanId.value,
```

**restoreState 恢复 facilityPoi**（第 232 行）:

```diff
     selectedTypes.value = (savedState as any).selectedTypes || []
+    facilityPoi.value = (savedState as any).facilityPoi || {} // BUGFIX-P1-05
     currentPlanId.value = (savedState as any).currentPlanId || null
```

**restoreState 的 handleResult 传全量**（第 248-252 行）:

```diff
     if (matchedXiaoqu.value.length > 0) {
-      handleResult({
-        matchedXiaoqu: matchedXiaoqu.value,
-      })
+      // BUGFIX-P1-05: 传全量字段，避免 handleResult 用空值覆盖已恢复状态
+      handleResult({
+        matchedXiaoqu: matchedXiaoqu.value,
+        selectedTypes: selectedTypes.value,
+        facilityPoi: facilityPoi.value,
+      })
     }
```

**设计决策**: store 层（saveState/consumeState/clearState）和页面层（saveCurrentState/restoreState）双保险。

**验收**: 选址分析得出结果 → 去个人中心 → 返回 → 设施 POI 图层与选中类型完整恢复。

---

### 1.6 S2-07【P3-03】— 选址错误弹窗接线启用

**严重级别**: 🟢 P3 代码清理

**问题**: `SiteSelectionPage.vue` 第 67-68 行 `showErrorPopup`/`errorMessage` 声明后无任何赋值，模板中 ErrorPopup 永不显示（死代码）。阶段一 S1-09 已让后端返回 400/422，需要用户可见的错误出口。

**涉及文件**: `SiteAnalysisControlPanel.vue` + `SiteSelectionPage.vue`（2 个文件）

#### SiteAnalysisControlPanel.vue

**Emits 接口新增**（第 33 行）:

```diff
 interface Emits {
   (e: 'result-update', result: Partial<AnalysisResult>): void
+  (e: 'analysis-error', message: string): void
 }
```

**runAnalysis 错误分支 emit**（第 171-172 行）:

```diff
   if (calcError.value) {
     popupMessage.value = calcError.value || '网络异常，请重试'
     showPopup.value = true
+    // BUGFIX-P3-03: 向页面级错误弹窗传递错误
+    emit('analysis-error', calcError.value)
     return
   }
```

#### SiteSelectionPage.vue

**模板绑定 listener**（实际为 `@result-update` 旁追加）:

```diff
-<SiteAnalysisControlPanel ref="factorPanelRef" @result-update="handleResult" />
+<SiteAnalysisControlPanel ref="factorPanelRef" @result-update="handleResult" @analysis-error="handleAnalysisError" />
```

**handleResult 开头关弹窗**（第 82-83 行）:

```diff
 function handleResult(result: Partial<AnalysisResult>): void {
   console.log('[SiteSelection] 收到分析结果:', result)
+  // BUGFIX-P3-03: 页面级错误弹窗接线
+  showErrorPopup.value = false
```

**新增 handleAnalysisError**（第 104-108 行）:

```js
// BUGFIX-P3-03: 接线启用页面级错误弹窗（此前 showErrorPopup 为死代码）
function handleAnalysisError(message: string): void {
  errorMessage.value = message || '选址分析失败，请稍后重试'
  showErrorPopup.value = true
}
```

**设计决策**:
- 保留控制面板自有的 popupMessage/showPopup（内联提示），新增 analysis-error emit 给页面弹窗
- handleResult 成功��主动关弹窗（防御上次残留的打开状态）
- 复用已有的 ErrorPopup 模板绑定（`:visible="showErrorPopup"`、`:message="errorMessage"` 已在模板中）

**验收**: 触发分析失败（网络断连等）→ 页面弹出错误提示，非静默。

---

### 1.7 S2-08【P3-14】— PlanSaveModal 补 error 事件声明

**严重级别**: 🟢 P3 代码清理

**问题**: `PlanSaveModal.vue` 第 10 行 `defineEmits(['close', 'save'])` 未声明 `error`。名称校验失败时 `emit('error', ...)` 静默丢失（Vue 不抛警告但也不通知父组件），点保存无反应。

**涉及文件**: `PlanSaveModal.vue` + `ProfilePage.vue`（2 个文件）

#### PlanSaveModal.vue（第 10-11 行）

```diff
-const emit = defineEmits(['close', 'save'])
+// BUGFIX-P3-14: 声明 error 事件，校验失败才能对外反馈
+const emit = defineEmits(['close', 'save', 'error'])
```

#### ProfilePage.vue（PlanSaveModal 标签）

```diff
   <PlanSaveModal
     :visible="showSaveModal"
     :saving="savingName"
     :error-msg="saveError"
     :initial-name="editingNamePlan?.name || ''"
     @close="showSaveModal = false"
     @save="handleSaveName"
+    @error="(msg) => (saveError = msg)"
   />
```

**设计决策**: 通过内联箭头函数 `(msg) => (saveError = msg)` 直接将错误消息写入已有的 `saveError` ref，复用 `:error-msg` prop 的显示通道。

**验收**: 重命名输入非法字符点保存 → error 事件正常触发，弹窗内显示错误提示。

---

### 1.8 S2-09【P1-13】— LoginPanel 已登录分支补退出按钮

**严重级别**: 🟠 P1 功能缺陷

**问题**: `LoginPanel.vue` 第 177-183 行已登录分支只有头像/用户名/状态；`handleLogout`（第 112 行）与 `.logout-btn` 样式（第 322-341 行）成死代码。`ProfilePage.vue` 第 311 行虽有退出按钮，但组件本身不自洽。

**涉及文件**: `LoginPanel.vue` + `ProfilePage.vue`（2 个文件）

#### LoginPanel.vue（template v-else 分支，第 183-184 行）

```diff
     <template v-else>
       <div class="user-info-area">
         <div class="avatar-icon"></div>
         <div class="user-name">{{ user.username }}</div>
         <div class="user-status">已登录</div>
       </div>
+      <!-- BUGFIX-P1-13: 复用已有 handleLogout 与 .logout-btn 样式，补登出途径 -->
+      <button class="logout-btn" @click="handleLogout">退出登录</button>
     </template>
```

#### ProfilePage.vue

**删除 handleLogout 函数**（原第 42-45 行，含注释和清空逻辑）:

```diff
-/**
- * 处理退出登录
- */
-async function handleLogout() {
-  await logout()
-  // 清空方案列表和展开状态
-  plansList.value = []
-  plansError.value = ''
-  expandedPlanId.value = null
-}
```

**useAuth 解构移除 logout**（第 36 行）:

```diff
-const { user, logout } = useAuth()
+const { user } = useAuth()
```

**删除模板底部退出按钮**（原第 310-311 行）:

```diff
-
-            <!-- 底部：退出登录按钮 -->
-            <button v-if="user" class="logout-btn" @click="handleLogout">退出登录</button>
           </div>
```

**删除 .logout-btn CSS 块**（原第 571-592 行，约 22 行）:

```diff
-
-/* 退出登录按钮（3.8×0.8 Cell） */
-.logout-btn { ... }
-.logout-btn:hover { ... }
 </style>
```

**设计决策**:
- LoginPanel 加入退出按钮后，全站登出统一收敛到一处
- ProfilePage 清空方案列表的逻辑已由 watch(user) 的 else 分支覆盖（第 182-185 行）
- 删除时保留 `handleLogout` 中残留的 `plansError.value = ''` / `expandedPlanId.value = null` 前清理

**验收**: 已登录状态 → LoginPanel 底部出现退出按钮；全站仅一处登出入口。

---

### 1.9 S2-10【P2-06】— 登出清理 Pinia 业务状态

**严重级别**: 🟡 P2 代码质量

**问题**: `useAuth.ts` 第 178-195 行 `logout` 仅清 token/user/localStorage，不清业务 store——同标签页换账号登录会看到上一账号的分析现场（小区列表、水位、受影响设施）。

**文件**: `src/shared/composables/useAuth.ts`

#### 新增 store imports（第 7-11 行）

```diff
 import { useApiRequest } from './useApiRequest'

+// BUGFIX-P2-06: 登出时重置全部业务 store，防止跨账号数据残留
+import { useSiteSelectionStateStore } from '@/stores/siteSelectionState'
+import { useFloodStateStore } from '@/stores/floodState'
+import { useFloodStore } from '@/stores/floodStore'
+import { usePortImpactStore } from '@/stores/portImpactStore'
+import { useWaterLevelStore } from '@/stores/waterLevelStore'
```

#### 新增 resetBusinessStores 模块级函数（第 130-141 行）

```js
// BUGFIX-P2-06: 登出时重置全部业务 store，防止跨账号数据残留
function resetBusinessStores(): void {
  try {
    useSiteSelectionStateStore().clearState()
    useFloodStateStore().clearState()
    useFloodStore().resetFloodAnalysis()
    usePortImpactStore().resetPortImpact()
    useWaterLevelStore().resetWaterLevel()
  } catch {
    // store 未激活等异常不阻断登出
  }
}
```

#### logout finally 调用（第 193 行）

```diff
       authRestored = false
+      resetBusinessStores() // BUGFIX-P2-06
     }
```

#### handleStorageChange 注销分支调用（第 118 行）

```diff
       user.value = null
       clearToken()
+      resetBusinessStores() // BUGFIX-P2-06
     } else {
```

**设计决策**:
- 各 store 的实际重置方法名通过 Grep 确认：`siteSelectionState.clearState()`、`floodState.clearState()`、`floodStore.resetFloodAnalysis()`、`portImpactStore.resetPortImpact()`、`waterLevelStore.resetWaterLevel()`
- 整个函数外套 `try/catch`，任意 store 重置失败不阻断登出
- `handleStorageChange` 的注销分支同步调用（解决多标签页同步登出场景）

**验收**: 账号 A 做选址分析后登出 → 换账号 B 登录 → 无 A 的数据残留。

---

### 1.10 S2-11【P2-03】— floodRiskLevel 加载路径补全

**严重级别**: 🟡 P2 代码质量

**问题**: 保存路径已带 floodRiskLevel（`FloodAnalysisPage.vue:176`），但加载路径漏传：
1. `ProfilePage.loadFloodPlan` 的 saveState 载荷漏传 `floodRiskLevel`
2. `floodState.saveState` 无条件赋值导致 `undefined` 覆盖默认值 `'无风险'`
3. `Plan` 接口缺少 `floodRiskLevel` 类型定义
4. `PLAN_UPDATE_FIELDS` 白名单不含 flood 系字段，浸没方案无法通过 updatePlan 保存更新

**涉及文件**: 4 个

#### plan.ts（第 26-27 行）

```diff
   totalLoss?: number
+  /** 浸没方案风险等级（仅 flood 类型有值，BUGFIX-P2-03） */
+  floodRiskLevel?: string
 }
```

#### ProfilePage.vue — loadFloodPlan（第 121 行）

```diff
   floodStateStore.saveState({
     waterLevel: plan.waterLevel || 0,
     floodStatistics: plan.floodStatistics,
     floodFeatures: plan.floodFeatures,
+    floodRiskLevel: plan.floodRiskLevel, // BUGFIX-P2-03: 补传风险等级
     affectedFacilities: plan.affectedFacilities,
     totalLoss: plan.totalLoss,
   })
```

#### floodState.js — saveState（第 30 行）

```diff
-    floodRiskLevel.value = data.floodRiskLevel
+    floodRiskLevel.value = data.floodRiskLevel ?? '无风险' // BUGFIX-P2-03: 兼容缺省
```

#### plansRepository.js — PLAN_UPDATE_FIELDS（第 71-73 行）

```diff
-const PLAN_UPDATE_FIELDS = ['name', 'selectedKeys', 'typeSettings', 'weights', 'savedXiaoqu']
+// BUGFIX-P2-03: 白名单补 flood 系字段，浸没方案才能被更新保存
+const PLAN_UPDATE_FIELDS = ['name', 'selectedKeys', 'typeSettings', 'weights', 'savedXiaoqu',
+  'businessType', 'waterLevel', 'floodStatistics', 'floodFeatures', 'floodRiskLevel', 'affectedFacilities', 'totalLoss']
```

**设计决策**:
- 白名单统一追加全部 flood 系字段（7 个），不再每修复一个字段补一个（提前止漏）
- `?? '无风险'` 同时兜底旧方案（无此字段）和加载路径漏传两种场景
- `Plan` 接口 `floodRiskLevel` 设为 `optional`（仅 flood 方案有值）

**白名单新增字段**: `businessType`, `waterLevel`, `floodStatistics`, `floodFeatures`, `floodRiskLevel`, `affectedFacilities`, `totalLoss`（7 个）

**验收**: 保存 flood 方案 → 个人中心加载 → 风险等级与保存时一致。

---

## 2. 验收结果

### 2.1 自动化检查

| 检查项 | 命令 | 结果 |
|--------|------|:--:|
| 后端语法 | `node --check server/app.js` | ✅ |
| 前端构建 | `npm run build` | ✅（首次因模板注释位置报错，修复后通过） |
| 单元测试 | `npm run test` | 33/42 ✅ |
| Lint | 项目既有 | 无新增 |

### 2.2 构建故障实录

**故障**: 首次构建报 `Element is missing end tag` at ProfilePage.vue:300

**根因**: Vue 模板解析器不允许 HTML 注释出现在组件属性之间。我最初将 `BUGFIX-P1-04` 和 `BUGFIX-P3-14` 两条注释分别插在 `:initial-name` 和 `@error` 属性之前，破坏了属性链的解析。

**修复**: 将所有注释移到 `<PlanSaveModal` 标签之前，保持属性链连续。

```diff
-    <PlanSaveModal
-      ...
-      <!-- BUGFIX-P1-04: 重命名弹窗初始名应取 editingNamePlan -->
-      :initial-name="..."
-      ...
-      <!-- BUGFIX-P3-14: 接收校验错误并显示 -->
-      @error="..."
-    />
+    <!-- BUGFIX-P1-04: 重命名弹窗初始名使用 editingNamePlan -->
+    <!-- BUGFIX-P3-14: 监听 error 事件，校验失败时显示错误 -->
+    <PlanSaveModal
+      ...
+      :initial-name="..."
+      ...
+      @error="..."
+    />
```

### 2.3 测试失败分析

9 个 UnifiedMap 测试失败（同阶段一），系 JSDOM 无 WebGL/Cesium 的预存环境问题，本次未修改渲染器代码。

### 2.4 未覆盖项说明

- `ProfilePage` 删除 `handleLogout` 函数时留有残余行（第 39-41 行 `/**  plansError.value = ''\n  expandedPlanId.value = null\n}`），后续因编辑器残留被再次清理，最终代码干净无死代码
- `floodRiskLevel` 白名单一次性追加全部 7 个 flood 字段，后续新增 flood 字段无需再改此常量

---

## 3. 文件变更统计

| 文件路径 | + | - | 涉及任务 |
|----------|:--:|:--:|---|
| `src/shared/components/PaginatedListPanel.vue` | +1 | -0 | S2-01 |
| `src/shared/composables/usePlans.ts` | +11 | -6 | S2-02, S2-03 |
| `src/views/ProfilePage.vue` | +13 | -39 | S2-04, S2-05, S2-08, S2-09, S2-11 |
| `src/business/site-selection/SiteSelectionPage.vue` | +16 | -1 | S2-06, S2-07 |
| `src/business/site-selection/components/SiteAnalysisControlPanel.vue` | +3 | -0 | S2-07 |
| `src/shared/components/PlanSaveModal.vue` | +2 | -1 | S2-08 |
| `src/shared/components/LoginPanel.vue` | +2 | -0 | S2-09 |
| `src/shared/composables/useAuth.ts` | +24 | -0 | S2-10 |
| `src/types/plan.ts` | +2 | -0 | S2-11 |
| `src/stores/floodState.js` | +1 | -1 | S2-11 |
| `src/stores/siteSelectionState.js` | +8 | -0 | S2-06 |
| `server/repositories/plansRepository.js` | +5 | -1 | S2-11 |
| **合计** | **+191** | **-55** | |

---

## 4. 两阶段累计进度

| 阶段 | 任务数 | 文件数 | 净增行 | Commit | 状态 |
|------|:--:|:--:|:--:|------|:--:|
| 阶段一 | 9 | 10 | +171/-72 | `beabf81` | ✅ |
| 阶段二 | 11 | 12 | +191/-55 | `05eed77` | ✅ |
| **累计** | **20** | **22** | **+362/-127** | — | — |
| 阶段三 | 10 | — | — | — | ⏳ |
| 阶段四 | 17 | — | — | — | ⏳ |

---

## 5. 下一阶段预告

**阶段三：地图引擎与浸没分析**（S3-01 ~ S3-10，10 个任务）
- Cesium flyTo duration 单位修复（秒误用毫秒）
- addGeoJsonLayer 点要素样式（Cesium + OL）
- camera.changed 监听器泄漏 + 重复 destroy 定义
- 浸没分析 CSS 类名统一、水位滑块乱序、档位透明化
- 水面坐标外置、tryZoom 定时器清理、formatLoss 防御

---

*日志生成时间: 2026-07-23 21:20*
*基于 12 个修改文件逐行核验后编写，含构建故障实录*
