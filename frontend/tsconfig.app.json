{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "preserve",

    /* JavaScript 支持 */
    "allowJs": true,
    "checkJs": false,

    /* Linting */
    "strict": true,
    "noImplicitAny": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    /* Path mapping（TS 5.0+ paths 无需 baseUrl，路径相对于 tsconfig 解析） */
    "paths": {
      "@/*": ["./src/*"]
    },
    /* node 内置模块类型（schemas.test.ts 等读取 backend/data 真实样本做校验验证） */
    "types": ["node"]
  },
  "include": ["./src/**/*.ts", "./src/**/*.tsx", "./src/**/*.vue", "./src/**/*.js", "./test/**/*.ts", "auto-imports.d.ts", "components.d.ts"]
}
