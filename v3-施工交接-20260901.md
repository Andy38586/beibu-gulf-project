# v3 迁移施工交接文档（2026-09-01，T3.1~T3.5 途中）

> 给接手的 agent/人：本档回答「做到哪、还差什么、怎么接手」。权威状态以 `docs/v3-施工手册.md` 状态总表为准；本档是会话级补充（谁在做、卡在哪、坑在哪）。

## 一、当前状态

- 分支：`experiment/v3-backend-migration`（开工先 `git branch --show-current` 确认，勿 detached）。
- 已完成并收口（均含桌面日志 `C:\Users\JionHappY\Desktop\_北部湾项目\文档\后端nest，pg，postGIS\`，**日志已全部中文化**，手册锚点已同步）：
  - T3.1 auth：diff 11/11，提交 d03978c2（feat）/8a45fd79（docs）
  - T3.2 favorites：diff 8/8，提交 fe70e90a / a2ee775b（chore eslint 排除 .tmp-pip）/ 96c28e92
  - T3.3 plans：diff 8/8，提交 b037f8e7 / 686d061b
  - T3.4 forecast：diff 38/38，提交 6e2419e1 / ba61dedc
  - 前置修复：2662c623（nest 依赖声明失步 fix）、55656d41（P2 状态补录+认领）、b669484d（日志中文化+认领 T3.3~T3.6）
- 手册状态表：T3.1~T3.4 done；**T3.5/T3.6 仍 in-progress（已认领）**。
- nest 全套测试：15 文件 / 127 用例全绿（T3.4 时点）。

> **【2026-09-01 后续会话补记】T3.5 / T3.6 已全部收口**（本节上文是交接时的旧状态，保留作历史）：
>
> - T3.5 flood：diff **28/28**，提交 `ab9c3512`（含五路由 + 27 单测 + 17 e2e）；顺手修复 T3.4 遗留两处类型债。
> - T3.6 site-analysis：diff **41/41 字节级 PASS**，提交 `bfcef928`（九步计算逐行等价移植 + rbush 3.0.1 钉版 + 54 单测 + 16 e2e）。
> - 至此 **T3.1~T3.6 全 done**，T6.2（前端 adapter 逐模块切换）前置满足。
> - nest 测试规模：24 文件 / 284 用例（243 通过 + 41 跳过；本机无 PG 时 5 个真库套件必红，见 §四 第 9 条）。
> - 详细过程见桌面日志 `T3.5-Nest洪涝读API.md` / `T3.6-Nest选址分析.md`。

## 二、进行中：T3.5 flood 读 API（只写了一半，未提交）

> **【2026-09-01 后补】本任务已完成并提交 `ab9c3512`，详见桌面日志 `T3.5-Nest洪涝读API.md`。** 下文保留为历史待办原文，勿再照做。

**已完成（工作区未提交）**：

- `backend/nest/package.json` + lockfile：新增 `@turf/turf` **钉死 7.3.5**（与 Express 同版，防空间计算双端漂移），已 npm install 成功
- `backend/nest/src/modules/flood/flood.repository.ts`（5 个读文件方法，走 DataFilesService 统一缓存）
- `backend/nest/src/modules/flood/flood.service.ts`（assessDisaster：turf booleanPointInPolygon 空间筛选 + loss=value×damageRate，逐行移植）

**还差（按序）**：

1. `flood.controller.ts`：5 路由（GET flood-areas/flood-statistics/terrain-profiles/water-area + POST analysis/disaster，**全公开免鉴权**）。要点：`MAX_WATER_LEVEL=25`；水位校验用 **parseFloat**（非 Number，'2.5abc' 语义对齐）；6 档向上取档（find(≥level) ?? 最高档兜底，15<水位≤25 取 15 档）；`deriveRiskLevel` 六档派生导出（0无/2低/5中/8高/10极高/15灾难级）；water-area 空/缺 coordinates → 404001『水域坐标数据缺失或格式无效』；terrain-profiles 逐条注入 `datumOffset`（metadata.datumOffset ?? 0）；POST disaster 缺 waterLevel → 400001『缺少水位参数』，**@HttpCode(200)**（Express sendSuccess 默认 200）
2. `flood.module.ts`（providers: FloodRepository/FloodService/DataFilesService）+ app.module 挂载
3. 测试：controller 单测移植 Express `floodAnalysisController.test.js` 20 用例语义（水位校验四态、向上取档 2.5→3.0、等值命中、deriveRiskLevel 表、water-area 三态、缓存读一次、TTL 过期用 Date.now spy、LRU 淘汰）+ e2e（真数据文件）+ 双后端 diff（waterLevel 参数组合：缺省/0/2.5/5/15.1/超界错误）
4. 桌面日志 `T3.5-Nest洪涝读API.md` + 手册状态表 done + 提交

## 三、待办：T3.6 site-analysis 移植（2 人日，最重）

> **【2026-09-01 后补】本任务已完成并提交 `bfcef928`**（九步计算逐行等价移植；rbush 3.0.1 钉版 + 手写 ambient 类型；54 单测 + 16 e2e 全绿；双后端 diff 41/41 字节级 PASS；日志 `T3.6-Nest选址分析.md`）。下文保留为任务卡原文。

- POST /nest-api/site-analysis，九步计算**逐行等价移植**（turf buffer/union/intersect + rbush），源码参照 `backend/services/siteAnalysisService.js`；POI/小区数据读文件（`backend/data/site-selection/` 三城 JSON）；空结果（无重叠区域）是合法 data 不是 422。
- Express 侧测试：`siteAnalysisController.test.js`（4）+ `siteAnalysisService.test.js`（11）逐条移植；验收要求双后端 diff **字节级一致**（确定性计算，同参数响应完全一致，键序除外）。
- 依赖：rbush 需加进 nest package.json（同样钉死与 Express 同版本）；turf 已就绪。
- 完成后 T3.x 全齐：auth/plans/favorites 同批切换前置满足（T6.2 第一批），forecast/flood/site-analysis 可独立切换。

## 四、接手必读的坑（本会话实锤）

1. **环境铁律**（手册 §2.2 + 会话实测）：提交 message 用 Write 写 UTF-8 文件 + `git commit --no-verify -F <file>`（argv 会 GBK 乱码）；提交前 `npx prettier --write` 必须最后跑（eslint --fix 之后要重跑，T3.1 首轮 format 红）；`git status backend/data` 必须零变更；门禁 `NODE_OPTIONS= npm run ci:local`。
2. **双后端 diff 流程**（脚本在 `.tmp-pip/`，已入 eslint ignores，勿放别处）：
   - 3000 被用户 `.staging-react` Next dev 占用 → Express 用 `PORT=3001 node --env-file-if-exists=backend/.env backend/index.js` 起；Nest 用 `JWT_SECRET=$(cat .tmp-pip/jwt-secret.txt) node backend/nest/dist/src/main.js`（产物在 dist/**src**/main.js）。
   - **服务必须用 run_in_background 起**（`&` 起的会在 Bash 调用结束后被回收，ECONNREFUSED 实锤）；收尾 TaskStop。
   - **diff 前快照** `backend/data/users.json`（auth/plans diff 会写入）、`plans.json`/`favorites.json`（原本不存在则 diff 后删除）→ 收尾复原 + PG 清理（`substr(username,1,N)` 前缀，先删 favorites/plans 再删 users 防 FK）。
   - 比对用 **canonical 序列化**（键递归排序，jsonb 会重排键序）+ 易变字段（id/userId/createdAt/updatedAt/savedAt/token）归一化。
3. **已修并固化的坑**（新代码别重踩）：
   - TS ES2022 define-fields：DTO 类声明字段会以 undefined 进入自有属性集，部分更新语义被破坏——**parse 返回普通对象只含已定义键**（plans.dto 有注释）。
   - **POST 默认 201 vs Express sendSuccess 默认 200**：非"资源创建"端点必须 @HttpCode(200)（favorites add、plans saveXiaoqu 都实锤过）。
   - Nest DI 会把带默认值的构造参数当依赖 → 加 `@Optional()`（data-files.service 有注释）。
   - 测试清理用 substr 前缀而非 LIKE 反斜杠转义；删 users 前先按用户名前缀联删子表。
4. 测试前缀统一 `__t3_`；e2e 连真库 v3_dev（docker 容器 beibu-postgis 在跑，数据是 T2.3 迁移后的 2650 POI 基线）。
5. 遗留（T6.3 统一）：helmet 未复刻、trust proxy 未设、限流多实例存储；契约文档错误码表 7→9 项补记待用户裁决。
6. **T3.5 会话新坑（2026-09-01）**：`nest build` 对 TS 类型错误 **exit 0**（错误仅打印 stdout），vitest esbuild 不查类型 → 「build 成功」≠类型健康。T3.4 遗留两处类型错（data-files.service TS2300 / model-loader metadata 过注解）已顺手修复。后续门禁建议补 `npx tsc --noEmit -p tsconfig.build.json`。
7. **flood 真数据实跑结论（e2e 已固化）**：设施点命中仅 15 档 1 个（loss=3600），5/8/10 档均 0 命中——空评估是合法 data，勿当缺陷。
8. **coverage 本地假红配方（2026-09-01 实测三态）**：跑 coverage 前先 `git clean -f -d -x <模块>/coverage`（git 原生 exe 不经 safe-delete shim），再 shim 开启跑；跑后 exit 1 仅剩 cleanAfterRun 删 .tmp 被拦——测试与覆盖率数字有效。shim 关（NODE_OPTIONS=）时 frontend 必须加 `--no-file-parallelism`（否则挂死/留僵尸 worker），backend/nest 会炸 runner。vite build 前 `mv frontend/dist <备份>`。backend(Express) coverage 本地 40.55%<50% 属继承状态（代码零改动），勿在 T3.x 顺手凑数。

9. **T3.6 会话新坑（2026-09-01）**：
   - **成功信封无 `error` 键**：`EnvelopeInterceptor` 成功只返回 `{ code, data }`，`error` 由异常过滤器在失败时注入。e2e 别写 `expect(res.body.error).toBeNull()`（实为 undefined）。
   - **rbush 3.0.1 无自带类型也无 `@types/rbush`**：按「不新增依赖」红线手写最小 ambient 声明 `backend/nest/src/types/rbush.d.ts`（19 行，只声明本项目实际调用的 load/insert/search/all）。
   - **turf 7.3.5 未导出 `Feature` 类型**（TS2304）：几何类型统一取 `@types/geojson` 的 `Feature<Polygon | MultiPolygon>`；`booleanPointInPolygon` 要求 Feature 必带 `properties`，规范化对象需补 `properties: {}` 占位。
   - **Express 启动必须带 `.env`**：裸 `node backend/index.js` 会因 `JWT_SECRET` 未设而 FATAL 退出（`middleware/auth.js:10`）。用 `PORT=3001 npm --prefix backend run start`（脚本含 `--env-file-if-exists=.env`）。Nest 裸跑 `node dist/src/main.js` 可起（健康检查不查 JWT）。
   - **停服务用 PowerShell `Stop-Process -Id`**：bash 侧 `netstat | awk` 取到的 PID 杀不掉 node（taskkill 无效），须用 `Get-NetTCPConnection -LocalPort <port> -State Listen` 拿 OwningProcess 再 Stop-Process。
   - **本机无 Postgres**（docker 守护未运行、无 5432 服务）→ nest 全量跑时 `auth.e2e / favorites.e2e / favorites.repository.spec / plans.e2e / plans.repository.spec` 五套件 `ECONNREFUSED 5432` 必红。**这是环境限制不是回归**，判据：这些文件在改动中零修改。CI 有 PG 服务时不受影响。
   - **真数据无自然空结果**：三城 site-selection 正常半径下 hospital+park 恒有重叠；下探到最小合法半径（`defaultRadius 0.3 × importance 1` = 0.1km）才在 fcg 命中无交集。服务级 empty 分支另用合成远距坐标覆盖（两点都必须在北部湾框内，否则 coverage 被静默剔除，测不到 failKey）。
10. **分支 ref 落盘事故（2026-09-01 实锤两次：一次坏一次好）**：`git commit` 会打印成功哈希，但分支 loose ref 可能不落盘（HEAD 悬空、报 unborn branch）。**每次 commit 后必须跑** `git rev-parse HEAD` + `git log --oneline -1` 确认；未落盘时用 `git cat-file -t <hash>` 验对象 → 手写 `.git/refs/heads/<branch>`（**必须写完整 40 位哈希**，短哈希 git 拒解析）→ 复核。**packed-refs 的 main 仍指 08-08 的 `1c610841`（靠松散 ref 覆盖）未拆，仍是地雷。**

## 五、收尾清单（全部完成后）

- `npm run ci:local`（NODE_OPTIONS=）exit 0
- 手册状态总表 T3.5/T3.6 done + 日志锚点（中文文件名）
- 用户汇报：任务完成情况、验收数据、偏离记录、遗留、下一步（T4.1/T5.1/T6.1 可认领；T6.2 第一批切换前置已满足）
- 不要 push，除非用户明确要求
