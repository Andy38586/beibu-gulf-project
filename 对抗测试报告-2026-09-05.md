# 北部湾 WebGIS 迁移后对抗性测试报告

- 测试时间：2026-09-05
- 测试对象：`C:\workspace\beibu-gulf-project`（前端 Vue3 + Vite；后端 NestJS（由 Express 迁移，Express 已退役，全局前缀 `nest-api`，端口 3000）；FastAPI 在线演算服务 algorithm-service，端口 8000；PostGIS 容器 `beibu-postgis`，库 `v3_dev`）
- 测试方式：真实浏览器（自动化 Chrome）逐一路由操作 + 控制台（console）/网络（network）面板采集 + 逐行对照源码定位根因；另对后端做了入参/停服/错误信封的对抗探测。
- 覆盖范围：**全量 6 条路由 + 深链/未知路由/异常输入/依赖停服等边界**，非抽查。
- 访问入口：http://localhost:5173/ ；测试账号：advtest0905（测试中注册，仅落 dev 库）。
- 测试中未修改任何项目源码；仅在 dev 库写入 1 个测试用户、临时创建并删除了 1 条方案。

---

## 一、结论速览

| 级别                       | 数量 | 编号                                                                               |
| -------------------------- | ---- | ---------------------------------------------------------------------------------- |
| P0 严重（阻断主流程）      | 1    | B1 刷新即掉登录                                                                    |
| P1 高（功能缺失/数据错误） | 3    | B2 方案无法新建、B3 航线里程单位错 1000 倍、B4 start:dev 起不来                    |
| P2 中（健壮性/体验缺陷）   | 5    | B5 后台标签遮罩卡死、B6 水深恒 0、B7 无 404 页、B8 标题不更新、B9 onUnmounted 警告 |
| P3 低（性能/卫生）         | 4 组 | B10 请求风暴、B11 地形 404、B12 限流头泄露、B13 杂项                               |

最需要立刻修的是 **B1（刷新即掉登录）**：Cookie 在服务端始终有效，但前端因 `/auth/me` 返回字段与 zod 校验契约不一致，每次刷新都把用户判定为未登录并清空本地态——这也是"明明登录了，收藏却提示要登录"等一系列现象的总根源。

---

## 二、逐路由"所见"与实测结果

### 1. `/` 首页（2D，北部湾智慧港口平台）

- **所见**：顶部钦州/北海/防城港快捷跳转、城市与图层开关、影像/矢量底图切换、明暗主题（`html.dark` 生效）、左下两个 ECharts 面板（港口吞吐量趋势折线、对比柱状，canvas 尺寸正常渲染）、右下图层控制、底部 6 项导航、右上调试 HUD。
- **实测**：冷启动自动拉 `forecast/overview`（200）、FastAPI 暖机（200）、Cesium 预热；快捷跳转、行政区划/港口图层、底图与主题切换均正常，控制台无业务报错。
- **瑕疵**：`terrain/layer.json` 被重复预加载两次（B13）；调试 HUD 记录冷启动 min FPS 1.0、9 个 >50ms 长帧（B13）；2D 页面也预加载了大量 Cesium worker（network 里一片 `blob:` inflight，B13）。

### 2. `/profile` 个人中心（2D，含登录/注册与已登录工作台）

- **未登录所见**：登录/注册切换、用户名+密码表单；空提交、弱口令、两次密码不一致都有前端校验；账号不存在返回 401002 时自动切到注册态并保留用户名；注册成功后自动拉收藏/方案。
- **已登录所见**：头像首字母、用户名、"我的收藏"（按"选址小区/浸没设施"分组平铺，可取消收藏）、"我的方案"（可展开→加载/重命名/删除）、左侧两个吞吐量图表、底部退出登录。
- **实测通过**：登录、收藏写入（POST `/favorites` 200，☆→★）、取消收藏（DELETE 200）、方案重命名（PUT 200，空名/纯空格被拦截）、方案加载（跳回选址页并还原类型选择）、方案删除（confirm 弹窗→DELETE 200→列表刷新）、退出登录（POST `/auth/logout` 200，回到登录表单）。
- **关键问题**：①刷新本页立刻掉登录（B1）；②"我的方案"在真实 UI 里永远无法产生新方案（B2）。

### 3. `/site-selection` 选址分析（2D）

- **所见**：设施类型多选（医院/小学/中学/公园/公交站/商场，选中后出现半径滑块与重要度）、城市作用域异步解析后按钮变"分析钦州市区"、结果"小区名单"（评分、☆收藏、分页 1/2）、地图三结果图层（分析覆盖范围/匹配小区/附近设施）开关。
- **实测**：选医院+小学→POST `/nest-api/site-analysis` 200（约 2.3s），三层图层与分页正常；空选有守卫；Nest 停服（模拟）时结果区保持空态并弹"网络异常"，不崩。
- **问题**：结果项可"收藏"，但**没有任何"保存为方案"入口**（B2）；登录态被 B1 清掉时，点 ☆ 会误报"收藏功能需要登录"（而服务端 Cookie 实际有效）。

### 4. `/forecast` 预测分析（2D）

- **所见**：4 指标（cargo/container/berth/traffic）切换、年/月粒度、时间轴（▶/⏸、滑块 65→73）、4 个热力图层。
- **实测**：指标/粒度切换、播放、热力图层均正常，无控制台错误；非法指标后端返回 404/404001（错误信封正常）。
- **问题**：时间轴播放时**每月固定 3 个请求**（indicator/cargo、indicator/container、map）且无前端缓存，约 2 请求/秒、全周期约 360 次（B10 请求风暴）。

### 5. `/flood-analysis` 浸没分析（全站唯一 3D）

- **所见**：Cesium 三维地球、7 档潮位（0/2.5/5/7.5/10/12.5/15m）、浸没分析报告（受影响设施数/预估损失/影响等级/平均水深/最大水深）、受影响设施清单（按损失分页）、剖面分析（4 条剖面线）、水面/淹没范围/受影响设施/真实地形图层、2D⇄3D 切换。
- **实测**：Cesium 单实例初始化成功；调 FastAPI `/flood/online`、`/flood/impact` 均 200（约 30ms）；起淹阈值约 4m（4m→538.06km²/26 要素，15m→3603.76km²）；calculate 模式真实地形回退请求 `/static/dem/dem_hillshade.png`（200）；3D→2D→3D 往返后仍是单 viewer、相机状态导入正常；**去个人中心再 SPA 返回的快照恢复经干净复测完全正常**（水位/面积/风险等级均还原，此前一次 2.5m 是测试操作串扰，非缺陷，已排除）；FastAPI 停服时 online+impact 分别被捕获、面板归零不崩。
- **问题**：平均/最大水深在默认 calculate 数据源下**恒为 0m**（B6）；地形根瓦片 404（B11）；后台标签下"地图加载中"遮罩/toast 离场动画会卡住（B5）；并行失败时两条错误各弹一个 toast，出现重复提示（B13）。

### 6. `/route-analysis` 航线分析（2D）

- **所见**：拾取起点/拾取终点（点按钮后在地图点选，按钮回显经纬度）、最短距离/最快时间切换、查询路径（起终点齐了才可点）、清除、结果摘要（里程/时长/接驳）、路径线+起终点图层。
- **实测**：用落在真实路网上的坐标走通全链路——FastAPI `/route/path` 返回 `found:true, distanceM:5572.5, durationMin:5.1, edgeCount:9, coordinates[...]`；模式切换（最快时间 3.3 分）、未吸附空结果提示（origin/destination_not_snapped、unreachable 文案齐全）、清除均正常；非法 mode→400、经纬度越界→422、起终点相同→distance 0 的合法空路径；FastAPI 停服时内联错误+toast，不崩。
- **问题**：**里程单位错 1000 倍**（B3）。

---

## 三、Bug 清单（现象 / 复现 / 根因 / 修复）

### B1【P0 严重】刷新任意页面即掉登录（Cookie 其实有效）

- **现象**：登录后整页刷新（F5/直接深链），用户被退回未登录态；业务页点收藏会提示"收藏功能需要登录"，但服务端 `GET /nest-api/auth/me` 实际返回 200。
- **复现**：advtest0905 登录 → 浏览器整页 reload `/profile` → 页面回到登录/注册表单；此时 `fetch('/nest-api/auth/me',{credentials:'include'})` 仍 200，body 为 `{code:200,data:{user:{id,username}}}`。
- **根因链（前后端契约不一致 + 错误分类过宽）**：
  1. 后端 `backend/src/modules/auth/controllers/auth.controller.ts:75-81` 的 `me()` 只回 `{ user: req.user }`，而 `req.user` 是 JWT 载荷，**只有 `{id, username}`，没有 `createdAt`**；对比 `services/auth.service.ts:101` 的 login（以及 register:63）是带 `createdAt` 的。
  2. 前端 `frontend/src/types/schemas.ts:8-12` 的 `userSchema` 把 `createdAt: z.string()` 设为**必填字符串**；`authResponseSchema` 嵌套它。`useAuth.restoreAuth`（`shared/composables/useAuth.ts:64`）用该 schema 校验 `/me` 响应 → 缺字段，zod 抛错。
  3. `useAuth.ts:76-92` 的 catch 只对"网络/超时/5xx"网开一面，**schema 校验错误落入 else 分支**，执行 `clearToken() + user=null + 清 localStorage`，于是被判定为登出。
  4. 隐患叠加：后端 `createdAt` 类型本身是 `string | null`（auth.service.ts:15/22），即便 `/me` 补了该字段，只要值为 null，`z.string()` 仍会校验失败。
- **修复建议（任选其一，推荐 A+B 一起）**：
  - A. 后端 `/me` 查库返回与 login 同形的用户视图（含 `createdAt`），保证各认证端点响应结构一致；
  - B. 前端 `userSchema.createdAt` 改为 `z.string().nullable().optional()`；并让 `restoreAuth` 对"响应结构/schema 错误"与"401 未授权"区别对待——结构错误不应清空一个 Cookie 仍有效的登录态（至少保留 localStorage 用户、打点告警）。

### B2【P1 高】"我的方案"在 UI 上永远无法新建（创建链路整体断裂）

- **现象**：个人中心能看到"我的方案"列表，但选址页/浸没页都没有"保存方案/存入方案"按钮，普通用户无法产生任何方案。
- **根因（迁移后遗留的死代码）**：`frontend/src/shared/composables/usePlans.ts` 暴露了 `createPlan`（74）、`saveXiaoqu`（166）、`removeXiaoqu`（189），全仓检索三者**除了定义与导出，没有任何调用方**；`usePlans()` 全前端只在 `views/components/PlansPanel.vue:34` 实例化，且只用了 getPlans/updatePlan/deletePlan。后端 `POST /nest-api/plans` 本身正常（测试直接调返回 201）。`business/flood-analysis/components/AffectedFacilityListPanel.vue:4` 注释仍写着"收藏功能与选址分析共用保存方案接口"，但该组件实际已无任何方案/收藏调用，注释过时。
- **修复建议**：在选址结果面板与浸没设施面板补"存入方案/新建方案"入口（接 createPlan + saveXiaoqu），或若产品决定下线方案功能，则同步移除 PlansPanel 的"我的方案"区与相关死代码，避免出现一个永远为空的模块。

### B3【P1 高】航线分析里程把"米"当"公里"，数值放大 1000 倍

- **现象**：一条实际 5.6km 的路径，界面显示"里程 5572.5 km · 约 5.1 分""含接驳共 5572.5 km"。
- **根因**：后端字段是 `distanceM`（米，实测 5572.5）。`frontend/src/business/route-analysis/RouteAnalysisPage.vue:53` `里程 ${distanceM.toFixed(1)} km` 与 `:60-61` 的 `totalDistanceText`（distanceM+snap 米数）都**漏了 /1000**，却标注 km。
- **修复建议**：`(distanceM/1000).toFixed(1)`，接驳 total 同理；并给该纯展示逻辑补单测（用 5572.5m 断言渲染为 5.6km）。

### B4【P1 高】`npm run start:dev` 直接崩溃，且报错指向迁移前旧路径

- **现象**：`backend` 的 `start:dev` 是 `nest start --watch`（package.json），不会加载 `backend/.env`，缺 `JWT_SECRET` 启动即抛错；错误文案让去 `backend/nest/.env` 配置——但目录已从 `backend/nest` 迁到 `backend`，路径是旧的。当前只能 `nest build` 后用 `node --env-file=.env dist/src/main.js` 起服务。
- **根因**：开发脚本未注入环境变量；`backend/src/common/utils/jwt.util.ts:18、26` 的报错文案、`infra/config/config.service.ts:9` 注释仍残留 `backend/nest`。
- **修复建议**：start:dev 改为经 `node --env-file=.env` 加载（或引入 `@nestjs/config`/dotenv）；全仓清理 `backend/nest` 残留文案。

### B5【P2 中】页面处于后台/非前台标签时，地图加载遮罩与 toast 离场动画卡死

- **现象**：自动化/用户把标签切到后台再回来，可能看到"地图加载中…"全屏遮罩一直不消失、toast 堆积；但组件内 loading 其实已经是 false。
- **根因**：`frontend/src/core/map/UnifiedMap.vue:698-703` 用 `<Transition name="fade">` 包裹 `.map-loading`，GCSToast 同理；Vue Transition 离场依赖 `requestAnimationFrame` 推进，而 `document.visibilityState==='hidden'` 时浏览器暂停 rAF，离场态停在 `fade-leave-from`。
- **修复建议**：关键状态遮罩不要依赖 Transition 离场（直接 v-if 卸载），或加一个 `setTimeout` 兜底强制移除离场节点；toast 队列设上限与超时强制清理。

### B6【P2 中】calculate 默认数据源下，浸没报告"平均水深/最大水深"恒为 0

- **现象**：默认 `VITE_DATA_SOURCE=calculate` 时，任意水位报告里平均水深、最大水深都是 0m。
- **根因**：`frontend/src/services/adapters/floodAdapter.ts:144-148` 的 calculate 分支，`statistics` 只给了 `floodArea` 与 `riskLevel`，没有 `averageDepth/maxDepth`；而 `business/flood-analysis/components/FloodAnalysisReportPanel.vue:63、67` 绑定 `floodStatistics.averageDepth ?? 0 / maxDepth ?? 0`，于是恒显示 0。只有 fetch 分支（:164 从 `/flood/flood-statistics` 取）才有这两个值。
- **修复建议**：calculate 分支用淹没 features 的水深字段现算平均/最大值；若 calculate 口径本就无水深，则在该数据源下隐藏这两行，避免展示无意义的 0。

### B7【P2 中】未知路由是"空白页"，没有 404 兜底

- **现象**：访问 `/no-such-page-xyz`，URL 不变、`$router.currentRoute.matched` 为空数组，页面只剩地图的 +/– 缩放控件，正文全空（body 文本仅 2 字符），无任何提示或跳转。
- **根因**：`frontend/src/router/index.ts` 的路由表只有 6 条业务路由，**没有 `/:pathMatch(.*)*` catch-all**。
- **修复建议**：补一个 404 路由（友好提示 + 返回首页/导航），或未知路径重定向到 `/`。

### B8【P2 中】浏览器标签标题不随路由变化，且名称停留在旧产品名

- **现象**：所有页面 `document.title` 恒为 index.html 里的静态"北部湾城市群选址分析平台"；首页 meta.title 已是"北部湾智慧港口平台"。
- **根因**：`router/index.ts` 给每条路由声明了 `meta.title`，但**没有 `router.afterEach` 去写 `document.title`**，全仓也搜不到任何 `document.title` 赋值。
- **修复建议**：`router.afterEach(to => document.title = to.meta.title ? `${to.meta.title} - 北部湾智慧港口平台` : 默认)`，并同步更新 index.html 静态 title。

### B9【P2 中】每次路由切换控制台报 `onUnmounted is called when there is no active component instance`

- **现象**：在首页/选址/个人中心之间 SPA 切换，控制台稳定出现一条 Vue warn。
- **根因（已用调用栈精确定位）**：`App.vue:108-147` 的路由 watcher（immediate）在回调里调 `waitForRenderer`（:137/:140）→ `shared/composables/useWaitForRenderer.ts:35` 执行 `onUnmounted(cancel)`。immediate 首次执行发生在 setup 内尚有实例；但**之后每次路由变化都在 setup 之外重跑该回调**，此时无活跃组件实例 → 报警，且本次的 `cancel` 清理钩子**根本没注册成功**（重试定时器靠 10×500ms 自停，泄漏有限但属于真实缺陷）。
- **修复建议**：`useWaitForRenderer` 内用 `getCurrentInstance()` 守卫，无实例时不注册 onUnmounted（改为返回 cancel 由调用方管理）；或在 App 级持有单一 timer，下一次 waitForRenderer 前先 cancel 上一个。

### B10【P3 低·性能】预测时间轴播放形成请求风暴

- **现象/根因**：播放时每月固定 3 个请求（两个 indicator + map），`(indicator,time,confidence)` 无缓存，全周期约 360 请求、约 2 请求/秒。
- **修复建议**：结果缓存（已取过的指标-时间直接命中）、合并同帧请求、播放用 rAF/节流对齐；生产 nginx 已对 `/flood-online` 限流 10r/s，此风暴在生产可能触发 503，需一并评估。

### B11【P3 低】Cesium 地形根瓦片 404 + layer.json 的 MIME 不对

- **现象/证据**：`backend/static/terrain/layer.json` 没有 `availability` 声明，Cesium 按"全球覆盖"去请求 `/static/terrain/0/0/0.terrain`，但磁盘只有 `0/1/0.terrain`（无 `0/0/0`），实测该 URL 404（存在的 `0/1/0.terrain` 200）；`layer.json` 实测 200 但 `Content-Type: application/octet-stream`（应为 application/json）。非致命，但刷 404、控制台报错。
- **修复建议**：给 layer.json 补实际瓦片范围的 `availability`（或补齐缺失根瓦片）；修正 Nest 静态服务对 `.json` 的 MIME。

### B12【P3 低·安全卫生】限流头全量泄露、X-Powered-By 暴露技术栈

- **证据**：任意 Nest 响应都同时带 `X-RateLimit-Limit/Remaining/Reset` 的 **global、login、register 三套**头，等于把登录/注册限流阈值（50/窗）暴露给任何调用方；同时响应带 `X-Powered-By=Express`（Nest 底层即 Express，非旧服务残留，但建议隐藏）。
- **修复建议**：限流守卫只回当前命中桶的头（或仅 global）；`app.disable('x-powered-by')` 或上 helmet。

### B13【P3 低·杂项】

- 首页 `terrain/layer.json` 重复预加载两次（去重）。
- 冷启动 min FPS 1.0、9 个 >50ms 长帧；纯 2D 页面也预加载 Cesium worker（建议按路由引擎类型懒加载 Cesium，3D 页才拉）。
- 并行请求同时失败时 toast 双发（浸没 online+impact 各弹一条几乎相同的"服务器无响应"），建议同类错误合并/去抖。
- 方案重命名输入纯空格时只被静默拦截、无内联提示（仅显示字数 3/50），建议补"名称不能为空"提示。

---

## 四、对抗/边界测试结果（后端与降级）

- 错误信封规范：错密码 401/401003、账号不存在 401/401002、重复注册 409/409001、缺字段 400/400001、非法 forecast 指标 404/404001，均返回结构化错误体。
- FastAPI 入参校验：水位 -5 / 9999 / 非数字 → 422；非法 mode → 400；经纬度越界 → 422；起终点相同 → 合法的 0 距离路径；缺 city → 回全区结果；未知 city 的 impact → 空结果 200。整体健壮。
- 依赖停服降级（浏览器内模拟 fetch reject）：FastAPI 挂时浸没页/航线页、Nest 挂时选址页都能捕获错误、保空态、弹提示而不白屏崩溃（仅 B13 的重复 toast）。
- 深链刷新：Vite dev 与生产 `nginx.conf:28` 都有 SPA fallback（`try_files ... /index.html`），深链本身不会 404；**深链后掉登录是 B1，不是 fallback 问题**。
- 旧 `/api/*` 404 属预期（Express 已退役；生产 nginx 还保留了 /api→/nest-api 的兼容 rewrite）。

## 五、已排除的"疑似 bug"

- "影响等级=低"与"洪水风险等级=灾难级"看似矛盾：二者是两套口径（前者按 totalLoss 金额分段、后者按水位分段），非缺陷，仅建议在文案上说明区别。
- 浸没页 SPA 往返后水位变 2.5：干净复测证明快照恢复正常，此前是测试中混入整页重载导致，非缺陷。
- PowerShell 读到错误响应 body 为空：是 StreamReader 读取问题，curl 实测错误信封正常。

## 六、修复优先级建议

1. 先修 B1（登录契约）——它连带造成"收藏要登录""刷新丢现场"等多个表象问题，收益最大。
2. 同迭代修 B3（一行单位换算，影响数据可信度）、B4（保证开发/部署可复现启动）。
3. 产品确认 B2 方案功能去留后，补入口或删死代码。
4. 排期处理 B5–B9 的健壮性/体验问题与 B10 性能；B11–B13 作为卫生项收尾。
5. 建议为 usePlans、RouteAnalysisPage 摘要、/me 恢复逻辑补单元测试，防止迁移期再次出现"代码在、链路断"和契约漂移。
